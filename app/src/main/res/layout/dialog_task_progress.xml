<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- 현재 단계 — 무엇을 하는 중인지. 총량을 아는 작업만 이 다이얼로그를 쓴다(R-26). -->
    <TextView
        android:id="@+id/stageText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.App.Body"
        android:textColor="?attr/colorOnSurface" />

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:indeterminate="false"
        android:max="100" />

    <!--
        총량을 알 수 없는 구간(예: 크기를 보고하지 않는 콘텐츠 프로바이더에서 복사)용 막대.
        같은 뷰의 indeterminate를 토글하지 않고 **둘을 두고 바꿔 보이는** 이유는
        Material의 LinearProgressIndicator가 보이는 동안 불확정으로 전환하면
        IllegalStateException을 던지기 때문이다. 한 작업 안에서도 구간마다 총량을 아는지가
        갈리므로(복사는 모를 수 있고 해제는 안다) 전환은 실제로 일어난다.
    -->
    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/indeterminateBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:indeterminate="true"
        android:visibility="gone" />

    <TextView
        android:id="@+id/countText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="end"
        android:textAppearance="@style/TextAppearance.App.BodySmall"
        android:textColor="?attr/colorOnSurfaceVariant" />
</LinearLayout>
