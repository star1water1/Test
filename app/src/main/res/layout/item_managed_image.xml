<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="3dp"
    android:background="?attr/selectableItemBackground">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="110dp">

        <ImageView
            android:id="@+id/thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/image_manager_thumb_desc"
            android:background="?attr/colorSurfaceVariant" />

        <!-- 상태 배지 (고아/휴지통) — 참조본이면 숨김 -->
        <TextView
            android:id="@+id/statusBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_margin="4dp"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:textAppearance="@style/TextAppearance.App.LabelSmall"
            android:textColor="#FFFFFF"
            android:background="#CC000000"
            android:visibility="gone"
            tools:text="고아"
            tools:visibility="visible" />

        <!-- 크기 오버레이 -->
        <TextView
            android:id="@+id/sizeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="4dp"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:textAppearance="@style/TextAppearance.App.LabelSmall"
            android:textColor="#FFFFFF"
            android:background="#CC000000"
            tools:text="1.2 MB" />

        <!-- 링크 그룹 표식 — 같은 캐릭터 묶음으로 링크된 이미지 -->
        <TextView
            android:id="@+id/linkBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:layout_margin="4dp"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:textSize="@dimen/text_badge"
            android:textColor="#FFFFFF"
            android:background="#CC1565C0"
            android:text="@string/image_link_badge"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- 선택 모드 스크림 — 선택된 셀 위에 반투명 강조를 덧씌운다 -->
        <View
            android:id="@+id/selectionScrim"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#553F51B5"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- 선택 체크 표식 -->
        <TextView
            android:id="@+id/selectionCheck"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="4dp"
            android:minWidth="22dp"
            android:gravity="center"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="1dp"
            android:paddingBottom="1dp"
            android:text="@string/image_manager_selected_mark"
            android:textStyle="bold"
            android:textAppearance="@style/TextAppearance.App.BodySmall"
            android:textColor="#FFFFFF"
            android:background="#CC3F51B5"
            android:visibility="gone"
            tools:visibility="visible" />
    </FrameLayout>

    <TextView
        android:id="@+id/ownerText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAppearance="@style/TextAppearance.App.LabelSmall"
        android:textColor="?attr/colorOnSurfaceVariant"
        android:layout_marginTop="2dp"
        tools:text="캐릭터 · 홍길동" />

    <!-- 태그 줄 — 항상 1줄 고정(빈 태그도 공백 유지)해 그리드 행 높이가 들쭉하지 않게 한다 -->
    <TextView
        android:id="@+id/tagText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:lines="1"
        android:ellipsize="end"
        android:textSize="@dimen/text_badge"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="#여성 · #흑발" />
</LinearLayout>
