<?xml version="1.0" encoding="utf-8"?>
<resources>
    <item name="image_load_job" type="id" />
    <!-- 인라인 오류 감시자 중복 부착 방지 (util/AlertDialogExt.kt) -->
    <item name="inline_error_watcher" type="id" />
    <!-- 이미지 스트립 항목의 썸네일과 대표 지정 ☆ (ui/character/CharacterImageStripController) -->
    <item name="strip_thumbnail" type="id" />
    <item name="strip_representative_star" type="id" />
    <!--
      AI 정리 검토 목록의 컨테이너 (ui/fieldlibrary/AiOrganizeReviewSheet).
      레이아웃 XML 없이 코드로 짓는 화면이라 id를 여기서 준다 — **회전 뒤 되살아난 창에
      제안을 다시 먹이려면(rebind) 그 목록을 찾을 수 있어야 하고**, 찾는 유일한 길이 id다.
    -->
    <item name="aiOrganizeReviewList" type="id" />
</resources>
