<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_import_fields"
        android:title="@string/import_fields_from_other"
        app:showAsAction="never" />

    <!-- 전역 기본 필드 관리 (B-119) — 필드를 만드는 화면에서 "모든 세계관이 갖는 필드"로
         가는 길이다. 설정에도 같은 진입이 있다(설계 1-4). -->
    <item
        android:id="@+id/action_default_fields"
        android:title="@string/default_field_manage_title"
        app:showAsAction="never" />

    <!-- U-5(발견성): 오버플로에 묻혀 있어 "있는 줄 몰랐다"는 회신이 나온 자리다.
         필드를 만드는 화면에서 값 정리로 가는 길이라 툴바에 아이콘으로 올린다. -->
    <item
        android:id="@+id/action_field_library"
        android:icon="@drawable/ic_field_library"
        android:title="@string/field_library_title"
        app:showAsAction="ifRoom" />

</menu>
