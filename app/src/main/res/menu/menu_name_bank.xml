<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 선택 모드에서만 노출 -->
    <item
        android:id="@+id/action_bulk_register"
        android:title="@string/name_bank_bulk_register"
        android:visible="false"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_select_all"
        android:title="@string/name_bank_select_all"
        android:visible="false"
        app:showAsAction="never" />

    <!-- 은행이 소비 창고에서 생성 도구가 되는 자리 (B-123 · 설계 8-2 ⓒ).
         B-124에서는 열 시트가 없어 빠져 있던 항목이다 — 이제 그 시트가 섰다. -->
    <item
        android:id="@+id/action_ai_name"
        android:title="@string/name_bank_ai_create"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_select"
        android:title="@string/name_bank_select_mode"
        app:showAsAction="never" />
</menu>
