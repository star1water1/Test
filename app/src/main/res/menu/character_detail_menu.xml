<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_growth_chart"
        android:title="@string/growth_chart"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_share_card"
        android:title="@string/share_character_card"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_share_pdf"
        android:title="@string/share_pdf_export"
        app:showAsAction="never" />

    <!--
        대표 이미지 사용법(B-103 D7). 이름을 '도움말'로 두지 않는 것은 이 화면의 도움말
        전체가 아니라 한 주제이기 때문이다 — 정리 폴더가 쓰는 방식과 같다.
    -->
    <item
        android:id="@+id/action_representative_image_help"
        android:title="@string/representative_image_help"
        app:showAsAction="never" />

</menu>
