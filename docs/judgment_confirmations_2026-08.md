# 판정 확정 기록 (2026-08) — 사용자 판정의 단일 소스

## 이 문서의 지위

**백로그와 그 파생에서 나온 "판정 대기"를 사용자에게 묻고 받은 답의 단일 소스다.**
1차(2026.08.03)가 백로그 24건, 2차(2026.08.04)가 **파생 7건 + 새 백로그 3건**이다.
**제목에서 날짜와 건수를 뺀 것은 일부러다** — 2차가 들어오는 순간 둘 다 낡았다.

> **2026.08.04 현재 이 저장소에 살아 있는 판정 대기는 0건이다**(1장 24건 · 3장 P-1~P-10 ·
> 7장 3건이 전부 닫혔다). **다음 판정은 새 슬라이스가 만들 때 생긴다.**
`remaining_work_2026-07.md` 4장의 각 행에는 `→ 판정 확정 (2026.08.03)`과 답만 짧게 적고,
**원문·해석·파생 판정은 전부 이 문서가 갖는다.** 24건을 각 행에 풀어 쓰면 표가 읽을 수 없게 되고,
같은 내용이 두 자리에 갈라져 낡는다(B-81 한 행이 이미 그 크기다).

**이 세션은 기록만 한다.** 사용자 지시 원문:

> "이거 답이랑 판정대기 기록만 해두고 제대로 해석하고 설계하고 계획하는 건 새로운 세션에서 진행하자."

따라서 **해석·설계·계획은 다음 세션의 몫이고**, 이 문서는 그 세션이 읽을 입력이다.
아래 "확정"은 **무엇을 할지가 정해졌다**는 뜻이지 **어떻게 만들지가 정해졌다**는 뜻이 아니다.

## 묻는 데 쓴 자료

판정 대기 24건을 쉬운 말로 풀어 사용자에게 제시했다(선택지 + 추천 + 추천 이유).
그 자료는 대화 산출물이라 저장소에 없다 — **이 문서의 각 항목이 그 내용을 담고 있다.**

---

## 1. 확정된 답 — 24건

기호는 제시 자료의 갈래 번호다. **원문 그대로 적고, 해석이 필요한 곳은 따로 표시했다.**

| # | 항목 | 사용자 답 | 뜻 |
|---|------|-----------|-----|
| 1 | B-103 대표 이미지 | ㄱ1, ㄴ3, ㄷ1, ㄹ1 | 파일 기준 저장 · **화면 진입마다 랜덤** · 삭제 시 알리고 랜덤 복귀 · 엑셀에 실음 |
| 2 | B-104 대결 | ㄱ1, **ㄴ1+ㄴ3**, ㄷ1, ㄹ1, ㅁ→재판정, ㅂ1 | 점수 방식 · **전수 지향 + 자동보조 + 직접 지정** · 세계관 단위 · 엑셀에 실음 · 이름은 '대결' |
| 3 | B-105 엑셀 앱 설정 | ㄱ1, ㄴ1 | API 키는 **기본 제외 + 별도 동의** · 화면 흔적은 안 실음 |
| 4 | B-100 완성도 가중 | **ㄱ1+ㄱ2**, ㄴ1 | 기본 배수를 두되 **사용자가 바꿀 수 있게** · 필수 0개면 현행 유지 |
| 5 | B-101 '동일' 판정 | ㄱ1 | 다음 큰 작업으로 잡고 **범주 전수** |
| 6 | B-102 실행 맥락 모사 | ㄱ1 | **5번과 묶어서** 함께 |
| 7 | B-93 🎲 생성 옵션 | ㄱ1, ㄴ1 | 세계관 단위 · 수치와 이름만(단계 개수는 안 엶) |
| 8 | B-94 작품 평균 | **ㄱ1+ㄱ2로 해석** ⚠️ | 필드 설정에 기본 기준 + 체형 카드에서 즉석 전환 |
| 9 | B-95 숨은 설정 둘 | ㄱ2 | **`underbustEstimation` 설정을 없앤다** · `defaultBodyType`은 칸 신설 |
| 10 | B-36 패턴 감지 사건 축 | ㄱ1 | 연다 — **11번과 묶어서** |
| 11 | B-70 패턴 민감도 | ㄱ1 | 유형별 민감도를 설정으로 연다 |
| 12 | B-66 작품 교차분석 | ㄱ2 | **보류** |
| 13 | B-67 작품 카드 필드값 | ㄱ1, ㄴ1 | 사건 카드와 같은 개수 · '카드에 표시' 토글도 연다 |
| 14 | B-63 종류 바꿔 가져오기 | **옵션화** | 허용 타입을 **고정하지 말고 사용자가 정하게** |
| 15 | B-65 남의 세계관 값 | ㄱ1, ㄴ1 | 열 이름이 맞으면 수용 · 캐릭터처럼 **별도 시트도 만듦** |
| 16 | B-43 사건 AI 추천 | **ㄱ1/ㄱ2 인앱 선택**, ㄴ2 | 재료 범위를 사용자가 고름 · **사건 전용 프롬프트를 따로** |
| 17 | B-44 필드 설명 읽기 화면 | **옵션화** | 설정으로 켜고 끄게 |
| 18 | B-74 휴지통 정책 | ㄱ1 | 개수·기간 **둘 다** 설정으로 |
| 19 | B-75 프리셋 상한 | ㄱ1 | **권고로 통일** — 앱에서도 경고 후 저장 허용 |
| 20 | B-60 usageCount 의미 | ㄱ1 | **"지금 쓰이는 횟수"** — 수확 쪽에서 이력을 뺀다 |
| 21 | B-26 되돌리기 덮어쓰기 | ㄱ1 | 현행 유지(보충만 + 정직한 고지) |
| 22 | B-18 복원 트랜잭션 | **ㄱ2 + 진행도** | 전부 한 덩어리로 · **로딩·진행률을 잘 보여주는 조건부** |
| 23 | `매칭` 용어 | ㄱ1 | **표1에 등재.** 대결은 '대결'로 — **두 말이 공존한다** |
| 24 | 분석 탭 카드 순서 | ㄱ1 | 순서를 **사용자가 끌어서** 바꾸게 |

---

## 2. 사용자가 직접 물은 것 — 답과 근거

### 2-1. 2-ㅁ(대결 모순 감지) — "너가 생각하는 최고가 뭐야?"

사용자 원문:

> "ㅁ은 너가 생각하는 최고가 뭐야? 구현하기 편한 게 아니라. 여기 모든 개발의 원칙은
> 개발이 편하냐가 아니라는 걸 명심해. 위에 내가 답변한 것도 너가 말한거 기준으로
> 답한거지 더 좋은 게 있으면 그걸 써야해"

**제시했던 ㅁ1(축↔필드 연결 대조)이 잘못된 제안이었음을 인정하고 갈음한다.**

**ㅁ1이 왜 부족한가.** 필드 대조는 **대결이 가장 안 필요한 곳에서만 작동한다.**
근력처럼 수치 필드가 있는 축은 애초에 필드로 정렬하면 되므로 대결의 값어치가 낮다.
반대로 아름다움·카리스마·위압감처럼 **필드가 없어서 대결을 쓰는 축**에는 대조할 상대가 없어
모순 감지가 아무 일도 하지 않는다. 즉 ㅁ1은 필요와 반비례한다.

**갈음안 — 순환을 축으로 삼는다(3층).**

**층 A · 순환 감지(필드 불필요).** A>B, B>C, C>A를 찾는다.
대결 데이터만으로 성립하므로 **축이 무엇이든 작동한다.**

**층 B · 순환의 처분을 사용자가 가른다 — 여기가 핵심.** 순환이 뜨면 셋 중 고르게 한다.

| 처분 | 뜻 | 결과 |
|---|---|---|
| ① 잘못 눌렀다 | 조작 실수 | 해당 판을 되돌린다 |
| ② 아직 안 정했다 | 창작자도 서열을 모른다 | '미정'으로 표시, 나중에 다시 묻는다 |
| ③ **상성이다** | A는 B에 강하고 B는 C에 강하고 C는 A에 강하다 | **상성 관계로 등재된다** |

③이 결정적이다. 사용자가 같은 답에서 **"상성도 있을 거 아니야"**라고 직접 말했다.
**순환을 오류로 처리하면, 상성을 가진 세계관에서 앱이 계속 틀렸다고 잔소리하는 기능이 된다.**
발견으로 처리하면 **대결이 상성 관계를 캐내는 도구**가 된다 —
원칙 02(실질적 인사이트)와 원칙 05(유기적 연결)에 정면으로 맞는다.

**층 C · 필드 대조(옛 ㅁ1).** 축↔필드 연결이 설정된 축에서만 어긋남을 알린다.
층 A·B와 달리 **선택적**이며, 어느 쪽이 맞는지는 앱이 정하지 않는다.

**덧 · 흔들림 감지.** 같은 짝을 여러 번 붙였는데 결과가 갈리면
"이 축은 기준이 모호할 수 있다"를 알린다. 축 자체의 품질 신호다.

> **설계상 귀결(중요).** **상성이 있으면 단일 점수로는 원리적으로 표현할 수 없다.**
> A>B>C>A는 어떤 1차원 숫자로도 만들어지지 않는다. 그래서 사용자가 고른 **ㄱ1(점수)은 그대로 쓰되,
> 상성은 점수 위에 별도 층으로 얹혀야 하고** 순위표가 "이 축에 상성 N건"을 함께 말해야 한다.
> 점수 하나로 뭉개면 사용자가 요구한 상성이 표현될 자리가 없어진다.

**→ 갈음안 채택 확정 (2026.08.03, 사용자 확정 — P-1 해소).** 단 **조건이 붙었다.** 원문:

> "p1좋다. 다만 해당 작업이 있을 세션에서 더 제대로 더 좋은 기능이나 알고리즘을
> 만들 수 없을지 조사하고 검토하게 하는 것도 좋을듯."

**이 3층안은 바닥이지 천장이 아니다.** B-104 착수 세션은 **구현에 들어가기 전에
"이보다 나은 것이 있는가"를 조사·검토하는 단계를 먼저 밟는다** — 아래 6장이 그 의무를
규정한다. 조사 결과 더 나은 것이 나오면 **이 안을 갈음해도 된다**(그것이 사용자가
이 조건을 단 이유다). 다만 갈음하려면 **왜 이것으로 부족한지를 근거로 들어야** 한다.

### 2-2. 2-ㄴ — "최대한 붙이되 최대한 보조가 가능한가"

사용자 원문:

> "ㄴ1+ㄴ3(전부 붙이는 편이 좋음. 하지만 혹시 모르니 최대한 경합할만한 애들은 모두 붙여야 하고
> 완전 승산없다 수준은 걸러주면 좀 줄일 수있지 않을까, 그리고 나름대로 강한놈이 이긴다도 있겠지만
> 상성도 있을 거 아니야? 그러니까 최대한 붙이되 동시에 최대한 보조하는 걸 목표로 하는 게 좋을 거
> 같은데. 가능한 모든 알고리즘을 동원해서)"

**가능하다. 3층으로 성립한다.**

| 층 | 하는 일 | 왜 |
|---|---|---|
| 1 · 전수 지향 | 목표는 전 조합. **진행률을 보여준다**("1,234 / 44,850판") | "전부 붙이는 편이 좋음"이 목표선이다 |
| 2 · 접어두기 | 점수 차가 충분히 벌어진 짝은 '사실상 결정됨'으로 대기열에서 내린다. **버리는 게 아니라 접는 것** — 언제든 펼쳐 붙일 수 있다 | "완전 승산없다 수준은 걸러주면" |
| 3 · 표본 재확인 | 접어둔 짝 일부를 **다시 꺼내** 붙여 본다. 뒤집히면 **상성 후보** → 2-1의 층 A로 넘긴다 | "상성도 있을 거 아니야" |

**층 3이 사용자의 우려를 정확히 막는다.** 층 2만 두면 상성 때문에 뒤집힐 짝을
**영영 안 붙여 보게 된다** — 점수 차가 크다는 이유로 접었는데 그 점수 차 자체가
상성을 못 본 결과일 수 있기 때문이다. 표본 재확인이 그 고리를 끊는다.

> **규모 사실.** 목표 규모(캐릭터 수백)에서 한 축의 전 조합은 **수만 판**이다.
> 300명이면 44,850판. 그래서 층 1의 '전수'는 **도달점이 아니라 방향**이고,
> 실질은 층 2·3이 만든다. 진행률 표시가 필수인 이유도 이것이다 —
> 끝이 안 보이는 작업에 끝이 어디인지는 보여야 한다.

### 2-3. 8번 — 갈래가 없는 기호 ⚠️

사용자 답은 **"ㄱ1+ㄴ1이 낫지 않나? 아닌가?"**인데 **8번에는 ㄴ 갈래가 없다**(ㄱ1/ㄱ2/ㄱ3뿐).
7번이 ㄱ·ㄴ 두 갈래여서 섞인 것으로 보인다.

**`ㄱ1+ㄱ2`로 해석했다** — 필드 설정에 기본 기준을 두고, 체형 카드에서 즉석 전환.
둘을 합치면 각각보다 낫다(한 번 정해 두고 쓰다가, 볼 때 잠깐 바꿔 보는 것이 가능해진다).
**→ 해석 확정 (2026.08.03, 사용자 확정 — "p2맞다"). P-2 해소.**
따라서 8번은 **필드 설정에 기본 기준 + 체형 카드에서 즉석 전환**이다.

---

## 3. 파생 판정 10건 — **전부 해소** (2026.08.04)

확정된 답에서 **파생된** 것들이다. **P-1·P-2·P-3은 2026.08.03에**,
**P-4~P-10은 2026.08.04에 해소됐다**(7장) — **이 표에 살아 있는 판정은 이제 없다.**

> **P-9·P-10은 "값을 정했다"가 아니라 "정하는 방법을 정했다"이다.** 둘 다 근거가
> 아직 없는 자리라(실기기 측정 전 / 조사·검토 전) 지금 숫자를 박으면 근거 없는 값이 된다.
> **해당 슬라이스가 그 근거를 만든 뒤 정한다** — 그것이 사용자가 고른 처분이다.
> 그래서 이 둘은 **닫힌 판정이되 슬라이스에 숙제를 남긴다.**

**그 방식이 실제로 한 번 돌았다 — P-3이 그렇게 해소됐다**(2026.08.03, B-103 설계 세션).
슬라이스에 들어간 세션이 **코드를 먼저 보고**, 답의 절반이 이미 코드에 있음을 근거로 들어
추천과 함께 물었다. **파생 판정을 물을 때의 본보기다** — 선택지만 나열해 되묻지 말고
**현행 동작을 실측해 함께 낼 것.**

| # | 무엇 | 어디서 왔나 |
|---|---|---|
| ~~**P-1**~~ | ~~2-ㅁ 갈음안(순환 3층)을 채택하는가~~ **→ 해소 (2026.08.03, 사용자 확정 "p1좋다").** 단 **조사·검토 의무가 조건으로 붙었다 — 6장** | 2-1 |
| ~~**P-2**~~ | ~~8번을 `ㄱ1+ㄱ2`로 읽은 것이 맞는가~~ **→ 해소 (2026.08.03, 사용자 확정 "p2맞다").** | 2-3 |
| ~~**P-3**~~ | ~~**ㄴ3(화면 진입마다 랜덤)이 목록에서도 매번 바뀌는가**~~ **→ 해소 (2026.08.03, 사용자 확정 — "같은 주기"). 목록·배너·상세가 **한 규칙**을 쓴다: 화면 진입 1회 재추첨 · 그 화면 안에서는 고정.** 설계 착수 세션이 근거 셋을 들어 물었다(`docs/representative_image_design_2026-08.md` 8장): ① **캐릭터 목록은 이미 그렇게 돈다**(`CharacterListFragment:587`) — 확정 ㄴ3과 코드가 이미 일치했다 ② 바인드마다 뽑으면 **스크롤에서 깜빡인다**(이 행이 스스로 경계한 것) ③ 방출마다 뽑는 것은 **성능 수리 P2-1이 없앤 바로 그 동작**이다(세션 로그 1-12) | 1번 |
| ~~**P-4**~~ | ~~**완성도 가중 배수 설정을 어디에 두는가**~~ **→ 해소 (2026.08.04, 사용자 확정 — "A. ㄱ" = 앱 전체 하나). 구현이 이미 그 자리이므로 갈아 끼울 것이 없다.** 되돌릴 수 있는 쪽으로 세워 둔 것이 그대로 답이 됐다 — **미회신일 때 되돌릴 수 있는 쪽을 고르는 처분이 값을 한 첫 사례다.** 종전 기술:  **물었으나 회신 전이다**(2026.08.04, B-100 구현 세션). **구현은 앱 전체(`stats_prefs`)로 세워 두었다**: 둘 중 **되돌릴 수 있는 쪽**이라서다 — prefs 키 하나라 DB 마이그레이션도, 엑셀 왕복·월드패키지·복원 미리보기 정합(R-33)도 건드리지 않는다. 세계관 단위로 확정되면 **바뀌는 것은 `CompletionWeightPrefs` 하나**이고 계산 계층과 소비처 열 자리는 그대로다(배수를 인자로 받기 때문이다 — R-34). **실측을 함께 냈다**(3장 머리의 본보기대로): 완성도를 읽는 화면이 여섯이고 그중 **데이터 현황·데이터 건강은 여러 세계관 캐릭터를 한 평균으로 낸다** — 배수가 세계관마다 다르면 그 평균은 서로 다른 자로 잰 값이 섞인 수가 된다 | 4번(ㄱ1+ㄱ2). 세계관 단위인가 앱 전체인가. 완성도는 여러 화면이 읽는 값이라 자리가 성격을 바꾼다 |
| ~~**P-5**~~ | ~~**`underbustEstimation`을 없애면 옛 엑셀 파일의 그 열을 어떻게 하는가**~~ **→ 해소 (2026.08.04 — "5-ㄴ" = 한 줄 고지).** 조용히 무시하지 않고 *"더 이상 쓰지 않는 설정 N개를 건너뛰었습니다"*를 결과에 싣는다. **실측이 물음의 모양을 정정했다** — 이 값은 **전용 열이 아니라** 필드 정의 시트의 `설정(JSON)` 셀 **안**에 있다(`FieldConfigColumns`가 여는 열은 `AI추천`·`필드설명` 둘뿐). 따라서 처분은 *열을 없애는 것*이 아니라 **파싱이 모르는 키를 만났을 때 세어서 알리는 것**이다 | 9번(ㄱ2) × 3번(B-105). 무시인가 고지인가. **설정을 없애는 것은 되돌리기 어려운 축**이라 왕복 처리가 함께 정해져야 한다 |
| ~~**P-6**~~ | ~~**필드 가져오기 허용 타입 옵션의 기본값**~~ **→ 해소 (2026.08.04 — "6-ㄱ" = 안전 우선).** 글자·선택·숫자만 켜진 채로 시작하고 나머지는 사용자가 연다. **실측:** 지금은 옵션 자체가 없고 **같은 종류만** 가져온다(`FieldViewModel.collectSources(universeId, entityType)`) — 즉 기본값은 *현행에서 한 칸 넓히는 값*이지 *좁히는 값*이 아니다 | 14번. 안전 우선(글자·선택·숫자만)으로 시작인가, 전부 허용으로 시작인가 |
| ~~**P-7**~~ | ~~**사건 AI 재료 범위 선택을 어디에 저장하는가**~~ **→ 해소 (2026.08.04 — "7-ㄱ" = 앱 전체).** **실측이 근거를 냈다:** AI 설정 저장소 셋(`AiKeyStore`·`AiPromptSettings`·`AiProviderStore`)이 **전부 앱 전체**이고 세계관 단위 AI 설정은 **하나도 없다** — 여기서만 세계관 단위로 가면 AI 설정이 두 축으로 갈린다 | 16번. 세계관인가 앱 전체인가 |
| ~~**P-8**~~ | ~~**필드 설명 읽기 화면 옵션의 기본값**~~ **→ 해소 (2026.08.04 — "8-ㄱ" = 기본 꺼짐).** **실측:** `ⓘ`는 지금 **필드 편집·필드 관리·입력 폼** 셋에 있고 읽기 화면(`DynamicFieldRenderer`)에는 없다. 기본 꺼짐이면 **현행 화면이 한 픽셀도 안 바뀌고**, 켜는 것은 사용자 선택이 된다(자율성 우선) | 17번. 켜짐인가 꺼짐인가 |
| ~~**P-9**~~ | ~~**복원 단일 트랜잭션의 규모 상한과 진행률 전달 경로**~~ **→ 해소 (2026.08.04 — "9-그렇게 해") — 단 답은 값이 아니라 *순서*다: 실기기에서 실제 시간을 잰 뒤 상한을 정한다.** 지금 숫자를 박으면 근거 없는 값이 되므로 **착수 슬라이스가 측정을 먼저 한다.** **실측이 범위를 넓혔다:** `TrashRepository.restoreOperation`은 항목을 하나씩 복원할 뿐 아니라 **루프가 끝난 뒤** 값 라이브러리 수확(`harvestRestoredCharacters`)과 이미지 링크 잇기(`resolveDeferredImageLinks`)를 따로 한다 — 한 덩어리로 묶으면 **이 뒷정리도 트랜잭션 안으로 들어가고** 잠금 시간이 등재가 말한 것보다 길어진다. 측정 대상에 이 둘을 함께 넣을 것 | 22번(ㄱ2). 수백~수천 항목을 한 트랜잭션으로 묶으면 **그동안 DB가 잠긴다.** 진행률을 보여주려면 트랜잭션 안에서 콜백이 나가야 하는데 현행 구조가 그것을 허용하는지 미확인 |
| ~~**P-10**~~ | ~~**대결 점수와 상성 층의 관계를 순위표에서 어떻게 보이는가**~~ **→ 해소 (2026.08.04 — "10-그렇게 해") = 순위표에 "이 축에 상성 N건" 배지 + 눌러서 상세.** **단 이것은 바닥이지 천장이 아니다** — 6장의 조사·검토 의무가 **상성 표현**을 조사 축 셋 중 하나로 이미 지목하고 있으므로, 조사가 더 나은 표현을 내면 **근거를 들어 갈음해도 된다**(6장의 갈음 규칙이 그대로 적용된다) | 2-1 귀결. 점수는 1차원인데 상성은 아니다 |

---

## 4. 확정이 만든 순서 강제

다음 세션이 착수 순서를 정할 때 쓸 것. **이 문서가 순서를 정하지는 않는다**(그것은 `remaining_work` 5장의 몫).

> **소진된 강제는 취소선으로 남긴다** — 지우지 않는 것은 *"그때 왜 그 순서였는가"*가 기록이기
> 때문이고, 취소선을 긋는 것은 **살아 있는 강제만 읽히게** 하기 위해서다. 종전에는 1번→2번이
> 소진된 뒤에도 현행처럼 서 있어, 다음 세션이 이미 풀린 제약을 다시 대조해야 했다.

| 강제 | 내용 |
|---|---|
| ~~**1번 → 2번**~~ | ~~대결로 이미지 우열을 쌓으면 그 승자가 대표 이미지의 **기본값 후보**가 된다. 1번(대표 이미지)이 먼저 서야 2번 결과를 얹을 자리가 생긴다~~ **→ 소진 (2026.08.03, B-103 구현·병합). 1번이 섰으므로 2번(대결)은 이제 이 강제에 막히지 않는다** |
| ~~**5번 = 6번**~~ | ~~둘 다 **복원 미리보기의 범주를 전수로 훑어야** 한다. 사용자가 6번을 "5번과 묶어서"로 확정했다~~ **→ 소진 (2026.08.03, B-101+B-102 한 슬라이스로 구현·병합. 설계 `docs/restore_preview_parity_2026-08.md`)** |
| **10번 = 11번** | 사건 축을 열려면 "사건 몇 개부터 패턴인가"가 필요한데 그것이 11번의 민감도와 같은 자리다. 사용자가 10번을 "11번과 묶어서"로 확정했다 |
| **9번 → 3번** | `underbustEstimation` 제거가 엑셀 '앱 설정' 시트의 열 구성을 바꾼다(P-5) |
| **23번 ↔ 2번** | `매칭`을 표1에 등재하고 대결은 '대결'로 부른다 — **두 말이 공존하므로** 대결 화면 문구에 `매칭`을 쓰지 않는다 |

---

## 5. 확정으로 닫힌 것 — 2건

**되살리려면 사용자 판정이 필요하다**(세션 착수 규칙 3번 — 사용자 확정은 세션이 뒤집지 않는다).

- **12번 B-66(작품 축 교차분석) — 보류 확정.** 작품은 수가 적어 교차분석의 값어치가 캐릭터만큼 크지 않다.
  실제로 비교하고 싶은 것이 생기면 그때 연다.
- **21번 B-26(되돌리기 덮어쓰기) — 현행 유지 확정.** 보충만 하고 정직하게 고지하는 지금 동작을 유지한다.
  되돌리기가 사용자가 나중에 손댄 값까지 덮어쓰는 것이 더 위험하다는 판단.

---

## 6. 착수 전 조사·검토 의무 — B-104(대결)

**P-1을 확정하면서 사용자가 조건을 달았다.** 원문:

> "다만 해당 작업이 있을 세션에서 더 제대로 더 좋은 기능이나 알고리즘을 만들 수 없을지
> 조사하고 검토하게 하는 것도 좋을듯."

**왜 이 조건이 붙었는지가 중요하다.** 2-ㅁ에서 실제로 벌어진 일이 근거다 —
내가 처음 낸 ㅁ1은 **말이 되기는 했지만 필요와 반비례하는 안**이었고,
사용자가 *"구현하기 편한 게 아니라 최고가 뭐냐"*고 **되물었기 때문에** 갈음안이 나왔다.
**되묻지 않았으면 ㅁ1이 그대로 확정됐을 것이다.**

즉 이 저장소의 기본값은 *"처음 낸 안이 말이 되면 그대로 간다"*였고, 그것이 한 번
실패했다. 그래서 **B-104 착수 세션은 되묻기를 기다리지 않고 스스로 한다.**

### 무엇을 하는가

**설계에 들어가기 전에 조사·검토 단계를 먼저 밟는다.** 최소한 아래 셋을 본다.

| 축 | 무엇을 조사하는가 |
|---|---|
| **순위 산출** | 점수 방식(ㄱ1 확정) 안에서도 갈래가 여럿이다. **적게 눌러도 순위가 빨리 자리 잡는 것**이 목표이므로, 판당 정보량을 높이는 방법이 있는지 본다 |
| **짝 고르기** | 2-2의 3층(전수 지향 → 접어두기 → 표본 재확인)보다 나은 것이 있는가. 특히 **어떤 짝을 먼저 내야 순위가 가장 빨리 확정되는가** |
| **상성 표현** | 순환을 상성으로 등재하는 것(2-1) 위에, **상성 자체를 어떻게 저장·조회·표시할 것인가.** 점수는 1차원인데 상성은 아니다(P-10) |

### 이 문서의 안을 갈음해도 되는가

**된다. 그것이 사용자가 이 조건을 단 이유다.** 다만 갈음하려면
**왜 이것으로 부족한지를 근거로 들어야 한다** — "더 좋아 보여서"는 근거가 아니다.
확정된 뼈대(ㄱ1 점수 · ㄷ1 세계관 단위 · ㄹ1 엑셀 포함 · ㅂ1 이름 '대결')는
**사용자 확정이므로 그대로 두고**, 갈음의 범위는 **순환 3층안과 2-2의 짝 고르기**다.

> **이 의무는 B-104에만 붙는다.** 다른 슬라이스로 일반화하지 않는다 —
> 매 세션 앞머리에 조사 단계를 붙이면 원칙 04(조작 마찰 최소화)에 어긋나고,
> 「세션 착수 규칙」 1번이 *"전수 감사는 이 규칙의 몫이 아니다"*라고 이미 정해 두었다.
> **B-104가 예외인 것은 그 크기 때문이다** — 이 대기열에서 가장 큰 작업이고,
> 한 번 저장 형식을 정하면 되돌리기 어렵다(엑셀 왕복까지 실린다).

---

## 7. 2차 확정 (2026.08.04) — 백로그 판정 3건

**1장이 24건을 다룬 뒤 새로 쌓인 판정을 물어 받은 답이다.** 3장의 파생 판정 7건(P-4~P-10)도
같은 자리에서 함께 확정됐고 그쪽은 3장 표가 소유한다.

> **묻는 방식은 3장 머리의 본보기를 따랐다** — 선택지만 나열하지 않고 **현행 동작을 코드로 세어
> 함께 냈다.** 그 실측이 실제로 답을 바꾼 자리가 아래 ⓕ이고, 물음의 모양을 바꾼 자리가 P-5·P-6다.

### 7-1. B-108 — AI 제공자 자동 전환

사용자 요청(2026.08.03)의 확정. **바탕은 이미 서 있으므로 새로 만드는 것은 '전환'이 아니라
'언제·어디로·무엇을 알리며'다**(백로그 행이 그렇게 적었고 실측이 그것을 확인했다).

| 갈래 | 확정 | 근거 |
|---|---|---|
| ⓐ 전환 방아쇠 | **429(요청 한도)와 크레딧 소진을 가른다** — 앞은 재시도, 뒤는 전환 | 한 규칙으로 묶으면 **일시적 혼잡에 키를 갈아치우고 돌아오지 않는다.** 오류 갈래는 이미 `AiErrorKind.RATE_LIMITED`·`QUOTA_EXCEEDED`로 나뉘어 있어 배선만 하면 된다 |
| ⓑ 고지 | **결과에 한 줄** — *"OO 한도로 △△가 이어서 처리했습니다"* | 조용히 바꾸면 사용자는 **자기가 고른 모델의 답인 줄 알고** 다른 회사 글을 받는다(개발 의도 2번) |
| ⓒ 우선순위 저장 위치 | **전역 하나** | 기능별은 나중에 얹을 수 있다. **실측:** 지금 목록은 `sortedBy { createdAt }`(만든 순서)이라 **우선순위라는 개념 자체가 없다** — 정렬 값을 더하는 것이므로 DB 마이그레이션 불요 |
| ⓓ 키 없는 제공자 | **건너뛴다** | 시도 자체가 낭비다. `AiKeyStore`가 아는 사실이므로 순회 전에 거른다 |
| ⓔ 무한 루프 방지 | **한 요청당 최대 3곳 + 한도로 밀린 제공자는 10분 쿨다운** | 쿨다운이 없으면 매 요청이 죽은 키부터 다시 두드린다 |
| ⓕ 답이 덜 나온 상태의 처분 | **필드 단위로 가른다 — 다 나온 필드의 답은 쓰고, 덜 나온 것만 버리고 다시 받는다** | 아래 참조 |

> **ⓕ는 사용자가 낸 제3안이다 — 내가 낸 추천(전부 버리고 다시)보다 낫다.**
> **그리고 실측이 물음의 전제를 정정했다: 스트리밍 경로는 현행 코드에 없다**
> (`ai/` 전체에 `stream` 참조 0건). 응답은 통째로 받으므로 *"반쯤 받은 화면"*은 아직 없다.
> **그런데 답은 버려지지 않는다 — 더 넓고 실재하는 자리에 그대로 맞는다:**
> 한 요청이 **필드 여럿을 담고**(`CharacterFieldAiSuggester.suggest(fields: List<FieldDefinition>)`),
> 그 요청이 한도로 죽으면 지금은 **전부** 버린다. 완결된 필드를 살리는 규칙이 정확히 그 자리다.
> 나중에 스트리밍이 들어와도 **같은 규칙이 그대로 적용된다** — 그래서 이 답은 선행 투자다.

**착수 시 R-23을 함께 볼 것** — *'이 키는 한도에 걸렸다'*도 학습한 사실이라
`hasLearnedFacts()` 부류이고, **그 자리에 등재하지 않으면 초기화 고지에서 빠진다**
(백로그 행의 ⑥이 지목한 것이며, 위 ⓔ의 쿨다운이 바로 그 '학습한 사실'이다).

### 7-2. B-111 — 가져오기 결과의 '갱신 N'이 무엇을 세는가

**확정: 미리보기 쪽에 맞춘다 — '갱신' = 실제로 바뀐 행.**

같은 파일에서 미리보기가 *"변경 3 · 동일 7"*이라 하고 결과가 *"갱신 10"*이라 하던 것을 없앤다.
어느 쪽도 틀린 것이 아니라 **말의 뜻이 안 정해져 있었다** — 사용자가 '실제로 바뀐 행'으로 정했다.

근거: *"아무것도 안 고친 파일을 다시 넣었더니 갱신 10"*은 왕복 멱등 확인(A7)에서
사용자가 어긋남으로 읽는다. **B-101이 미리보기를 정확하게 만든 덕에 배선은 싸다** —
각 범주의 `merge*`가 이미 *"바뀌는가"*를 알고 있다.

**함께 움직이는 것:** `result.updated*`를 세는 자리 전부 · 결과 화면 · `RestoreTallyTest`.

### 7-3. B-106 ⓑ — 작품·세계관 카드 사진의 랜덤 주기

**확정: 캐릭터와 같은 주기로 통일한다**(화면 진입 1회 재추첨 · 그 화면 안에서는 고정).

**실측이 근거를 냈다:** 캐릭터는 B-103이 시드 방식으로 그 주기를 세웠는데,
`NovelAdapter`·`UniverseAdapter`는 **재방출마다 재추첨**이고 저장된 **인덱스**를 쓴다 —
목록이 줄면 `idx % size`가 **다른 그림을 가리킨다.** 통일하면 그 어긋남도 함께 사라진다
(캐릭터 몫에서 이미 없앤 병이다).

> **B-106 ⓐ(경로 정규화 복붙 8곳)는 판정이 아니라 정리 부채라 이 표에 없다** —
> 묻지 않고 착수할 수 있다.

---

## 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.5 | 2026.08.04 | **2차 확정 — 살아 있는 판정 대기가 0건이 됐다.** 파생 판정 **P-4~P-10 일곱 건**(3장)과 새 백로그 판정 **셋**(7장 신설 — B-108 여섯 갈래 · B-111 · B-106 ⓑ)을 한 자리에서 물어 전부 받았다. **문서 제목과 지위에서 날짜·건수를 뺐다** — *"(2026-08-03) 대기 24건"*은 2차가 들어오는 순간 둘 다 낡았고, 이 문서는 이제 **판정 확정 일반의 단일 소스**다. **실측을 함께 낸 것이 세 자리에서 값을 했다**(3장 머리의 본보기가 시킨 것): ⓐ **B-108 ⓕ의 전제였던 스트리밍이 현행 코드에 없다**(`ai/`에 `stream` 0건) — 그래도 답은 버려지지 않고 *한 요청이 담은 필드 여럿*이라는 **더 넓고 실재하는 자리**로 옮겨 붙었다 ⓑ **P-5는 '열'이 아니라 `설정(JSON)` 셀 안**이라 처분이 *열 삭제*가 아니라 *모르는 키를 세어 알리기*가 됐다 ⓒ **P-7은 AI 설정 셋이 전부 앱 전체**라는 실측이 그대로 근거가 됐다. **그리고 ⓕ에서 사용자가 제3안을 냈다** — 2-ㅁ의 `_분리됨`에 이어 **둘째다**(내 추천보다 나은 안이 되물음에서 나온다는 6장의 근거가 또 실증됐다). **P-9·P-10은 값이 아니라 *정하는 순서*로 닫혔다**(측정 뒤 / 조사 뒤) — 근거가 없는 자리에 숫자를 박지 않는 것이 답이며, 그래서 **닫힌 판정이되 슬라이스에 숙제를 남긴다** |
| v1.4 | 2026.08.04 | **4번(B-100 완성도 가중) 구현 완료**(세션 로그 1-bx) — 확정 ㄱ1+ㄱ2·ㄴ1이 전부 코드에 들어갔다. **P-4는 물었으나 회신 전**이라 앱 전체로 세우고 그 사실과 갈아 끼우는 법을 3장 행에 적었다 — **되돌릴 수 있는 쪽을 골랐다는 것이 요점**이다(세계관 단위는 DB 마이그레이션·엑셀 왕복까지 여는 되돌리기 어려운 축이라 회신 없이 지를 자리가 아니다. 착수 규칙 3번). 구현이 확정을 넓힌 것도 함께 적어 둔다: **가중을 얹으려면 '칸의 집합'이 먼저 하나여야 했는데 그것이 열 자리로 갈려 있었다**(규약 R-34로 잠갔다) |
| v1.3 | 2026.08.04 | **4장 순서 강제 둘을 소진 표시**(1번→2번은 B-103 병합으로, 5번=6번은 B-101+B-102 병합으로). 지우지 않고 취소선으로 남기는 이유와 관행을 장 머리에 적었다 — **소진된 강제가 현행처럼 서 있으면 다음 세션이 이미 풀린 제약을 다시 대조한다**(실제로 1번→2번이 그 상태로 하루 서 있었다) |
| v1.2 | 2026.08.03 | **P-3 해소**(사용자 확정 — "같은 주기"). B-103 설계 세션(세션 로그 1-bo)이 **코드를 먼저 실측하고** 물어서 나온 답이다 — **캐릭터 목록은 이미 "화면 진입마다 랜덤"으로 돌고 있었고**(`CharacterListFragment:587`), 따라서 확정 ㄴ3과 현행 코드가 **이미 일치했다.** 남은 파생 판정은 7건이다. **3장 머리에 그 방식을 본보기로 적었다** — 파생 판정은 *"선택지만 나열해 되묻지 말고 현행 동작을 실측해 함께 낼 것"*. 1번 항목의 설계 정본은 **`docs/representative_image_design_2026-08.md`**다 |
| v1.1 | 2026.08.03 | **P-1·P-2 해소**(사용자 확정 — "p1좋다" / "p2맞다"). 그 둘이 *"설계 착수 전에 필요한 것"*이었으므로 **지금 설계 착수를 막는 것은 없다**(남은 8건은 해당 슬라이스에서 정한다). **P-1에 조건이 붙어 6장을 신설했다** — *"해당 작업이 있을 세션에서 더 좋은 기능이나 알고리즘을 만들 수 없을지 조사하고 검토하게"*. **근거는 2-ㅁ에서 실제로 벌어진 일이다**: 처음 낸 ㅁ1은 말이 되기는 했지만 **필요와 반비례하는 안**이었고, 사용자가 되물었기 **때문에** 갈음안이 나왔다 — **되묻지 않았으면 그대로 확정됐을 것이다.** 그래서 B-104 착수 세션은 되묻기를 기다리지 않고 스스로 조사한다. **의무를 B-104에만 붙이고 일반화하지 않은 것은 일부러다** — 매 세션에 조사 단계를 붙이면 원칙 04에 어긋나고 「세션 착수 규칙」 1번이 *"전수 감사는 이 규칙의 몫이 아니다"*를 이미 정했다. B-104가 예외인 것은 **크기와 되돌리기 어려움**(엑셀 왕복까지 실린다) 때문이다 |
| v1.0 | 2026.08.03 | 초기 작성 — 판정 대기 24건 일괄 확정 기록. 사용자 지시로 **기록만** 하고 해석·설계·계획은 다음 세션에 넘긴다. 2-ㅁ은 사용자가 "최고가 뭐냐"고 되물어 **ㅁ1을 철회하고 순환 3층안으로 갈음**(확인 대기 P-1), 8번은 원문에 없는 갈래 기호라 **해석 후 확인 대기**(P-2). 파생 판정 10건과 순서 강제 5건을 함께 등재 |
