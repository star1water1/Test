# 실기기 확인 — 체크 시트

> **체크박스를 끄면서 따라가면 된다.** **461단계** 전수이고, **한 번에 할 수 있는 것은 한 묶음**에 모았다.
> 각 묶음 맨 앞의 **준비물**만 갖추면 그 안은 앉은자리에서 끝난다.
>
> | 표식 | 뜻 |
> |---|---|
> | ⚠️ | 그 묶음에서 **가장 깨지기 쉬운 자리**. 시간이 없으면 이것만 밟아도 된다 |
> | 💰 | **실제로 돈이 나간다**(AI 실호출) |
> | 💀 | **되돌리기 어렵다.** 묶음 머리의 백업 지시를 먼저 따를 것 |
> | ⟳ | 화면을 **눕혔다 세우는** 확인이 들어 있다 |
> | 📏 | **작은 화면 기기**(또는 글꼴 크게)가 필요하다 |
> | `3-141-ㄱ` | 원 절 번호. **`3-144`까지**는 `docs/remaining_work_2026-07.md` 3장에서 그 번호를 찾으면 된다 |
> | `3-149-12` | **`3-145`부터는 워크스루가 낳은 절**이라 그 문서에 없다 — 원문은 `tools/walkthrough/steps.json`의 그 `ref`를 가진 단계이고, 왜 그렇게 정했는가는 그 단계를 등재한 커밋에 있다 |

---

## 먼저 읽을 것 — 이 시트가 종전과 다른 점

**종전 시트는 순서대로 밟으면 스스로를 파괴했다.** 4회차 앞머리에 초기화가 있어서, 위에서부터 내려가면 **5~12회차의 재료가 통째로 사라졌다.** 그래서 순서를 다시 짰다.

| | 종전 | 지금 |
|---|---|---|
| 짜임 | 주제별 14회차 | **준비물별 25묶음** |
| 필드 편집 창 | 6개 회차에 흩어져 **6번 연다** | **S3에서 한 번** |
| 캐릭터 편집 창 | 7개 회차에 흩어짐 | **S5·S6·S7 연속** — 창을 거의 안 나온다 |
| 캐릭터 200명·작품 60개 | 손으로 4~6시간 | **엑셀 시드 왕복 40분**(S4) · 손 클릭 0회 |
| GB급 백업 | 따로 만들어야 함 | **S13의 900장이 저절로 만든다** |
| 대결 판 | 회차마다 다시 돌림 | **후보 10명으로 좁혀 50판 · 2분**(S14) |
| AI 실호출 | 줄마다 따로 = 약 40회 | **약 15회** — 검토 시트 하나에 여러 확인을 얹는다 |
| 전체 백업 생성 | 최대 4회 | **1회**(S23) + 마지막 안전 백업 1회 |
| 파괴적 단계 | 여기저기 · 4회차 앞머리의 초기화가 뒤를 지웠다 | **대부분 S22~S25로.** 1부에 남은 셋은 각 묶음 **맨 끝**에 뒀다(아래 ⚠️) |

**⭐ S2 + S4가 이 시트의 재료를 가장 많이 세운다** — 뼈대·필드 전종과 규모(캐릭터 200명 · 작품 60개 · 값 12종)가 여기서 선다. 그다음이 S13(이미지 900장)과 S21(가져오기 변형 파일)이다.

> **전체는 25묶음 · 약 17시간이다 — 하루에 끝낼 일이 아니다.** 한 묶음이 20~90분이고 그 안은 앉은자리에서 끝난다. **며칠에 나눠 밟는 것을 전제로 짰다.**

### 순서 규칙

**번호 순서대로 밟으면 된다.** 그렇게 짠 것이고, 아래는 *왜 그 순서인가*다.

1. **S1이 맨 앞이다.** 구버전 앱이 깔린 기기는 **나중에 못 만든다.**
2. **S2~S19는 서로 파괴하지 않지만 재료 의존이 있다** — 그래서 번호를 건너뛰지 말 것. `S3 → S6·S7·S15`(필드·등급 체계) · `S4 → S5·S10·S11`(200명·60작품·값 12종) · `S6 → S7 → S8` · `S14 → S15`(쌓은 판) · `S17 → S18`(프로바이더) · `S13 → S19`(이미지)·`S13 → S23`(900장).
3. **묶음 안에서도 위에서 아래로.** 몇 자리는 순서가 곧 확인이다 — `S4`는 확인 한 줄이 준비물보다 앞이고, `S13`은 고정점을 먼저 본 뒤에야 폴더를 손대고, `S8`의 마지막 한 줄은 `S7`의 결과를 날린다.
4. **S20~S23(엑셀·백업)은 S19 다음이다.** 정본 파일에 모든 재료가 실려야 한다.
5. **S24~S25가 맨 끝이다.** 여기서 지운 것은 안 돌아온다.

> ⚠️ **"1부는 파괴가 없다"고 읽지 말 것.** 대부분은 그렇지만 셋은 아니다 — `S5`의 마지막 셋(이미지 **파일**이 지워진다) · `S16`(세계관을 통째로 지웠다 되살린다 · 프리셋 덮어쓰기) · `S8`의 마지막 한 줄(`S7`이 고친 축 이름을 되돌린다). 그 셋은 각 묶음 안에서 **맨 끝**에 뒀고 머리에 백업 지시를 달아 두었다.

### 알려 줄 것

깨진 것만 **`S6-ㄱ-3, 핸들이 한쪽만 움직임`** 정도로 알려 주면 된다. 한 묶음이 다 통과하면 **`S6 통과`** 한 줄이면 그 묶음이 통째로 닫힌다. 못 밟은 것은 **못 밟았다고** 적어 주면 된다 — 통과로 세지 않는 것이 요점이다.

---

## 1부 — 세우기 (파괴 없음)

### S1 · 설치·생존 · 15분 · ⚠️ 필수·맨 앞 · ⟳

**준비물**

- [ ] ⚠️ **구버전 앱이 깔린 기기** — 이것만은 나중에 못 만든다. 지금 지우면 `ㄱ`은 영영 못 본다
- [ ] 💾 구 빌드에서 **전체 백업을 떠서 PC로** 옮겨 둘 것
- [ ] 💾 **API 키를 앱 밖에 메모** — 초기화가 지우지 않지만 기기를 잃으면 끝이다
- [ ] 새 APK

> 여기서 막히면 나머지는 볼 필요가 없다. **52판 동안 아무도 이 답을 모른다.**
>
> **첫 단계를 마치면 곧바로 [설정 → 자동 백업]을 켜 둘 것** — `S25`의 마지막 항목이 *며칠*을 요구하는 유일한 자리라, 오늘 켜 두면 마지막 날 공짜로 회수된다.

**확인**

- [ ] **`0-ㄱ`** ⚠️ **기존 앱이 깔린 기기**에 새 APK를 덮어 설치하고 연다
      → 앱이 뜨고 **기존 데이터가 그대로**다. 튕기면 **여기서 멈추고 알려 줄 것** — DB 마이그레이션 사고이고 가장 위험하다  `3-127`
- [ ] **`0-ㄴ`** 홈 화면·앱 서랍의 아이콘을 본다
      → 빈칸이나 기본 안드로이드 아이콘이 아니다. 작은 크기에서 인물 셋이 다 보인다  `3-55`
- [ ] **`0-ㄷ`** 하단 탭 다섯(홈·세계관·캐릭터·분석·설정)을 차례로 누른다
      → 다섯 다 뜬다. 빈 화면·튕김·무한 로딩이 없다  `—`
- [ ] **`0-ㄹ`** 세계관 → 작품 → 캐릭터를 하나씩 만든다
      → 세 단계가 다 저장되고 목록에 뜬다  `—`
- [ ] **`0-ㅂ`** 앱을 완전히 죽였다 다시 연다
      → 방금 만든 것이 그대로 있다  `—`
- [ ] **`0-ㅅ`** ⚠️ **작품에 속한 캐릭터**의 편집 창을 열고 **이름**과 **커스텀 필드(생일·나이)**를 각각 고친 뒤 **기기를 눕혔다 세운다**
      → **고친 값이 셋 다 그대로 있다.** 이름만 남고 생일·나이가 DB 값으로 되돌아가면 실패 — B-267이 이 자리에서 나왔고(2026.08.19 보고) 같은 날 수리됐다  `3-82`
- [ ] **`3-ㅇ-15`** ⚠️ **구버전에서 올린 기기**로 첫 실행한 뒤, ⓐ 나이가 적힌 캐릭터의 **프로필 나이**와 **연표 시점의 나이**를 견주고 ⓑ 생존여부만 골라 둔 캐릭터(사망·출생 이력이 없는)를 내보내 '캐릭터 상태변화' 시트에 그 캐릭터의 `__alive` 행이 있는지 본다
      → ⓐ **두 나이가 같다**(둘째 줄이 있었다면 첫 실행이 지우고 로그에 남긴다 — 설정 > 로그) ⓑ **`__alive` 행이 있고 값이 고른 것과 맞는다**('불명'을 골랐으면 `unknown`이지 `alive`가 아니다). 종전 마이그레이션은 사망·출생 이력이 있는가만 보고 정해 그 캐릭터를 통째로 건너뛰었고, 플래그가 서면 다시 돌지 않았다  ``
- [ ] **`3-ㅇ-23`** ⚠️ **구버전에서 올린 기기**로 첫 실행한 뒤 ⓐ 내보내 '필드 템플릿' 시트의 **수정일과 생성일**을 견주고 ⓑ 설정 > 로그를 본다
      → ⓐ **수정일이 생성일보다 이르지 않다** ⓑ 무엇을 몇 건 올렸는지 로그에 한 줄 남아 있다(수정일·색 각각). 종전에는 '필드 템플릿' 두 행의 수정일이 생성일보다 **넉 달 이르게** 남아 있었고, 그 열로 정렬한 화면이 조용히 틀린 차례를 냈다  ``

---

### S2 · 씨앗 ① — 뼈대와 필드 전종 · 40분

**준비물**

- [ ] S1 완료. **앱 안에서만 만든다**(엑셀 없이)
- [ ] 세계관 **A · B · 빈 C** 셋 — B는 A와 **같은 키의 필드**를 갖게 한다
- [ ] 작품 2개 · 캐릭터 5명(**무소속 1명 · 동명이인 2명** 포함)
- [ ] 사건 3개 + 사건 필드 6개 · 관계 3개 · 세력 2개(하나는 이름에 쉼표: `기사, 단`)

> **이 묶음과 S4가 이 시트의 재료를 가장 많이 세운다.** 여기가 가장 값이 크다.
>
> ⚠️ **마지막 항목(`12-ㅈ2`)은 AI 키를 넣기 *전에만* 볼 수 있다** — S17에서 키를 등록하면 그 상태를 다시 만들려면 프로바이더를 지워야 한다. 그래서 여기로 앞당겼다.

**확인**

- [ ] **`2-ㄱ-2`** 각 종류로 필드를 하나씩 만든다(글자·숫자·선택·목록)
      → 타입에 맞는 입력칸이 폼에 뜬다  `3-27-2`
- [ ] **`2-ㄱ-3`** **드래그 핸들**로 순서를 바꾼다
      → 핸들로만 움직이고, 롱프레스는 옵션 창만 뜬다  `3-23-1`
- [ ] **`2-ㄱ-4`** 필드 관리의 **보기 정렬**을 기본이 아닌 것으로 바꾼다
      → 목록 순서가 바뀌고 **드래그가 잠긴다**(사유가 보인다)  `3-95`
- [ ] **`2-ㄱ-5`** 정렬을 되돌린다
      → 원래 순서로 돌아오고 드래그가 다시 된다  `3-95-4`
- [ ] **`2-ㄴ-1`** ⚠️ 태그 칸에 `"주인공, 남자"`처럼 **쉼표가 든 값**을 넣고 저장
      → 감싼 따옴표가 보이는 것이 정상. **태그는 둘**이다  `3-139-ㄱ`
- [ ] **`2-ㄴ-4`** 이명 칸에 `"홍길동, 어릴 적 이름"`을 적고 저장
      → 목록 부제·검색에서 **한 이름**으로 잡힌다  `3-139-ㄹ`
- [ ] **`2-ㄴ-5`** 읽기 화면에서 다중값 필드를 본다
      → 칩(또는 글머리)이 값 수만큼이다  `3-104`
- [ ] **`2-ㄷ-1`** 필드 편집 → 고급 → **[전역 기본 필드]** 켜고 저장
      → 고지가 두 수를 갈라 말한다("새로 심음 N곳 · 기존에 연결 M곳")  `3-62-1`
- [ ] **`2-ㄷ-2`** 다른 세계관을 열어 본다
      → 그 필드가 보통 필드로 서 있다  `3-62-2`
- [ ] **`2-ㄷ-3`** ⚠️ **작품이 없는(무소속) 캐릭터**를 연다
      → 전역 필드가 폼에 뜬다(종전에는 빈 폼이었다). 값을 넣고 저장 → 다시 열면 남아 있다  `3-65-2`
- [ ] **`2-ㄷ-5`** 무소속 캐릭터에 전역 필드 값을 넣고 **작품을 지정한다**. 그다음 **값이 없는** 무소속 캐릭터로 같은 것을 한 번 더 한다
      → **값이 있던 쪽**은 보관으로 넘어가거나 넘어간다고 말한다. **값이 없던 쪽**은 조용하다 — 둘을 갈라 보지 않으면 어느 쪽이 나와도 통과가 된다  `3-65-5`
- [ ] **`2-ㄷ-6`** 세계관↔무소속으로 캐릭터를 옮긴다. **① 대상 필드가 빈 경우**와 **② 대상에 이미 값이 있는 경우**를 각각 만들어 둘 다 해 본다
      → ①은 조용히 이어진다. ②는 **덮지 않는다**(기존 값이 그대로다)  `3-98`
- [ ] **`2-ㅁ-1`** 필드 관리 툴바의 **책 아이콘**을 누른다
      → 값 라이브러리가 열리고 위에 목적문이 있다  `3-28-1·2`
- [ ] **`2-ㅁ-2`** ⚠️ 홈에서 정렬을 바꾼 뒤 값 목록으로 들어간다
      → **죽지 않는다**(종전에는 크래시였다)  `3-24`
- [ ] **`2-ㅁ-3`** 값에 표시 라벨·별칭·카테고리를 적는다
      → 통계에서 **표시라벨로** 보이고, 검색에서 **별칭으로도** 찾힌다  `3-28-4`
- [ ] **`12-ㅈ2`** AI 프로바이더를 **지운 상태**에서 각 ✨ 자리를 본다
      → **줄 자체가 안 뜬다**  `3-64-2`
- [ ] **`11-ㄱ3`** 작품 편집에서 이미지 여러 장을 고르되 **하나를 아주 큰 파일**(20MB 초과)로 섞는다
      → 붙은 것만 목록에 들어오고 **"이미지 저장에 실패했습니다"**가 뜬다 — 종전에는 아무 말이 없었다  `3-151`
- [ ] **`11-ㄴ3`** 세계관 편집에서 같은 것을 한다
      → 작품과 **똑같이** 실패를 알린다 — 두 화면이 같은 유틸을 쓰므로 답도 같아야 한다  `3-151`
- [ ] **`11-ㅁ3`** 세계관 목록 ⋮ → **순서 변경**에서 카드를 **연달아 여러 번** 끌어 놓고, 작품 목록에서도 같은 것을 한다
      → 카드가 **제자리에서 튀거나 굳지 않는다**. 모드를 끄고 나갔다 들어와도 **마지막으로 끌어 놓은 차례 그대로**다 — 종전에는 끌 때마다 목록이 다시 방출돼 화면이 굳거나 튕겼다  `R-18`
- [ ] **`2-ㅅ-4`** ⚠️ 값이 들어 있고 값 라이브러리 항목도 있는 필드를 하나 골라 **삭제**를 누른다(먼저 취소해 본다)
      → 확인창이 **입력된 값 N건 · 값 라이브러리 항목 N개 · 상태변화 이력 N건**을 세어 말하고 **"휴지통에 보관되므로 되돌릴 수 있습니다"**를 말한다 — 종전에는 참조 계산 필드가 있을 때만 창이 떴고, 없으면 **아무것도 묻지 않고** 지웠다  `R-4`

---

### S3 · 필드 편집 창 한 벌 · 25분 · ⟳ · 📏 작은 화면 기기

**준비물**

- [ ] S2의 필드들
- [ ] 등급 체계 1개 · **필드가 20개 넘는 프리셋** 1개 · **체형(BWH) 필드** 1개 · **계산 수식 필드** 1개
- [ ] **설명을 적어 둔 필드** 1개(`8-ㄹ-5`가 쓴다)
- [ ] 📏 **작은 기기 또는 글꼴 크게** — 이 묶음의 절반이 *끝까지 스크롤되는가*를 묻는다

> 종전 시트는 **필드 편집 창을 여섯 회차에 걸쳐 여섯 번** 열게 했다. 여기서 한 번에 끝낸다.

**확인**

- [ ] **`2-ㅂ-1`** 수식에 `sqrt(4)`를 넣고 저장
      → "없는 함수입니다: sqrt (쓸 수 있는 함수: …)"가 뜬다  `3-11-1`
- [ ] **`2-ㅂ-2`** `abs (field('키'))`처럼 함수명과 괄호를 **띄워** 저장
      → 잡아낸다  `3-11-2`
- [ ] **`6-ㄹ3`** 등급 체계를 만들어 필드 둘이 참조하게 한다 → 체계에 `SS`를 더한다
      → **둘 다 SS 행이 생긴다.** 입력 폼·통계 선택지에도 SS가 보인다  `3-31-3`
- [ ] **`11-ㄱ`** ⚠️ **필드 편집 창**을 열어 끝까지 스크롤한다
      → 끝까지 간다. **짧은 창이 쓸데없이 커지지 않는다**  `3-50-1·3`
- [ ] **`11-ㄴ`** ⚠️ 그 창을 **가로로 돌린다**
      → 넘치지 않는다  `3-50-4`
- [ ] **`11-ㄷ`** 등급 체계 편집에서 등급을 15줄쯤 더한다
      → 스크롤된다  `3-41-1`
- [ ] **`11-ㄹ`** 필드가 20개 넘는 프리셋의 필드 편집을 연다
      → 끝까지 간다  `3-41-9`
- [ ] **`11-ㄴ2`** 체형 필드 편집 → **파트 연결** 드롭다운
      → 칸 이름에 맞는 기본값이 잡혀 있고, 바꾸면 저장된다  `3-35`
- [ ] **`11-ㄷ2`** 모든 행을 '사용 안 함'으로 두고 저장
      → 사유를 말한다  `3-35-7`
- [ ] **`11-ㄹ2`** 표시할 분석 **토글 이름**과 카드의 항목 이름을 견준다
      → **같다**. 토글을 끄면 그 항목이 사라진다  `3-35-9·10`
- [ ] **`2-ㅂ-5`** 작품 미배정 캐릭터에서 계산 필드를 본다
      → "산출할 수 없다"는 줄이 뜬다. 작품을 고르면 사라진다  `3-92`
- [ ] **`8-ㄹ-1`** 필드 관리 → 체형 필드 → **목표 비율 기준** 스피너
      → 있다. 손대지 않으면 '자동'으로 종전과 같다  `3-114-ㄱ·ㄴ`
- [ ] **`2-ㅅ-1`** ⚠️ **필드 관리(기본 필드)**에서 편집 창을 열고 글을 고친 채 **회전** → 저장
      → 저장이 **실제로 반영**되고 전파 미리보기가 뜬다 — 창이 조용히 닫히고 아무 일도 없으면 결함이다  `3-148-13`
- [ ] **`2-ㅅ-2`** ⚠️ **프리셋 필드 편집·추가** 창을 열고 **회전** → 저장
      → 저장된 프리셋에 반영되고 **통보가 뜬다** — 목록 창이 사라졌어도 입력이 허공으로 가지 않는다  `3-148-14`
- [ ] **`2-ㅅ-3`** 이미 있는 키와 **겹치는 필드키로 저장**해 본다(회전 전·후 각각)
      → 창이 **닫히지 않고** 사유가 뜨며 **입력이 그대로** 남아 있다  `3-148-15`
- [ ] **`5-ㅊ3`** ⟳ 필드 편집 창에서 **등급 표 행을 고치거나 분석 항목을 더한 뒤 회전**한다
      → 짜 놓은 행이 **그대로 있다**(저장본으로 되돌아가지 않는다). 타입을 바꾼 뒤 회전해도 분석 방법 목록이 **새 타입의 것**이다  `R-38`
- [ ] **`5-ㅋ2`** 필드 편집 창에서 종류를 **계산·숫자·체형**으로 바꿔 보고, TEXT에서 **구조화 입력을 켜** 본다
      → **'값 데이터 라이브러리' 섹션이 통째로 사라진다**(사전 등록 칸 · '입력 모드'까지). 종류를 다시 TEXT로 되돌리면 섹션이 돌아오고 **'입력 모드'는 '제안'이다** — 잠자던 '제한'이 되살아나지 않는다  `R-24`
- [ ] **`5-ㅌ2`** 지원 종류(TEXT 등) 필드를 **'값 제한'으로 두고 허용 값은 하나도 없이** 저장한다
      → 저장은 되되 **"허용 값이 없는 상태로 '값 제한' 모드입니다"**를 말한다 — 값 라이브러리 화면의 같은 설정이 이미 하던 그 고지다(막지는 않는다)  `R-18`
- [ ] **`5-ㅍ2`** 계산 필드의 수식에서 **텍스트·선택지·다중 텍스트·체형 필드**를 참조해 본다(예: `field(메모) + 1`). 이어서 숫자·등급·계산 필드만 참조하는 수식도 써 본다
      → 앞쪽에는 **“값이 수로 읽힌다는 보장이 없습니다 — 글자가 들어 있으면 그 자리는 0으로 계산됩니다”**가 뜨고, 뒤쪽에는 **아무 경고도 없다**. 경고는 **막지 않는다**(그대로 저장된다) — 텍스트 칸에 숫자를 적어 두고 쓰는 수식은 오늘도 그대로 돈다  `R-27`

---

### S4 · 씨앗 ② — 엑셀 시드로 규모를 한 번에 만든다 · 40분 (PC 30 + 앱 10)

**준비물**

- [ ] ⚠️ **아래 확인 한 줄(`10-ㅈ`)을 먼저 밟는다** — 부풀리고 나면 다시 못 본다
- [ ] 앱에서 **임시 내보내기 1회**
- [ ] PC에서 그 파일을 불린다 — **세계관 시트**에 붙인다('전체 캐릭터' 시트는 읽기 전용이라 무시된다): **① 코드 칸은 비운다**(앱이 새로 발급한다 — 복사하면 동명이인 오류로 파일 전체가 막힌다) **② 이름에 일련번호**를 붙여 200행 · 작품 60행 **③ 어느 필드 값 12종 이상 · 키를 150~190에 흩기**
- [ ] **④ 숫자 칸에 글자를 넣어 타입 불일치를 셋 만든다 — 캐릭터·사건·작품 시트에 각 1건**(S11이 셋을 다 요구한다)
- [ ] **⑤ 필수 칸이 빈 행** 1개
- [ ] 그 파일을 **가져오기**

> ⚠️ **손으로 만들지 말 것.** 캐릭터 200명·작품 60개·값 12종은 클릭으로 4~6시간인데 엑셀 시드 왕복이면 **40분**이고 손 클릭이 0회다.
>
> ⚠️ **확인 한 줄이 준비물보다 먼저다** — 아래 `10-ㅈ`은 *캐릭터 200명 미만*을 요구하므로 **부풀리기 전에** 밟아야 한다. 종전 시트가 순서로 스스로를 파괴한 것이 정확히 이 모양이었다.

**확인**

- [ ] **`10-ㅈ`** ⚠️ 관계도를 연다(캐릭터 200명 미만)
      → **달라진 것이 없다**  `3-120-ㄱ`

---

### S5 · 캐릭터 편집 ① — 이름·작품·이미지·관계 · 45분 · ⟳

**준비물**

- [ ] 이미지 3장 이상 (**그중 1장은 두 캐릭터가 함께 쓰게** 한다)
- [ ] 이름은행에 미사용 이름 몇 개 + **이미 남이 쓰는 이름** 1개
- [ ] 작품 60개(S4에서 이미 섰다)
- [ ] **보충 탭**에서 인라인 편집을 열 캐릭터 1명(이미지 2장 이상 · 그중 하나를 대표로 지정한다)

> ⚠️ **파일을 지우는 셋(`1-ㅁ`·`1-ㅂ`·`1-ㅅ`)은 이 묶음 맨 끝**에 뒀다 — 앞의 확인들이 그 이미지를 쓴다.

**확인**

- [ ] **`1-ㄱ`** 캐릭터에 이미지를 3장 넣는다
      → 스트립에 들어오고 저장된다  `3-40`
- [ ] **`1-ㄴ`** 썸네일 코너의 **☆**로 대표를 지정한다
      → 목록·생일 배너·통계 순위에서 **그 장**이 쓰인다  `3-91`
- [ ] **`1-ㄷ`** ⚠️ 대표를 지정한 캐릭터를 편집해 아무거나 고치고 저장
      → 대표 지정이 **살아남는다**  `3-91-8`
- [ ] **`1-ㄹ`** ⚠️ 편집 중에 이미지 뷰어로 갔다 뒤로 온다
      → 적던 입력이 그대로다  `3-82-1`
- [ ] **`10-ㄱ`** 이름 줄의 **[은행]**을 누른다
      → 목록이 서고 **성별이 같은 이름이 먼저** 온다  `3-83-1·2`
- [ ] **`10-ㄴ`** ⚠️ 고른 뒤 **화면 이동·회전**을 해 본다
      → 고른 것이 살아남는다  `3-83-4`
- [ ] **`10-ㄷ`** 저장한다
      → 저장 시점에 링크가 걸린다. **취소하면 무해**하다  `3-83-3`
- [ ] **`10-ㄹ`** ⚠️ 이미 남이 쓰는 이름을 고른다
      → **빼앗지 않는다**  `3-83-6`
- [ ] **`10-ㅂ`** 은행 화면의 행을 본다
      → **누가 쓰는지**를 말한다  `3-83-9`
- [ ] **`10-ㅍ`** 관계가 둘 이상인 캐릭터에서 관계를 **드래그**한다
      → 기본 정렬에서는 잡힌다  `3-44-7`
- [ ] **`10-ㅎ`** 정렬을 '이름순'으로 바꾸고 롱프레스
      → **드래그가 잠기고 사유가 화면에 보인다**  `3-44-8·9`
- [ ] **`10-ㄱ2`** ⚠️ 정렬을 오갔다가 기본으로 되돌린다
      → **수동 순서가 살아 있다**(이 자리의 최대 위험)  `3-44-10`
- [ ] **`11-ㅂ`** **S4에서 이미 선 작품 60개** 상태에서 작품 드롭다운을 연다(손으로 만들지 않는다)
      → **살아 있다**(길어도 죽지 않는다)  `3-124-ㄱ`
- [ ] **`1-ㅁ`** ⚠️ 이미지를 롱프레스 → **"앱에서 삭제"**를 고르고 **저장하지 않고** 나간다
      → 파일이 **안 지워진다**(저장해야 지워진다)  `3-52-8`
- [ ] **`1-ㅂ`** 같은 것을 고르고 **저장**한다
      → 파일까지 사라진다. 단, **다른 캐릭터도 쓰는 이미지면 안 지우고 알린다**  `3-52-9·10`
- [ ] **`1-ㅅ`** 캐릭터 편집에서 이미지를 빼고(캐릭터에서만) 이미지 탭 → **뗀 이미지 서랍**
      → 거기 뜬다. **'미배정' 칩에는 안 뜬다**(두 칩에 동시에 잡히면 실패)  `3-52-1·2`
- [ ] **`1-ㄱ2`** ⚠️ **보충 탭**에서 캐릭터를 인라인 편집해 **대표 이미지를 지정** → 화면을 눕혔다 세운 뒤 저장한다
      → 대표 지정이 **살아남는다**(목록·상세·통계·카드가 그 그림을 쓴다). 랜덤 그림으로 돌아가면 **고지 없는 유실**이라 실패다  `3-149-1`
- [ ] **`1-ㄴ2`** 보충 탭 인라인 편집에서 이미지 하나를 **[앱에서 삭제]**로 골라 저장한다 (⚠️ 파일이 실제로 지워진다)
      → 그 파일이 **실제로 사라지고** 결과 고지가 지운 수를 말한다. 아무 일도 안 일어나면(조용한 no-op) 실패다  `3-149-2`
- [ ] **`1-ㅁ2`** 보충 탭에서 **세계관·작품 필터**를 걸고 '전체 캐릭터' 수와 **완료율 막대**를 본다
      → 목록·'보충 대상'과 **같은 범위**로 센다. 손댈 것이 산더미인 작품을 골랐는데 완료율이 90%대면 실패다. **이슈 필터는 완료율을 바꾸지 않는다**  `3-149-3`
- [ ] **`10-ㄴ3`** 캐릭터가 많은 상태에서 **이름은행을 연 첫 순간**을 본다
      → 쓰이고 있는 이름 행에 **'캐릭터를 찾을 수 없음'이 잠깐이라도 뜨지 않는다** — 이름표가 늦으면 '사용됨'까지만 뜨고 곧 이름이 붙는다  `3-149-21`
- [ ] **`1-ㄱ3`** ⚠️ 관계 하나에 **관계 변화를 두어 건 쌓은 뒤** 그 관계를 삭제해 본다(취소). 이어서 관계 변화 편집창의 **[삭제]**를 눌러 본다(취소)
      → 관계 삭제창이 **함께 사라지는 관계 변화 N건**과 **휴지통을 거치지 않는다**는 것을 말한다. 관계 변화 [삭제]도 **바로 지우지 않고** 확인창을 세운다 — 종전에는 저장소에서 이 삭제만 즉시 영구 삭제였다  `R-4`
- [ ] **`1-ㄴ3`** MULTI_TEXT(다중 텍스트) 필드를 **필수**로 지정하고 캐릭터 편집 폼을 연다
      → 그 칸의 힌트가 **`이름 * (쉼표로 구분)`**으로 표식을 단다 — 이 갈래만 표식을 건너뛰어 같은 폼 안에서 필수 표시가 갈렸다  `R-18`
- [ ] **`1-ㅎ2`** **작품을 고르지 않은** 캐릭터를 편집 화면에서 연다. 전역 기본 필드 칸을 탭해 자동완성 드롭다운이 뜨는지 본다(같은 필드에 값이 있는 다른 캐릭터가 필요하다)
      → 값 라이브러리 제안 또는 기존값 제안이 **뜬다**. 종전에는 전역 구역에서 자동완성이 통째로 꺼져 있어 필드는 그려지는데 제안만 없었다  ``
- [ ] **`1-ㅎ3`** 같은 무소속 캐릭터를 **보충(랜덤) 탭**에서 편집 모드로 연다. 이어서 그 캐릭터의 **상세 화면**에서 [상태 변화 추가]를 눌러 필드 스피너를 본다
      → 보충 탭에도 **전역 기본 필드가 그려지고** [✨ AI 추천] 버튼이 보인다(종전에는 구역이 통째로 비었다). 상태 변화 스피너에도 **전역 필드가 함께 뜬다**(종전에는 셋뿐이었다)  `R-33`
- [ ] **`1-ㅎ6`** 엑셀로 **100자가 넘는 이름**의 캐릭터를 들여온 뒤, 그 캐릭터를 열어 **이름은 그대로 두고** 메모만 고쳐 저장한다
      → 저장된다. 종전에는 «이름이 너무 깁니다»로 막혀, 이름을 줄이기 전에는 그 캐릭터의 어떤 칸도 고칠 수 없었다(그리고 문구는 왜 막혔는지 말하지 않았다)  ``
- [ ] **`1-ㅎ7`** 캐릭터 편집에서 이미지를 **여러 장 한 번에** 고르고, 붙는 도중에 기기를 돌린다
      → 고른 이미지가 **하나도 사라지지 않는다** — 폼이 못 받은 몫은 «이미지 N장을 … 미배정으로 담아 두었습니다» 고지와 함께 **이미지 탭에 미배정으로** 들어가 있다. 종전에는 나머지가 말없이 버려지고 파일만 고아로 남았다  ``

---

### S6 · 캐릭터 편집 ② — 🧍 실루엣과 🎲 생성기 · 40분 · ⟳ · ⭐ 가성비 1위

**준비물**

- [ ] BWH 필드 + **한 부위를 비워 둔** 캐릭터 1명
- [ ] 같은 작품에 수치를 가진 캐릭터 **2명 이상**
- [ ] **키 칸이 비어 있는** 캐릭터 1명 — 실루엣 편집기가 그 칸을 건드리지 않는지 본다
- [ ] **밑가슴처럼 다른 부위에서 파생되는 행**이 있는 BWH 필드

> ⭐ **준비가 3분인데 17단계가 열린다** — 시트 전체에서 가성비가 가장 높다. 이어지는 S7·S8까지 합치면 **41단계가 이 준비 하나에 걸려 있다.** 원 문서가 스스로 *"가장 큰 미검증 면"*이라 적어 둔 자리이고, **렌더·터치·동기가 전부 여기서 처음 증명된다.**

**확인**

- [ ] **`8-ㄱ-1`** 캐릭터 편집에서 BODY_SIZE 필드(예: `BWH`) 라벨 줄을 본다
      → **🧍와 🎲**가 있다  `3-125-1`
- [ ] **`8-ㄱ-2`** ⚠️ 🧍를 눌러 편집기를 연다
      → 실루엣이 그려지고 어깨·가슴·허리·엉덩이에 **동그란 핸들**이 있다. 값 없는 부위는 **점선 + 흐린 핸들**  `3-125-2`
- [ ] **`8-ㄱ-3`** ⚠️ 핸들 하나를 좌우로 끈다
      → **좌우가 함께** 움직이고 수치가 따라온다  `3-125-3`
- [ ] **`8-ㄱ-4`** 수치 칸에 숫자를 직접 적는다
      → **그림과 슬라이더가 따라온다**(반대 방향도 같다)  `3-125-4`
- [ ] **`8-ㄱ-5`** 허리에 `30` 같은 극단값을 적는다
      → **입력은 그대로 남고** "그림의 표현 한계입니다" 캡션 + 경고색 핸들이 뜬다  `3-125-5`
- [ ] **`8-ㄱ-6`** 마음껏 끌고 굴린 뒤 **[취소]**로 닫는다
      → **아무것도 안 바뀐다**  `3-125-10`
- [ ] **`8-ㄱ-7`** **[적용]**을 누른다
      → BWH 칸에 값이 들어가고 "실루엣 수치를 폼에 적었습니다"가 뜬다  `3-125-11`
- [ ] **`8-ㄱ-8`** 열자마자 [적용]을 본다
      → **회색(비활성)**이다  `3-125-12`
- [ ] **`8-ㄱ-9`** ⚠️ 끌어 만든 뒤 **기기를 돌린다**
      → 편집기가 그대로 뜨고 **수치가 살아 있다.** [적용]이 켜져 있다  `3-133-ㄱ`
- [ ] **`8-ㄱ-10`** ⚠️ 회전 뒤 [적용]
      → **회전 전과 같은 칸에 같은 값**이 적힌다. 안 만진 빈 칸은 여전히 비어 있다  `3-133-ㄴ`
- [ ] **`8-ㄱ-11`** 같은 작품에 수치를 가진 캐릭터가 둘 이상인 상태에서 비교 대상을 고르고 회전
      → **그 캐릭터가 다시 골라져 있다.** 작품 평균 오버레이도 켜진 채다  `3-133-ㄹ`
- [ ] **`8-ㄴ-1`** 생성 패널에서 **몸통·가슴·힙** 세 축을 고른다
      → 셋 다 고를 수 있다  `3-125-6`
- [ ] **`8-ㄴ-3`** **프리셋**(슬렌더/내추럴/아워글래스/글래머)을 누른다
      → 세 축 선택이 함께 바뀐다  `3-125-8`
- [ ] **`8-ㄴ-4`** 두 번 굴린 뒤 **[직전으로]**
      → **바로 앞 결과**로 돌아간다  `3-125-9`
- [ ] **`8-ㄴ-5`** ⚠️ 패널을 펼치고 축을 고른 상태에서 **회전**한다
      → 펼쳐진 채이고 **고른 축 넷과 컵 크기가 그대로**다. [직전으로]도 살아 있다  `3-133-ㄷ`
- [ ] **`8-ㄴ-6`** 컵 지정을 켠다
      → 가슴 축이 **잠긴다**(흐려진다)  `3-133-ㄷ`
- [ ] **`8-ㄱ-12`** **키 칸이 빈** 캐릭터에서 실루엣 편집기를 연다 → 열자마자 [적용]을 보고, **부위 슬라이더만** 만져 적용한다
      → 열자마자는 **회색(비활성)**이고, 적용 뒤에도 **키 칸이 여전히 비어 있다**. 165 같은 초안 값이 들어가 있으면 적은 적 없는 값이 저장된 것이라 실패다  `3-149-27`
- [ ] **`8-ㄴ-7`** 두 번 굴린 뒤 **[직전으로]** → **다른 칸 하나만** 더 만지고 [적용]
      → 굴리기가 채웠던 **가슴·허리·엉덩이가 다시 들어가지 않는다**(비어 있던 칸은 비어 있는 채다). 되돌리기가 끝난 것처럼 보인 뒤 값이 살아 들어오면 실패다  `3-149-28`
- [ ] **`8-ㄱ-13`** **밑가슴처럼 다른 부위에서 파생되는 행**의 슬라이더를 움직인다(수치 칸 쪽에서도 해 본다)
      → **같은 행의 슬라이더와 수치 칸이 같은 수**를 말하고 다른 행도 함께 갱신된다. 한 행 안에서 둘이 어긋나면 실패다  `3-149-29`
- [ ] **`8-ㄱ-14`** 실루엣 편집기에서 핸들 하나를 **길게 천천히** 끌어 본다
      → 수치와 그림이 손가락을 **끊김 없이** 따라온다. 종전에는 프레임마다 몸 전체 기하를 새로 지어 끄는 동안 걸림이 났다  ``

---

### S7 · 캐릭터 편집 ③ — 축·프리셋 편집 창 · 25분 · ⟳

**준비물**

- [ ] S6 그대로 — **창을 안 나온다**
- [ ] 다른 세계관의 캐릭터 1명(마지막 항목용)

> S6에서 이어서 바로 한다. 🎲 패널 맨 아래 [축·프리셋 편집]을 열면 끝날 때까지 이 창 안이다.

**확인**

- [ ] **`8-ㄷ-1`** 🎲 패널 맨 아래 **[축·프리셋 편집]**
      → 창이 뜬다  `3-113-ㄱ`
- [ ] **`8-ㄷ-2`** 몸통 축 이름을 바꾸고 저장
      → **창을 닫자마자** 그 이름으로 선다  `3-113-ㄴ`
- [ ] **`8-ㄷ-5`** 다른 세계관의 캐릭터를 연다
      → 그쪽은 **옛 기본값 그대로**다(세계관마다 다르다)  `3-113-ㅁ`
- [ ] **`8-ㄷ-6`** ⚠️ **힙 축 구간을 좁게** 적는다
      → **적자마자**(저장 전에) 붉은 줄이 뜨고 문구가 **그 축 이름**을 말한다  `3-135-ㄴ`
- [ ] **`8-ㄷ-7`** 값을 다시 벌린다
      → **곧바로 사라진다**  `3-135-ㄴ`
- [ ] **`8-ㄷ-8`** ⚠️ 붉은 줄이 뜬 채로 **[저장]**
      → **막지 않는다** — 닫히고 저장된다. 다시 열면 값도 붉은 줄도 그대로다  `3-135-ㄷ`
- [ ] **`8-ㄷ-10`** ⚠️ 값을 적다가 **기기를 돌린다**
      → **적던 것이 남아 있다.** 마저 적고 [저장]하면 닫힌다  `3-140-ㄱ·ㄴ`
- [ ] **`8-ㄷ-11`** 두세 번 연속 돌린다
      → 같다  `3-140-ㄷ`
- [ ] **`8-ㄷ-12`** **[기본값으로]**를 누른다
      → **창은 안 닫히고** 칸만 되돌아간다  `3-140-ㄹ·3-113-ㅅ`
- [ ] **`8-ㄷ-13`** 이름을 비우거나 구간 끝을 앞 축보다 작게 적고 저장
      → 사유를 말한다  `3-113-ㅂ`
- [ ] **`8-ㄷ-14`** 축을 고친 뒤 필드 관리 → 그 체형 필드를 연다
      → **축이 안 지워진다**  `3-113-ㅈ`

---

### S8 · 캐릭터 상세 · 체형 카드 · 35분

**준비물**

- [ ] S6·S7의 결과(적용해 저장해 둔 수치)
- [ ] **다크 모드**로 한 번 볼 것
- [ ] **같은 작품에 체형을 적은 캐릭터가 자기 하나뿐인** 캐릭터 1명(`8-ㄹ-4`·`8-ㄹ-11`이 함께 쓴다)
- [ ] 타입이 **'복수 텍스트'**인 필드에 값 2개 이상을 적어 둔 캐릭터 1명

> ⚠️ **맨 끝의 [기본값 복원](`8-ㄹ-7`)이 S7에서 고친 축 이름을 날린다** — 그래서 마지막에 뒀다.

**확인**

- [ ] **`1-ㅇ`** 캐릭터 상세의 **체형 분석 카드**를 본다(BWH를 채운 캐릭터로)
      → 왼쪽에 실루엣, 오른쪽에 요약 한 줄. **BWH 중 하나만 채워도 카드가 뜬다**  `3-34`
- [ ] **`1-ㅈ`** 그 카드의 그림을 누른다 → **크게 보기**
      → 전체 화면으로 열리고 [측면]·[라벨]이 동작한다  `3-34-5·6`
- [ ] **`1-ㅋ`** **다크 모드**로 바꿔 같은 화면을 본다
      → 그림·칩·막대가 읽힌다  `3-34-12`
- [ ] **`8-ㄷ-4`** 적용해 저장한 뒤 캐릭터 상세의 체형 카드를 본다
      → **같은 말**을 한다  `3-113-ㄹ`
- [ ] **`8-ㄹ-2`** 카드의 자세히 ▾ → 목표 비율에서 **즉석 전환**
      → 된다  `3-114-ㄷ`
- [ ] **`8-ㄹ-3`** 같은 작품에 체형을 적은 캐릭터가 둘 이상일 때 **작품 평균**
      → 재료가 있을 때만 선다  `3-114-ㄹ`
- [ ] **`8-ㄹ-4`** 목표 비율 기준을 **'작품 평균'**으로 두고, **같은 작품에 체형을 적은 캐릭터가 자기 하나뿐인** 캐릭터를 연다(재료가 없는 상태를 일부러 만든다)
      → **조용히 넘기지 않는다** — 못 쓴다고 말하거나 자동으로 되돌린다  `3-114-ㅁ`
- [ ] **`8-ㄹ-6`** 체형 설정의 **기본 체형 이름 칸**을 본다
      → 있고, 저장된 값이 채워져 있다. 바꾸면 **카드에 나온다**  `3-89-1·2·3`
- [ ] **`8-ㄹ-7`** 그 칸을 비운다
      → 기본값으로 돌아간다. **[기본값 복원]**도 이 칸을 되돌린다  `3-89-5·6`
- [ ] **`1-ㄷ2`** 캐릭터 상세 **읽기 화면**에서 타입이 **'복수 텍스트'**인 필드를 본다(쉼표 목록·글머리 목록이 아니라)
      → 칩(또는 글머리)이 **값 수만큼** 서고 표시 라벨·별칭이 통계·자동완성과 같은 글자다. 한 덩어리 글자로 붙어 있으면 실패다  `3-149-4`
- [ ] **`1-ㄹ2`** 실루엣 **크게 보기**를 열어 둔 채 화면을 눕혔다 세우고, 다크 모드로도 바꿔 본다
      → 그림이 **그대로 있다**. 창은 떠 있는데 안이 **빈 화면**이면 실패다. '작품 평균' 오버레이 토글도 그대로다  `3-149-5`
- [ ] **`8-ㄹ-11`** 같은 작품에 체형을 적은 캐릭터가 **자기 하나뿐**인 캐릭터에서 목표 비율 기준을 '작품 평균'으로 둔다
      → '작품 평균' 칩이 **아예 안 뜨거나** 못 쓴다고 먼저 말한다. **편차 0.0% · 점수 100**이 뜨면 자기 대 자기를 잰 것이라 실패다  `3-149-30`
- [ ] **`1-ㅎ4`** ⚠️ 커스텀 필드에 상태 변화를 하나 만든 뒤 그 캐릭터를 **작품 미배정**으로 옮긴다. 상세에서 그 행을 눌러 [수정]을 열고 연도만 고쳐 저장한다. 이어서 [삭제]도 눌러 본다
      → 필드 스피너가 **'출생'이 아니라 원래 필드**를 가리킨다('… (지금 이 캐릭터에 없는 필드)'로 적힌다). 저장해도 fieldKey가 바뀌지 않는다 — 종전에는 조용히 `__birth`로 뒤바뀌었다. 창 문구도 내부 키가 아니라 **필드 이름**이고, [삭제]는 **한 번 더 묻는다**  `R-4`

---

### S9 · 캐릭터 목록 · 전역 검색 · 어시스턴트 · 25분 · ⟳

**준비물**

- [ ] 캐릭터 3명 이상 · 필수로 지정한 필드 + **그것을 비운 캐릭터**
- [ ] 프리셋을 **21개**까지 만들 각오(이름만 바꿔 반복 저장하면 빠르다)

> 프리셋 만들기(`10-ㅁ2`·`10-ㅅ2`)를 먼저 하고 **덮어쓰기 확인(`10-ㅂ2`)을 뒤에** 둔다.

**확인**

- [ ] **`1-ㅌ`** 캐릭터 여럿을 롱프레스로 고른다
      → 선택 바에 **[비교하기]와 [작업]이 둘 다** 보인다. 4명 이상 고르면 이유를 말한다  `3-48`
- [ ] **`1-ㅍ`** ⚠️ 3명쯤 고른 상태에서 **화면을 돌린다**
      → 선택 모드와 고른 것이 살아남는다  `3-48-7`
- [ ] **`10-ㄴ2`** ⚠️ 전역 검색 → 필터에서 **세계관 A의 필드값**을 건다
      → **세계관 B의 캐릭터도 나온다**(크로스-세계관)  `3-107-3`
- [ ] **`10-ㄷ2`** B에서 **별칭으로만** 가진 값이 있는지 본다
      → 그것도 걸린다  `3-107-4`
- [ ] **`10-ㄹ2`** 칩의 **✕**를 누른다
      → 필터가 **완전히** 사라진다(하나만 남으면 실패)  `3-107-6`
- [ ] **`10-ㅁ2`** 프리셋 이름을 **비운 채** 저장한다
      → 창이 안 닫히고 사유가 뜬다. 글자를 치면 오류가 스스로 사라진다  `3-107`
- [ ] **`10-ㅅ2`** 프리셋을 21개 만든다
      → "권장 개수(20개)를 넘었습니다"가 뜬다  `3-107`
- [ ] **`10-ㅂ2`** ⚠️ 이미 있는 이름으로 프리셋을 저장한다(캐릭터 탭 · 전역 검색 **둘 다**)
      → **덮어쓸지 묻는다**  `3-128-ㄱ·ㄴ`
- [ ] **`10-ㅇ2`** 어시스턴트 탭을 연다
      → 정렬 버튼에 '심각도순'. **정렬을 바꿔도 카드가 사라지거나 늘지 않는다**  `3-44-1·2`
- [ ] **`10-ㅈ2`** ⚠️ 필수 칸을 비운 캐릭터를 만든다 → 어시스턴트
      → **필수 빈 칸 카드**가 뜨고 숫자가 맞다. 다 채우면 사라진다  `3-51`
- [ ] **`10-ㅋ2`** 필수를 켠 캐릭터를 **빈 채로 저장**한다
      → **막히지 않는다**(경고이지 차단이 아니다)  `3-51-8`
- [ ] **`14-ㄷ`** ⚠️ 어시스턴트에서 카드 하나를 ⋮ → **숨기기** → 뜨는 〈숨겼습니다〉의 **[실행취소]**를 누른다
      → 카드가 **돌아온다**. 문구만 바꾼 판이었으므로 되돌리기가 깨졌으면 **별건의 결함**이다  `3-18-2⑤`
- [ ] **`14-ㄹ`** 카드가 가리키는 캐릭터를 **지운 뒤** 그 카드의 실행 버튼을 누른다
      → **죽지 않고 사유를 말한다**(없어진 대상을 가리키는 카드의 가드)  `3-18-2③`
- [ ] **`1-ㅎ`** 사건에 엮이고 이미지가 있는 캐릭터를 **단건 삭제**해 보고(취소), 같은 캐릭터를 **선택 모드로 일괄 삭제**해 본다(취소)
      → 단건 확인 창에 **'사건 연계 N건'** 줄이, 일괄 확인 창에 **'이미지 N장'**이 **둘 다** 있다 — 두 창의 고지 범위가 같다  `3-148-12`
- [ ] **`10-ㅌ2`** ⚠️ 캐릭터 목록에서 **순서 변경**으로 두어 번 끌어 놓은 뒤 모드를 끄고, 방금 옮긴 캐릭터의 **이름·핀·이미지**를 본다
      → 끌어 놓은 차례가 남고 **이름·핀·대표 이미지가 옛것으로 되돌아가지 않는다** — 순서 저장은 순서 열만 쓴다  `R-18`
- [ ] **`10-ㅌ3`** 전역 검색 → **필드 필터 시트**에서 세계관과 필드를 **빠르게 여러 번** 갈아 대고 마지막으로 고른 필드의 값 칩을 본다
      → 칩이 **마지막에 고른 필드의 값**이다 — 종전에는 늦게 온 조회가 칩을 덮어 **다른 필드의 값이 붙은 필터**가 저장될 수 있었다(쌍둥이 시트는 이미 막아 둔 자리다)  `R-18`
- [ ] **`1-ㅎ5`** 캐릭터 목록에서 **작품 미배정 캐릭터만** 골라 일괄 [필드값 설정]을 연다
      → 스피너에 **«전역 구역 (작품 미배정)»**이 뜨고 전역 필드를 고를 수 있다. 적용하면 그 캐릭터들에게만 값이 들어간다. 종전에는 스피너가 비고 버튼이 죽은 채 **이유를 말하지 않았다**  ``

---

### S10 · 통계 · 패턴 · 교차분석 · 35분 · 📏 작은 화면 기기

**준비물**

- [ ] S4의 규모(값 12종 · 키 분포 · 캐릭터 200명)
- [ ] 값 라이브러리에 적어 둔 표시라벨(S2)
- [ ] 세력(S2)
- [ ] **이름이 똑같은 세력 둘** — 각각에 캐릭터를 넣는다(S2의 세력 하나를 개명하면 된다)

> ⚠️ **`5-ㅌ`('통계에 포함' 끄기)은 확인 즉시 되돌릴 것** — 안 되돌리면 S11과 AI 묶음이 어긋난 상태로 돈다.

**확인**

- [ ] **`5-ㄱ`** 분석 탭을 연다
      → 차트가 뜬다. 숫자만 나열돼 있지 않다  `—`
- [ ] **`5-ㄴ`** ⚠️ 값 종류가 12개 이상인 필드의 카드를 본다
      → 표의 **비율 합이 100%**이고, 마지막에 **'기타 N종'** 행이 있다  `3-10-4`
- [ ] **`5-ㄷ`** ⚠️ **'기타' 조각을 탭한다**
      → 잘린 값을 가진 캐릭터 목록이 뜬다(**0명이면 실패**)  `3-10-4`
- [ ] **`5-ㄹ`** ⚠️ 수치 필드(키·나이)의 파이 조각을 탭한다
      → **그 구간의 캐릭터**가 뜬다(종전에는 항상 0명이었다)  `3-111-ㄴ`
- [ ] **`5-ㅁ`** 수치 요약 아래 **히스토그램 막대**를 누른다
      → 눌린다(종전에는 탭이 꺼져 있었다)  `3-111-ㄷ`
- [ ] **`5-ㅂ`** 값이 많은 수치 필드를 본다
      → 조각이 **구간(150~158 꼴)**으로 묶이고, **"…수치 구간으로 접었습니다"** 고지가 카드 안에 있다  `3-130-ㄱ`
- [ ] **`5-ㅅ`** 사용자 구간을 사이가 비게 설정한다
      → **'구간 밖' 막대**가 뜨고, 눌러서 목록이 뜬다  `3-130-ㄴ`
- [ ] **`5-ㅈ`** **교차 분석**을 연다
      → 표가 사람이 읽을 크기다. **합이 맞고**, 아래 차트의 막대 수가 표의 행 수와 같다  `3-119-ㅅ·ㅇ`
- [ ] **`5-ㅊ`** 사건 필드를 축으로 교차 분석을 돌린다
      → 표가 나오고 제목 앞에 **"사건 ·"**이 붙는다  `3-6-1`
- [ ] **`5-ㅋ`** 사건 필드 드릴다운 → 행을 탭한다
      → **연표 탭이 열리고 그 사건의 연도로** 간다  `3-9-6·7`
- [ ] **`5-ㅍ`** 통계 → 감지된 패턴 → **설정**
      → 유형 여섯 줄이 뜨고, 값이 `60·35·5·50·100·3`이다. **작은 기기에서 끝까지 스크롤된다**  `3-110`
- [ ] **`5-ㄱ2`** 값을 비우거나 범위 밖(9999)을 적고 확인
      → 창이 안 닫히고 **틀린 칸 전부에** 오류가 붙는다  `3-110`
- [ ] **`5-ㅌ`** ⚠️ 필드 편집에서 **'통계에 포함'을 끈다**
      → 그 필드가 통계·패턴 카드·어시스턴트에서 **다 빠진다**. 단 **완성도 목록에는 남는다**  `3-10-1·2`
- [ ] **`5-ㅂ2`** 통계 → **데이터 현황** → '그룹별 필드 완성도' 카드 오른쪽의 **가중 배수 설정**을 연다. 배수를 3.0으로 바꾼다
      → 카드 위 안내줄이 **"필수 칸을 3.0배로 세고 있습니다"**처럼 바꾼 배수를 말한다  `3-54`
- [ ] **`10-ㅅ`** 세력을 만들고 캐릭터를 넣는다 → 통계·세력 관리의 수를 본다
      → 두 화면의 수가 같다  `3-117-ㅊ`
- [ ] **`10-ㅇ`** 세력이나 태그 이름에 **쉼표**를 넣는다(`기사, 단`)
      → 한 값으로 산다  `3-117-ㅈ`
- [ ] **`14-ㄱ`** ⚠️ 교차 분석을 연다. **표를 만들기 전에** 목적문이 보이는가
      → 필터 칸 위와 축 선택 위에 목적문이 **고르기 전에** 있다(원문이 *"이번 변경의 핵심"*이라 적은 자리다)  `3-21-3`
- [ ] **`14-ㄴ`** ⚠️ **캐릭터 필드만 2개 이상 · 사건 필드는 2개 미만**인 세계관에서 교차 분석을 연다
      → 축 선택기와 **그 목적문이 함께** 사라진다. **라벨만 남거나 설명만 남으면 결함이다**  `3-21-4`
- [ ] **`5-ㅅ2`** '그룹별 필드 완성도'의 **가중 배수를 바꾼 뒤 화면을 나가지 않고** 카드의 %와 안내줄을 함께 본다
      → 둘이 **같은 배수**로 선다. 안내줄만 바뀌고 %가 그대로면 다른 자로 잰 숫자에 다른 이름표를 단 것이라 실패다  `3-149-19`
- [ ] **`10-ㄱ3`** **이름이 똑같은 세력 둘**을 만들어 각각에 캐릭터를 넣고 통계의 '소속 멤버' 수를 본다
      → 두 세력이 **각각** 세어져 합이 세력 관리 화면의 합과 같다. 한쪽이 통째로 빠지면 실패다  `3-149-20`
- [ ] **`5-ㅎ2`** 캐릭터 상세 통계에서 **태그가 11종 이상**인 캐릭터를 연다. 파이 조각의 비율과 '기타 N종' 조각을 본다
      → 상위 10종 + **'기타 N종'** 조각이 함께 서고, **각 조각의 비율 합이 100%**다. 종전에는 상위 10종끼리 100%가 되어 실제보다 부풀었고 나머지는 조각도 범례도 없이 사라졌다  `R-14`
- [ ] **`5-ㅎ3`** **제목이 같은 작품 둘**을 서로 다른 세계관에 두고 각각 캐릭터를 넣는다. 캐릭터 상세 통계의 '작품별 캐릭터 수' 막대를 본다
      → 막대가 **둘** 서고 라벨이 `외전 (동방)`처럼 **세계관명으로 갈린다**. 종전에는 한 막대만 서고 한쪽 작품의 인원이 통째로 사라졌다. 사건 상세의 '작품별 사건 수'도 같다  `R-20`
- [ ] **`5-ㅎ4`** 이름뱅크에 이름을 여럿 넣고 일부만 캐릭터에 쓴 뒤, 통계 탭에서 **작품 필터**를 특정 작품으로 바꾼다. 이름뱅크 상세를 연다
      → 사용률이 **100%가 되지 않고** 필터 없을 때와 같은 값이다. 미사용 이름 목록도 그대로 보인다. 화면 아래에 **'이 범위가 쓴 이름 N개'** 카드가 새로 서고, 이름뱅크가 전역 목록이라는 한 줄이 붙는다  ``
- [ ] **`5-ㅎ5`** 세계관에 캐릭터를 둘 만들고 필드값을 채운 뒤, 그중 하나의 작품을 '없음'으로 바꾼다(값 보존 고지가 뜬다). 통계 → 필드 인사이트에서 그 필드 카드를 본다
      → 채움률이 **100%를 넘지 않는다**(종전에는 `3 / 2 (150%)`처럼 떴다). 분포에 보관 값이 함께 실리면 카드에 **'아래 분포에는 이 모수 밖의 보관 값도 함께 실립니다'** 한 줄이 붙는다  `R-34`
- [ ] **`5-ㅎ6`** 이름뱅크에 첫 글자가 **16종 이상**이 되게 이름을 넣고 이름뱅크 상세의 '첫 글자 분포' 막대를 본다. 세력에 **겸직 캐릭터**를 만든 뒤 통계 메인의 세력 요약 줄도 본다
      → 첫 글자 막대 아래에 **'상위 15종만 그렸습니다 — 나머지 N종 M건은 차트에 없습니다'**가 뜬다. 세력 요약의 **'소속 멤버 N명' + '미소속 M명'이 캐릭터 총원을 넘지 않는다**  `R-14`
- [ ] **`10-ㅎ3`** 이름 제안 시트에서 [담기]를 **빠르게 두 번** 탭한다
      → 같은 이름이 **한 벌만** 들어간다(담는 동안 버튼이 내려간다). 종전에는 두 회차가 서로의 삽입을 못 보고 같은 이름을 두 벌 넣었다  ``
- [ ] **`10-ㅎ4`** 세력 관리에서 세력 하나의 **이름·색을 고친 뒤**, 목록으로 돌아가 **다른 세력을 끌어 순서를 바꾼다**
      → 방금 고친 이름·색이 **그대로다**. 종전에는 순서 저장이 엔티티를 통째로 되써서, 화면이 들고 있던 옛 값으로 조용히 되감겼다  ``

---

### S11 · 건강도 · 관계도 · 30분 · ⟳

**준비물**

- [ ] 타입이 안 맞는 값 **3건**(캐릭터·**사건**·작품 각각 — S4에서 심어 둔다)
- [ ] 캐릭터 200명 이상(S4)
- [ ] 스톱워치(마지막 측정 한 줄)
- [ ] **세력 하나에 인물이 몰린 상태**와 **서로 이어진 인물이 없는 세력** 하나 — `10-ㄹ3`이 둘 다 쓴다(S4의 200명이면 접힘 고지도 함께 선다)

> 관계도를 여는 김에 **측정 하나(`13-ㄷ`)를 함께 끝낸다** — 따로 열 이유가 없다.

**확인**

- [ ] **`5-ㅇ`** ⚠️ 캐릭터 50명이 넘는 세계관에서 건강도 상세를 연다
      → 접히고 **접었다고 말한다**. **[더 보기]**로 한 묶음씩 붙는다  `3-119-ㄴ·ㄷ`
- [ ] **`5-ㄴ2`** ⚠️ **데이터 건강도 상세**를 연다
      → 맨 위에 **'타입이 안 맞는 값'** 구역, 맨 아래에 **'입력 현황'** 구역이 갈려 있다  `3-112-ㄱ·ㅂ`
- [ ] **`5-ㄷ2`** ⚠️ 타입이 안 맞는 값 줄을 **누른다**
      → **그 값을 고칠 자리가 열리고 그 칸이 잡힌다**(캐릭터·사건·작품 셋 다)  `3-134`
- [ ] **`5-ㄹ2`** 사건 줄을 누른 상태에서 **화면을 돌린다**
      → 시트가 **하나만** 있고 초점이 안 튄다  `3-134-ㄹ`
- [ ] **`5-ㅁ2`** 관계 상세의 **고립 캐릭터 목록**을 본다
      → 접히고 더 보기가 있다  `3-119-ㄹ`
- [ ] **`10-ㅊ`** ⚠️ 200명이 넘게 만든다(또는 필터를 넓힌다)
      → 접히고 **접었다고 말한다**. 확대·축소가 뻑뻑하지 않다  `3-120-ㄴ·ㄷ`
- [ ] **`10-ㅋ`** 시점 슬라이더를 끈다
      → 접힌 인물이 되살아나지 않는다  `3-120-ㅁ`
- [ ] **`10-ㅌ`** 작품·세력으로 필터를 좁힌다
      → 접힌 인물이 나온다  `3-120-ㅂ`
- [ ] **`13-ㄷ`** **캐릭터 200명** 상태에서 관계도를 열고 확대·축소·이동을 30초쯤 한다
      → **손가락을 놓친 채 멈추는 구간이 없다.** 있으면 그 배율·조작을 적어 준다  `**B-214**`
- [ ] **`10-ㄷ3`** 관계도에서 **'좁히기'가 꺼진 채**(기본) 세력 칩을 눌러 본다
      → 노드가 **한 명도 줄지 않고** 강조/흐림만 바뀐다. 접힘 고지도 실제로 좁히는 축(작품·관계 유형)만 말하고 **세력으로 좁히려면 '좁히기'를 켜라**고 알려 준다  `3-149-22`
- [ ] **`10-ㄹ3`** 세력 칩 줄의 **'좁히기'를 켜고** 세력 하나를 고른다 → 인물이 많아 접힘 고지가 뜨던 상태에서도 해 본다 → 마지막으로 **서로 이어진 인물이 없는 세력**을 골라 본다
      → ① 고른 세력 밖 인물이 **그래프에서 빠지고** 남은 인물은 흐려지지 않는다 ② 접힘 고지가 **세력도 좁힘 축이라고** 말하고, 좁히면 접혀 있던 그 세력 인물이 나온다 ③ 비었을 때 *"선택한 유형의 관계가 없습니다"*가 아니라 **세력 때문이라고** 말한다. 칩을 끄면 종전대로 강조로 돌아온다  `3-149-40`
- [ ] **`10-ㅍ2`** ⚠️ 멤버와 세력 간 관계가 있는 세력을 **삭제**해 본다(취소)
      → 창이 **소속 기록 N건 · 세력 간 관계 N건 · 자동 관계 N건**을 세어 말하고 휴지통 보관도 말한다. **선택지 둘(자동 관계 함께 삭제 / 유지)이 그대로 보이고** 고른 대로 동작한다 — 형제 여섯 창 중 이 창만 규모를 말하지 않았다  `R-4`
- [ ] **`10-ㅎ2`** 관계도에서 **'세력 관계' 칩을 끈다**(세력 자동 관계가 많은 세계관에서)
      → 노드 수·엣지 수 표기가 **그린 것만** 세고, 세력 관계만 있던 인물은 접히거나 빠진다. 자동 관계만 있는 세계관에서 끄면 **빈 화면**이 되고 사유도 맞다 — 종전에는 그리지 않을 선을 세고 그 선으로 등수를 매겨 사람을 접었다  `R-19`

---

### S12 · 연표 · 사건 · 45분

**준비물**

- [ ] 사건 여러 개 + 사건 필드 5개 이상 + 값(S2)
- [ ] **같은 날짜(연·월·일이 모두 같은) 사건 셋** — 그중 둘만 남기는 작품 필터가 걸리게 작품을 갈라 둔다
- [ ] 실패를 유발할 수단 — **`9-ㅍ`의 안내(아래)와 같은 방법**을 `9-ㄹ2`에도 그대로 쓴다(저장공간 채우기는 쓰지 않는다)

> ⚠️ **`9-ㅍ`(사건 저장을 일부러 실패시킨다)은 유발 방법이 원 시트에 없다.** 쓸 만한 방법: **저장을 누르는 순간 앱을 강제 종료**한다(최근 앱에서 밀어내기). 저장공간을 채우는 방법은 **쓰지 말 것** — S13의 약 3GB와 S23의 GB급 백업과 충돌한다.
>
> 그 앞의 `14-ㅁ`이 **원문이 "이 판이 고친 것의 절반"이라 적은 성공 고지**를 본다 — 실패 재현이 안 되더라도 **그 한 줄만으로 값이 있다.**

**확인**

- [ ] **`9-ㄱ`** ⚠️ 연표에서 사건을 만든다
      → **그래프에 사건이 보인다**(연도를 열어 봐야 알면 원칙 04 위반)  `—`
- [ ] **`9-ㄴ`** 사건에 필드값을 채우고 연표로 돌아온다
      → 카드에 `필드명: 값` 줄이 뜬다. 5개 이상이면 **4줄까지 + 초과 고지**  `3-6-7·9`
- [ ] **`9-ㄷ`** 줌을 낮춰 묶음 보기로 만든다
      → 머리글 카드에는 필드값 줄이 **안 나온다**  `3-6-10`
- [ ] **`9-ㄹ`** 재정렬 모드에서 사건을 끌어 옮기고 모드를 끈다
      → **끌어 옮긴 순서가 유지된다**  `3-6-11`
- [ ] **`9-ㅁ`** ⚠️ 사건 이름·연도·설명을 적다가 **[사건 필드 추가]**를 누른다
      → **적어 둔 것이 그대로 남아 있다**  `3-26-1·3-32-3`
- [ ] **`9-ㅅ`** **[추천에서 고르기]**를 누른다
      → 여섯(회차·장소·시점 인물…)이 뜬다. 아무것도 안 고르고 확인하면 **창이 안 닫히고** 말한다  `3-32-3·4`
- [ ] **`9-ㅇ`** 심은 뒤 다시 연다
      → 심은 것에 **(이미 있음)**이 붙는다  `3-32-5`
- [ ] **`9-ㅈ`** ⚠️ 사건 필드값을 채운 사건에서 **작품 체크를 전부 해제**하고 저장
      → **필드값이 안 지워진다**  `3-7-2`
- [ ] **`9-ㅊ`** 사건을 다른 세계관 작품으로 옮겨 저장
      → 보존 고지가 뜬다  `3-7-3`
- [ ] **`9-ㅋ`** 필드값 하나만 지우고 저장
      → **그 값만** 삭제된다(비움 의도는 존중)  `3-7-5`
- [ ] **`9-ㅌ`** 사건을 열자마자(폼이 다 서기 전에) 저장
      → 값이 날아가지 않는다  `3-7-4`
- [ ] **`14-ㅁ`** ⚠️ **캐릭터 편집 화면**에서 사건을 하나 만들어 저장한다 → 이어서 **캐릭터 상세 화면**에서도 같은 것을 한다
      → 양쪽 다 **성공을 알린다**. 그리고 **두 화면의 문구가 같다** — 원문이 *"이 판이 고친 것의 절반은 성공도 안 알려졌다는 쪽"*이라 적었다  `3-100-1·2`
- [ ] **`9-ㅍ`** ⚠️ 사건 저장을 **일부러 실패**시킨다
      → **실패했다고 알린다**. 연표에도 그 사건이 없다  `3-100`
- [ ] **`9-ㅎ`** 관계 변화 추가에서 **연도를 비우고** 저장
      → 적은 입력이 안 버려진다  `3-7-8`
- [ ] **`9-ㅎ2`** 캐릭터가 연결된 사건을 **연표에서 길게 눌러 삭제**한다
      → 바로 지워지지 않고 **확인 창**이 뜬다 — 연결된 캐릭터 수와 휴지통 보관을 말하고, 취소하면 남는다  `3-148-11`
- [ ] **`9-ㄱ2`** ⚠️ 재정렬 모드에 들어가 **안내 토스트를 읽고**, 사건을 **다른 날짜의 사건 위로** 끌어 본다
      → 안내가 **같은 날짜 안에서만** 옮길 수 있다고 알리고, 다른 날짜 위에는 **놓이지 않는다**. "저장되었습니다"라 말한 뒤 모드를 끄니 되돌아가면 실패다  `3-149-31`
- [ ] **`9-ㄴ2`** 같은 날짜의 사건 셋 중 **둘만 보이도록 작품 필터를 건 채** 순서를 바꾸고 필터를 푼다
      → 보이지 않던 셋째의 **앞뒤가 바뀌지 않는다**. 손대지도 않은 사건이 자리를 옮겼거나 번호가 겹쳐 순서가 뒤집히면 실패다  `3-149-32`
- [ ] **`9-ㄷ2`** 연도 슬라이더를 움직이거나 **줌을 바꾼 뒤** 밀도 바를 보고, 바를 **탭·롱프레스**해 본다
      → 같은 x가 슬라이더와 **같은 해**를 가리키고 창 밖 연도의 사건 표시도 남아 있다('지금 보는 구간'은 강조로 따로 보인다). 탭은 그 해로 뛰고 롱프레스는 간편 사건 추가를 연다. **줌을 최대로 올려도** 바가 배경만 남지 않는다  `3-149-33`
- [ ] **`9-ㄹ2`** 사건 저장·삭제·**순서 변경**을 일부러 실패시킨다 (`9-ㅍ`이 쓰는 방법 그대로 — 저장을 누르는 순간 앱을 강제 종료하거나 중복 키를 만든다. **저장공간을 채우는 방법은 쓰지 않는다**)
      → 스낵바가 **무엇이 실패했는지**(저장·수정·삭제·순서 변경)를 말하고 **[상세]**가 붙어 원인을 볼 수 있다. 예외 원문 한 줄만 뜨면 실패다  `3-149-34`
- [ ] **`9-ㅁ2`** 사건 필드값을 채운 사건을 **사건 필드가 0개인 세계관**의 작품으로 옮겨 저장한다
      → **보존 고지가 뜬다**(값은 지워지지 않고 남는다). 아무 말이 없으면 사용자는 지워졌는지 남았는지 알 길이 없다  `3-149-35`
- [ ] **`9-ㅁ3`** ⚠️ 연표에서 **캐릭터 필터**를 건 채 사건을 하나 더하고, 하나 지우고, 하나의 연도를 고친다
      → *이전/다음* 이동과 **`N / M`** 표기가 **방금 바꾼 것을 그대로 반영한다** — 종전에는 캐릭터 필터의 사건 목록이 갱신되지 않아 없는 사건을 세거나 새 사건을 못 봤다(작품 필터만 갱신됐다)  `R-33`
- [ ] **`9-ㅎ3`** 사건이 수백 건인 연표를 **위아래로 빠르게** 훑는다
      → 카드가 끊김 없이 흐르고 **등장 캐릭터 칩이 뒤늦게 깜빡이며 붙지 않는다**(목록을 받을 때 한 번에 실어 온다). 종전에는 카드가 그려질 때마다 질의가 새로 났다  ``

---

### S13 · 이미지 탭 + 정리 폴더 왕복 · 55분 (PC 30 + 앱 25)

**준비물**

- [ ] ⚠️ **순서가 이 묶음의 전부다.** 900장을 미리 폴더에 넣으면 왕복 고정점(`7-ㅁ`)이 **성립하지 않는다**
- [ ] **① 먼저 앱에서 내보낸다**(`7-ㄹ`) → **② 아무것도 안 건드리고 되받아 고정점을 본다**(`7-ㅁ`) → **③ 그다음에** 내보내기가 만들어 준 `이름#코드/` 폴더에 PC에서 900장을 채운다(코드는 ②까지 와야 알 수 있다)
- [ ] 900장 만드는 법: **이미지 1장(2~4MB)을 900개로 복제하고 연번으로 리네임**한다. 이 900장이 뒤에서 **GB급 백업을 공짜로** 만든다(S23)
- [ ] 손으로 만들 폴더 5종: `이름` · `이름(작품명)` · `이름#틀린코드` · `기타/` · `여행/`
- [ ] **'기타'라는 이름의 캐릭터** 1명 · 카드 이미지를 랜덤 모드로 둔 작품·세계관
- [ ] **대표 이미지를 지정한 캐릭터**를 담은 작품 1개 — 카드 연동을 대표 쪽으로 둔다
- [ ] **동명이인 둘의 이름으로 만든 폴더** 1개(S2의 동명이인을 쓴다)

> ⚠️ **고정점을 먼저 본다.** 내보내기 → 되받기에서 결과가 전부 0인 것을 확인한 *뒤에야* 폴더를 손댄다. 순서를 뒤집으면 뒤의 실패가 무엇 때문인지 갈리지 않는다.

**확인**

- [ ] **`11-ㅅ`** 설정 → **이미지 정리 폴더** 섹션
      → 4행(내보내기·받아오기·폴더 설정·사용법)이 있다  `3-14-1`
- [ ] **`11-ㅇ`** 폴더 미지정 상태에서 '받아오기'를 누른다
      → 폴더 지정 안내가 먼저 뜬다  `3-14-5`
- [ ] **`7-ㄴ`** **정렬·필터** 시트를 열어 **링크 상태**(전체/링크됨/링크 없음/캐릭터 자동)를 하나씩 적용해 본다
      → 각각 걸리고, 활성 필터 칩이 검색 줄 아래에 서며 ×로 그 자리에서 풀린다  `3-19-6`
- [ ] **`7-ㄷ`** **정렬·필터** 시트에서 **태그 없는 이미지만**을 켠다
      → 태그 없는 것만 남고 **시트의 태그 칩이 잠긴다**. 앱을 껐다 켜도 유지된다  `3-64-1`
- [ ] **`7-ㄹ`** 이미지 → 정리 폴더로 **내보내기**
      → 캐릭터별 폴더가 생긴다. 동명이인은 `이름#코드` 꼴이다  `3-13-1·2`
- [ ] **`7-ㅁ`** ⚠️ 내보낸 폴더를 **아무것도 안 건드리고** 그대로 받아오기(왕복 고정점)
      → 받아오기 **결과 창의 숫자가 전부 0**이다(새로 편입 0 · 옮김 0 · 배정 0). 900장을 눈으로 대조하지 않는다  `3-13-3`
- [ ] **`7-ㄱ`** 이미지 탭에서 격자를 위아래로 여러 번 훑는다
      → **재디코드로 버벅이지 않는다**  `3-116-ㄱ`
- [ ] **`7-ㅂ`** `홍길동#코드A/`의 사진을 `홍길동#코드B/`로 옮기고 받아오기
      → 그 캐릭터로 옮겨 배정된다  `3-13-4`
- [ ] **`7-ㅅ`** 손으로 `홍길동` 폴더를 만들고 받아오기
      → **누구인지 묻는다**. 고르면 그 캐릭터에, '배정하지 않음'이면 배정 없이  `3-13-5·6·7`
- [ ] **`7-ㅇ`** `홍길동(작품명)` 폴더를 만든다
      → **묻지 않고** 그 작품의 캐릭터에 붙는다  `3-13-9`
- [ ] **`7-ㅈ`** 틀린 코드 폴더(`홍길동#0000…`)를 만든다
      → "코드와 맞는 캐릭터가 없다"고 말한다  `3-13-10`
- [ ] **`7-ㅊ`** ⚠️ 정리 후 **원 폴더와 `_처리됨/`의 파일 개수를 각각 센다**(PC에서 폴더 속성으로 보면 된다)
      → **두 폴더의 합이 원본 장수와 같다** — 어느 한 장도 두 군데에 동시에 있지 않다  `3-43-7`
- [ ] **`7-ㅋ`** 못 옮긴 것이 있으면 그 문구를 본다
      → 참이다. **다음 정리에서 다시 편입되지 않는다**  `3-43-8·9`
- [ ] **`7-ㅌ`** ⚠️ 배정 안 된 이미지 2장을 `여행/`에 넣고 받아오기 → 다시 내보내기
      → **묶음이 풀린다**(종전 결함이 이것이었다)  `3-15-1`
- [ ] **`7-ㅍ`** 인앱에서 3장을 한 묶음으로 만들고 폴더로 한 장 더 넣는다
      → **넷이 한 묶음**이 된다(하나만 새 묶음으로 빠지면 결함)  `3-15-2`
- [ ] **`7-ㅎ`** `기타/` 폴더에 새 이미지 3장을 넣는다
      → 낱개로 들어온다. `_기타/`도 같다  `3-19-1·2`
- [ ] **`7-ㄱ2`** '기타'라는 **이름의 캐릭터**를 만든 뒤 `기타/`에 넣는다
      → **캐릭터가 이긴다**  `3-19-5`
- [ ] **`7-ㄴ2`** ⚠️ 링크 묶음의 일부만 `기타/`에 넣어 쪼갠다
      → **흩어진 묶음 카드**가 뜨고, 다시 묶으면 사라진다  `3-19-7`
- [ ] **`7-ㄷ2`** 캐릭터에서 이미지를 뗀 뒤 **`_분리됨/`** 동선을 본다
      → 확인창이 무엇이 일어날지 미리 말한다  `3-52-13·14`
- [ ] **`7-ㅂ2`** 작품/세계관 카드에 이미지를 건다(랜덤 모드)
      → **화면 안에서는 고정**, 나갔다 들어오면 바뀐다  `3-93-1·2`
- [ ] **`7-ㅅ2`** ⚠️ 그 이미지를 뺀다
      → **엉뚱한 그림이 안 뜬다**  `3-93-3`
- [ ] **`7-ㅇ2`** 작품 목록을 열자마자 위아래로 훑는다
      → **그림이 안 튄다**  `3-115-ㅂ`
- [ ] **`7-ㅈ2`** **직접 지정이 아닌 모드**(랜덤·연동 등)로 카드 이미지를 건 목록을 위아래로 훑는다
      → 카드 그림이 **스스로 바뀌지 않는다**. 스크롤로 나갔다 들어와도 같은 그림이고, **옆 카드의 그림이 박히지 않는다**(화면을 나갔다 들어오면 새로 뽑히는 것이 맞다)  `3-149-24`
- [ ] **`7-ㅊ2`** 작품 카드의 연동을 **대표 이미지** 쪽으로 두고, 대표를 지정한 캐릭터를 담는다
      → 카드가 **그 대표 그림**을 쓴다. 랜덤으로 뽑히면 지정이 저장돼 있어도 이 화면에서만 효력이 없는 것이라 실패다  `3-149-25`
- [ ] **`7-ㅋ2`** 손으로 **동명 폴더**(같은 이름의 캐릭터가 둘)를 만들어 받아온다
      → 고지가 *"파일은 폴더에 그대로 남습니다"*라 하지 않고 실제 처분(**배정 없이 편입 · 폴더끼리 묶음 · 원본은 `_처리됨`**)을 말하며, **인앱에서 한 장만 배정하면 같은 묶음이 따라간다**고 남는 행동을 준다  `3-149-26`
- [ ] **`7-ㅌ2`** 링크 묶음의 한 장에서 **태그 편집**을 연다(묶어 보기 꺼짐 · 상세 시트에서)
      → 단추가 **태그 편집 (묶음 N장)**이고, 시트 위에 링크 묶음 고지가 서며 **묶음 전체의 태그 합집합**이 보인다. 태그 하나를 더해 저장하면 **묶음의 다른 장** 상세에도 그 태그가 있다  `3-150-1`
- [ ] **`7-ㅍ2`** 링크 묶음의 **일부만** 골라 **일괄 태그 추가**를 한다
      → 시트에 **선택 N장 + 링크 M장 = 총 T장** 고지가 서고, 적용하면 **고르지 않은 식구에도** 붙는다(완료 고지도 T장을 센다)  `3-150-2`
- [ ] **`7-ㅎ2`** 같은 선택(묶음 일부)으로 **일괄 태그 제거**를 연다
      → **고르지 않은 형제만 가진 태그도 칩에 뜨고**, 지우면 묶음 전원에서 빠진다  `3-150-3`
- [ ] **`7-ㄱ3`** **세로로 찍은 폰 사진**(회전 정보가 EXIF에만 있는 것)을 이미지 탭에서 편입한다
      → 목록 썸네일이 **바로 서 있다** — 종전에는 여기서 옆으로 누웠다  `3-151`
- [ ] **`7-ㄴ3`** 그 사진을 **전체 화면 뷰어**로 열고, 캐릭터 카드·대표 이미지에서도 본다
      → **모든 자리에서 같은 방향**이다 — 목록과 뷰어가 다르면 한쪽만 고쳐진 것이다  `3-151`
- [ ] **`7-ㄷ3`** 설정에서 **이미지 압축을 켜고** 같은 세로 사진을 한 장 더 편입한다
      → 역시 바로 선다. 그리고 이쪽은 **파일 자체가 세워져** 저장되므로, 정리 폴더로 내보내 PC에서 열어도 바로 서 있다  `3-151`
- [ ] **`7-ㄹ3`** 전체 화면 뷰어에서 **오른쪽 위 회전 버튼**을 한 번 누른다
      → 이미지가 시계 방향 90도로 돌고, **뒤로 나가면 목록 썸네일도 같이 돌아 있다**  `3-151`
- [ ] **`7-ㅁ3`** 같은 이미지에서 회전을 **세 번 더** 눌러 제자리로 돌린다
      → 원래 방향으로 돌아오고 **화질이 눈에 띄게 나빠지지 않는다** — JPEG는 태그만 고치므로 무손실이다  `3-151`
- [ ] **`7-ㅂ3`** **투명한 PNG**(배경이 뚫린 이미지)를 편입해 뷰어에서 회전한다
      → 돌아가고 **투명 배경이 검게 되지 않는다** — 이쪽은 픽셀을 다시 쓰는 폴백 갈래다  `3-151`
- [ ] **`7-ㅅ3`** ⚠️ **자동 링크된 이미지**(캐릭터에 등록해 묶인 것) 한 장과 미링크 한 장을 골라 **링크**한다
      → 새 수동 묶음이 서고, 고지가 *"자동 묶음에서 N장을 옮겼습니다"*를 말한다. **캐릭터를 다시 저장해도 그 수동 묶음이 풀리지 않는다** — 종전에는 다음 저장 한 번에 조용히 사라졌다  `R-1`
- [ ] **`7-ㅎ3`** 내보낸 사본 **한 장**을 정리 폴더 안의 새 폴더(예: `풍경/`)에 넣고 받아오기를 누른다. 그 뒤 이미지 탭에 다시 들어가 진입 배너를 본다
      → 파일이 **`_처리됨/`으로 옮겨지고** 배너가 더 이상 *'정리 폴더에 새 이미지 1장'*이라 말하지 않는다. 종전에는 몇 번을 받아와도 같은 배너가 영원히 떴다  ``
- [ ] **`7-ㅎ4`** 이미지 탭에서 이미지를 골라 배정 → [캐릭터 새로 만들기]로 이름을 적고 확인한 **직후** 기기를 돌린다
      → 캐릭터가 생기고 **이미지도 그 캐릭터에 붙어 있다**. 종전에는 빈 캐릭터만 생기고 배정은 조용히 건너뛰었다  ``

---

### S14 · 대결 ① — 축·판·순위·상성 · 40분

**준비물**

- [ ] 축 4종: 일반 / 이미지 / 등급 연결 / 다른 세계관에서 불러올 것
- [ ] ⭐ **후보 필터로 10명만 남기고 50판**(2분이면 된다) — 그러면 나머지 190명이 자동으로 *한 판도 안 한 캐릭터*가 되어 `6-ㅇ`·`6-ㅊ`·`6-ㄴ3`이 **공짜로 열린다**
- [ ] 영향 필드를 비워 둔 캐릭터 2명을 섞어 둘 것
- [ ] 범주형(선택지) 필드를 영향 필드로 걸고, **그 값이 빈 캐릭터만 낀 판**을 몇 개 붙인다 — 값 있는 참가자끼리 붙은 판은 하나도 없게

**확인**

- [ ] **`6-ㄱ`** ⚠️ 대결 화면을 그냥 연다
      → **얼굴이 안 잘린다**(사용자가 직접 지적했던 자리)  `3-57-1`
- [ ] **`6-ㄴ`** 툴바를 본다
      → **[다른 축]**이 ⋮ 안이 아니라 **표면에 글자로** 있다  `3-57-2`
- [ ] **`6-ㄷ`** 축 편집 → **영향 필드**를 둘 이상 건다
      → 대결 카드의 이름 아래에 그 값이 순서대로 뜬다  `3-57-3·4`
- [ ] **`6-ㄹ`** 축 편집 → **프로필 필드**
      → 산출 필드로 건 것은 **회색으로 잠기고 사유가 붙는다**  `3-61-1`
- [ ] **`6-ㅁ`** 프로필을 다섯 개 이상 건다
      → 카드에 **넷만 뜨고 "외 N개"**가 선다  `3-61-2`
- [ ] **`6-ㅋ2`** 축 편집 → **후보 필터**에 조건을 넣는다
      → 대결 화면이 좁혀지고, 1명으로 좁히면 **막지 않고 말한다**  `3-81`
- [ ] **`6-ㅌ2`** 축 목록 ⋮ → **축 불러오기**
      → 다른 세계관의 축이 늘어서고, 고르면 맨 아래에 붙는다  `3-117-ㄴ·ㄹ`
- [ ] **`6-ㅍ2`** 축에 **세력**을 건다
      → 겸직이면 두 이름이 쉼표로 뜬다  `3-117-ㅂ·ㅅ`
- [ ] **`6-ㅎ2`** 축에 **이명·태그**를 건다
      → 목록에 시스템 열이 뜨고, 프로필로 걸면 카드에 값이 뜬다. 산출 필드에는 안 보인다  `3-80`
- [ ] **`6-ㄷ3`** **걸러낼 후보 칩**을 본다
      → 뜨고 수를 센다. 후보가 없으면 **왜 없는지 말한다**  `3-86-6·7`
- [ ] **`6-ㅅ`** ⚠️ **[방금 그거 취소]**를 누른다
      → **직전에 본 그 판**이 되돌려진다  `3-56-2`
- [ ] **`6-ㅇ`** 순위표를 연다
      → 점수 옆에 **±오차와 판 수**가 있고, 한 판도 안 한 캐릭터가 구분된다  `3-56-4`
- [ ] **`6-ㅋ`** 상성 화면을 아래로 내린다
      → **"값끼리의 전적"** 구역이 있고 수가 실제와 맞는다  `3-58-1·2`
- [ ] **`6-ㅌ`** 그 필드를 비워 둔 캐릭터를 섞어 몇 판 붙인다
      → **못 센 것을 말한다**  `3-58-6`
- [ ] **`6-ㄷ2`** 순위표에 남의 캐릭터 판이 섞이는지 본다
      → **'지워진 참가자'로 안 뜬다**  `3-118-ㄱ`
- [ ] **`6-ㅇ3`** 범주형 필드를 영향 필드로 걸고 **그 값이 빈 캐릭터만 낀 판**을 붙인다(값 있는 참가자끼리 붙은 판은 하나도 없게)
      → "값끼리의 전적" 구역에 **그 필드가 뜨고** 못 센 판 수를 말한다. *"차례가 없는 영향 필드가 걸려 있지 않습니다"*가 뜨면 실패다  `3-149-23`
- [ ] **`6-ㅈ3`** 이미지가 여러 장인 캐릭터로 대결 판을 몇 번 열고, 앱을 나갔다 다시 들어와 같은 캐릭터의 카드를 본다
      → 판을 열 때마다가 아니라 **화면에 다시 들어올 때** 다른 그림이 뽑힌다 — 종전에는 이 카드만 시드에 캐릭터 id를 넘겨 **한 캐릭터의 카드 그림이 영원히 한 장으로 고정**됐다  `R-18`
- [ ] **`6-ㅎ3`** 대결 화면에서 카드를 눌러 판정한 **직후**(1초 안에) 기기를 돌리거나 [기록]으로 나간다. 이어서 대결 축 목록에서 축을 끌어 옮기고 **손을 떼자마자** 뒤로 나간다
      → 방금 누른 판정이 **기록에 남아 있다**(종전에는 조용히 사라져 대기열에 다시 나왔다). 축 순서도 **끝까지 저장된다** — 종전에는 앞쪽 몇 개만 새 번호로 굳었다  ``
- [ ] **`6-ㅎ5`** 상성 상세에서 [잘못 눌렀다]로 여러 판을 지우는 **직후** 기기를 돌린다
      → 판이 **전부** 지워져 있다(상성 판정이 그 결과 위에서 다시 계산된다). 종전에는 절반만 지워진 채 멈췄고 아무 말도 없었다  ``

---

### S15 · 대결 ② — 이미지 축·등급·정렬 · 35분 · ⟳

**준비물**

- [ ] 이미지 2장 이상인 캐릭터
- [ ] 등급 체계(S3)
- [ ] S14에서 쌓은 판

**확인**

- [ ] **`6-ㅎ`** 그 캐릭터의 그림 둘로 판을 돌린다
      → 순위표가 **그 캐릭터 안에서만** 줄을 세운다  `3-85-3·5`
- [ ] **`6-ㄱ2`** ⚠️ 이미지 축 순위표의 **고지 줄 뒷부분**을 누른다
      → 기록 화면이 **거르개가 켜진 채** 뜨고, **요약의 총 수가 고지의 N과 같다**  `3-138-ㄱ·ㄴ`
- [ ] **`6-ㄴ2`** 그 화면에서 스위치를 끄고 **회전**한다
      → **꺼진 채로 있다**(도로 켜지면 실패)  `3-138-ㅁ`
- [ ] **`6-ㄱ3`** ⚠️ 이미지 축에 **[기준 축]** 스위치를 켠다
      → 이미지 축에만 뜬다. **세계관당 하나**가 지켜지고, 켜기 전에 말해 준다  `3-86-1·2`
- [ ] **`6-ㄴ3`** 대표 이미지 추첨을 **10번** 본다(목록을 나갔다 들어오기를 반복). 대결에서 많이 이긴 그림이 몇 번 나오는지 센다
      → **10번 중 6번 이상** 이긴 쪽이 나온다. 한 판도 안 한 캐릭터는 종전 그대로 균등하다  `3-86-3·5`
- [ ] **`6-ㄹ2`** k지선다를 **셋**으로 바꾼다
      → 2×2 격자가 되고, 빈 자리를 눌러도 아무 일이 없다  `3-87-3·4`
- [ ] **`6-ㅁ2`** ⚠️ 둘로 되돌린다
      → **종전과 똑같다**(이 판의 가장 중요한 확인)  `3-87-2`
- [ ] **`6-ㅂ2`** 등급 필드에 **[대결 등급 산정]**을 켠다
      → 컷 슬라이더가 뜬다. 구분선을 끌면 **이웃 구간만** 줄고 늘고 합이 100%다  `3-60-2`
- [ ] **`6-ㅅ2`** ⚠️ 저장하고 **다시 연다**
      → 켠 스위치가 **저장돼 있다**(2026.08.07 사용자 보고로 고친 자리)  `3-67-1`
- [ ] **`6-ㅊ2`** **[등급 반영]**을 누른다
      → 묶음이 셋으로 갈리고 기본 체크가 다르다. 반영 뒤 **휴지통에 되돌릴 백업**이 생긴다  `3-60-4`
- [ ] **`6-ㅈ`** ⚠️ 캐릭터 탭 → 정렬 → **대결 점수로 정렬**
      → 뜬다. **순위표의 수와 목록의 수가 같다**  `3-59-1·2`
- [ ] **`6-ㅊ`** 한 판도 안 한 캐릭터를 본다
      → 오름·내림 어느 쪽에서도 **맨 뒤**이고, 고지 줄이 그 사실을 말한다  `3-59-3·4`
- [ ] **`6-ㅎ4`** 등급 반영 시트에서 **손값 묶음의 체크 몇 개를 끄고** 기기를 돌린다. 이어서 [반영 (N)]을 누른 **직후** 다시 돌린다
      → 회전 뒤에도 **끈 체크가 그대로**이고 버튼의 (N)이 그 수와 같다. 반영 직후 회전에서도 **«등급 N건 반영» 고지와 작업 이력이 남는다** — 종전에는 체크가 기본값으로 돌아갔고 완료 고지는 통째로 사라졌다  ``

---

### S16 · 휴지통 · 35분

**준비물**

- [ ] 💾 **엑셀 내보내기를 한 벌 떠 둘 것**(이 묶음이 지운다)
- [ ] 캐릭터가 많이 든 세계관 하나(통째로 지웠다 되살린다)
- [ ] 필드 프리셋 1개
- [ ] 스톱워치
- [ ] ⚠️ **여벌 대결 축을 셋** 만들어 둘 것 — 이 묶음이 연달아 지운다(쓰던 축을 지우면 S14·S15의 재료가 죽는다)

> ⚠️ **맨 끝의 보관 설정(`4-ㅊ`)이 휴지통 항목을 조기 소멸시킬 수 있다** — 그래서 마지막이다.
>
> 복원하는 김에 **측정 하나(`13-ㄱ` 묶음 복원 잠금 시간)를 같이 잰다.**

**확인**

- [ ] **`4-ㄱ`** 캐릭터가 여럿 든 세계관을 지운다 → 휴지통
      → **삭제 작업 단위**로 묶이고, 머리글이 "세계관 '…' 삭제 · 항목 N개 · 시각"을 말한다  `3-103-1·2`
- [ ] **`4-ㄴ`** 머리글을 눌러 펼쳤다 접는다
      → 접힌다. 검색어를 친 상태에서도 접힌다  `3-103`
- [ ] **`4-ㄷ`** 검색칸에 이름 일부를 친다
      → 걸린 머리글이 **`항목 3/214개`**처럼 둘 다 적는다  `3-103`
- [ ] **`4-ㄹ`** 아무것도 안 걸리는 검색어를 친다
      → "조건에 맞는 항목이 없습니다"가 뜨고, 지우면 기본 상태로 돌아온다  `3-103-6·7`
- [ ] **`4-ㅁ`** ⚠️ 삭제 확인 창의 **유실 예고 숫자를 적어 둔다** → 복원 → 결과와 대조
      → 예고와 결과가 **같다**  `3-102-2·3`
- [ ] **`4-ㅂ`** ⚠️ 복원된 캐릭터를 열어 본다
      → **필드값·태그·관계·상태변화가 함께** 돌아왔다  `3-102-4`
- [ ] **`4-ㅅ`** ⚠️ 캐릭터를 **편집 중에 크게 고치고** 저장 → 휴지통
      → *편집 직전 백업*이 따로 잡히고, 복원하면 **복제**가 된다(원본이 안 없어진다)  `3-5-1`
- [ ] **`4-ㅇ`** ⚠️ 전역 필드 값이 있는 캐릭터를 지웠다 복원한다
      → 값이 돌아오고, **거짓 경고("근거가 id뿐")가 안 뜬다**  `3-69`
- [ ] **`4-ㅈ`** 프리셋을 덮어쓰기로 병합한다
      → 휴지통에 **"필드 정의(덮어쓰기 직전)"**가 생긴다. 입력해 둔 값이 살아남는다  `3-49-2·3`
- [ ] **`13-ㄱ`** **묶음 복원 잠금 시간**
      → 항목이 많은 삭제 작업 하나를 [전체 복원] → 화면이 멈춘 체감 시간을 스톱워치로. **"항목 N건 / 합계 M초"** 한 줄이면 된다  `**B-18**`
- [ ] **`4-ㅊ`** 휴지통 ⋮ → 보관 설정
      → 안내 문구가 고른 값과 **같은 수**를 말한다  `3-103`
- [ ] **`4-ㅋ2`** 대결 축을 **연달아 셋** 지운 뒤 휴지통을 연다
      → 삭제마다 **작업 묶음이 따로** 서고 **하나만 골라** 되살릴 수 있다. 셋이 한 묶음으로 붙어 있으면 실패다  `3-149-16`
- [ ] **`4-ㅌ2`** 프리셋 덮어쓰기로 생긴 백업을 휴지통에서 **되돌리고**, 그 되돌리기가 남긴 백업을 본다
      → 되돌리기의 백업이 **원래 삭제와 다른 묶음**으로 선다(한 묶음에 붙으면 되돌리기의 되돌리기를 따로 고를 수 없다)  `3-149-17`
- [ ] **`4-ㅋ3`** ⚠️ 그 필드를 실제로 지운 뒤 **휴지통**을 연다. 묶음의 **[전체 복원]**을 누른다
      → 휴지통에 **'필드 정의'** 묶음이 서고(‘덮어쓰기 직전’이 아니다) 그 안에 **'필드 데이터(값·라이브러리·이력)'** 행이 함께 있다. 복원하면 필드가 돌아오고 **각 캐릭터의 값·값 라이브러리 항목·상태변화 이력까지 되살아난다** — 종전에는 이 삭제만 휴지통을 지나지 않아 전부 영구 소멸했다  `R-18`

---

## 2부 — AI 💰

### S17 · AI 설정 — 💰 호출 0~3회 · 20분 · 💰(거의 안 나간다)

**준비물**

- [ ] 프로바이더 **2개**: 비전 지원 1개 + **이미지 미지원** 1개
- [ ] S2에서 `12-ㅈ2`(프로바이더 없는 상태)를 이미 끝냈을 것

> ⭐ **여기 대부분은 사실상 공짜다** — 등록·순서·삭제·승계는 호출이 없고, `12-ㅍ`·`12-ㄱ3`은 **실행 전 다이얼로그**라 취소하면 요청이 안 나간다. 종전 시트는 이것들을 💰 회차에 묶어 두어 미뤄지게 만들었다.

**확인**

- [ ] **`12-ㄱ`** 설정 → AI에 프로바이더·키를 넣는다
      → 저장되고 연결이 확인된다  `3-68`
- [ ] **`12-ㄴ`** 프로바이더를 여럿 등록하고 **끌어서 순서**를 바꾼다
      → 저장된다. '사용 중' 표시가 순서와 따로 논다  `3-88-2·3`
- [ ] **`12-ㄷ`** ⚠️ **활성 프로바이더를 지운다**
      → 키 있는 다음이 이어받고, 문구가 그 사실을 말한다  `3-96`
- [ ] **`12-ㄹ`** 키 없이 저장해 본다
      → 활성을 밀어내지 않는다  `3-68-3`
- [ ] **`12-ㅌ`** 설정 화면에서 창작도를 바꾼다
      → 거기서는 **안 뜬다**  `3-46-4`
- [ ] **`12-ㅎ`** (S18의 첨부 줄이 뜬 상태에서) 첨부에서 **마지막 한 장을 뺀다**
      → **"이미지를 보내지 않습니다"**가 된다 — 다른 그림이 대신 뽑히면 안 된다  `3-63-2`
- [ ] **`12-ㅎ2`** **필드 관리 → 사건 필드 칩 → 그 필드 편집 → AI 추천 대상을 '끄기'**로 두고 저장 → 연표에서 사건을 연다
      → 그 필드 줄에 **✨이 없다**  `3-109`
- [ ] **`12-ㅁ3`** **설정 → AI → 'AI 메시지 양식' → 양식 편집** → 목록에서 아무 양식이나 연다
      → 열넷이 축별로 보이고 각 줄에 **기본/고침** 표시가 있다. 창의 자리표 칩을 누르면 **커서 자리에** 들어간다  `3-147-1`
- [ ] **`12-ㅁ4`** ⚠️ 양식 창에서 **필수 자리표를 지우고 저장**을 누른다
      → **창이 닫히지 않고** 그 칸 아래에 사유가 뜬다. 쓰던 글이 그대로 남아 있다  `3-147-2`
- [ ] **`12-ㅁ5`** 양식을 아무렇게나 고쳐 저장 → 다시 열어 **'기본값으로'**를 누른다
      → **창이 안 닫히고 칸만** 기본 양식으로 돌아온다. 취소로 나가면 고친 글이 그대로다  `3-147-3`
- [ ] **`12-ㅁ6`** 양식 창에서 글을 고치다 **화면을 눕힌다**
      → 쓰던 글이 살아 있다  `3-147-4`
- [ ] **`12-ㅁ7`** 💰 고친 양식으로 캐릭터 필드 ✨을 한 번 돌린다
      → 고친 말투가 결과에 드러난다. 형식 오류가 나지 않는다  `3-147-5`
- [ ] **`12-ㅎ3`** 설정 → AI 연동 → '이미지 태그 기조'에 **상한(600자)을 넘는 글**을 붙여 넣고 칸 밖을 누른다
      → **'길이 상한 600자를 넘어 앞 600자까지만 저장했습니다'**가 칸 아래에 뜨고 칸의 글자도 저장된 값으로 줄어든다. 종전에는 아무 말 없이 잘렸다. 경계에 이모지를 두어도 **반쪽 글자가 남지 않는다**  `R-62`

---

### S18 · AI ① — 캐릭터 편집 ✨ · 35분 · 💰 · ⟳

**준비물**

- [ ] ⚠️ **키 있는 프로바이더를 둘만 남긴다** — 비전 1 + 이미지 미지원 1. 이 묶음이 둘 다 요구한다(`12-ㅍ`·`12-ㅌ2`는 비전 · `12-ㅊ2`는 이미지 미지원). **셋 이상 두면 실패 한 번이 여러 곳에 결제된다**
- [ ] **활성 프로바이더를 비전 쪽으로 지정해 둔다**(S17의 `12-ㄷ`가 활성을 옮겨 놨을 수 있다)
- [ ] 이미지 3장인 캐릭터 · 선택지 2~3개인 목록형 필드 · **앱 밖 경로 이미지가 섞인** 캐릭터
- [ ] 창작도 ON + 최신 모델

> ⭐ **한 번의 검토 시트에 여러 확인을 얹는다** — 결과가 뜬 상태에서 회전 → 상태 확인 → 실행 중 회전 → 취소를 **연달아** 밟으면 호출 한 번으로 넷이 끝난다.

**확인**

- [ ] **`12-ㅁ`** 💰 캐릭터 편집 → 필드 **✨** → 추천을 돌린다
      → 검토 시트가 뜨고 항목이 안 잘린다  `3-45`
- [ ] **`12-ㅂ`** ⚠️ 💰 검토 시트에서 **기기를 돌린다**
      → **받아 온 결과가 안 사라진다**(돈이 이미 나갔다)  `3-71-1`
- [ ] **`12-ㅅ`** ⚠️ 회전 뒤 끈 칩·필터·펼침 상태를 본다
      → **그대로다**(다시 켜져 있으면 실패)  `3-71-2`
- [ ] **`12-ㅇ`** 💰 실행 **중에** 돌린다
      → 진행 창이 다시 서고 눈금이 **이어진다**(0으로 안 돌아간다)  `3-71-3`
- [ ] **`12-ㅈ`** 회전 뒤 **취소**를 누른다
      → 남은 요청이 안 나가고, 받은 것으로 시트가 뜬다  `3-71-4`
- [ ] **`12-ㅊ`** 목록형 필드에서 **목록 밖 값**이 나오게 한다
      → **[목록 밖]** 표식이 붙고 뜻을 알려 준다. 채택하면 "추가하고 저장"을 묻는다  `3-47`
- [ ] **`12-ㅍ`** 💰 이미지가 있는 캐릭터에서 추천을 돌린다
      → **첨부 줄**이 뜨고, 뽑힌 장이 또렷하다. 비용 고지의 M이 N × 요청 수다  `3-63`
- [ ] **`12-ㅊ2`** 💰 **이미지를 안 받는 모델**로 첨부를 켜고 돌린다
      → "이 모델이 이미지를 받지 않아 글만 보냈습니다"가 뜨고, **근거 줄에 '이미지 1' 같은 말이 없다**  `3-73`
- [ ] **`12-ㅋ2`** 앱 밖 경로 이미지가 섞인 캐릭터에서 첨부를 켠다
      → **"앱이 보관하는 파일이 아니어서 보내지 않았습니다"**가 뜬다("읽지 못해"가 아니다). 정상 그림만이면 그 줄이 안 뜬다  `3-74`
- [ ] **`12-ㅌ2`** 💰 **창작도를 켜고 최신 모델**로 돌린다
      → 추천이 실제로 온다(종전에는 "23개 중 0개"였다). HTTP 400이 안 뜬다  `3-75`
- [ ] **`12-ㄹ3`** 캐릭터 ✨ 검토 시트에서 체크를 몇 개 끄고 [보완]으로 값을 손수 고친 뒤 **화면을 눕혔다 세운다**(새로 호출하지 않는다)
      → 끈 체크와 고친 값이 **그대로다**. 되켜지거나 원래 제안으로 돌아가면 사용자가 방금 한 판단이 말없이 되돌아간 것이라 실패다  `3-149-39`

---

### S19 · AI ② — 서술형·이미지·값·이름·사건 · 35분 · 💰 · ⟳

**준비물**

- [ ] 서술형 필드 2개를 가진 캐릭터
- [ ] **확연히 다른 이미지 6장**(요청당 3장 → 두 요청으로 태그 확인이 다 된다)
- [ ] **단색 배경이거나 빈 종이인 이미지 1~2장**(`12-ㄹ2`용 — 아래 안내)
- [ ] 값이 **150개 이상**인 라이브러리

> ⚠️ **`12-ㄹ2`(제안이 하나도 없이 끝나게)는 유발 방법이 원 시트에 없다.** 확률을 올리는 길은 **단색 배경·빈 종이·균일한 질감** 이미지를 고르는 것이다(이미 태그가 붙었는지는 무관하다).
>
> **판정 주의:** 제안이 **오면 실패가 아니라 *이 경로를 못 본 것*이다.** 기대하는 것은 *"AI가 태그로 삼을 만한 것을 찾지 못했습니다"* 부류의 고지이고, 안 나오면 **미확인으로 적어 주면 된다.**

**확인**

- [ ] **`12-ㅁ2`** 💰 보충 탭 → ✨ → **서술형 일괄 초안**
      → "서술형 초안을 쓰는 중 (n/총)"이 뜨고, 검토 창이 뜬다. **원문이 안 덮인다**  `3-137`
- [ ] **`12-ㅂ2`** 도는 동안 다시 뽑기·이전/다음을 누른다
      → **막히고 사유가 뜬다**  `3-137-ㄹ`
- [ ] **`12-ㄴ3`** 결과 창에서 한 필드만 체크를 끄고 **[선택 적용]**
      → 끈 것은 안 들어간다. **원문이 안 덮인다**  `3-101-7·8`
- [ ] **`12-ㄷ3`** [취소]로 닫은 뒤 다시 연다
      → **소비된 결과가 되살아나지 않는다**  `3-101-9`
- [ ] **`12-ㄱ2`** 💰 이미지 여러 장을 골라 **AI 태그 제안**을 돌린다
      → 진행 창이 요청 N/M을 말한다. **취소하면 남은 요청이 안 나간다**  `3-64-3`
- [ ] **`12-ㄴ2`** ⚠️ 태그가 **그 이미지의 내용과 맞는지** 본다
      → 맞는다(엉뚱한 이미지에 붙으면 번호 봉인이 깨진 것)  `3-64-3`
- [ ] **`12-ㄷ2`** ⚠️ 적용해 본다
      → **0건 성공이 실패로 안 보인다.** 실패면 검토 창이 돌아온다  `3-77`
- [ ] **`12-ㄹ2`** 제안이 **하나도 없이** 끝나게 해 본다
      → **말은 한다**(조용히 안 끝난다)  `3-79`
- [ ] **`12-ㅅ2`** 값 라이브러리 **AI 정리**를 돌리고 회전한다
      → 요청이 살아 있고 체크가 넘어간다  `3-126`
- [ ] **`12-ㅇ2`** 💰 이름 줄의 **✨**을 누른다
      → 라운드 시트가 서고 ♥/✕/적용이 각각 동작한다. **회전을 넘는다**  `3-84`
- [ ] **`12-ㅍ2`** 💰 사건 편집에서 **✨**을 누른다
      → 비용 고지가 먼저 뜨고, 취소하면 요청이 안 나간다. 검토 창의 근거가 **사건 재료**(설명·때·관련 캐릭터)를 말한다  `3-109`
- [ ] **`12-ㄱ3`** 💰 서술형 일괄에서 **출력 상한을 최소로** 두고 [길게]를 누른다
      → 막히거나 사유를 말한다  `3-101-4`
- [ ] **`12-ㅂ3`** 💰 링크 묶음에서 **1장만** 골라 **AI 태그 제안**을 돌린다
      → 실행 창에 묶음 옵션과 **"묶음 전체 N장에 붙습니다"**가 서고, 검토 시트 행에 **(N장)**이 붙으며, 적용하면 **묶음 전원**에 태그가 있다  `3-150-4`
- [ ] **`12-ㅅ3`** 💰 **링크 묶음 단위로 보내기**를 끄고 같은 묶음으로 다시 돌린다
      → 붙는 범위 고지(묶음 전체 N장)는 **꺼도 그대로** 서고, 적용도 여전히 **묶음 전원**에 붙는다 — 스위치는 보낼 장수만 바꾼다  `3-150-5`
- [ ] **`12-ㅇ3`** ⟳ 검토 창에서 **한 필드만 체크를 끄고 회전**한다
      → 창이 다시 서고 **껐던 그 필드만 꺼진 채**다(초안이 사라졌다고 하지 않는다)  `R-38`
- [ ] **`12-ㅎ4`** 💰 이미지 AI 태그 제안을 받아 검토 시트에서 [적용]을 **빠르게 두 번** 탭한다
      → 적용이 **한 번만** 돌고 결과 고지도 한 번이다(버튼이 즉시 비활성화된다). 실패하는 상황이라면 검토 시트가 **되돌아온다** — 종전에는 두 번째 회차가 되살린 응답을 지웠다  `R-38`

---

## 3부 — 엑셀 (파일 한 벌로)

### S20 · 엑셀 ① — 정본 A를 뜨고 읽기만 한다 · 25분

**준비물**

- [ ] ⚠️ **S2~S19가 다 끝나 모든 시트에 재료가 있을 것** — 필드·전역·값 라이브러리·등급 체계·대결 축·상성·사건·AI 설정. 하나라도 비면 그 시트는 확인이 성립하지 않는다
- [ ] 💾 **정본 A를 사본 2벌로 복사** — 하나는 봉인(비교 원본), 하나는 S21에서 망친다
- [ ] PC의 표 프로그램에서 **필터·정렬·`=ISBLANK()`를 실제로 걸어 볼 것** — `ㅇ-3`·`ㅇ-4`는 파일을 열어만 봐서는 안 갈린다

> 이 묶음은 **파일을 만들고 눈으로만 본다.** 가져오기는 하나도 없다.

**확인**

- [ ] **`11-ㅈ`** 설정 → 내보내기 → **[전부 해제]** → 하나만 켠다
      → 라벨이 '전부 해제'로 돌아온다  `3-14-6·7·8`
- [ ] **`11-ㅊ`** '이미지'만 켜고 내보낸다
      → 실제로 이미지만 든 파일이 나온다  `3-14-9`
- [ ] **`3-ㄱ-1`** 설정 → 내보내기 → 전체
      → 파일이 만들어지고 저장 위치를 묻는다  `3-36`
- [ ] **`3-ㄱ-5`** 파일을 열어 시트마다 **Ctrl+A → 글꼴 칸**을 본다
      → 글꼴 이름이 **하나로 보인다**. 칸이 **비어 있으면 혼재**(일부 셀이 Calibri)라 실패다  `3-121-ㅅ·ㅇ`
- [ ] **`3-ㄱ-6`** 머리글을 본다
      → 필터 단추가 붙어 있고 첫 행이 고정돼 있다  `3-29-4`
- [ ] **`3-ㄷ-1`** 캐릭터 0명인 세계관을 만들고 내보낸다
      → 그 세계관 시트가 **머리글만 있는 빈 시트**로 나온다  `3-39-5`
- [ ] **`3-ㄷ-4`** **'전체 캐릭터'** 시트를 연다
      → 전체 인원과 행 수가 같고 **미분류도 들어 있다**  `3-29-2`
- [ ] **`3-ㄷ-6`** **'미분류 캐릭터'** 시트를 본다
      → 전역 필드 열이 있고 겹쳐 나가지 않는다  `3-97`
- [ ] **`3-ㄹ-3`** **'사용 안내'** 시트를 본다
      → '필드 데이터 시트 (값 정리)' 절이 있다  `3-28-3`
- [ ] **`3-ㅁ-3`** ⚠️ **'앱 설정'** 시트를 연다
      → 바꾼 설정이 실려 있다. **API 키는 없다** — 묻지 않으면 안 나간다  `3-90-1·2`
- [ ] **`3-ㅁ-14`** 값 안의 **쉼표**가 든 필드를 내보낸다
      → 감싸여 나가고, 되읽어도 그대로다. 헤더에 `(쉼표 구분)` 안내가 붙는다  `3-99`
- [ ] **`3-ㅁ-15`** **'앱 설정'** 시트에서 `설정키`·`설정값` 옆 두 칸을 본다
      → **회색 `설명`·`입력 가능한 값`**이 있고, 행마다 그 설정이 무엇이고 어떤 값을 받는지 적혀 있다  `3-147-6`
- [ ] **`3-ㅁ-16`** 같은 시트에서 `ai_tpl_`로 시작하는 행 열넷을 본다
      → AI에 보내는 글이 **여러 줄로 보이고**(첫 줄만 잘려 있지 않다), `입력 가능한 값` 칸에 **쓸 수 있는 자리 이름**이 뜻과 함께 적혀 있다  `3-147-7`
- [ ] **`3-ㅂ-1`** 설정 → 내보내기에서 **'골라서 내보내기'**와 **'전체 백업'** 문구를 본다
      → '필드 정의' 라벨이 **(등급 체계·기본 필드·필드 데이터 포함)**을 말하고, 전체 백업 목적문에 **휴지통·작업 이력·최근 활동 미수록** 줄이 있다  `3-148-1`
- [ ] **`3-ㅂ-2`** 내보낸 파일의 캐릭터 시트에서 **'이미지경로' 헤더 색**을 본다
      → 회색이 아니라 **파란색(편집 가능)**이다 — 세계관·작품 시트와 같다  `3-148-2`
- [ ] **`3-ㅅ-15`** ⚠️ 계산 필드가 있는 정본을 **한 글자도 고치지 않고** 다시 들인다. 이어서 **빈 앱**(또는 캐릭터를 전부 지운 상태)에 같은 파일을 들인다
      → 두 경우 다 결과에 *“계산 필드라 저장하지 않습니다”*가 **한 줄도 없고** '건너뜀'도 0이다 — 종전에는 **새로 만들어지는 캐릭터 행마다** 그 경고가 났다(빈 앱 복원이면 전 행)  `R-33`
- [ ] **`3-ㅅ-16`** 선택지(SELECT)와 등급(GRADE) 커스텀 필드를 **캐릭터·작품·사건 셋 다**에 만들고 정본을 뜬 뒤, PC 엑셀에서 그 열의 셀을 눌러 본다
      → 세 시트 모두 그 열에 **드롭다운 목록**이 뜨고 값이 같다 — 종전에는 캐릭터 시트의 SELECT에만 있었고, 같은 필드가 시트마다 다르게 나갔다. 등급 표를 만들지 않은 GRADE 필드에는 목록이 붙지 않는다(들이기가 받는 것보다 좁은 목록을 걸지 않는다)  `R-18`
- [ ] **`3-ㅇ-1`** **'전체 캐릭터'** 시트의 머리글 행을 훑어 **이름이 같은 열이 있는지** 본다
      → 같은 이름의 열이 **하나도 없다.** 같은 필드키가 세계관마다 타입이 다르면 그 열만 `키(height·NUMBER)`·`키(height·TEXT)`처럼 타입까지 적혀 갈린다 — 그 글자는 '필드 정의' 시트의 '타입' 열과 같다. 종전에는 `키(height)` 열이 두 자리에 서서 65명의 키가 이름이 같은 두 열로 갈려 있었다  ``
- [ ] **`3-ㅇ-2`** **'대결 기록'** 시트에서 **이미지 축**의 행을 본다(축 칸이 이미지 축 이름인 행)
      → '참가자1'·'참가자2' 칸에 **파일 이름이 적혀 있다**(`char_….jpg`). 종전에는 그 두 칸이 통째로 비어 있었다 — 빨간(필수) 머리글인데도. 캐릭터 축에서 삭제된 캐릭터를 가리키는 판도 이름 칸에 코드가 적힌다  ``
- [ ] **`3-ㅇ-3`** 아무 데이터 시트에서 머리글의 **필터 화살표**를 누르고, 다른 열을 **정렬**해 본다
      → 값 목록에 **데이터 행의 값들이 뜨고** 정렬이 전 행에 걸린다. 종전에는 저장된 필터 범위가 머리글 한 줄(`A1:M1`)이라, 그 범위를 그대로 믿는 프로그램에서는 필터가 데이터를 한 줄도 못 잡았다  ``
- [ ] **`3-ㅇ-4`** '전체 캐릭터'의 **빈 칸** 하나를 골라 옆 칸에 `=ISBLANK(그 칸)`을 적고, 같은 열의 필터 목록도 연다
      → `TRUE`가 뜨고 필터 목록에 **'(필드 값 없음)'**이 있다. 종전에는 빈 칸의 절반 이상이 *길이 0 문자열*이라 `ISBLANK`가 `FALSE`였고, 같은 행 안에서도 열마다 답이 갈렸다  ``
- [ ] **`3-ㅇ-5`** 이미지가 **하나도 없는** 캐릭터·세계관·작품 행의 **'이미지경로'** 칸을 본다
      → **비어 있다.** 종전에는 그 칸에 `[]`가 적혀 있어, 같은 안내가 *"칸을 비우면 값이 지워집니다"*라 말하는데도 그 글자를 지워야 하는지 둬야 하는지 알 수 없었다  ``
- [ ] **`3-ㅇ-6`** **'사용 안내'** 시트를 아래로 훑어 **'캐릭터 상태변화' 시트** 절을 찾는다
      → 절이 있고 `__birth`·`__alive`가 무엇인지, **고칠 자리는 캐릭터 시트의 출생연도·생일·사망연도·생존 여부 칸**이라는 것이 적혀 있다. 종전에는 그 시트가 안내에서 통째로 빠져 있었다 — 237행 전부가 그 밑줄 키인데도  ``
- [ ] **`3-ㅇ-8`** **'이미지'** 시트의 행 수를 세고, **그림이 딱 한 장뿐인 캐릭터**를 골라 그 그림의 파일명을 이 시트에서 찾는다
      → **있다.** 태그·묶음이 아직 없는 그림도 행으로 실리고, 그 행은 '태그'·'링크그룹'·'뗀날짜' 칸이 비어 있다. 종전에는 라이브러리 표만 실어 **그림이 한 장뿐인 캐릭터의 그림이 전부 빠졌다**(실측 6/6) — 엑셀에서 태그를 줄 방법이 없었다  ``
- [ ] **`3-ㅇ-9`** **'전체 캐릭터'** 시트의 머리글 행을 **가로로 끝까지** 훑는다(필드키가 붙은 오른쪽 열들)
      → 열 이름이 **끝까지 다 보인다** — 이웃 칸에 막혀 잘리지 않는다. 종전에는 43열 중 여덟이 잘렸고 `초월자 세대(transcendent_generation)`는 절반도 안 보였다. 그 시트의 쓸모가 정렬·필터·피벗인데 열 이름이 안 읽혔다  ``
- [ ] **`3-ㅇ-10`** **'사용 안내'** 시트의 「주의사항」에서 **이미지경로** 줄과, 「빈 시트 안내」의 마지막 줄을 읽는다
      → 이미지경로 줄은 **JSON 배열 예시**(`["/…/char_1.jpg","/…/char_2.jpg"]`)를 보이고, '이미지' 시트에서 가리키는 칸을 **'파일명'**이라 적는다(종전에는 그 시트에 없는 '이미지경로'를 가리켰다). 빈 시트 안내는 **예외 셋**('캐릭터 필드값'·'작품 필드값'·'사건 필드값')을 적는다  ``
- [ ] **`3-ㅇ-11`** **'필드 템플릿'**·**'검색 프리셋'** 시트의 '생성일'과 '수정일'을 견준다
      → **수정일이 생성일보다 이르지 않다.** 종전에는 기본 프리셋을 다시 심은 뒤 옛 파일을 들이면 생성일만 보호되어 수정일이 넉 달 과거로 끌려갔다(실측 2행)  ``
- [ ] **`3-ㅇ-16`** 세력을 하나 만들어 **자동관계유형에 목록에 없는 글자**(예: `가문원`)를 적고 멤버 둘을 넣은 뒤 내보내, '캐릭터 관계' 시트의 '관계 유형' 칸을 **눌러 드롭다운을 연다**
      → **그 글자가 목록에 있다.** 다른 값을 골랐다가 원래 값으로 되돌릴 수 있다. 종전 목록은 기본 유형 + 세계관 커스텀 유형뿐이라, 세력이 만든 관계 87행이 자기 시트의 드롭다운 밖 값이었고 되돌리기를 엑셀이 막았다  ``
- [ ] **`3-ㅇ-17`** 세력 둘 사이에 관계를 만들고(유형에 `동맹`처럼 목록에 없는 글자) 내보내 '세력 관계' 시트의 '관계 유형' 드롭다운을 연다
      → **`동맹`이 목록에 있다.** 세력 자동관계유형(`가문원` 등)은 **없다** — 캐릭터 사이의 관계라 세력끼리 고를 수 없는 값이다  ``
- [ ] **`3-ㅇ-18`** **성·이름 칸을 채운 캐릭터**(예: 이름 `유리엘 실라키아스` · 성 `실라키아스` · 이름(First) `유리엘`)로 대결을 한 판 치른 뒤 내보내, '대결 기록'의 참가자·승자 칸과 **캐릭터 시트의 '이름' 칸**을 견준다
      → **글자가 같다.** 종전에는 대결 시트만 `"성 이름"`으로 다시 조립해 `실라키아스 유리엘`로 적혔고, 승자 칸의 이름이 어느 캐릭터 시트에도 없는 글자였다  ``
- [ ] **`3-ㅇ-19`** 작품에 속하지 않은 **미분류 캐릭터**의 성별·나이·종족을 채운 뒤 내보내, '전체 캐릭터' 시트에서 **세계관 칸이 빈 행**의 그 열들을 본다
      → **값이 차 있다.** 종전에는 미분류 행의 필드 칸이 통째로 빈칸이라, 그 시트로 건 성별·나이 피벗이 미분류 인원을 말없이 빼고 셌다. **열 수는 늘지 않는다** — 전역 필드가 새 열을 만들지는 않는다  ``
- [ ] **`3-ㅇ-20`** 내보낸 파일의 '사용 안내' 시트에서 **'대표이미지'**와 **'세력 관계'** 두 낱말을 찾는다
      → ⓵ 대표이미지 절이 있고 **경로가 아니라 파일명**이라 적혀 있다(비우면 앱이 골라 보여 준다 — 첫 장 고정이 아니다) ⓶ 세력 관계 절이 **'관계 유형'을 고치면 새 관계가 생긴다**고 경고한다. 종전에는 둘 다 한 줄도 없었다  ``

---

### S21 · 엑셀 ② — 되돌릴 수 있는 가져오기 · 80분 (PC 50 + 앱 30)

**준비물**

- [ ] PC에서 A 사본으로 **변형 파일 11개(+ 아래 여섯)**를 한자리에서 만든다(제목 줄 얹기 · 헤더 지우기 · 설정 열 지우기 · 필수 칸 비우기 · 동명이인 코드 없애기 · 빈 시트에 행 적기 · 대결 상성 고치기 · 전역 행 없는 옛 파일 흉내 …)
- [ ] 💾 시작 전 **전체 백업**(이름: BK-D)
- [ ] **계산 수식 필드가 든 사본** 셋: ⓐ 한 글자도 안 고친 것 ⓑ 입력 필드 칸만 고친 것 ⓒ 계산 열의 값을 사람이 고친 것
- [ ] 이름이 **` (쉼표 구분)`으로 끝나는 필드**를 만든 뒤 뜬 사본 1개
- [ ] **'이미지' 시트** 표 위에 제목 줄을 얹은 사본 1개
- [ ] **'대결 축' 시트로 새 축을 만들면서** 그 축을 쓰는 '대결 기록'·'대결 상성' 행을 함께 넣은 사본 1개
- [ ] `ㅇ-7` 몫: **이미지 축의 판을 몇 개 쌓은 뒤 이미지 포함 백업(ZIP)** 을 따로 떠 둘 것 — 복원이 파일을 새 이름으로 채워야 이 단계가 성립한다

> 여기 22단계는 **되돌릴 수 있는 것만** 모았다. 덮어쓰기 가드는 다음 묶음이다.

**확인**

- [ ] **`3-ㄱ-2`** ⚠️ **아무것도 고치지 않고** 그대로 다시 가져오기
      → 미리보기가 **전부 '동일'**이라고 말한다. '변경'이 뜨면 결함이다  `3-42-1·3-53-1`
- [ ] **`3-ㄱ-3`** ⚠️ 실행한 뒤 결과 요약을 본다
      → **미리보기가 예고한 수와 결과가 같다**  `3-94-1·2`
- [ ] **`3-ㄱ-4`** 한 칸만 고쳐서 가져온다
      → 그 줄만 '변경 1'이고 나머지는 '동일'이다  `3-42-2`
- [ ] **`4-ㄷ2`** 미리보기의 필드값 줄을 본다
      → **'캐릭터/작품/사건 필드값' 세 줄**이 있고, 값 칸을 지우면 **'비움'**이 별도 줄로 뜬다  `3-131-ㄱ·ㄴ`
- [ ] **`3-ㄷ-3`** 빈 시트에 행을 적고 가져오기
      → 그대로 만들어진다  `3-39-2`
- [ ] **`3-ㄷ-5`** 그 시트를 고쳐서 가져오기
      → 앱이 **안 바뀐다**(읽기 전용). "인식되지 않아 무시" 경고도 **안 뜬다**  `3-29-5`
- [ ] **`3-ㄴ-6`** 필수 칸이 **빈 행**을 만들어 가져오기
      → '건너뜀'에 뜨고 문구가 사유를 말한다  `3-123-ㄱ·ㄴ`
- [ ] **`3-ㄴ-7`** 같은 이름의 캐릭터 둘을 만들고 코드 없이 가져오기
      → 동명이인 오류가 뜨고, **안내한 대로 코드를 채우면 통과**한다  `3-20-1·2`
- [ ] **`3-ㅁ-6`** '앱 설정'에 **모르는 값·모르는 키**를 적어 들인다
      → 세어서 말한다(조용히 안 버린다)  `3-90-8·9`
- [ ] **`8-ㄹ-8`** 없앤 설정이 담긴 **옛 파일**을 들인다
      → 그 사실을 말하고, 나머지 설정은 멀쩡히 들어온다. 한 번 저장하면 고지가 사라진다  `3-89-8·9·10`
- [ ] **`3-ㅁ-8`** 계산 필드 수식을 깨뜨린 파일을 들인다
      → 결과 창에 **필드 이름과 사유**가 뜬다. 값은 저장된다(거부가 아니라 고지)  `3-105`
- [ ] **`3-ㅁ-9`** 대결 축 시트의 `영향필드`에 `sys:anothername`(밑줄 빠짐)을 적어 들인다
      → **행 번호와 함께** 경고가 뜨고, 연결이 안 지워진다  `3-105`
- [ ] **`3-ㅁ-12`** **'필드 정의'**에 `"D":0.25`를 넣어 들인다
      → 등급 표에 **D 행이 화면에 나타난다**  `3-30-3`
- [ ] **`3-ㅁ-13`** **'등급체계'** 칸에 없는 이름을 적어 들인다
      → 관대하게 받고 사유를 말한다  `3-31-6`
- [ ] **`4-ㅅ2`** ⚠️ 대결 축·상성이 있는 상태로 내보내 **'대결 상성'** 시트를 고쳐 들인다
      → 미리보기에 **'대결 상성' 줄이 선다**(`신규 1 · 변경 1 · 동일 N`). 종전에는 그 줄 자체가 없었다  `3-141-ㄱ`
- [ ] **`4-ㅇ2`** 축 이름을 오타로 바꾼 행을 하나 둔다
      → **'건너뜀'**으로 예고된다  `3-141-ㄷ`
- [ ] **`3-ㄴ-1`** ⚠️ 표 **위에 제목 줄**을 한 줄 끼워 넣고 가져오기
      → 들어온다. **'백업에 N건'이 원본보다 늘지 않는다**(늘면 제목이 데이터 행으로 세어진 것)  `3-132-ㄱ`
- [ ] **`3-ㄴ-2`** 제목 줄을 **10줄 이상** 넣어 헤더를 밀어낸다
      → 사유와 함께 거부하고, 문구가 "맨 앞 10행"을 말한다  `3-132-ㄴ·3-123-ㅁ`
- [ ] **`3-ㄴ-3`** ⚠️ 어느 시트의 **헤더 행 자체를 지우고** 가져오기
      → **말없이 넘어가지 않는다** — 사유와 함께 거부한다  `3-123-ㄹ`
- [ ] **`3-ㄴ-8`** 깨진 xlsx(확장자만 바꾼 파일)로 가져오기
      → 실패 창이 뜨고 **기존 데이터가 멀쩡**하다  `3-43-2·3`
- [ ] **`3-ㄴ-9`** 아무 ZIP(사진 압축본)을 가져오기로 고른다
      → "지원하지 않는 ZIP" 부류로 거부한다  `3-8-7`
- [ ] **`11-ㅁ`** 나머지 다이얼로그도 열어 본다(AI 공급자 편집 · 교차분석 · 세계관 편집 · 가져오기 충돌 목록)
      → 다 정상이다  `3-50-6·3-41`
- [ ] **`3-ㅁ-17`** `ai_tpl_` 행 하나의 `설정값`을 셀 안에서 고쳐 저장하고 **그대로 가져온다**
      → 앱의 양식이 바뀌고, **아무것도 안 고친 왕복에서는 '복원됨'이 뜨지 않는다**. 칸을 비우면 기본 양식으로 돌아간다  `3-147-8`
- [ ] **`3-ㅂ-3`** ⚠️ '앱 설정' 시트 `image_quality_percent`에 **500**을 적고 가져온다
      → 결과 경고에 **"조정해 저장했습니다"**가 그 행 단위로 뜨고, 경고의 행 번호가 **엑셀 왼쪽 행 머리글과 일치**한다  `3-148-3`
- [ ] **`3-ㅂ-4`** 불리언 설정값 하나에 **켬**, 다른 하나에 **아무말**을 적고 가져온다
      → 켬은 **켬으로 복원**되고, 아무말은 **거절 사유(허용 어휘)**가 뜨며 종전 값이 유지된다  `3-148-4`
- [ ] **`3-ㅂ-5`** ⚠️ 가져오기가 도는 중에 **홈으로 나가** 끝날 때까지 둔다
      → 알림이 **"앱을 열면 상세를 보여 드립니다"**라 말하고, 재진입 시 보관함 창에 **오류·경고 상세 목록**이 실제로 뜬다  `3-148-5`
- [ ] **`3-ㅂ-6`** 설정 → **작업 이력**을 연다
      → 방금 가져오기가 **한 줄로 남아 있고** 상세(경고 목록)를 볼 수 있다  `3-148-6`
- [ ] **`3-ㅂ-7`** 텍스트 파일의 확장자만 .xlsx로 바꿔 가져와 본다
      → **"엑셀 통합문서가 아니어서 열 수 없습니다"** 부류의 형식 안내가 뜬다 — 시트를 확인하라는 안내가 아니다  `3-148-7`
- [ ] **`3-ㅂ-8`** 어느 시트의 **생성일 셀 하나를 비우고, 하나는 글자로 깨뜨려** 가져온다
      → 두 행 다 **기존 생성 시각이 유지**되고(생성일 정렬 순서 불변), 깨뜨린 행에는 **경고**가 뜬다  `3-148-8`
- [ ] **`3-ㅂ-9`** 캐릭터 시트의 **이미지경로 셀 하나를 비워** 가져온다
      → 그 캐릭터의 이미지 배정이 풀리되, 결과의 **"빈 셀로 지워진 값 N건"에 계수**된다  `3-148-9`
- [ ] **`3-ㅂ-10`** 캐릭터 시트의 **이미지경로 셀에 아무말**(JSON 아님)을 적고 가져온다
      → 배정이 풀리지 않고 **기존 그대로**이며, "목록으로 읽을 수 없어 유지합니다" 경고가 뜬다  `3-148-20`
- [ ] **`3-ㅂ-11`** 엑셀에서 **암호를 걸어 저장**한 백업을 가져와 본다
      → "통합문서가 아니다"가 아니라 **"암호로 보호되어 있다"**는 안내가 뜬다  `3-148-21`
- [ ] **`3-ㅅ-7`** **계산 수식 필드가 있는** 상태로 내보낸 파일을 **한 글자도 안 고치고** 그대로 다시 가져온다
      → 미리보기의 '캐릭터 필드값'에 **'건너뜀'이 안 붙고**, 결과 창에 *"계산 필드라 저장하지 않습니다"* 경고가 **한 줄도 없다**  `3-149-9`
- [ ] **`3-ㅅ-8`** 같은 파일에서 **입력 필드 칸만** 고쳐 가져온다 → 그 다음엔 **계산 열의 값을 사람이 직접** 고쳐 가져온다
      → 앞에서는 계산 열이 **여전히 침묵**하고, 뒤에서만 그 필드에 대해 **한 줄** 경고가 뜬다  `3-149-10`
- [ ] **`3-ㅅ-9`** 이름이 **` (쉼표 구분)`으로 끝나는 필드**를 만들어 내보내고 그대로 다시 가져온다
      → 그 열이 **원래 필드**로 들어간다. '자동 생성' 필드가 새로 생기거나 같은 값이 두 필드에 갈려 통계가 두 번 세면 실패다  `3-149-11`
- [ ] **`3-ㅅ-10`** **'이미지' 시트** 표 위에 제목 줄을 한 줄 얹고 가져온다
      → '건너뜀 1'이나 *"이미지 1건: 파일을 찾을 수 없어 태그·링크 복원을 건너뛰었습니다"*가 **뜨지 않는다**  `3-149-12`
- [ ] **`4-ㅍ2`** **'대결 축' 시트로 새 축을 만들면서** 그 축을 쓰는 '대결 기록'·'대결 상성' 행을 함께 넣는다
      → 그 행이 **'신규'로 예고되고 실제로 들어간다**('건너뜀'이 되면 이번 수리가 반대로 넘어간 것이다). 어디에도 없는 축을 가리키는 행만 '건너뜀'이다  `3-149-18`
- [ ] **`3-ㅎ`** ⚠️ 백업을 내보낸 뒤 캐릭터 시트의 **필수 칸 하나를 비워** 건너뛰게 만들고 다시 가져온다. 복원 미리보기 문구와 **덮어쓰기** 경고, 병합 모드의 '엑셀에 없는 항목 삭제' 목록을 본다
      → *'백업에 없음'*이 아니라 **'이 파일이 갱신하지 않음'**으로 적히고, 덮어쓰기 경고와 병합 삭제 목록 **양쪽에** *'이 수에는 앱이 읽지 못한 행의 몫이 섞여 있습니다 …'* 한 줄이 붙는다  ``
- [ ] **`3-ㅎ2`** 캐릭터 태그를 **전각 글자**로 단다(예: `ＮＰＣ`). 내보낸 뒤 **한 글자도 고치지 않고** 다시 가져와 태그를 확인한다. 프리셋 시트의 '수정일' 칸에 `2026-08-22` 같은 글자를 적어 넣고도 가져와 본다
      → 태그가 `ＮＰＣ` **그대로**다(종전에는 `NPC`로 조용히 개명됐다). '수정일'은 **경고가 뜬다** — 종전에는 옆 '생성일'만 경고하고 이쪽은 조용했다  `R-47`
- [ ] **`3-ㅎ3`** 캐릭터 시트에 **«캐릭터명»**이라는 열을 새로 만들어 값을 적고 들이기한다(«이름» 열은 그대로 둔다)
      → 경고가 *«… 커스텀 필드 열로 읽습니다»*라 말하고 실제로 그 이름의 **필드가 생겨 값이 들어온다**. 종전에는 «뒤쪽 열을 무시했습니다»라 말해 놓고 값을 넣었다 — 사용자는 들어온 열을 두고 안 들어왔다고 들었다  ``
- [ ] **`3-ㅎ4`** 이름 은행 시트에서 어떤 행의 **«사용여부»를 «아니오»로 바꾸되 «사용캐릭터»는 그대로** 두고 들이기한다
      → 결과창이 *«사용여부가 아니오인데 사용 캐릭터가 지정되어 사용 중으로 맞춥니다»*라 말하고, 그 이름이 **«미사용 이름» 목록에 뜨지 않는다**. 종전에는 조용히 어긋난 채 들어와 이미 쓰는 이름이 후보로 다시 떴다  ``
- [ ] **`3-ㅎ5`** 대결 기록 시트의 **«판정일»** 칸에 숫자가 아닌 글자를 적고 들이기한다(대결 축의 «생성일»도 같이)
      → 결과창이 그 행과 열을 짚어 **«숫자로 읽을 수 없어 지금 시각으로 새로 만듭니다»**라 경고한다. 종전에는 아무 말 없이 가져오기 시각으로 굳었다 — 읽기 전용 열을 잘못 건드린 사실을 어디서도 알 수 없었다  ``
- [ ] **`3-ㅇ-7`** 이미지 축의 판을 몇 개 쌓고 **이미지를 포함해 백업(ZIP)**을 뜬 뒤, 앱 초기화(또는 다른 기기)로 그 ZIP을 복원한다. 그리고 **그 이미지 축의 순위표**를 연다
      → **쌓아 둔 판이 그대로 살아 있다.** 복원은 사라진 파일을 새 이름으로 채우는데, 종전에는 판의 참가자 코드(=경로)가 옛 이름 그대로 남아 **판 전체가 고아가 되거나** 가져오기가 *"코드 '…'는 다른 참가자의 판입니다"*로 거부했다. 결과창에 그 오류가 **하나도 없어야** 한다  ``
- [ ] **`3-ㅇ-12`** 정본 A를 **한 글자도 고치지 않고** 그대로 다시 가져온 뒤, '이미지' 시트의 행 수와 [이미지] 탭의 장 수를 견준다
      → **라이브러리가 불어나지 않는다.** 태그·묶음이 없는 빈 행은 아무 일도 하지 않고 결과에 '변경 없음'으로 센다 — 행이 있다는 것만으로 라이브러리에 편입하지 않는다  ``
- [ ] **`3-ㅇ-13`** '이미지' 시트에서 **빈 행 하나**(그림이 한 장뿐인 캐릭터의 그림)의 '태그' 칸에 글자를 적고 가져온다
      → 그 그림이 **라이브러리 관리로 들어오고** 적은 태그가 붙는다. 결과가 그 행을 '신규'로 센다 — 편입의 진입점이 태그·링크·뗀 표식이라는 규약 그대로다  ``
- [ ] **`3-ㅇ-14`** '캐릭터 상태변화' 시트에서 `__birth` 행 하나의 **'연도'만** 고치고(코드 칸은 그대로) 가져온 뒤, 다시 내보내 그 캐릭터의 `__birth` 행 수를 센다
      → **한 행이다.** 그 자리의 행을 고치고 결과가 «캐릭터당 한 행이라 새 행을 만들지 않고 기존 행을 고쳤습니다»라 알린다. 종전에는 자연키가 빗나가 **둘째 줄**이 들어갔고, 그때부터 같은 캐릭터의 나이가 프로필과 연표에서 갈렸다  ``
- [ ] **`3-ㅇ-21`** '대결 기록'의 **승자 칸**에 **캐릭터 시트에 적힌 이름**을 그대로 붙여 넣고 가져온다(그 행의 참가자 칸이 다른 표기인 옛 파일로도 한 번)
      → **그 판의 승자로 받아들인다.** 결과가 그 행을 건너뛰지 않는다 — 이름 색인이 이름 칸 표기와 조립 표기를 둘 다 든다. 두 참가자가 그 글자로 함께 읽히면 «승자 칸에 참가자 코드를 적어 주세요»라 알린다  ``
- [ ] **`3-ㅇ-22`** '세계관' 시트의 **테두리색** 칸에 `#`을 빼고 적고(`7E57C2`), '세력' 시트의 **색상** 칸도 같게 한 뒤 가져온다. 다시 내보내 그 두 칸을 본다
      → **`#7E57C2`로 저장돼 나온다** — 값은 그대로이고 표기만 맞춰진다. `red`처럼 알아볼 수 없는 글자는 종전대로 그대로 두고 버리지 않는다  ``

---

### S22 · 엑셀 ③ — 덮어쓰기 가드 · 85분 (PC 40 + 앱 45) · 💀 되돌리기 어려움 · ⟳

**준비물**

- [ ] 💾 **BK-D가 살아 있는지 다시 확인**(이 묶음은 진짜로 지우려 든다)
- [ ] PC 30분 — **가드마다 파일 하나씩, 9개(+ 아래 셋)**를 따로 만든다: `3-ㄴ-5`(설정 열 삭제) · `3-ㄹ-2`(별칭·카테고리 열 삭제) · `3-ㄹ-1`(표시라벨 수정) · `3-ㅁ-1`(자기 백업 그대로) · `3-ㅁ-2`(전역 행 없는 옛 파일) · `3-ㄴ-4`(내용만 지운 시트) · `3-ㄷ-2`(빈 시트) · `3-ㅁ-11`(구버전 캐릭터 필드값) · `3-ㅁ-5`(키 포함 내보내기)
- [ ] ⚠️ **한 파일에 한 가드씩** — 두 손상을 섞으면 가드가 실행되지도 않고 초록으로 보인다
- [ ] ⚠️ `3-ㅁ-5`가 만든 **평문 API 키가 든 파일은 확인 즉시 폐기할 것**
- [ ] 추가 파일 셋: **세계관 시트 하나만 데이터 행 삭제**(헤더 유지 · 시트는 여럿) · **'대결 축' 시트 통째 제거** · **'기본 필드' 시트만 든 파일**(내용은 DB와 이미 같게)

> **한 건 돌리고 결과를 확인하고 다음.** 몰아서 하면 무엇이 지켰고 무엇이 안 지켰는지 갈리지 않는다.

**확인**

- [ ] **`3-ㄴ-5`** **설정 열**(필드 정의·기본 필드 시트)을 통째로 지우고 가져오기
      → 선택지·계산식·등급표가 **그대로 살아 있다**. 미리보기가 '변경 없음'이라 한다  `3-76·3-78`
- [ ] **`3-ㄹ-2`** 별칭·카테고리 열을 통째로 지우고 가져오기
      → 기존 값이 안 지워진다  `3-42-6`
- [ ] **`3-ㄹ-1`** **'필드 데이터'** 시트에서 표시라벨·별칭을 고쳐 가져오기
      → 앱의 라이브러리에 반영되고, 통계·검색에 나타난다  `3-28-4`
- [ ] **`3-ㅁ-1`** ⚠️ 전역 필드가 있는 상태로 내보내 **자기 백업을 덮어쓰기**로 들인다
      → **전역 필드가 그대로 있다.** 무소속 캐릭터 값도 그대로다(종전에는 정의가 지워지며 값까지 CASCADE로 사라졌다 — B-130이 낸 사고 그 자체다)  `3-66-2`
- [ ] **`3-ㅁ-2`** 전역 행이 **없는 옛 백업**을 덮어쓰기로 들인다
      → 지우지 않고 **"시트가 다루지 않은 구역…삭제하지 않고 유지했습니다"**라고 말한다  `3-66-3`
- [ ] **`3-ㄴ-4`** ⚠️ 어느 시트의 **내용만 지우고**(헤더는 남김) 덮어쓰기
      → 기존 데이터를 **안 지운다**. "복원 재료가 없어 삭제하지 않았습니다"가 뜬다  `3-121-ㄱ·3-132-ㄷ`
- [ ] **`3-ㅁ-11`** **'캐릭터 필드값'** 시트로 미분류 캐릭터 값을 왕복시킨다
      → 값이 되살아난다. 구버전 파일 덮어쓰기에 경고가 뜬다  `3-22`
- [ ] **`3-ㅁ-5`** 설정 → 내보내기 → **'API 키 포함' 스위치를 켜고**(확인 창의 경고를 읽는다) 내보낸다
      → 그때는 키가 실린다. 되돌려 넣으면 설정과 프로바이더가 그대로 온다. ⚠️ **확인 뒤 그 파일을 즉시 지운다**  `3-90-5·6·7`
- [ ] **`3-ㅅ-1`** ⚠️ **같은 이름의 세계관 둘**(각각 캐릭터 있음)을 만들어 내보낸다 → 시트가 `이름`·`이름(2)`로 갈린 것을 확인 → **'엑셀에 없는 항목 삭제'로** 그대로 가져온다
      → 두 세계관의 캐릭터가 **각자 제자리에 온전**하다 — 한쪽이 비거나 양쪽에 중복되면 결함이다  `3-148-10`
- [ ] **`3-ㅅ-2`** 내보내기 저장(자리 고르기)이 도는 중에 **홈으로 나가** 끝날 때까지 둔다
      → 성공·실패가 **알림으로** 오고, 재진입 시 보관함 창에도 뜬다 — 무고지면 결함이다  `3-148-16`
- [ ] **`3-ㅅ-3`** ⚠️ 저장을 **일부러 실패**시킨다(쓰는 중 저장소 회수 등)
      → 목적지의 **반쪽 파일이 지워지고**, 문구가 지웠는지 남았을 수 있는지를 **가려 말한다**  `3-148-17`
- [ ] **`3-ㅅ-4`** 실패한 회차의 **재시도 창**에서 [다시 저장]을 누른다
      → SAF가 다시 열리고 **같은 산출물이** 저장된다 — 내보내기를 처음부터 다시 돌리지 않는다  `3-148-18`
- [ ] **`3-ㅅ-5`** 실패 후 **앱을 강제 종료**하고 다시 연다
      → 다음 진입의 보관함 창이 **재시도 버튼과 함께** 선다(영속 보관 — 프로세스가 죽어도 산다)  `3-148-19`
- [ ] **`3-ㅅ-6`** 재시도 창에서 [다시 저장] → 자리 고르기에서 **뒤로가기(취소)**
      → 파일이 지워지지 않고 **"보관해 두었으니 다음에 다시 물어봅니다"**가 뜨며, 재진입 시 같은 창이 다시 선다  `3-148-22`
- [ ] **`3-ㅅ-11`** ⚠️ 세계관 시트가 **여럿**인 파일에서 하나만 **데이터 행을 지우고**(헤더는 남김) 덮어쓰기
      → *"백업에 '세계관' 시트가 없어"*가 아니라 **"데이터 행이 없어"**라고 말한다 — 사용자가 할 일이 다르다(다시 내보내기 ↔ 그 시트에 행 적기)  `3-149-13`
- [ ] **`3-ㅅ-12`** **'대결 축' 시트를 통째로 빼고** 덮어쓰기
      → **"시트가 없어"**라고 말한다. *"데이터 행이 없어"*가 뜨면 반대 방향으로 접힌 것이다  `3-149-14`
- [ ] **`3-ㅅ-13`** **'기본 필드' 시트만** 든 파일(내용은 DB와 이미 같게)을 가져온다
      → **"바뀐 것 없음 (N행이 파일과 이미 같습니다)"**가 뜨고 N이 실제 행 수와 맞는다. "데이터 없음"으로 보고되면 실패다  `3-149-15`
- [ ] **`3-ㅅ-14`** ⚠️ '엑셀에 없는 항목 삭제'에서 **상태변화**를 켜고, 상태변화가 몇 건 빠진 파일을 들인다
      → 결과에 *"엑셀에 없는 상태변화 N건을 삭제했습니다 — 휴지통에서 복구할 수 있습니다"*가 뜨고, **휴지통에 그 N건이 실제로 있다.** 종전에는 같은 데이터가 인앱에서는 되돌릴 수 있고 엑셀에서는 영영 사라졌다  `R-18`

---

### S23 · 백업·복원·자동 백업 · 측정 · 105분 · 💀 · ⟳

**준비물**

- [ ] S13의 900장이 앱에 들어 있을 것 → **GB급 백업이 저절로 선다**(따로 만들 것 없다)
- [ ] 스톱워치 · 알림 권한 · **zip을 풀어 볼 PC**
- [ ] ⚠️ **`4-ㅎ`(이미지 참조를 깨뜨린다)의 복구법을 먼저 정해 둘 것** — 깨뜨리는 방법이 *앱 밖에서 이미지 파일을 옮기는 것*이라면 **그 파일을 제자리에 되돌리면** 복구된다. 복구 못 하면 S25의 백업이 불완전해진다
- [ ] **'이식 가능 백업'(암호 설정)**으로 뜬 `.enc` 백업 1개 — 복원을 두세 번 반복한다
- [ ] **이미지가 든 백업** — '이미지 되살리기' 막대가 도는 동안을 노려 화면을 돌린다

> 전체 백업을 뜨는 김에 **측정 하나(`13-ㄴ` 소요·크기)를 같이 잰다.**

**확인**

- [ ] **`4-ㅋ`** ⚠️ 설정 → **전체 백업(이미지 포함)**
      → 확인 창에 **"이미지 약 N GB 포함"**이 뜨고, 진행이 **막대 + N/M**으로 돈다(스피너가 아니다)  `3-36-2·3`
- [ ] **`13-ㄴ`** **내보내기 소요·파일 크기**
      → 전체 백업(이미지 포함) 한 번. **걸린 시간 + 파일 크기**. 가능하면 OOM 없이 끝나는지도  `**B-72 ⓑ**`
- [ ] **`4-ㅍ`** 끝까지 두고 완료 고지를 본다
      → "이미지 N장 포함 — 완전한 백업입니다"  `3-36-5`
- [ ] **`4-ㄱ2`** 받은 zip을 풀어 본다
      → `data.xlsx` · `images/` · `image_map.json`이 있다  `3-36-6`
- [ ] **`4-ㅌ`** ⚠️ 이미지를 담는 도중 **취소**한다
      → 파일이 **안 남는다**. 곧바로 다시 내보내면 정상이다  `3-36-4·3-136-ㄴ`
- [ ] **`4-ㄹ2`** 저장 위치를 **안 고르고** 나온다
      → "저장을 취소했습니다 — 만든 파일은 지웠습니다"가 뜬다  `3-136-ㄱ`
- [ ] **`3-ㅁ-4`** ⚠️ **전체 백업**으로도 확인한다
      → 키가 **안 샌다**  `3-90-3`
- [ ] **`4-ㅁ2`** 설정 → 자동 백업 → **[지금 백업]**
      → 돈다. 도는 동안 버튼이 안 눌리고, 끝나면 복원 목록이 깨끗하다  `3-36-9·10·11`
- [ ] **`3-ㅁ-7`** **자동 백업** 파일을 본다
      → 거기에도 키가 없다  `3-90-11`
- [ ] **`4-ㅊ2`** 이미지가 많은 세계관에서 '지금 백업'
      → 완료된다  `3-38-3`
- [ ] **`11-ㅋ`** 백업 가져오기의 **항목 선택 창**에서 취소·뒤로가기
      → 가져오기가 **중단된다**  `3-14-15`
- [ ] **`11-ㅍ`** ⚠️ 큰 백업을 가져오다 **화면을 돌린다**
      → **끊김 고지 또는 완료 중 하나가 반드시 뜬다.** 아무 말 없이 진행 창만 사라지면 **실패**다  `3-129-ㄱ·ㄴ`
- [ ] **`11-ㅎ`** 큰 작업 중에 **홈 버튼으로 앱을 벗어난다**
      → 알림으로 결과가 온다  `3-129-ㅂ`
- [ ] **`11-ㅌ`** 설정 → 저장공간
      → 캐시 크기가 보인다. 백업 전후로 **임시 파일이 안 쌓인다**  `3-106`
- [ ] **`11-ㄱ2`** 어디서든 진행 막대를 본다
      → 퍼센트가 음수이거나 100%를 넘지 않는다  `3-37-12`
- [ ] **`4-ㅎ`** ⚠️ 이미지 참조를 하나 깨뜨린 채 백업한다
      → **"완전한 백업입니다"가 안 뜬다** — 안 담긴 장을 말한다  `3-122-ㄴ`
- [ ] **`11-ㅁ2`** 설정 → 저장공간에서 **캐시 항목의 이름과 크기**를 본다(큰 백업을 내보낸 직후에도)
      → **'앱 캐시 (임시 파일 · 비울 수 있음)'**으로 뜨고 크기가 실제로 움직인다. 0으로 굳어 있으면 `exports` 밖을 한 바이트도 안 세는 것이다  `3-149-36`
- [ ] **`11-ㅂ2`** **'이식 가능 백업'(암호 설정)**으로 뜬 `.enc` 백업을 복원한 뒤 설정 → 저장공간을 다시 본다(두세 번 반복)
      → 복원할 때마다 캐시가 **백업 크기만큼 쌓이지 않는다** — `restore_ext_*.enc`(옮겨 받은 원본)와 `restore_*.xlsx`(복호화 산출물)가 복원 수만큼 남아 있으면 실패다  `3-149-37`
- [ ] **`11-ㅅ2`** ⚠️ 이미지가 든 백업을 복원하다 **'이미지 되살리기' 막대가 도는 동안** 화면을 돌린다
      → **중단 고지 또는 완료 결과 중 하나가 반드시** 뜬다(알림·보관함·작업 이력 어디든). 진행 창만 사라지고 아무 데도 한 줄이 없으면 실패다  `3-149-38`
- [ ] **`13-ㄹ`** ⚠️ ⟳ **이식 가능 백업 내보내기**에서 자리를 고른 직후, 저장 진행 창이 떠 있는 동안 **화면을 돌린다**
      → 저장이 끝까지 가고 고른 자리의 파일이 **실제 백업 크기**다 — **0바이트가 아니다.** 종전에는 복사가 뷰 수명에 묶여 회전 한 번에 끊겼고, 끊긴 자리에 멀쩡한 이름의 빈 파일이 남았다  `R-26`
- [ ] **`13-ㅁ`** ⚠️ 백업 내보내기로 자리를 고르되, **쓸 수 없는 자리**를 고른다(읽기 전용 위치·꺼내 버린 SD 카드 등). 재현이 어려우면 저장 도중 저장 매체를 뽑는다
      → **"저장되었습니다"가 뜨지 않는다.** 실패를 말하고, 만들다 만 파일을 지웠으면 지웠다고·못 지웠으면 확인해 달라고 **가려서** 말한다  `R-26`
- [ ] **`13-ㅂ`** ⚠️ 이식 가능 백업 복원에서 파일을 고른 뒤 **암호 창을 취소**한다. 그다음 설정 → 저장공간에서 **[앱 캐시 비우기]**를 누른다
      → *"저장을 기다리는 파일 N개는 남겼습니다"*가 뜨지 않고 캐시가 실제로 줄어든다 — 종전에는 취소한 그 백업 사본(원본과 같은 크기)이 주인 없이 남고, 비우기가 그것을 *지키느라 남겼다*며 건너뛰었다  `R-26`

---

## 4부 — 되돌릴 수 없는 것 (맨 뒤)

### S24 · 대량 비가역 정리 · 45분 · 💀💀 되돌리기 매우 어려움

**준비물**

- [ ] 💾 **전체 백업**(BK-F) + 💾 **이미지 원본을 PC로** — 재압축은 백업으로도 안 돌아온다
- [ ] ⚠️ **여벌 대결 축을 하나 만들어 그것을 지운다**(쓰던 축을 지우면 S14·S15가 죽는다)
- [ ] **전역(무소속) 기본 필드** 1개(값이 없는 그림자가 딸린 상태) · 값이 든 필드 1개(타입을 바꾼다)

> ⚠️ **순서대로 밟을 것.** 아래 차례가 뒤집히면 앞 단계의 재료가 사라진다.
>
> 맨 끝의 전역 필드 해제(`2-ㄷ-4`)는 **확인 즉시 다시 켤 것.**

**확인**

- [ ] **`6-ㅂ3`** 체계가 있는 세계관을 `.ncworld`로 내보내 들인다
      → 결과에 **"등급 체계 N"**이 뜬다  `3-31-7`
- [ ] **`6-ㅅ3`** 대결 축이 든 세계관을 월드패키지로 왕복시킨다
      → 결과에 **'대결 축 N · 대결 기록 N · 대결 상성 N'**이 뜨고, 기준 축 표식이 하나만 켜져 있다  `3-106`
- [ ] **`6-ㅁ3`** ⚠️ 그 체계를 **✕로 지운다**
      → "참조하는 필드 N개는 독자 표로…"가 뜨고 **값이 안 없어진다**  `3-31-4`
- [ ] **`6-ㅇ2`** 연결한 축을 **지운다**
      → 스피너가 "축을 고르세요"를 보이고 사유가 뜬다(다른 축 이름이 보이면 실패)  `3-67-2`
- [ ] **`8-ㄹ-9`** 필드 관리 → **필드 합치기** → 심을 종류를 '사건 필드'로 바꾼다
      → 묶음 머리글이 **사건 N개**로 바뀌고, 합친 뒤 사건 탭에 있다. **원래 필드는 그대로 남는다**  `3-108`
- [ ] **`8-ㄹ-10`** 허용 타입 밖 필드(신체 사이즈·등급)를 섞는다
      → **"넘어가지 못한 필드 N개"**가 서고, [허용 타입 고치기]로 열 수 있다  `3-108`
- [ ] **`7-ㅁ2`** **900장이 넘는** 이미지를 골라 태그를 지운다
      → 죽지 않는다(쿼리 상한)  `3-37-9`
- [ ] **`7-ㄹ2`** 이미지 관리에서 수백 장을 골라 **재압축**한다
      → 진행이 막대로 돌고, **취소가 실제로 먹는다**  `3-37-5·6`
- [ ] **`2-ㄹ-1`** ⚠️ 값이 든 필드의 **타입을 바꾼다**(글자 → 숫자)
      → 저장 전에 **"값 N개가 초기화됩니다"**가 뜬다. N이 실제와 맞는다(빈 값은 안 센다)  `3-70·3-72`
- [ ] **`2-ㄹ-2`** 기본 필드 관리 → **[전파]**
      → 세계관마다 줄이 서고, 타입이 바뀌는 줄에만 경고가 붙는다  `3-70-1·3`
- [ ] **`2-ㄹ-3`** 전파 미리보기에 **"무소속" 행**이 있는지 본다
      → 있다. 그 줄에도 영향 분석이 붙는다  `3-65-3`
- [ ] **`2-ㄹ-4`** 밀고 나서 실제 결과를 본다
      → 미리보기가 말한 대로다  `3-70-4`
- [ ] **`2-ㄷ-4`** ⚠️ 스위치를 **끄고** 확인 창을 본다 → 해제한다
      → "모든 세계관에서 연결이 풀립니다"가 뜨고, **해제 뒤에도 필드와 값이 남는다**(여기가 어긋나면 데이터 유실)  `3-62-3`
- [ ] **`2-ㄷ-7`** **전역(무소속) 기본 필드**의 전역 스위치를 끄고 확인 창을 읽은 뒤 해제한다
      → *"필드와 값은 지워지지 않습니다"*로 뭉뚱그리지 않고 **ⓐ 세계관에 심긴 필드·캐릭터 값은 그대로 ⓑ 값 없는 무소속 필드만 정리**로 갈라 말한다. 결과도 정리된 수를 말하고 **0이면 그 절이 안 붙는다**  `3-149-6`
- [ ] **`2-ㄹ-5`** 기본 필드 관리 → 템플릿 ⋮ → **[전파]** 미리보기에서, 그 세계관에서 **손댄 적 없는** 줄을 본다
      → *"이 세계관에서 고침"*이 아니라 **"템플릿과 다름 (누가 바꿨는지 알 수 없음)"**으로 뜬다. 밀 수는 있고 기본으로 켜지지는 않는다  `3-149-7`
- [ ] **`2-ㄹ-6`** 값이 든 필드의 타입을 바꿀 때 뜨는 **확인창의 타입 이름**을 본다
      → 스피너에서 고른 그대로 **'텍스트'·'숫자'**로 부른다. `TEXT`·`NUMBER` 같은 영문 식별자가 뜨면 실패다  `3-149-8`
- [ ] **`2-ㅅ-5`** **작품을 «없음»으로 바꾼 캐릭터**(그 세계관의 값과 상태변화 이력을 가진 채)를 만들어 둔 뒤, 값 라이브러리에서 그 값의 이름을 바꾸거나 병합한다
      → 확인 창이 **«미분류 캐릭터 N명의 상태변화 이력은 소속 세계관을 가릴 수 없어 옛 표기로 남습니다»**를 함께 말한다 — 값과 이력이 어긋난다는 사실을 조용히 넘기지 않는다. 종전에는 값만 새 표기가 되고 이력은 옛 표기로 남은 채 아무 말이 없었다  ``

---

### S25 · 초기화 → 빈 앱 복원 · 25분 + 며칠 · 💀💀💀 맨 끝 · 되돌릴 수 없다

**준비물**

- [ ] ★ 💾 **BK-Z — 이 순서를 그대로 밟을 것:** ⓐ `4-ㅎ`에서 깨뜨린 이미지 참조가 복구됐는지 확인 → ⓑ 전체 백업을 **다시** 뜬다 → ⓒ 기기 밖(PC·클라우드)으로 복사 → ⓓ 가능하면 다른 기기에서 복원 예행
- [ ] ⚠️ **그 백업이 살아 있는지는 두 번째 항목에서야 판명된다.** 그 전에 기기를 비우는 것이다

> **여기서 앱이 비워진다.** 앞의 모든 묶음이 끝난 뒤에만 온다.
>
> 마지막 항목은 S1부터 켜 둔 자동 백업을 **회수하는 것**이다 — 며칠이 지나 있어야 한다.

**확인**

- [ ] **`4-ㅂ2`** 앱 초기화 → 휴지통·작업 이력·프리셋 칩을 본다
      → 다 비었다. **테마 · AI 설정(키·공급자) · 자동 백업 설정은 그대로**다 — 자동 백업이 꺼졌으면 **곧바로 다시 켜고** 그 사실을 적어 준다  `3-12-5·6·7`
- [ ] **`4-ㄴ2`** ⚠️ **빈 앱(초기화 후)**에 그 백업 하나만 복원한다
      → 미리보기의 **'캐릭터' 백업에 N건이 0이 아니다**(파일의 캐릭터 수와 같다). 실행 뒤 실제와 예고가 맞는다  `3-131-ㄷ`
- [ ] **`4-ㅈ2`** **3일 이상** 두고 자동 백업을 회수한다 — ⓐ 날마다 백업 파일이 늘었는가 ⓑ 그중 하나로 **복원이 되는가** ⓒ 헛된 실패 알림이 없었는가
      → 셋 다 참이다. 하나라도 아니면 그 항목을 적어 준다  `3-38`
- [ ] **`11-ㅎ2`** 설정 → [모든 데이터 삭제]를 확인까지 눌러 진행 창이 뜬 **그 순간 기기를 돌린다**('백업도 삭제'를 켠 채)
      → **«초기화 완료»가 뜬다.** 그리고 이미지 파일·오류 로그·`backups/` 폴더·마지막 백업 시각이 **전부 함께 지워져 있다** — 종전에는 회전 한 번이면 DB와 설정만 비고 나머지는 그대로 남았고, 게다가 «초기화 실패»라는 토스트가 떴다  ``

---

## 다 밟으면

**138절 · 461단계가 전부 닫힌다.** 그 순간 이 저장소는 *"앱이 실제로 도는가"*의 답을 **2026.08.01 이후 처음으로** 갖게 된다.

깨진 것이 나오면 그 자리가 **다음 판의 최우선**이다 — 화면 결함은 자동 검증이 원리적으로 못 보는 유일한 부류라, 어떤 백로그 항목보다 앞선다.


---

## 부록 — 뺀 것과 그 사유 (기록)

원 문서(`docs/remaining_work_2026-07.md` 3장)의 **절이 전부 위에 산다**(그 뒤 워크스루가 낳은 절은 위 범례가 가리키는 자리에 있다). 줄인 것은 절이 아니라 **절 안의 항목**이다(원 1,348개 → 지금 461개). **뺀 부류와 그 사유를 여기 남긴다** — 적어 두지 않으면 다음 사람이 *유실*로 읽는다.

| 뺀 부류 | 사유 |
|---|---|
| **`3-18`의 말투 항목**(어시스턴트·보충 문구 13개) | string 값만 바뀐 자리다. 원문 스스로 *"기능 회귀 위험은 사실상 없다"*고 적었다. **쓰다가 어색하면 그때 말해 주면 된다** |
| **`3-21`의 말투 항목**(통계 화면에서 이름이 바뀐 자리·패턴 유형 설정 라벨) | 같은 부류 |
| **`3-20` 6번**(설정 → 저장공간 문구가 '산출물' → '임시 **파일**'로 보이는가) | 같은 부류. 같은 절의 **기능 항목**(동명이인 행 오류 · 세계관 이동 경고 · 월드패키지 오류 문구)은 **`S3`·`S21`이 덮는다** |
| 절 안의 **"종전과 같은가"만 묻는 회귀 항목** — 가장 큰 몫 | 앱을 실제로 쓰면 저절로 드러난다 |
| 절 안의 **순수 계산 대조**(집계·파싱·병합·수식 값) | 순수 JVM 시험 **3,321건**이 이미 실행으로 잠갔다. **화면에 그렇게 그려지는가**만 남겼다 |
| **`3-12` 3번 (b)** — 64MB 큰 백업 스트리밍 | **2026.08.01 사용자 지시로 이미 제외.** 실사용 데이터로 그 크기를 만들 수 없어 절차로는 영영 안 닫힌다. 증명은 JVM 동치 시험 8건이 맡는다 |

> ⚠️ **첫 판은 `3-18`·`3-21`을 *절째* 뺐고, 그 면책이 거짓이었다.** *"기능 항목은 S10이 이미 덮는다"*고 적었는데 **덮지 않았다** — 그 두 절에는 말투가 아니라 **동작 확인**이 섞여 있었다(교차분석 목적문이 표를 만들기 전에 보이는가 · 축이 하나뿐일 때 선택기와 목적문이 *함께* 사라지는가 · 카드 숨기기의 [실행취소]가 여전히 동작하는가 · 없어진 대상을 가리키는 카드의 가드). 적대 검증이 잡아 **`14-ㄱ`~`14-ㄹ`로 되살렸다.**
>
> 같은 검증이 `3-100`(사건 저장)에서도 하나 찾았다 — 첫 판은 **재현이 가장 어려운 실패 확인만 남기고 성공 고지 확인을 버렸는데**, 원문이 *"이 판이 고친 것의 절반은 성공도 안 알려졌다는 쪽"*이라 적어 둔 자리였다. `14-ㅁ`으로 되살렸다.
>
> **가져갈 것: 뺀다고 적을 때는 *무엇이 남는지*까지 확인해야 한다.** 면책 한 줄이 유실을 덮는다.

---

## 부록 2 — **이번에 걷어낸 단계** (전수 · 사유와 근거)

**21단계를 시트에서 걷어냈다** (2026.08.19 사용자 지시 — *"할 수 있는 건 다 걷는 거로 하자. 많아도 너무 많아. 쓸데없이. 간단하게 코드 보고 알 수 있는 것도 있을텐데 그런것까지 복잡하게 검증할 거 없잖아."*).

**지우지 않고 여기 남긴다.** 지우면 *유실*과 구별되지 않는다 — 위 부록이 적어 둔 교훈이 그것이다(*면책 한 줄이 유실을 덮는다*). 아래 표가 **한 단계씩** 사유를 대고, `tools/walkthrough/verify.py`가 그 사유를 기계로 검산한다.

| 사유 | 뜻 | 되살리는 법 |
|---|---|---|
| `시험` | 순수 JVM 시험이 그 판정을 **실행으로** 본다 | 그 시험을 지우면 이 단계가 돌아와야 한다 |

> ⚠️ **`시험`만으로 걷힌 단계는 하나도 없다 — 그럴 수 있는 단계가 거의 없다.** 이 표의 단계는 정의상 *기기에서 눈으로 보는 것*이라, `expect`가 순수 계산의 값만 묻는 일이 사실상 없다. 시험이 계산을 덮어도 **화면이 그 계산을 쓰는가**는 늘 남는다. 그래서 근거는 거의 언제나 둘이다(2026.08.19 콜드 검토 — 처음에 `시험`으로 걷었던 13건을 전부 `시험+코드`로 옮겼다).

| `시험+코드` | **계산은 시험이 실행으로 보고, 화면이 그 계산을 쓰는가는 한 줄이 답한다** | 둘 중 하나가 무너지면 돌아온다 |
| `코드` | 분기·상수·문구 한 자리라 **읽으면 끝난다**. 시험을 새로 만들지 않는다 | 그 자리가 복잡해지면 돌아온다 |

### 시험+코드 — 13단계 (계산은 시험이 · 배선은 한 줄이 답한다)

| 단계 | 원 절 | 무엇을 밟던 것인가 | 근거 |
|---|---|---|---|
| `2-ㄴ-2` | `3-139-ㄱ` | 아무것도 고치지 말고 **두세 번 열었다 저장**한다 → 따옴표가 `"""…"""`로 자라지 않는다. 태그 수가 안 늘어난다 | `MultiValueInputTest.roundTrip_isStableOnSecondPass` · `MultiValueInputTest.roundTrip_tokenWithComma_survivesOpenAndSave` · `CsvTokensTest.joinThenSplit_roundTripsInBothModes` <br> `app/src/main/java/com/novelcharacter/app/ui/character/CharacterEditFragment.kt` 에서 `val tags = MultiValueInput.parse(binding.editTags.text.toString())` — 편집 창의 태그 칸이 저장 때 MultiValueInput.parse를, 적재 때 format을 그대로 지난다 |
| `2-ㄴ-3` | `3-139-ㄷ` | 캐릭터 여럿 고르기 → **일괄 태그** → 쉼표 든 칩을 누른다 → 감싸인 채로 붙고, 적용하면 태그가 **하나** 붙는다(둘이 아니다) | `MultiValueInputTest.roundTrip_tokenWithComma_survivesOpenAndSave` · `MultiValueInputTest.format_isTheSameRuleAsTheExcelCell` <br> `app/src/main/java/com/novelcharacter/app/ui/character/batch/BatchTagBottomSheet.kt` 에서 `binding.tagInput.setText(MultiValueInput.format(existing))` — 칩 누르기가 parse → 중복 거르기 → format 한 벌을 그대로 지난다 |
| `2-ㅂ-3` | `3-11-3` | 자기 자신을 참조하는 수식을 넣는다 → "자기 자신(A)을…"이 뜬다 | `FormulaValidatorTest.자기 자신을 참조하면 알린다` <br> `app/src/main/java/com/novelcharacter/app/ui/field/FieldEditDialog.kt` 에서 `getString(R.string.formula_warn_self_ref, problem.key)` — SelfReference 문제 하나가 그 문구로 바뀌는 자리 — 문구는 여기서만 나온다 |
| `2-ㅂ-4` | `3-11-7` | 정상 수식(`field(힘) + field(민첩) * 2`)을 저장 → 조용히 저장된다 | `FormulaValidatorTest.정상 수식에는 경고가 없다` <br> `app/src/main/java/com/novelcharacter/app/ui/field/FieldEditDialog.kt` 에서 `formula, currentKey, universeFieldKeys, calculatedFormulas, universeFieldTypes` — 문제 목록이 비면 입힐 문구가 없다 — 그것이 곧 "조용히 저장된다"이다. 2026.08.22에 이 호출이 타입 표(`universeFieldTypes`)를 하나 더 받게 바뀌어 앵커가 걸렸고, 그 자리를 다시 읽어 판정이 그대로임을 확인했다 — 새 사유(NonNumericKeys)는 **수를 못 낼 수 있는 타입을 참조할 때만** 붙으므로 숫자 필드만 참조하는 정상 수식은 여전히 조용하다(그 사실은 시험이 잠근다). |
| `3-ㄷ-2` | `3-39-3` | 빈 시트가 든 파일로 덮어쓰기 → **기존 데이터를 지우지 않는다** | `OverwriteGuardTest.내용 있는 행이 없으면 EMPTY이고 지우지 않는다` · `OverwriteGuardTest.스타일만 남은 행은 데이터 행이 아니다 — 내용을 지운 시트는 EMPTY로 판정된다` <br> `app/src/main/java/com/novelcharacter/app/excel/ExcelImportService.kt` 에서 `if (source == RestoreSource.HAS_ROWS) return true` — 덮어쓰기 경로가 판정기에게 물어보고 나서 지운다 — 지울 수 있는 상태(HAS_ROWS)일 때만 삭제가 서고 나머지 둘은 경고만 남기고 건너뛴다. 2026.08.21에 이 자리의 **문구**가 keepReason으로 갈렸을 때 앵커가 깨져 이 단계가 돌아왔고, 다시 읽어 보니 **삭제 게이트는 한 글자도 안 바뀌었다** — 그래서 앵커를 문구가 아니라 게이트 자체로 옮겼다(문구 쪽은 3-ㅅ-11이 기기에서 본다) |
| `3-ㅁ-10` | `3-105` | 대결 축 시트의 영향필드 칸을 본다 → `▼나이` 꼴이다(옛 `-나이`가 아니다). 옛 파일도 같은 뜻으로 들어온다 | `DuelFieldLinksTest.writes only the new marker` · `DuelFieldLinksTest.legacy markers still read as lower wins` <br> `app/src/main/java/com/novelcharacter/app/excel/ExcelExporter.kt` 에서 `row.createCell(4).setTextSafe(DuelFieldLinks.toText(links.influences))` — 영향필드 칸이 toText가 낸 글을 그대로 적는다 |
| `6-ㅂ` | `3-56-3` | 판을 몇 번 돌린다 → 진행률 네 숫자(붙임·접힘·남음/전체)가 바뀐다 | `DuelPairingTest.진행률은 붙인 것 접힌 것 남은 것으로 정확히 갈린다` · `DuelPairingTest.이행 추론으로 접힌다 — 안 붙인 짝이 대기열에서 빠지고 접힘으로 센다` <br> `app/src/main/java/com/novelcharacter/app/ui/duel/DuelPlayFragment.kt` 에서 `value.played, value.folded, value.remaining, value.total` — 진행률 문구가 Progress의 네 수를 그대로 받는다 |
| `6-ㅈ2` | `3-67-3` | 맨 아래 등급을 하나 더한다 → **종전 최하 구간이 0%가 안 된다** | `DuelGradeAssignTest.reconcile - 맨 아래에 등급이 붙으면 옛 마지막 등급이 100퍼센트를 받는다` <br> `app/src/main/java/com/novelcharacter/app/ui/field/FieldEditDialog.kt` 에서 `val reconciled = com.novelcharacter.app.util.DuelGradeAssign.reconcile(` — 등급을 고쳐 저장하는 자리가 reconcile이 낸 구간을 그대로 쓴다 |
| `8-ㄴ-2` | `3-125-7` | 몸통에서 **슬림**을 고르고 굴린다 → 그림 아래 요약이 **슬림**이라고 말한다(고른 말과 돌아오는 말이 같다) | `BodyGeneratorTest.몸통 축은 요약의 몸통 라벨과 일치한다` <br> `app/src/main/java/com/novelcharacter/app/ui/character/BodySilhouetteEditorSheet.kt` 에서 `val summary = BodySilhouetteSpec.axisSummary(current, analysisConfig)` — 편집기의 요약 줄이 axisSummary가 낸 말을 그대로 건다 |
| `8-ㄷ-3` | `3-113-ㄷ` | 그 축을 골라 굴린다 → 요약이 **바꾼 이름**으로 말한다 | `BodyGenerationConfigTest.사용자가 정의한 몸통 축도 요약이 같은 이름으로 답한다` · `BodyGenerationConfigTest.사용자가 정의한 힙 축도 요약이 같은 이름으로 답한다` <br> `app/src/main/java/com/novelcharacter/app/ui/character/BodySilhouetteEditorSheet.kt` 에서 `val summary = BodySilhouetteSpec.axisSummary(current, analysisConfig)` — 같은 자리 — 축 이름을 바꿔도 요약이 그 함수에서 온다 |
| `8-ㄷ-9` | `3-135-ㄹ` | 그 축을 고른 채 🎲를 여러 번 굴린다 → 요약의 힙 이름이 **고른 축과 다르게** 나온다(경고가 예언한 그 일이다) | `BodyBandGapTest.잡힌 힙 축을 굴리면 요약이 이웃 축 이름으로 답한다` <br> `app/src/main/java/com/novelcharacter/app/ui/character/BodySilhouetteEditorSheet.kt` 에서 `val summary = BodySilhouetteSpec.axisSummary(current, analysisConfig)` — 같은 자리 — 잡힌 축의 이웃 이름도 그 함수가 낸다 |
| `10-ㅁ` | `3-83-7` | 캐릭터 이름을 바꾼다 → 옛 링크가 풀린다 | `NameBankMatchTest.이름이 바뀌면 옛 링크를 푼다` · `NameBankMatchTest.이름이 그대로면 풀지 않는다` <br> `app/src/main/java/com/novelcharacter/app/ui/character/CharacterViewModel.kt` 에서 `val plan = com.novelcharacter.app.util.NameBankMatch.planOnSave(` — 저장 경로가 planOnSave의 계획을 그대로 실행한다 |
| `12-ㅋ` | `3-46-1·2` | 창작도를 바꾼다 → 그 자리에 고지가 뜨고, **되돌리면 사라진다** | `AiCreativityTest.globalChangeNotice_namesTheNewDefault` · `AiCreativityTest.globalChangeNotice_disappearsWhenRevertedInPlace` · `AiCreativityTest.globalChangeNotice_silentWhenUnchanged` <br> `app/src/main/java/com/novelcharacter/app/ui/character/CreativityChipRow.kt` 에서 `globalNote.visibility = if (changed == null) View.GONE else View.VISIBLE` — 고지 줄의 보임 여부가 globalChangeNotice의 답 그대로다 — 되돌리면 null이라 사라진다 |

### 코드 — 8단계 (코드를 읽으면 끝나는 것)

| 단계 | 원 절 | 무엇을 밟던 것인가 | 근거 |
|---|---|---|---|
| `0-ㅁ` | `—` | 캐릭터를 열어 칸 몇 개를 채우고 나온다 → 버튼을 여러 번 누르지 않아도 저장된다 | `app/src/main/java/com/novelcharacter/app/ui/character/CharacterEditFragment.kt` 에서 `CharacterDraftPrefs.save(ctx, characterId, collectDraft())` — onPause가 CharacterDraftPrefs.save(ctx, characterId, collectDraft())를 부른다 — 저장 버튼 없이 남는 경로가 그 한 줄이다 |
| `1-ㅊ` | `3-34-4` | 카드 오른쪽 위 **⚙**를 누른다 → 그 필드의 편집 창이 열린다 | `app/src/main/java/com/novelcharacter/app/ui/character/DynamicFieldRenderer.kt` 에서 `/** 머리 행 — 제목 + ⚙(체형 분석 설정 바로가기, 터치 48dp). */` — 카드 머리 행의 ⚙이 그 필드의 편집 다이얼로그를 여는 인텐트 한 줄이다 |
| `2-ㄱ-1` | `3-27-1` | 필드 관리에서 칩이 **셋**(캐릭터/사건/작품)인지 본다 → 셋 다 있다 | `app/src/main/java/com/novelcharacter/app/ui/field/FieldManageFragment.kt` 에서 `checkedIds.contains(R.id.chipEventFields) -> FieldDefinition.ENTITY_EVENT` — chipCharacterFields·chipEventFields·chipNovelFields 셋을 when이 그대로 가른다 |
| `5-ㅎ` | `3-10-3` | 그 창에서 **모든 유형의 체크를 해제**하고 저장 → 카드가 통째로 사라지지 않는다(**되돌릴 길이 남는다**) | `app/src/main/java/com/novelcharacter/app/ui/stats/StatsMainFragment.kt` 에서 `enabled.isEmpty() -> getString(R.string.stats_pattern_all_types_off)` — enabled.isEmpty() 가지가 stats_pattern_all_types_off 문구를 세운다 — 카드가 사라지는 가지가 아예 없다 |
| `6-ㅍ` | `3-85-2` | **이미지 축**을 만든다 → 누르면 대결이 아니라 **캐릭터 고르는 화면**이 먼저 뜬다 | `app/src/main/java/com/novelcharacter/app/ui/duel/DuelAxisListFragment.kt` 에서 `val target = if (axis.isImageAxis && destination != R.id.duelMatchesFragment) {` — axis.isImageAxis && destination != duelMatchesFragment 이면 duelImageCharacterFragment로 보낸다 — 분기 한 줄 |
| `8-ㄹ-5` | `3-114-ㅂ·ㅅ` | 설정에서 **필드 설명 ⓘ**를 켠다 → 설명을 적은 필드에만 ⓘ가 붙는다. 시간 보기·보충 탭도 따른다 | `app/src/main/java/com/novelcharacter/app/ui/character/DynamicFieldRenderer.kt` 에서 `val hasNote = showFieldNotes &&` — hasNote = showFieldNotes && FieldDescription.fromConfig(field.config).isNotBlank() — 설정과 설명 유무를 한 줄이 함께 본다 |
| `9-ㅂ` | `3-26-2` | 작품을 고르기 **전에는** 그 버튼이 안 보이는지 본다 → 안 보인다 | `app/src/main/java/com/novelcharacter/app/ui/timeline/EventEditDialogFragment.kt` 에서 `binding.btnAddEventField.visibility = if (globalScope) View.GONE else View.VISIBLE` — btnAddEventField.visibility = if (globalScope) GONE else VISIBLE — 작품이 없으면(전역 구역) 추가 버튼이 숨는다 (B-258 ⓐ 이후의 배선 — 판정 축이 fieldPathKnown에서 globalScope로 갈렸고 화면 동작은 같다) |
| `10-ㅊ2` | `3-51-4` | 그 카드를 누른다 → 그 자리로 데려간다 | `app/src/main/java/com/novelcharacter/app/ui/search/GlobalSearchFragment.kt` 에서 `findNavController().navigateSafe(R.id.globalSearchFragment, R.id.characterDetailFragment, bundle)` — 카드 종류마다 navigateSafe 한 줄이 그 자리로 보낸다(캐릭터·연표·목록) |


---

> **시간이 정말 없으면 이 넷만:** `S1`(15분 · 앱이 사는가) · `S6`(30분 · 준비 3분에 17단계) · `S20`~`S22`(엑셀 왕복 · 개발 의도의 핵심) · `S16`(휴지통 · 되돌릴 수 없는 것).
