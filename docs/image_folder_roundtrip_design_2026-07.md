# 이미지 폴더 왕복 — 구현 설계·착수 문서 (2026-07)

> **위상: 착수 — 이 문서가 구현의 단일 소스다.** 결정 근거·논의 이력은
> `docs/image_external_management_2026-07.md`(v0.2, 결정 D1~D8)에 있고, 여기는 그 결정을
> 그대로 구현할 수 있는 사양·파일 지도·순서를 담는다.
>
> **v1.1 갱신:** 착수 전 비판적 검토가 v1.0의 "미결정 사항 없음" 선언을 **정정했다** —
> 결함 1건(심각)과 미결정 8건을 찾아 9장에 기록하고, 그 결론을 2·3·6·7·8장에 반영했다.
> 아래 본문은 이미 정정본이다(9장은 근거 기록).

## 0. 새 세션 착수 절차

1. **읽기 순서**: 이 문서 전체 → `image_external_management_2026-07.md` 0장(결정 표) →
   `CLAUDE.md`(원칙·체크리스트·브랜치 규칙). 자동 링크의 동작 계약은
   `util/AutoLinkPlanner.kt`·`util/CharacterImageAutoLinker.kt` 상단 KDoc이 단일 소스다.
2. **브랜치**: CLAUDE.md 규칙대로 최신 `origin/master`에서 지정 작업 브랜치를 분기
   (`git fetch origin master && git checkout -B <작업브랜치> origin/master`). PR base는 항상
   `master`, 병합 후 브랜치 삭제.
3. **검증 환경**: `JARS_DIR=<스크래치패드> tools/setup_jvm_env.sh` → `tools/run_jvm_tests.sh`
   (착수 시점 기준선 **760건** — 손대기 전에 이 숫자부터 재현할 것.
   `dl.google.com` 차단으로 `./gradlew`는 로컬에서 실패한다 — CI가 최종 컴파일 검증).
4. **구현 순서는 8장** — PR-1(받아오기)부터. 순수 로직(플래너)을 먼저 만들고 JVM 테스트로
   계약을 고정한 뒤 UI·SAF를 배선한다.

## 1. 기능 요약과 용어

**정리 폴더**: 사용자가 지정하는 SAF 폴더 하나. 두 방향으로 쓴다.

- **받아오기(PR-1)**: 정리 폴더를 스캔해 ① 새 파일(토큰 없음)을 라이브러리로 편입하고
  ② 토큰 있는 파일(앱이 내보낸 사본)의 폴더 위치를 캐릭터 배정·링크에 반영한다.
  처리한 원본은 폴더 안 `_처리됨/`으로 이동한다.
- **정리용 내보내기(PR-2)**: 현재 라이브러리를 폴더 구조(캐릭터별/미배정/세트)로 사본
  내보내기. 파일명에 정체성 토큰을 심어, 바깥에서 폴더만 옮긴 뒤 받아오기가 그 재배열을
  메타데이터에 반영할 수 있게 한다.

**설계 불변식** (전 항목에 우선):
- **앱이 원본을 소유한다.** 폴더의 파일은 유입 대기물 또는 내보낸 사본이다. 정리 폴더에서
  파일이 없어져도 앱 데이터는 불변 — **"폴더에 없음 ≠ 삭제"**. 삭제는 인앱 명시 조작뿐.
- **받아오기는 메타데이터(배정·링크)와 신규 편입만 쓴다.** 기존 이미지 파일 내용을 덮어쓰는
  경로는 없다(내보낸 사본을 바깥에서 편집해도 앱 원본은 그대로 — 교체는 2차 후보도 아님,
  하려면 별도 명시 기능으로).
- **캐릭터 배정만 다룬다.** 작품·세계관 배정은 읽지도 쓰지도 않는다(그쪽은 엑셀 경로 유지).
- 양방향 동기화(폴더가 원본)는 **영구 반려**(결정 D8) — 이 문서 범위에서 재론하지 않는다.

## 2. 이름 토큰 규약 (결정 D4)

- 내부 파일명은 `char_<UUID>.jpg`·`img_<UUID>.jpg` 형태다(`StorageAnalyzer.IMAGE_PREFIXES`).
  **토큰 = 그 UUID의 소문자 16진 앞 12자**(하이픈 제거 기준, 즉 `[0-9a-f]{12}`). 12자(48비트)면
  수천 장에서도 충돌 확률이 무시 가능하고, 내보내기 시점에 전 파일 대조로 유일성을 **검증**한다
  — 겹치면 겹친 파일들만 전체 UUID(하이픈 제거 32자)로 연장(결정적).
- 내보내기 파일명: **`[라벨]-[번호].[토큰].[확장자]`** (예: `가온-01.3f9a2c407b16.jpg`).
  - 라벨 = 캐릭터명(미배정은 `이미지`)을 파일명 안전 문자로 정리(금지 문자 `\/:*?"<>|` +
    제어문자 → `_`, 앞뒤 공백·마침표 제거, **32자로 절단**, 빈 결과면 `이미지`).
    절단은 파일명 길이 상한(255바이트 · 한글 UTF-8 3바이트) 대비다.
    **라벨·번호는 사람용일 뿐** — 정체성은 토큰만.
  - 번호 = 폴더 안 순번 2자리(01부터, 100 이상은 자리수 확장). 정렬용일 뿐 의미 없음.
- 받아오기 매칭: 파일명 끝에서 두 번째 점 구간을 토큰 후보로 읽는다(`이름.토큰.확장자`).
  스캔 시점에 앱 관리 파일 전체로 "토큰 → 내부 경로" 사전을 같은 규칙(12자 + 연장분은 전체
  UUID도 등재)으로 만들고 **정확 일치만** 매칭한다. 같은 토큰이 폴더에 두 번 있으면
  그 토큰은 **반영 보류 + 고지**(어느 위치가 의도인지 알 수 없다 — 변수 제어).
- **토큰은 영구 식별자가 아니다(검토 C-1).** 앱이 파일을 새 UUID로 **개명하는 경로가 둘** 있다:
  재압축 커밋(`ImageManagerViewModel.commitInternal`)과 zip 복원의 경로 재배치
  (`ExcelImporter.buildImageRemap` — 원 파일이 없을 때). 개명 뒤 돌아온 사본은 사전에 없어
  **중복 편입**된다(D4가 막겠다던 결과 그 자체). 그래서:
  - 두 개명 지점이 `FolderRoundtripPrefs.recordRename(옛 경로 → 새 경로)`로 **토큰 별칭**을
    남기고, 토큰 사전이 별칭도 함께 싣는다(상한 있는 SharedPreferences — 스키마 변경 없음).
  - 별칭으로도 못 찾은 **토큰꼴이지만 사전에 없는 파일**은 신규로 편입하되 **개수를 고지**한다
    (조용한 중복 금지). 토큰꼴이 아닌 파일은 평범한 신규이므로 고지 대상이 아니다.
- UUID 형태가 아닌 내부 파일명(레거시)은 토큰을 만들 수 없다 → 내보내기에서 개수 고지 후
  제외(받아오기 위치 반영 불가 — 신규 편입만 가능한 파일로 남는다).

## 3. 폴더 규약 (결정 D3·D5)

예약 폴더명은 `_` 접두다. 스캔 대상은 정리 폴더 직속 + **2단계 하위까지**이며, 2단계에서
반영 대상이 되는 것은 `_미배정/<세트명>/`뿐이다(그 밖의 2단계 파일은 개수만 고지하고 무시).
v1.0은 "직속 + 1단계"라 적어 아래 표의 `_미배정/<세트명>/`(2단계)과 서로를 부정했다 —
정정본이 이것이고, 코드의 단일 소스는 `FolderRoundtripPlanner.MAX_SCAN_DEPTH`다(검토 C-11).
**예약 폴더 하위는 계수에서도 뺀다** —
`_처리됨/<원폴더명>/`이 곧 2단계라, 세면 받아올 때마다 "무시된 파일 N장"이 처리 이력만큼
불어나 거짓 신호가 된다(검토 C-10).

| 위치 | 신규 파일(토큰 없음) | 토큰 파일(기존 이미지) |
|---|---|---|
| `<캐릭터명>/` (캐릭터 정확 일치) | 편입 + 그 캐릭터 배정 → 자동 링크가 묶음 | 캐릭터 배정을 그 캐릭터로 **이동** |
| `<기타 이름>/` (일치 캐릭터 없음) | 편입 + 미배정 + **그 폴더끼리 링크 세트**(2장 이상일 때, 수동 UUID 그룹) | 배정 변경 없음 + 그 폴더 세트로 링크(기존 수동 링크 규약대로 흡수·병합 판정) |
| 정리 폴더 직속 · `_미배정/` 직속 | 편입 + 미배정(링크 없음) | 캐릭터 배정 **전부 해제**(미배정으로 이동 — 기존 배정 해제 규약: 파일 유지·라이브러리 승격) |
| `_미배정/<세트명>/` | 편입 + 미배정 + 링크 세트 | 배정 해제 + 그 세트로 링크 |
| `_공유/` | 편입 + 미배정(고지) | **반영 제외**(공유 이미지는 위치 이동의 의미가 모호 — 고지만) |
| `_처리됨/` | 스캔 제외 | 스캔 제외 |

- **캐릭터명 매칭**: 트림 후 정확 일치(대소문자 구분은 이름 그대로 — 한글 중심이라 무의미).
  **동명 캐릭터가 2명 이상이면 배정 보류** — 그 폴더는 "기타 이름" 행처럼 처리(신규는 링크
  세트로 편입)하고 요약에 "동명 캐릭터 N명 — 폴더 '가온' 배정 보류"로 고지한다. 어느 한 명을
  임의로 고르는 것은 오배정이고, 오배정은 생략보다 나쁘다(R-1과 같은 취지).
  링크 세트로 들어가므로 인앱에서 한 장만 올바른 캐릭터에 배정하면 세트 전체가 따라간다
  (부드러운 실패 — 결정 D3의 근거).
- **캐릭터 배정 "이동"의 범위**: 단독 소유(캐릭터 소유자 1명 이하) 이미지에만 적용한다.
  캐릭터 소유자가 2명 이상인 이미지는 내보내기가 `_공유/`로 분류하고 받아오기가 반영을
  제외한다(위 표). 작품·세계관 소유는 이동 판정과 무관하며 불변이다(1장 불변식).
  - **판정의 축은 폴더 위치가 아니라 현재 소유 상태다(검토 C-2).** 위 표는 폴더 위치로,
    이 문단은 소유자 수로 판정해 둘이 충돌한다 — 사용자가 `_공유/`에서 파일을 꺼내
    캐릭터 폴더에 넣으면 표는 "이동", 문단은 "제외"다. **현재 상태가 이긴다**: 캐릭터
    소유자가 2명 이상이면 폴더 위치와 무관하게 이동·해제를 **보류하고 고지**한다.
    한쪽을 임의로 고르는 이동은 오배정이고, 전부 떼는 해제는 사용자가 지시하지 않은
    파괴다 — 둘 다 생략보다 나쁘다(R-1과 같은 취지). 그래서 플래너 입력에는 파일별
    **현재 캐릭터 소유자 수**가 함께 들어간다.
- **처리 후 이동(D5)**: 반영·편입에 성공한 원본은 `_처리됨/<원폴더명>/`으로 이동한다.
  SAF의 `moveDocument`는 provider가 지원할 때만 되므로 **복사 후 원본 삭제**를 폴백으로 둔다
  (검토 C-4). 이동 실패(SAF 권한·이름 충돌) 시 파일은 그대로 두고 **지문(내용 무관:
  상대경로+크기+수정 시각)을 기록**해 다음 스캔에서 중복 편입을 막는다(지문 저장은
  SharedPreferences — DB 스키마 변경 없음. **상한 2000건, 오래된 것부터 폐기** — 무한 누적
  방지, 검토 C-9). 실패·보류 파일은 이동하지 않는다(남아 있는 것 = 미처리의 표식).

## 4. 받아오기 흐름 (PR-1)

1. **나열**: SAF 트리에서 대상 파일 목록 수집(이미지 확장자만, `_처리됨/` 제외, 2단계 이상
   깊이는 계수만). 총량 확정 → 진행도 시작.
2. **계획**: 순수 플래너(6장)가 파일 목록 + 캐릭터 사전 + 토큰 사전 + 현재 배정으로
   "편입/이동/해제/링크/보류" 계획과 고지 목록을 만든다.
3. **확인**: 요약 다이얼로그로 사전 고지(신규 N·배정 이동 M·배정 해제 K·링크 세트 s개·보류
   목록). **실행은 항상 명시 확인 뒤** — 폴더 재배열은 대량 메타 변경이라 조용히 반영하지
   않는다(이미지 탭의 링크 확장 사전 확인과 같은 취지 — 조용한 확대 금지). 진입 감지
   배너(5장)를 탭해 왔어도 이 확인은 거치게 한다.
   - **기존 링크 병합도 이 한 번의 확인에 싣는다(검토 C-8).** 인앱 `linkImages`는 기존 그룹
     2개 이상에 걸치면 그때마다 사용자 확인을 요구하는데(`LinkOutcome.NeedsMerge`), 폴더는
     세트가 여럿이라 그 규약을 그대로 쓰면 확인이 폴더 수만큼 뜬다. 요약에 "기존 링크
     N개가 합쳐집니다" 한 줄을 싣고 **개별 확인은 띄우지 않는다** — 사전 고지의 목적
     (조용한 확대 금지)은 그 한 줄로 충족되고, 반복 확인은 조작 마찰(원칙 04)이다.
4. **실행**: 항목 단위로 완결 처리(신규 편입은 기존 `ImageImportHelper.importImage` 압축
   파이프라인 재사용, 배정 변경은 트랜잭션). 진행도 갱신, **취소 = 중단 시점까지 반영 + 요약**
   (항목 단위 완결이라 반쪽 항목이 없다).
5. **마무리**: `CharacterImageAutoLinker.resyncIfEnabled` 1회 호출(배정 변경분의 자동 링크
   수렴 — 기존 진입점들과 동일 규약), 처리분 `_처리됨/` 이동, 결과 요약(성공/보류/실패 각
   개수 + 보류·실패는 파일명 목록), 이미지 탭 재로딩.

## 5. 진입점·설정 UI

- **이미지 관리 탭 메뉴**에 추가: "정리 폴더 받아오기" / "정리 폴더로 내보내기(PR-2)" /
  "정리 폴더 설정"(지정·해제·현재 경로 표시). 폴더 미지정 상태에서 앞 둘을 누르면 지정
  플로우로 안내.
- **진입 감지(D5)**: 이미지 탭 진입 시 폴더가 지정돼 있으면 비동기로 신규 후보 개수만 센다
  (나열만 — 실패·지연 시 조용히 생략, 수동 메뉴가 항상 있다). 있으면 배너/칩
  "정리 폴더에 새 이미지 N장" → 탭 = 4장 3단계(확인)로 진입.
- **SAF**: `ACTION_OPEN_DOCUMENT_TREE` + `takePersistableUriPermission`. URI는
  `ImageSettingsStore`에 문자열 키로 저장. 권한 소실(폴더 삭제·권한 회수) 시 "정리 폴더에
  접근할 수 없습니다" + 재지정 경로 고지(설정 자동 해제하지 않는다 — 사용자가 상황을 보고
  결정).
  - **`androidx.documentfile` 의존은 넣지 않는다(검토 C-4).** 지금 코드베이스에 SAF 트리
    전례가 0이고 그 의존도 없는데, `dl.google.com` 차단으로 새 androidx 의존은 **로컬에서
    해결조차 되지 않아** CI에서만 확인된다. 나열은 `DocumentsContract.buildChildDocumentsUriUsingTree`
    + `ContentResolver.query`(투영: id·이름·MIME·크기·수정시각)로 직접 한다 — 폴더당 쿼리
    1회라 `DocumentFile`(파일마다 쿼리)보다 수백 장 규모에서 확연히 빠르다.
- **진입 감지 결과는 캐시하지 않는다(검토 C-12).** "루트 수정 시각이 그대로면 직전 개수를
  재사용한다"는 가드를 검토했으나 **반려**했다 — SAF 트리의 루트 `lastModified`는 하위 폴더의
  변경을 반영하지 않아, 그 캐시는 하위 폴더에 넣은 새 이미지를 조용히 놓친다. 느린 것보다
  **못 보는 것**이 나쁘다. 진입 감지는 매번 실제로 나열하되 비동기이고 실패 시 조용히
  생략한다(수동 메뉴가 항상 있다). 나열은 폴더당 쿼리 1회라 수백 장 규모에서 감당된다.
- **텍스트**: 새 설정·메뉴에는 R-25 목적문 의무 + `tools/check_text_style.sh` 통과.
  정리 폴더 설정 목적문 초안: "폴더를 지정하면 그 폴더에 넣은 이미지를 이미지 탭에서
  받아올 수 있고, 정리용 내보내기의 대상 폴더로 쓰입니다."

## 6. 순수 로직(JVM 테스트 대상)과 파일 지도

**신규** (전부 Android 무의존 계획 로직 + 테스트, `ImageLinkResolver`/`AutoLinkPlanner` 관례):

| 파일 | 역할 |
|---|---|
| `util/FolderNameToken.kt` | 토큰 규약의 단일 소스 — 내부 파일명→토큰, 내보내기 파일명 조립/해석, 라벨 정리(32자 절단), 충돌 연장, 토큰 사전 조립(별칭 포함). |
| `util/FolderRoundtripPlanner.kt` | 스캔 항목(상대경로·토큰 유무) + 캐릭터명 사전(동명 목록) + 토큰 사전 + 현재 캐릭터 배정(소유자 수 포함) → 편입/이동/해제/링크 세트/보류 계획. 예약 폴더·깊이 규칙·중복 토큰·동명 보류·공유 보류 판정 전부 여기. |
| `util/FolderExportPlanner.kt` (PR-2) | 현재 상태 → 내보내기 배치 계획(캐릭터/`_미배정`/`세트-n`/`_공유` 버킷, 파일명 조립, 레거시 제외 계수, 총 용량 집계). |
| `app/src/test/.../FolderNameTokenTest.kt` 등 | 각 플래너의 계약 테스트. **`tools/run_jvm_tests.sh`의 SOURCES/TESTS 목록 갱신 필수**(빼먹으면 하네스가 조용히 그 파일을 검증 안 한다 — PR #88 전례). |

**신규 (Android 계층)**:

| 파일 | 역할 |
|---|---|
| `util/OrganizeFolderService.kt` | SAF 나열(`DocumentsContract` 직접)·이동(+복사 후 삭제 폴백) + 플래너 계획의 DB 적용(트랜잭션) + 지문 기록. `CharacterImageAutoLinker`처럼 object + suspend, IO 디스패처. |
| `util/FolderRoundtripPrefs.kt` | SharedPreferences 하나 — ① 이동 실패 지문(상한 2000, FIFO) ② 토큰 별칭(개명 경로, 상한 2000). 규칙(상한·FIFO·연쇄 접기·직렬화)은 `util/FolderRoundtripLedger.kt`(순수)에 있고 JVM 테스트 대상이다. |
| `ui/common/TaskProgressDialog.kt` + 레이아웃 | **공용 진행도 컴포넌트(R-26)** — 제목/현재 단계/결정형 막대/"N/M (p%)"/취소(옵션). `HelpDialog`(ui/common) 관례. 기존 `util/ProgressDialogs.kt`(불확정 스피너)와 **역할이 다르다** — 작업형=결정형(이쪽) / 조회형=불확정(기존). 두 KDoc에 서로를 적어 둘 것(검토 C-5). 이번 작업에서 신설하고 폴더 왕복에 적용, 기존 작업 소급은 B-51. |
| `ImageManagerFragment`/`ImageManagerViewModel` 확장 | 메뉴 3항목, 진입 감지 배너, 확인·요약 다이얼로그. |

**재사용(수정 최소)**: `ImageImportHelper.importImage`(압축 편입), `ImageMetaDao.adopt`/
`setGroup`/`clearGroupIfSingleton`(링크 세트 — 수동 UUID 그룹 생성은 기존 `linkImages` 흡수·
병합 규약과 동일하게), `ImageManagerViewModel.unassign`의 해제 규약(파일 유지·승격),
`CharacterImageAutoLinker.resyncIfEnabled`(마무리 재동기화), `ImageSettingsStore`(폴더 URI 키
추가), 가져오기 요약 다이얼로그 문구 관례(F1-A류 고지).

**DB**: 스키마 변경 **없음**(지문은 SharedPreferences). 마이그레이션·하네스 불필요.

**엑셀 곁다리(결정 D2, PR-1에 포함)**: `SheetSpec.imageMetaSpec`의 링크그룹 열 `readOnly`
해제 + 사용 안내 시트에 이미지 시트 편집 규약 3줄(태그·링크그룹 편집법, 같은 문자열 = 같은
묶음, `char:` 토큰은 캐릭터 자동 링크라 재가져오기 때 현재 배정 기준으로 재계산됨).

- **`importImageMeta`는 무변경이 아니다 — "빈 칸 = 링크 해제"를 함께 넣는다(검토 C-3).**
  지금 코드는 링크그룹 칸이 비면 **아무 일도 하지 않고 고지도 없다**
  (`ExcelImportService.importImageMeta`의 `if (groupToken.isNotBlank())`). 사용 안내는
  "열은 두되 칸을 비우면 값이 지워집니다"를 전역 규약으로 선언하고 바로 옆 태그 열은 그
  규약을 지킨다. 회색(앱이 채우는 열)일 때는 덜 문제였지만 편집 가능으로 여는 순간
  "엑셀에서 링크를 지웠는데 그대로"가 된다 — 조용한 무시다.
  규약은 태그 열과 동일하게 맞춘다: **열이 없으면 기존 유지, 열이 있고 빈칸이면 해제.**
- 해제 대상이 자동 링크(`char:`)면 재동기화가 도로 묶으므로 **그 수를 함께 고지**한다
  (`UnlinkResult.autoRelinkable`와 같은 취지 — 해제가 조용히 되돌아가지 않게).

## 7. 실패·경계 사례 (요약 고지 의무 — 조용한 유실 금지)

| 사례 | 처리 |
|---|---|
| 동명 캐릭터 폴더 | 배정 보류 + 링크 세트 편입 + 고지(3장) |
| 같은 토큰이 두 위치에 | 그 토큰 반영 보류 + 고지 |
| 토큰이 사전에 없음 | 신규 파일로 편입(정확 일치만 신뢰) + **토큰꼴이면 개수 고지**(C-1) |
| 캐릭터 소유자 2명 이상 | 폴더 위치와 무관하게 이동·해제 보류 + 고지(C-2) |
| 링크 세트가 기존 그룹에 걸침 | 사전 확인 요약에 병합 건수 한 줄, 개별 확인 없음(C-8) |
| 2단계 이상 깊이 | 무시 + 개수 고지 |
| 이미지 아닌 파일 | 무시 + 개수 고지 |
| 편입 실패(손상·복사 실패) | 실패 계수 + 파일명 고지, 원본은 이동하지 않음 |
| `_처리됨` 이동 실패 | 지문 기록으로 중복 방지 폴백(3장) |
| SAF 권한 소실 | 접근 불가 고지 + 재지정 경로(5장) |
| 공유(캐릭터 2+) 토큰 파일 | 반영 제외 + 고지(3장) |
| 취소 | 중단 시점까지 반영 + 요약(4장) |

## 8. 구현 순서와 검증

**PR-1 — 받아오기 + 공용 진행도 + 엑셀 곁다리** (이것만으로 신규 유입·폴더 묶음 완성):
1. `FolderNameToken` + `FolderRoundtripPlanner` + `FolderRoundtripPrefs`의 순수부
   + JVM 테스트(러너 등재) — 계약 고정 먼저.
2. `TaskProgressDialog`(공용) 신설.
3. `OrganizeFolderService` + SAF 유틸(`DocumentsContract` 직접) + 지문·별칭 기록,
   개명 두 지점(재압축 커밋·zip 복원)에 별칭 기록 배선(C-1).
4. UI 배선(메뉴·배너·확인/요약 다이얼로그·설정) + 문자열(R-25 목적문).
5. 엑셀 곁다리(D2 — 열 개방 + **빈 칸 = 해제** + 안내) + `remaining_work` 백로그 갱신.
6. 검증: `run_jvm_tests.sh`(기준선 760 + 신규 전부), `check_text_style.sh`,
   `check_resources.sh`, 차분 컴파일(기준선 대조 — 노이즈 판별 절차는 스크립트 헤더 참조),
   CI(gradle 컴파일 + 단위 테스트)까지 확인 후 master 병합.

**PR-1의 한계 — 실기기 확인 범위(고지)**: PR-2가 없는 동안 정리 폴더에는 **토큰 있는 파일이
존재하지 않는다**(내보내기가 유일한 발생원). 그래서 3장 표의 오른쪽 열(이동·해제·공유 제외)은
PR-1에서 JVM 테스트로만 계약이 고정되고 실기기 확인은 PR-2 이후가 된다. PR-1의 실기기
확인 대상은 신규 편입·링크 세트·동명 보류·`_처리됨/` 이동이다.

**PR-2 — 정리용 내보내기**:
1. `FolderExportPlanner` + JVM 테스트(버킷·파일명·충돌 연장·용량 집계).
2. 내보내기 실행(사본 복사 + 진행도 + 취소) + 대상 선택(전체/캐릭터별/미배정만) +
   사전 용량 고지 + "사본이므로 정리 후 폴더를 지워도 됩니다" 안내.
3. v1 범위 명시: 대상 = 캐릭터 배정 이미지 + 미배정 라이브러리. 작품·세계관 전용 이미지는
   제외하고 개수만 고지(폴더 규약이 캐릭터 중심이라 재배열 의미가 없음 — 2차에서 규약과
   함께 검토).
4. 검증 동일.

**새 기능 체크리스트(CLAUDE.md) 통과 근거 메모**: ①확장성(폴더 규약이 캐릭터 수·이미지 수에
독립, 세트 무제한) ②실질성(유입·정리 마찰 제거 — 실사용 동인) ③완결성(경계 사례 표 7장)
④연결성(자동 링크·라이브러리·기존 임포트 파이프라인 재사용) ⑤조작성(폴더에 넣기 = 조작의
전부, 진행도·취소) ⑥의도 부합(받쳐주는 확장성·변수 제어·자율성·엑셀 왕복 불침해)
⑦텍스트(R-25 목적문 + 검사 통과).

## 9. 착수 전 비판적 검토 (2026-07-28) — 근거 기록

v1.0은 "1차 범위의 미결정 사항은 없다"고 선언했다. 착수 전 현행 코드 대조로 그 선언을
검증한 결과 **결함 1건(심각)과 미결정 8건**이 나왔다. 결론은 이미 본문에 반영했고, 여기는
"왜 그렇게 정했는가"의 기록이다.

| # | 발견 | 근거 | 처분 |
|---|------|------|------|
| **C-1** | **토큰이 영구 식별자가 아니다(심각).** 재압축 커밋(`ImageManagerViewModel.commitInternal:500` — `"${prefix}_${UUID.randomUUID()}.jpg"`)과 zip 복원(`ExcelImporter.buildImageRemap:592` — 원 파일 부재 시 새 UUID)이 파일을 **개명**한다. "사전에 없는 토큰 = 신규"라는 v1.0 규칙은 그 사본이 돌아올 때 **중복 편입**을 만든다 — D4가 "중복 유입 원천 차단"이라 적은 바로 그 자리가 뚫려 있었다 | 코드 직접 확인 | 2장: 개명 지점이 **토큰 별칭**을 남기고 사전이 함께 싣는다 + 못 찾은 토큰꼴은 **고지 후** 신규 편입 |
| **C-2** | **'이동' 판정의 축이 둘이다.** 3장 표는 폴더 위치로, 바로 아래 문단은 캐릭터 소유자 수로 판정한다. `_공유/`에서 꺼내 캐릭터 폴더에 넣으면 두 규칙이 반대 답을 낸다 | 문서 내부 모순 | 3장: **현재 소유 상태가 이긴다** — 소유자 2명 이상이면 위치 무관 보류+고지 |
| **C-3** | **D2 곁다리가 조용한 무시를 노출한다.** `importImageMeta`는 링크그룹 칸이 비면 아무 일도 하지 않고 고지도 없다. 사용 안내의 전역 규약("칸을 비우면 값이 지워집니다")과 옆 칸(태그)이 지키는 규약을 링크그룹만 어긴다. 회색 열일 땐 덜 드러났지만 **편집 가능으로 여는 순간** "지웠는데 그대로"가 된다 | 코드 직접 확인 | 6장: D2에 **빈 칸 = 해제** 포함(+ 자동 링크 재결합분 고지) |
| **C-4** | **`androidx.documentfile` 의존이 없고 SAF 트리 전례도 0이다.** 새 androidx 의존은 `dl.google.com` 차단으로 **로컬 해결 불가** → CI에서만 확인된다. `DocumentFile`은 파일마다 쿼리라 느리기도 하다 | `app/build.gradle.kts` · 전수 grep | 5·6장: 의존 없이 `DocumentsContract`+`query` 직접, 이동은 `moveDocument`→복사+삭제 폴백 |
| **C-5** | 공용 진행도가 기존 `util/ProgressDialogs.kt`(불확정)와 이름·역할이 겹쳐 보인다 | 기존 파일 | 6장: 역할 분리(작업형=결정형 / 조회형=불확정)를 양쪽 KDoc에 명시 |
| **C-6** | 2장 토큰 예시 `3f9ak2c407b1`의 `k`는 16진이 아니다 — 규칙과 예시가 어긋난다 | 문서 오류 | 2장: 예시 교체 + `[0-9a-f]{12}` 명시 |
| **C-7** | 라벨 길이 상한이 없다. 캐릭터명이 길면 파일명이 255바이트를 넘어 내보내기가 실패한다(한글 3바이트) | 파일시스템 제약 | 2장: 라벨 **32자 절단** + 앞뒤 마침표·제어문자 정리 |
| **C-8** | 링크 세트가 기존 그룹에 걸치면 인앱 규약(`LinkOutcome.NeedsMerge`)은 **매번** 확인을 띄운다 — 폴더 수만큼 확인이 뜬다 | `ImageManagerViewModel.linkImages` | 4장: 사전 확인 요약에 병합 건수 한 줄, 개별 확인 없음 |
| **C-9** | 이동 실패 지문이 무한히 쌓인다(상한·정리 규칙 없음) | 3장 사양 공백 | 3장: 상한 2000건 FIFO |
| **C-10** | `_처리됨/<원폴더명>/`이 곧 깊이 2라, "2단계 이상 무시 N장" 고지가 처리 이력만큼 불어나 거짓 신호가 된다 | 3장 규칙 상호작용 | 3장: 예약 폴더 하위는 계수에서 제외 |
| **C-11** | 3장의 깊이 규칙("직속 + 1단계")과 폴더 규약 표의 `_미배정/<세트명>/`(2단계)이 서로를 부정한다 | 문서 내부 모순 | 스캔 깊이 = **2단계**, 반영 대상 2단계는 `_미배정/` 아래뿐(`FolderRoundtripPlanner.MAX_SCAN_DEPTH`가 단일 소스) |
| **C-12** | 진입 감지 캐시(루트 수정 시각 기준)를 두려 했으나, SAF 루트의 `lastModified`는 하위 폴더 변경을 반영하지 않아 새 이미지를 조용히 놓친다 | 구현 중 발견 | 5장: 캐시 **반려**, 매번 실제 나열(비동기·실패 시 생략) |
| **C-13** | 계획을 canonical 경로로 세우고 그대로 DB에 쓰면 같은 파일의 `image_meta` 행이 하나 더 생긴다 — 기기에 따라 `/data/user/0/…`가 심볼릭 링크라 저장형(absolutePath)과 문자열이 다르고, `getByPath`·유니크 인덱스는 **정확 일치**다 | 구현 중 자기 검토 | `PlanBundle.storedPathByCanonical`로 쓰기 직전 저장형으로 되돌린다(코드베이스 관례: 저장은 absolutePath, 비교만 canonical) |

**되새길 것:** C-1은 "안전망을 새로 만든 코드를 의심하라"의 변주다 — 재압축의 새 UUID 개명은
*되돌리기를 안전하게 하려고* 도입된 처리이고(원본을 백업으로 옮기는 동안 이름이 겹치면 안
되므로), 그 안전망이 뒤에 올 토큰 규약의 전제("파일명은 안정적이다")를 조용히 깼다.
**상위 계층이 하위 계층의 이름을 정체성으로 쓰기 시작하면, 그 이름을 바꾸는 모든 경로가
계약의 일부가 된다.**

## 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.0 | 2026.07.28 | 최초 작성 — 결정 D1~D8(external_management v0.2)의 구현 사양화: 이름 토큰(12자+연장)·폴더 규약 표·받아오기 5단계·경계 사례 10건·파일 지도·PR-1/PR-2 순서·검증 목록. 착수 대기 |
| v1.1 | 2026.07.28 | **착수 전 비판적 검토 반영(9장 신설)** — C-1 토큰 비영구성(개명 2경로 → 별칭 + 고지), C-2 이동 판정 축 일원화(현재 소유 상태 우선), C-3 D2에 "빈 칸 = 해제" 편입, C-4 documentfile 무의존 SAF, C-5~C-10(역할 분리·예시·라벨 상한·병합 확인 1회·지문 상한·예약 폴더 계수 제외). 파일 지도에 `FolderRoundtripPrefs` 추가, PR-1 한계(토큰 경로는 PR-2 이후 실기기 확인) 고지 |
