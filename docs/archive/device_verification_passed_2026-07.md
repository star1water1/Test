# 실기기 확인 — 통과분 (2026-07)

> ## ⏱ 이 문서는 **끝난 확인 절차서**다
>
> 아래 6건은 사용자가 실기기에서 **확인해 통과시킨** 항목이다. 원래 자리는
> `docs/remaining_work_2026-07.md` 3장이었고, 그 장이 다시 '할 일 목록'이 되도록
> 2026-07-29에 여기로 옮겼다(그 문서 5-a 착수 순서 2번).
>
> **여기 있는 것을 근거로 착수하지 않는다.** 지금 해야 할 확인은 전부 저쪽 3장에 있다.
> 이 문서의 값어치는 **재확인이 필요할 때**뿐이다 — 그 영역을 다시 건드려 회귀가
> 의심되면 해당 절을 그대로 다시 밟으면 된다.
>
> **번호는 원래 것을 그대로 쓴다**(3-1~3-4·3-16·3-17). `remaining_work` 1-g·1-h장과
> 문서 이력이 3-16·3-17을 가리키고 있어, 다시 매기면 그 참조가 조용히 어긋난다.

| 항목 | 무엇을 확인했나 | 통과 |
|------|-----------------|------|
| 3-1 | DB 마이그레이션 v42 → v43 (실기기 업그레이드) | 2026-07-26 |
| 3-2 | B-1 휴지통 확대 산출물 1~5번 | 2026-07-26 |
| 3-3 | 앞선 세션(N1~N3) 산출물 4건 — 예약 시트명 충돌 포함 | 2026-07-26 |
| 3-4 | 유령 유실 예고 일소 · 상태변화 왕복 6건 | 2026-07-26 |
| 3-16 | B-28 검증 실패 시 입력 보존 7건 | 2026-07-29 |
| 3-17 | 세력 소속 관리 F-1~F-3 + 정정 1~4, 11건 | 2026-07-29 |

> **본문은 옮길 때 한 글자도 고치지 않았다.** 그래서 "이번 세션이"·"종전에는" 같은
> 당시 시제가 그대로 있다 — 각 절이 **어느 시점의 절차서인지** 위 표의 통과일로 읽을 것.

---

### 3-1. DB 마이그레이션 (v42 → v43) — ✅ 실기기 확인됨
B-1이 `trash_snapshots.operationId`(nullable TEXT) + 인덱스를 추가해 **v43으로 올렸다.**
`tools/verify_room_migration_43.py`가 실제 SQLite로 39건을 검사해 전부 통과하고,
**실기기 업그레이드도 정상 동작을 확인했다**(기존 설치본이 데이터 유실 없이 켜짐).
절차는 `docs/excel_roundtrip_audit_2026-07.md` 2-1과 `docs/room_migration_verification.md` 참조.
v41→v42는 이 업그레이드에 포함돼 함께 통과했다.

**주의:** 이번 세션은 스키마를 건드리지 않았다(상태변화 스냅샷은 `entityType` 문자열 값을
하나 늘렸을 뿐 컬럼 변경이 없다). 따라서 v43 그대로이며 재검증이 필요 없다.

---

### 3-2. B-1 산출물의 실기기 확인 — ✅ 1~5번 전부 통과

1. **세계관 삭제 → 전체 복원.** 작품 2개·캐릭터 10명·세력 2개·사건 5건이 있는 세계관을 지운다.
   휴지통에 "세계관 'X' 삭제 · 항목 20개" 머리글 하나와 그 아래 항목들이 **세계관 → 작품 →
   세력 → 사건 → 캐릭터 순으로** 보여야 한다. '전체 복원'을 누르면 전부 되살아나고 캐릭터의
   작품 소속·세력 소속·사건 연계가 그대로여야 한다.
2. **보관 한도가 묶음을 자르지 않는가 (B-14).** 위 상태에서 캐릭터를 하나 더 지운다.
   종전 규칙이면 20개 묶음이 잘렸다 — 이제 묶음이 그대로 남아야 한다.
3. **세력만 개별 복원.** 세계관을 먼저 지우고 세력 행만 '복원'을 누르면
   "세계관이 없어 복원할 수 없습니다" 다이얼로그가 뜨고 **항목은 휴지통에 남아야** 한다.
   그다음 세계관을 복원하고 세력을 다시 복원하면 성공해야 한다.
4. **출생 사건 삭제 → 복원.** 캐릭터에 출생 사건을 만들고 연표에서 길게 눌러 삭제한 뒤 복원한다.
   캐릭터의 `__birth` 상태변화가 돌아오고, "파생 필드값(생년·나이)은 덮어쓰지 않았다"는
   안내가 떠야 한다.
5. **엑셀 왕복 후 복원(오배정 확인).** 세계관을 지운 뒤, 그 세계관이 들어 있는 엑셀을
   **덮어쓰기**로 가져와 같은 코드의 세계관을 되살린다. 그 상태에서 휴지통의 '전체 복원'을
   누르면 사본 세계관이 생기고, **복원된 캐릭터·세력·사건은 원본이 아니라 사본에 붙어야 한다.**

---

### 3-3. 앞선 세션(N1~N3) 산출물의 실기기 확인 — ✅ 전부 통과
스키마 무변경이라 위험도는 낮다. 값진 확인 순서:

1. **N2** — 필드값이 있는 캐릭터를 편집 화면에서 열어 작품을 '작품 없음'으로 바꿔 저장한다.
   "화면에 없는 필드값 N개는 지우지 않고 보관했습니다" 스낵바가 뜨고, 상세 화면에서 그 값들이
   그대로 보이는지 확인한다. 다시 원래 작품에 배정하면 폼에 값이 채워져야 한다.
2. **N1** — 캐릭터를 몇 명 삭제해 휴지통에 넣고, 엑셀로 전체 내보내기 → **덮어쓰기**로 다시
   가져온 뒤(세계관·필드 정의 포함) 휴지통에서 복원한다. 종전에는 필드값·관계·세력 소속이
   전부 생략됐다. 이제 "참조 N건은 id가 바뀌었지만 코드로 다시 연결했습니다"가 떠야 한다.
3. **N1 정리 보호** — 캐릭터가 30명 넘게 소속된 세계관을 삭제한 뒤 휴지통을 연다.
   종전에는 30개만 남고 나머지는 이미지 파일까지 사라졌다. 이제 전부 남아야 한다.
4. **N3 — 예약 시트명 충돌.** ("내보내기 → 가져오기"는 **엑셀 파일**을 말한다. 설정의
   *엑셀로 내보내기*로 .xlsx를 만들고, 그 파일을 다시 *엑셀에서 가져오기*로 읽는 것이다.)

   *무엇을 보는 확인인가:* 엑셀 파일에는 `세력`·`미분류 캐릭터` 같은 **앱이 예약한 시트 이름**이
   있고, 세계관마다 자기 이름으로 된 시트도 하나씩 생긴다. 세계관 이름이 하필 예약 이름과
   같으면 두 시트가 한 이름을 두고 부딪친다. 종전에는 먼저 쓴 쪽이 이겨서 **진짜 '세력' 시트가
   세계관 시트로 덮여 세력이 통째로 사라졌다.**

   *순서:* ① 세계관 하나의 이름을 정확히 `세력`으로 바꾼다. **그 세계관에 작품 1개와 그 작품
   소속 캐릭터 1명 이상이 반드시 있어야 한다** — 번호가 붙는 시트는 세계관의 *캐릭터 시트*인데,
   캐릭터 없는 세계관의 시트는 아예 만들어지지 않아(`ExcelExporter.kt`의
   `if (universeChars.isEmpty()) continue`) 충돌 자체가 일어나지 않는다. 캐릭터가 작품
   미지정이면 '미분류 캐릭터' 시트로 빠지므로 역시 안 된다. 세력도 1개 이상 있어야 예약
   `세력` 시트가 생겨 양쪽을 다 확인할 수 있다.
   ② 엑셀로 내보낸다. ③ **만들어진 .xlsx를 열어 시트 탭 이름을 눈으로 본다** —
   예약된 `세력` 시트는 그대로 있고, 캐릭터 시트는 `세력(2)`처럼 번호가 붙어 있어야 한다.
   ④ 그 파일을 덮어쓰기로 다시 가져온다. ⑤ 세력 목록·세계관·그 캐릭터가 **전부** 원래대로여야
   한다. ⑥ 세계관 이름을 `미분류 캐릭터`로 바꿔 ①~⑤를 한 번 더 한다.

   *2026-07-26 1차 시도:* 캐릭터 없는 세계관으로 진행되어 `(2)` 시트가 생기지 않았다(정상 —
   충돌이 없었을 뿐). *재시도:* 전제 조건을 채워 진행, **통과** — 접미사 배정과 왕복 복원이
   실기기에서 정상 동작 확인됨. 3-3은 이것으로 4건 전부 실기기 확인 완료다.

   *그냥 내보내고 덮어쓰기했을 때 원래대로인 것은 이 확인이 아니다* — 이름을 예약 이름과
   똑같이 만들어 **충돌을 일부러 일으키는 것**이 이 항목의 전부다. 충돌이 없으면 종전 코드도
   통과한다.

---

### 3-4. 이번 세션 산출물의 실기기 확인 — ✅ 전부 통과 (2026-07-26)

1. **유령 유실 예고가 사라졌는가.** 필드값을 많이 채운 캐릭터가 여럿 있는 세계관을 지운 뒤
   휴지통에서 **'전체 복원'**을 누른다. 확인 다이얼로그에 "필드값 N개(필드 정의를 찾을 수 없음)",
   "필드 데이터(값 라이브러리) 항목 N개", "보관 중이던 필드값 N개"가 **더 이상 뜨지 않아야**
   한다. 복원 후 캐릭터의 필드값이 그대로인지도 함께 본다(종전에도 값은 멀쩡했다 — 경고만
   거짓이었다).
2. **진짜 유실은 여전히 예고하는가(반대 방향).** 세계관을 지운 뒤, **캐릭터 한 명만** 개별
   '복원'을 누른다. 이때는 세계관이 없으므로 "필드값 N개(필드 정의를 찾을 수 없음)"가
   **떠야 한다** — 사실이기 때문이다. 여기서 경고가 사라졌다면 무음 유실로 바뀐 것이다.
3. **상태변화 삭제 → 복원.** 캐릭터 상세의 상태변화 목록에서 한 줄을 눌러 '삭제'한다.
   휴지통에 "상태변화 · `__birth → 1200`" 형태의 항목이 생기고, 복원하면 그 이력이 목록에
   돌아와야 한다. 출생·사망 이력이면 "파생 필드값(생년·나이)은 덮어쓰지 않았다"는 안내가 뜬다.
4. **상태변화가 중복으로 쌓이지 않는가.** 출생 사건을 연표에서 삭제한다. 휴지통에는 **사건
   항목 하나만** 생겨야 한다 — 상태변화 항목이 따로 또 생기면 파생 정리 경로가 스냅샷을
   남기고 있는 것이고, 복원하면 이력이 두 벌이 된다.
5. **캐릭터를 먼저 지운 뒤 상태변화만 복원.** 상태변화를 지우고 → 그 캐릭터를 지운 다음 →
   상태변화 행만 '복원'을 누른다. "캐릭터가 없어 복원할 수 없습니다" 다이얼로그가 뜨고
   **항목은 휴지통에 남아야** 한다. 캐릭터를 먼저 복원하면 그때는 성공해야 한다.
6. **캐릭터 화면에서 사건 편집·삭제.** 캐릭터 상세의 사건 목록에서 한 건을 **누르면** 편집
   다이얼로그가 열리고, **길게 누르면** 편집 / 이 캐릭터에서 연결 해제 / 삭제가 나와야 한다.
   - '연결 해제'는 그 캐릭터의 목록에서만 사라지고 연표 탭에는 사건이 남아야 한다.
   - '삭제'는 다른 캐릭터도 걸려 있으면 **몇 명이 함께 잃는지** 확인 문구에 나와야 하고,
     지운 뒤 휴지통에서 복원하면 참가 캐릭터 연결까지 돌아와야 한다.
   - 출생 사건을 이 경로로 지우면 연표 탭에서 지웠을 때와 **똑같이** 동작해야 한다
     (`__birth` 이력이 함께 정리되고, 복원하면 함께 돌아온다).

---

### 3-16. B-28(검증 실패 시 입력 보존)의 실기기 확인 — ✅ 통과 (2026-07-29)

> **사용자 회신:** 아래 항목 확인 결과 **전부 정상 동작**. 다만 '설정상 탈퇴' 항목을 확인하려다
> **가입 연도를 넣거나 볼 방법 자체가 없다**는 것이 드러났고, 그 조사에서 세력 멤버 결함 3건이
> 나왔다(1-h장에서 처리). 아래 절차는 재확인이 필요할 때의 기록으로 남긴다.

> **핵심 질문 하나:** 검증에 걸렸을 때 **창이 열린 채로 남고, 내가 적어 둔 나머지가 그대로
> 있는가?** 종전에는 전부 닫히면서 사라졌다. 아래 7건은 전부 "일부러 틀리게 적고 저장을 누른다"이다.

1. **상태변화 추가**(캐릭터 상세 → 상태변화 +) — 필드·값·설명을 채우고 **연도를 비운 채** 저장.
   → 연도 칸 아래 "연도를 입력하세요"가 뜨고 **창이 남으며 값·설명이 그대로인가?**
   연도에 `abc`를 넣으면 문구가 "올바른 연도를 입력하세요"로 **달라지는가**(종전에는 둘이 같았다).
   연도 칸을 고치기 시작하면 붉은 글씨가 **사라지는가?**
2. **상태변화 — 월/일 범위**: 월에 `13`, 일에 `32`를 넣고 저장 → 각 칸에 오류가 뜨고 창이 남는가.
3. **세력 편집**(세력 관리 → 세력 길게 누르기 → 편집) — 색을 고르고 강도를 옮긴 뒤
   **이름을 비우고** 저장 → 이름 칸에 오류, **고른 색과 강도가 유지되는가?**
4. **세력 관계 추가** — 설명·강도를 채우고 **유형을 비운 채** 저장 → 유형 칸 오류 + 창 유지.
   이어서 **이미 있는 상대·유형 조합**으로 저장 → "같은 상대·유형의 관계가 이미 있습니다"가
   유형 칸에 뜨고 **창이 남는가**(종전에는 닫힌 뒤에 토스트만 떴다). 유형만 바꿔 다시 저장되는가?
5. **세력원 탈퇴** — 가입 연도보다 **이른 연도**를 넣고 저장 → "탈퇴 연도(N)는 가입 연도(M)보다
   뒤여야 합니다"가 뜨고 창이 남는가. 고쳐 쓴 관계 유형이 유지되는가?
6. **세력원 추가** — **아무도 고르지 않고** 확인 → 종전에는 **말없이 닫혔다.**
   이제 "추가할 캐릭터를 한 명 이상 선택하세요"가 뜨고 창이 남는가?
7. **관계 추가**(캐릭터 상세 → 관계 +) — 상대 칸에 **목록에 없는 이름**을 직접 타이핑하고 저장
   → "목록에 있는 캐릭터를 선택하세요"(종전 문구는 사실과 다른 "다른 캐릭터가 없습니다"였다).
   **교차분석**(통계 → 필드 인사이트 → 교차분석)에서 필드가 2개 미만일 때 실행 → 토스트가 뜨되
   **창이 남아 고른 축·필터를 다시 고르지 않아도 되는가?**

> **회귀로 볼 것 둘:** ① 정상 입력으로 저장하면 **창이 정상적으로 닫히는가**(닫히지 않으면
> 이번 변경이 저장 경로를 막은 것이다) ② 취소·삭제 버튼은 종전대로 **즉시 닫히는가**.
> ③ 저장을 **연타**했을 때 관계가 두 번 들어가지 않는가(창이 살아 있어 새로 생긴 창이다).

---

---

### 3-17. 세력 소속 관리(1-h장 F-1~F-3 + 정정 1~4)의 실기기 확인 — ✅ 통과 (2026-07-29)

> **사용자 회신: 아래 항목 전부 확인, 문제 없이 작동.**
> 절차는 재확인이 필요할 때의 기록으로 남긴다. 아래 두 가지는 **점검 중에 드러난 것**이라
> 다음 사람이 같은 데서 막히지 않도록 본문에 반영해 두었다:
> ① 10번(관계 변화 동반 이동)은 **세력에 멤버가 2명 이상**이어야 성립한다 —
> 자동 관계는 멤버끼리 생기므로 1명뿐인 세력은 옮길 변화가 0건이고, 그것이 정상이다.
> ② 관계 변화는 관계 목록에 바로 보이지 않는다 — **관계를 길게 눌러** `관계 변화 이력`.
> 관계 목록 자체는 유형·강도만 보여 주고 연도는 표시하지 않는다.
> (상태 변화(시점 변화)와는 **별개 기능**이라 그쪽은 관여하지 않는다.)

> 진입: 세계관 목록 → 세력 관리 → 세력 **탭**(멤버 목록) / 세력 **길게 누르기**(옵션)

1. **가입 연도를 이제 고칠 수 있는가(F-2)** — 활성 멤버를 길게 눌러 `가입 연도 편집`.
   숫자를 넣고 저장 → 목록의 이름 옆이 `1000년~`으로 바뀌는가.
   다시 열어 **비우고** 저장 → 연도 표시가 사라지는가(= 시점 불명으로 되돌아감).
   글자를 넣으면 창이 남고 <q>연도를 숫자로 입력하세요</q>가 뜨는가(R-27).
2. **탈퇴한 멤버가 이제 반응하는가(F-1)** — 탈퇴시킨 멤버를 길게 누르기.
   종전에는 **아무 일도 없었다.** 이제 `다시 소속시키기` / `소속 기록 삭제`가 뜨는가.
3. **재가입이 되는가(F-3)** — 위에서 `다시 소속시키기` → 가입 연도를 넣고 저장.
   - 목록에 **같은 이름이 두 줄**로 보이는가(옛 `탈퇴 (999년)` + 새 `1000년~`) — **이것이 정상이다.**
   - 창에 미리 적힌 안내(<q>이전 탈퇴 기록은 이력으로 남고…</q>)와 실제가 맞는가.
4. **후보 목록이 상태를 보여 주는가(F-3)** — `[멤버 추가]`를 열면 이름 옆에
   `· 소속 중` / `· 탈퇴함`이 붙는가. 한 번도 소속된 적 없는 캐릭터는 이름만 나오는가.
5. **골랐는데 안 들어간 사유를 말하는가** — 후보 목록에서 **이미 `소속 중`인 사람**을 골라 확인.
   <q>N명은 이미 소속 중이라 건너뛰었습니다</q>가 뜨는가.
   종전에는 "N명 추가"의 숫자에서만 빠지고 **아무 설명이 없었다.**
6. **소속 기록 삭제** — 탈퇴 멤버 길게 누르기 → `소속 기록 삭제`.
   확인 창이 **먼저** 뜨고(R-4), 지우면 그 줄만 사라지는가.
   안내대로 **캐릭터 관계는 남아 있는가**(관계 탭에서 확인).

7. **재가입 연도가 이전 탈퇴보다 앞이면 막히는가** — 1000년 가입 → 1500년 탈퇴한 멤버를
   `다시 소속시키기`에서 **800**으로 저장 → <q>가입 연도(800)는 이전 탈퇴 연도(1500)보다
   앞일 수 없습니다</q>가 뜨고 창이 남는가. **1500**(같은 해)은 **통과해야 한다**(탈퇴 당해에는
   이미 나가 있으므로 겹치지 않는다). 연도를 **비우고** 저장하면 적으라고 하는가.
8. **다른 문에도 걸렸는가** — 같은 탈퇴자를 `[멤버 추가]`에서 골라 가입 연도 **800**으로 확인.
   여기서도 막히고 **누가 걸렸는지 이름이 나오는가**(여러 명이면 이름 + '외 N명').

9. **짧은 터치로 열리는가** — 멤버 행을 **그냥 한 번 탭**한다. 꾹 누르지 않아도
   선택지가 뜨는가(종전에는 꾹 눌러야만 했고, 탭하면 물결만 일고 아무 일이 없었다).
   꾹 누르기도 **여전히 같은 자리**로 가는가.

10. **탈퇴 기록을 고칠 수 있는가** — 탈퇴 멤버 선택 → `탈퇴 기록 편집`.
   가입/탈퇴 연도·탈퇴 후 관계 유형·강도가 **채워진 채로** 열리는가.
   탈퇴 연도를 1500 → 1600으로 바꿔 저장 → <q>탈퇴로 만들어진 관계 변화 N건도 함께
   옮겼습니다</q>가 뜨고, **관계 탭의 변화 연도도 1600으로 옮겨졌는가**.
11. **겹침이 막히는가** — 재가입해서 같은 캐릭터에 소속이 두 줄인 상태로,
   옛 줄의 탈퇴 연도를 **새 줄의 가입 연도보다 뒤로** 바꿔 저장
   → <q>이 캐릭터의 다른 소속 기간(…)과 겹칩니다</q>가 뜨는가.
   **맞닿는 값**(옛 탈퇴 = 새 가입)은 **통과해야 한다**.

> **회귀로 볼 것:** 활성 멤버의 `순수 제거`·`설정상 탈퇴`가 종전대로 동작하는가
> (선택지가 2개에서 3개로 늘면서 순서가 밀렸다 — 엉뚱한 동작이 실행되면 실패다).
