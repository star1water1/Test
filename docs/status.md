# 지금 상태 — 세션 진입점

현재 작업·재개 조건·최신 기준선의 정본이다. 공통 작업 절차는 [AGENTS.md](../AGENTS.md)를 따른다.
완료 경위는 커밋/PR에서 찾고, 이 문서에 회차별 이력을 쌓지 않는다.

## 0. 지금 무엇을 하는가

- **자율적으로 고를 수 있는 기능 작업은 0건이다.** 아래 대기 상태는 사용자의 새 요청을 막지 않는다.
- 최근 앱 변경은 [PR #395](https://github.com/star1water1/Test/pull/395)(2026.08.25)까지 병합됐다. 최신 검증 기록은 4장에만 둔다.
- **생일 값 라이브러리 정리와 세력 자동 관계 87건의 연결 수리는 코드에 반영됐다.** 같은 결함을 미해결로 다시 열지 않는다. 실제 기기에서의 정리 결과·삭제 고지는 2장의 확인 대상으로 남아 있다.
- 남은 백로그의 재개 조건은 3장에 있다. 현재 진행을 위해 먼저 받아야 할 사용자 판정은 없다. `B-258`의 미선택 갈래는 사용자가 그 작업을 열 때 판단한다.
- 우선 대기는 [실기기 워크스루](device_walkthrough.md)의 **S1(15분)** 회신이다. 새 파일·기기 회신·사용자 요청이 오면 해당 범위를 처리한다.

## 1. 판의 정의

작업 범위·범위 밖 발견의 처리·질문 기준·완료 보고는 [AGENTS.md](../AGENTS.md)가 정한다.
문서·검토·테스트 자체를 요청받은 작업도 그 요청을 완료하면 된다.

## 2. 실기기 확인

**실기기 통과를 새로 확인한 것은 없다.** 마지막 통과 기록은 2026.08.01이며, 절차와 현재 단계는
[device_walkthrough.md](device_walkthrough.md)와 `tools/walkthrough/steps.json`에서 확인한다.
S1이 먼저이고, 이후에는 준비물 의존 때문에 워크스루의 순서를 따른다.
실기기 확인은 별도 큐이며 앱 코드 PR의 CI 검증을 대체하거나 병합을 막는 조건이 아니다.

특히 놓치지 말아야 할 잔여 확인:

| 대상 | 확인할 것 | 워크스루 참조 |
|---|---|---|
| 최근 1회 데이터 정리 | 업데이트 후 생일 엔트리 정리·세력 재연결 로그와 세력 삭제 전 자동 관계 수 고지 | `3-ㅇ-33`·`3-ㅇ-34` |
| `B-267` (코드 병합 완료) | 작품에 속한 캐릭터의 이름·생일·나이를 편집한 뒤 회전해도 동적 필드가 유지되는가 | `0-ㅅ` |
| 기존 미검증 항목 | Excel 왕복·화면 동작 등 각 단계의 결과 | 해당 워크스루 묶음 |

단계·절·묶음을 확인할 때만 `python3 tools/walkthrough/verify.py`를 실행한다.
닫힌 `remaining_work_2026-07.md`의 옛 절 수를 현재 워크스루 단계 수로 사용하지 않는다.

**순회표 걷어내기는 관련 기능 작업에 딸려 갈 때만 한다.** 분류는 `steps.json`의 `axis`·`audit`가
가지고 있으므로 다시 분류·계수하거나 별도 작업으로 열지 않는다. 해당 단계의 실제 코드와 시험을
읽은 뒤, 삭제 대신 `drop`과 근거 코드 조각 `anchor`를 기록하고 `verify.py`로 확인한다.
코드로 확인되는 사실을 위해 시험을 새로 만들지 않으며, 애매한 단계는 남긴다.
Robolectric 도입 대신 필요한 경우 그 화면의 판정 로직을 순수 계층으로 추출하는 기존 방침을 따른다.

## 3. 살아 있는 백로그 — **11건**

상태 표식은 `tools/check_backlog_markers.sh`로 확인한다.
본문은 `docs/remaining_work_2026-07.md` 4장의 해당 행에 있다. 그 문서는 이력이 닫혔지만
백로그 본문은 유효하며, 이 표는 현재 재개 조건을 보여 주는 색인이다.

### 고를 수 있는 것 — **0건**

남은 11건은 기기 회신 3 · 사용자 선택 1 · 자료 대기 1 · 보류 6이다.
대기 조건이 충족되거나 사용자가 지정한 작업만 진행한다.

### 기기 회신을 기다리는 것 — 3건

| 행 | 심각도 | 무엇 |
|---|---|---|
| `B-18` | 🟡 | `restoreOperation`이 항목마다 트랜잭션을 연다 — 한 덩어리로 묶을 상한을 **실기기 측정 후** 정한다(확정됨) |
| `B-72 ⓑ` | 🟡 | 내보내기·자동 백업의 DOM 전량 적재 — 규모 신호가 실기기에서 나와야 한다 |
| `B-214` | 🔵 | 관계도 `onDraw`를 안 쟀다 — 그리기 축은 기기에서만 보인다 |

### 판정을 먼저 받아야 하는 것 — **0건**

기존 판정은 `docs/judgment_confirmations_2026-08.md` 20장에 있다. 같은 결정을 다시 묻지 않는다.

### 사용자가 열어야 하는 것 — 1건

| 행 | 심각도 | 무엇 | 여는 조건 |
|---|---|---|---|
| `B-258` ⓑⓒⓓ | 🟡 | **반만 닫힌 행.** ⓐ(무소속 사건에 전역 구역)만 처리했다. 남은 것은 **임자와 구역이 어긋난 값** — 창이 추가 구역으로 그릴 것인가(ⓑ) · 가져오기가 교정할 것인가(ⓒ) · 이미 들어온 것을 소급할 것인가(ⓓ) | 사용자가 고른다. **ⓓ는 되돌리기 축**(저장된 값이 바뀐다)이라 다시 물어야 한다 |

### 자료를 기다리는 것 — 1건

| 행 | 심각도 | 무엇 | 재개 조건 |
|---|---|---|---|
| `B-27 ①` | 🟡 | 임포트 원문 보존 잔여 — 날짜로 자동 변환된 셀의 재포맷 | 사용자 자료(실제로 깨진 파일) 필요 |

### 보류 — 6건 (재개 조건이 아직 안 섰다)

| 행 | 심각도 | 무엇 | 재개 조건 |
|---|---|---|---|
| `B-49` | 🟢 | 세계관·작품·세력 목록의 검색 부재 | 규모 신호(항목이 수십 개) |
| `B-53` | 🔵 | 폴더 왕복 2차 후보 셋 | 사용자가 우선순위 낮음으로 판정 |
| `B-57` | 🔵 | 의존 주입이 수동이다(`by lazy`) | 규모 신호 |
| `B-58` | 🔵 | 이미지 로딩이 수동 관리다 | 규모 신호 |
| `B-125` | 🟡 | 릴리스 빌드를 CI가 한 번도 밟지 않는다 | 방아쇠(릴리스 준비) |
| `B-209` | 🟡 | 통계 드릴다운이 스냅샷 필드값을 매번 훑는다 | 실측 0.4ms — 규모가 커지면 재개 |

## 4. 재현 기준선

**최근 기록값(2026.08.25, PR #395):** 순수 JVM **4,061 OK** · 컴파일 프로브 **569** · 뷰 프로브
**77** · 정적 검사 **54종 실패 0**. Room 스키마는 **v58**이며 해당 앱 변경에서 마이그레이션은 없었다.
이 값은 이전 실행 기록이다. 문서 수정만으로 실행일이나 통과 상태를 갱신하지 않는다.

코드 변경 전에는 `git fetch origin master`로 기준을 갱신하고, 변경 영역의 검증을 실행해 비교한다.
작업 규약의 필수 검사는 포함하며, 전체 실행 여부는 [AGENTS.md의 검증 기준](../AGENTS.md#검증)을 따른다.
프로브의 기준선은 변경 없는 `origin/master` 체크아웃/워크트리에서 만들고 수정본 결과와 비교한다.

아래 명령은 **저장소 루트의 Bash 환경**(Linux/WSL 등)용이다. Java 17·Python 3이 필요하고,
JVM 준비 스크립트는 필요한 jar을 내려받으므로 해당 다운로드 경로에 접근할 수 있어야 한다.
PowerShell에 Bash 명령을 그대로 붙여 넣지 않는다. `JARS_DIR`에는 쓰기 가능한 임시 경로를 명시한다.

```bash
export JARS_DIR="${TMPDIR:-/tmp}/novelcharacter-jvm"
bash tools/setup_jvm_env.sh
bash tools/run_jvm_tests.sh
bash tools/probe_compile.sh "$JARS_DIR/probe.txt"
bash tools/probe_view_compile.sh "$JARS_DIR/view.txt"
```

각 명령의 실패를 확인한 뒤 다음 단계로 진행한다. 준비된 환경은 재사용하며, 의존성이 바뀌면 다시 준비한다.
특정 규약의 정적 검사·마이그레이션 하네스는 해당 파일을 실행한다. 전체가 필요하면 다음처럼 실패를 전파한다:

```bash
(
  set -e
  for f in tools/check_*.sh; do bash "$f"; done
  for p in tools/verify_room_migration*.py tools/verify_reset_coverage.py; do python3 "$p"; done
)
```

- `run_jvm_tests.sh`는 **JUnitCore 종료 코드를 반환한다.** 0이 아니면 실패이며, 출력의 `OK (` / `FAILURES!!!`와 컴파일 오류도 함께 확인한다. 출력 일부는 잘릴 수 있다.
- 프로브에는 알려진 stub shadow가 있다. **오류 개수만 같다고 통과시키지 말고**, 기준선과 메시지·발생 위치를 비교해 새 회귀를 확인한다.
- `triage_unresolved.sh`는 입력한 차분 컴파일 결과에서 실제 오류 후보를 고른다. 종료 코드나 후보가 항상 일정하다고 가정하지 말고, 기준선과 현재 결과를 대조한다.
- CI가 다루는 범위는 `.github/workflows/build-apk.yml`과 `.github/workflows/migration-test.yml`이 정한다. 문서 전용 변경은 경로 필터로 실행되지 않을 수 있다.
- Excel 입출력을 바꾸면 가능할 때 실제 파일을 열어 왕복 결과를 확인한다. 원본을 다시 확인하지 않은 추정으로 수리하지 않고, 파일·기기가 없으면 남은 검증을 보고한다.

## 5. 착수 절차

[AGENTS.md의 세션 시작·범위와 판단·검증](../AGENTS.md)을 따른다. 이 문서에 같은 절차를 복제하지 않는다.
제품 원칙은 `CLAUDE.md`, 작업별 코드 규약은 `docs/conventions.md`의 관련 절만 읽는다.
