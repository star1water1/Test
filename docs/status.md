# 지금 상태 — 세션 진입점

> **이 문서 하나가 "지금 무엇을 하는가"의 단일 소스다.** 500줄을 넘기지 않는다.
> 넘으면 그것이 자른다는 신호다 — 이 문서는 2026.08.19에 **21,691줄짜리
> `remaining_work_2026-07.md`를 대체하며** 생겼고, 그 문서가 커진 방식이 곧
> 이 프로젝트가 느려진 방식이었다(아래 「왜 이 문서가 생겼나」).
>
> **경위·이력은 여기 안 쌓는다.** 그건 `git log`가 갖고 있고, 2026.08.19 이전은
> `docs/remaining_work_2026-07.md`(닫힘)와 `docs/session_log_2026-07.md`(닫힘)에 있다.

---

## 0. 지금 무엇을 하는가

### 🎯 **다음 판 — 실기기 순회표를 걷어낸다** (2026.08.19 사용자 지시)

**사용자 판정:** *"할 수 있는 건 다 걷는 거로 하자. 많아도 너무 많아. 쓸데없이. 간단하게 코드 보고
알 수 있는 것도 있을텐데 그런것까지 복잡하게 검증할 거 없잖아."*

**근거는 실측이다 — 324단계를 전수 분류했고, 그 결과가 `tools/walkthrough/steps.json`의
`axis`·`axiswhy` 칸에 한 단계씩 들어 있다.** 다음 판은 이 표를 다시 만들지 말 것.

| `axis` | 수 | 무엇인가 | 다음 판이 할 일 |
|---|---:|---|---|
| `자동` | **192** | 이미 JVM 시험·정적 검사가 그 판정을 본다 | **시트에서 뺀다** |
| `코드` | **11** | 코드 한 자리 읽으면 끝(분기·상수·문구) | **시트에서 뺀다 — 읽고 끝. 시험을 새로 만들지 않는다** |
| `자원` | 45 | SAF·권한·갤러리·알림·설치·실제 API | 일부만 에뮬레이터로 |
| `보임` | 26 | 잘림·배치·대비 — **코드에 그 정보가 아예 없다** | 측정 가능한 것만(잘림·스크롤 끝·대비비) |
| `수명` | 20 | 회전·프로세스 사망·백스택 | **여기가 최우선 — B-267이 이 부류다** |
| `조작` | 20 | 탭·드래그·롱프레스 | 콜백 직접 호출로 일부 |
| `성능` | 10 | 체감·OOM·스톱워치 | 기기만. 손대지 않는다 |

⚠️ **`자동`은 부풀려진 수다 — 실측 73%.** 표본 11개를 시험 파일까지 열어 읽었고
**8개만 실재했다**(`MultiValueInputTest.roundTrip_isStableOnSecondPass` ·
`DefaultFieldPlanTest.타입이_바뀌면_못_버티는_값을_센다` · `PatternDetectionAxisTest` ·
`BodyBandGapTest` · `FormulaValidatorTest` · `DetachedImageRuleTest` · `DuelStandingsTest` · 엑셀 시험군).
**없던 셋:** 통계 '기타 N종·비율 합 100%' · 엑셀 '맨 앞 10행' 헤더 창(`HEADER_SEARCH_ROWS`) ·
어시스턴트 카드 숨기기 실행취소.

**→ `자동` 192 중 실제로 덮인 것은 140 언저리이고, 50쯤은 *"원리적으로는 자동 가능한데 지금은
아무도 안 본다"*다.** 그것을 그냥 빼면 **조용한 구멍**이 된다. 뺄 때 규칙은 하나다:

> **시험 이름을 적을 수 있으면 뺀다. 못 적으면 — 그 시험을 쓰든지(순수 계산이라 대개 싸다),
> 단계를 남기든지 둘 중 하나다. 그냥 빼지 않는다.**

⚠️ **분류 자체에도 오분류가 있다.** 표본 6개에서 하나를 잡았다(`14-ㄷ` 어시스턴트 실행취소를
`자동`으로 뒀는데 실제로는 탭 → 스낵바 → 탭이라 `조작`이다. 이 검토에서 고쳤다).
**같은 비율이면 서른쯤 더 있다** — 옮길 때 한 건씩 볼 것.

⚠️ **검증을 흔한 낱말 grep으로 하지 말 것.** 이 검토가 처음에 그 방식으로 19개를 재서
**19/19 통과**를 받았는데, 전부 무관한 파일이었다(`따옴표 멱등` → `FactionRelationshipMatcherTest`가
*"멱등"* 한 낱말로, `패턴 기본값` → `AiTokenPolicyTest`가 *"60"*이라는 **숫자**로 걸렸다).
**시험 파일을 열어 시험 이름을 읽는 것 말고는 믿지 말 것.**

#### 환경 실측 (이 판이 쟀다 — 다시 재지 말 것)

- `dl.google.com`(AndroidX 전부)은 **이 컨테이너에서 정책 차단**이다(403 CONNECT).
  Maven Central에는 androidx가 없다(404). `maven.google.com`은 거기로 301 되보낸다.
- **그러나 CI는 된다** — `build-apk.yml`이 `./gradlew testDebugUnitTest`와 `assembleDebug`를 돌린다.
  즉 **막힌 것은 로컬이지 프로젝트가 아니다.**
- **에뮬레이터도 이미 켜져 있다** — `migration-test.yml`이 KVM·API 26·x86으로
  `connectedDebugAndroidTest`를 45분 예산으로 돌린다. **거기 든 계측 시험은 파일 하나뿐이다**
  (`AppDatabaseMigrationTest` — 시험 3개).
- Robolectric은 아직 없다. `app/build.gradle.kts`가 *"순수 JVM — Robolectric 불필요"*라 적어
  두었는데 **그것은 로컬 기준으로 쓴 말이고 CI에는 해당하지 않는다.**

#### 착수 순서 — 작은 증명부터

**로컬에서 못 돌린다.** CI 왕복 7분에 눈 감고 짜야 하므로 큰 것을 한 번에 세우지 말 것.

1. Robolectric 의존성 + `isIncludeAndroidResources` + **배선 확인용 최소 시험 하나** → CI 초록 확인.
2. 그 위에 **B-267 재발 방지 시험** — `recreate()` 뒤 생일 칸을 읽으면 끝이다.
3. `수명` 20단계를 옮긴다(값이 가장 큰 축).
4. 시트를 다시 짠다 — `steps.json`을 고치고 `emit.py`·`verify.py`를 돌린다. **마크다운을 손으로
   고치지 않는다**(그렇게 24단계를 잃은 적이 있다).

**하지 말 것(사용자 지시):** 코드 보면 아는 것에 시험을 만들지 않는다. 검증을 복잡하게 만들지 않는다.

> **v3.0 동결과의 관계.** `CLAUDE.md` 1장이 *"검사·도구는 동결에 가깝게"*를 박았고 이 판은 그것을
> 건드린다. 두 가지가 이것을 정당화한다: **ⓐ 사용자가 직접 지시했다**(동결의 명시된 예외).
> **ⓑ v3.0이 찾은 뿌리는 *"실기기 확인을 세션이 갚을 수 없어 화면을 피했다"*였는데, 이 작업은
> 그 부채를 세션이 갚을 수 있게 만드는 쪽이라 자가발전과 방향이 반대다.**

---

### ⏳ B-267 — 병합됨, 기기 확인 하나 남음

회전에 캐릭터 편집의 **동적 필드가 통째로** 사라지던 결함(이름 등 정적 칸은 살아남아 비대칭이었다).
**원인 확정 · 수리 · 병합 완료** — PR #364 · `f6808f07`. 원인은 부트스트랩 스피너 콜백(position 0)이
남의 복원 묶음을 열어 **한 줄도 복원하지 못한 채 비우고**, 뒤따르는 진짜 작품 콜백이 `saved == null`을
보고 DB 값으로 덮던 것이다. 자세한 것은 4장 `B-267` 행.

**남은 것:** 워크스루 `0-ㅅ` — 작품에 속한 캐릭터에서 이름과 생일·나이를 고친 뒤 회전.
그 단계는 이 결함이 다시 걸리도록 날카롭게 고쳐 뒀다.

---


**실기기 워크스루는 계속 유효하다.** `docs/device_walkthrough.md` — 다만 위의 걷어내기가
끝나면 324단계가 아니라 **65 언저리**가 된다. 그 전에 밟아도 손해는 없다(0회차 15분).

**백로그는 그다음이다 — 아래 3장에서 고른다.** 고를 수 있는 것은 지금 **1건**(`B-46`).
나머지는 기기 회신 대기(3) · 판정 대기(4) · 보류(7)다.

---

## 1. 판의 정의 — **사용자가 앱에서 볼 수 있는 변화가 없으면 판이 아니다**

**2026.08.19 사용자 판정.** 종전에는 도구·검사·문서만 바꾸는 세션도 한 판으로 셌고,
그것이 실측으로 **최근 20판 중 6판(30%)**이었다. 같은 기간 앱 코드 4,220줄에
도구·문서 6,683줄이 붙었다 — **메타작업이 산출물의 1.6배**였다.

- **한 판은 사용자가 앱에서 볼 수 있는 변화를 하나 이상 낸다.**
- **도구·검사·문서 작업은 독립 판으로 열지 않는다.** 기능 판에 딸려 간다.
- **예외는 사용자가 지시했을 때뿐이다**(전례: 이 문서를 만든 판, 콜드 검토 지시).
- **검사·도구는 동결에 가깝게 다룬다.** 새 검사는 *같은 부류의 앱 결함이 실제로 두 번 난 자리*에만.
  현재 51종·14,937줄이고, 그것들이 잡은 것의 대부분은 **다른 검사와 도구의 결함**이었다.

### 완료의 정의

- **병합은 CI 초록으로 완료된다.** 실기기 확인은 병합을 막지 않는다.
- **실기기 확인은 별도 큐**(`device_walkthrough.md`)에 쌓고 **회차 단위로 소화**한다.
  종전 규칙(*"화면 변경은 실기기 확인 전까지 완료로 보고하지 않는다"*)은
  갚을 수 없는 부채를 만들어 **세션이 화면을 피하게 했다** — 그래서 바꿨다.

---

## 2. 실기기 확인 — **124건 미검증 · 마지막 통과 2026.08.01**

| | |
|---|---|
| 통과 | **7건** (마지막 2026-08-01) |
| 일부 확인 | 9건 |
| **미검증** | **124건** |
| 그 사이 지나간 판 | **52판** |

**→ 절차는 `docs/device_walkthrough.md`.** **133절 전수**(미검증 124 + 일부 확인 9)를 읽고
**확인 항목 1,348개를 319단계 · 14회차로** 접었다. 절은 하나도 안 버렸고, 줄인 것은 항목이다
(말투 확인 · "종전과 같은가"만 묻는 회귀 · 순수 계산 대조 · 겹쳐 보는 자리).
**0회차(15분)가 필수**이고 나머지는 아무 순서로나 해도 된다.

세는 법(값을 여기 적지 않는다 — 적으면 낡는다):
```bash
grep -cE "^### 3-[0-9]+\..*⬜ 미검증" docs/remaining_work_2026-07.md
```

---

## 3. 살아 있는 백로그 — **15건**

세는 법: `tools/check_backlog_markers.sh` (그 출력이 정본이다).
**본문은 `docs/remaining_work_2026-07.md` 4장에 그대로 있다** — 그 문서는 닫혔지만
백로그 표는 여전히 유효하고, 이 표는 그 색인이다.

심각도: 🔴 데이터가 상한다 / 🟠 조용한 유실·거짓 고지가 실사용 경로에 /
🟡 걸리지만 유실 없음 / 🟢 불편하나 틀리지 않음 / 🔵 실사용에서 거의 안 걸림

### 고를 수 있는 것 — 1건

| 행 | | 무엇 | 크기 |
|---|---|---|---|
| `B-46` | 🔵 | 값 라이브러리 엔트리 설명이 AI 프롬프트에 안 실린다 | 화면 밖 · `ai/` · 프롬프트 상한을 함께 볼 것 |

### 기기 회신을 기다리는 것 — 3건

| 행 | | 무엇 |
|---|---|---|
| `B-18` | 🟡 | `restoreOperation`이 항목마다 트랜잭션을 연다 — 한 덩어리로 묶을 상한을 **실기기 측정 후** 정한다(확정됨) |
| `B-72 ⓑ` | 🟡 | 내보내기·자동 백업의 DOM 전량 적재 — 규모 신호가 실기기에서 나와야 한다 |
| `B-214` | 🔵 | 관계도 `onDraw`를 안 쟀다 — 그리기 축은 기기에서만 보인다 |

### 판정을 먼저 받아야 하는 것 — 4건

| 행 | | 무엇 | 물어야 할 것 |
|---|---|---|---|
| `B-258` | 🟡 | 창이 그리는 구역 밖의 필드값은 화면에서 고칠 자리가 없다(사건 축은 전역 구역을 안 그린다) | 전역 구역을 그릴 것인가, 다른 경로를 줄 것인가 |
| `B-259` | 🟢 | 타입 불일치 목록에서 캐릭터 줄만 칸까지 데려다주지 못한다 | 공용 렌더러 계약을 넓힐 것인가 |
| `B-262` | 🟢 | 화면 문구 검사가 코드에 사는 한국어 대부분을 안 본다(미등재 82파일) | 범위를 넓힐 것인가(위반이 대량으로 뜬다) |
| `B-263 ⓑ` | 🟢 | 복원 미리보기에 '앱 설정' 범주가 없다 | 셀 것인가, 안 세는 이유를 적을 것인가 |

### 보류 — 7건 (재개 조건이 아직 안 섰다)

| 행 | | 무엇 | 재개 조건 |
|---|---|---|---|
| `B-27 ①` | 🟡 | 임포트 원문 보존 잔여 — 날짜로 자동 변환된 셀의 재포맷 | 사용자 자료(실제로 깨진 파일) 필요 |
| `B-49` | 🟢 | 세계관·작품·세력 목록의 검색 부재 | 규모 신호(항목이 수십 개) |
| `B-53` | 🔵 | 폴더 왕복 2차 후보 셋 | 사용자가 우선순위 낮음으로 판정 |
| `B-57` | 🔵 | 의존 주입이 수동이다(`by lazy`) | 규모 신호 |
| `B-58` | 🔵 | 이미지 로딩이 수동 관리다 | 규모 신호 |
| `B-125` | 🟡 | 릴리스 빌드를 CI가 한 번도 밟지 않는다 | 방아쇠(릴리스 준비) |
| `B-209` | 🟡 | 통계 드릴다운이 스냅샷 필드값을 매번 훑는다 | 실측 0.4ms — 규모가 커지면 재개 |

---

## 4. 재현 기준선 — 손대기 전에 이 숫자부터 맞춘다

```bash
JARS_DIR=<스크래치패드> tools/setup_jvm_env.sh      # 세션마다 1회
JARS_DIR=<스크래치패드> tools/run_jvm_tests.sh      # 순수 JVM 시험
JARS_DIR=<스크래치패드> tools/probe_compile.sh /tmp/probe.txt
JARS_DIR=<스크래치패드> tools/probe_view_compile.sh /tmp/view.txt
for f in tools/check_*.sh; do "$f" || echo "FAIL $f"; done
for p in tools/verify_room_migration*.py tools/verify_reset_coverage.py; do python3 "$p"; done
```

**현행 값(2026.08.19):** 시험 **3321** · 프로브 **564** · 커스텀 뷰 프로브 **0** · 검사 **51종**

- ⚠️ **`run_jvm_tests.sh`의 종료 코드는 시험 실패를 말하지 않는다** — 언제나 0이다.
  산출에서 `OK (` / `FAILURES!!!`를 볼 것.
- ⚠️ **프로브 기준선을 뜨기 전에 `git fetch origin master`를 먼저 한다.**
  낡은 `origin/master`로 뜨면 프로브가 거짓말을 한다(양방향으로).
- ⚠️ **문서의 숫자보다 실제 실행 결과를 믿을 것.**

---

## 5. 착수 절차

1. **이 문서를 읽는다.** 그리고 `docs/conventions.md`에서 **손댈 영역의 규약만** 읽는다.
2. **기준선을 재현한다**(4장).
3. **손댈 자리의 설계 문서가 코드와 맞는지 본다.** 어긋나면 고치고 착수한다.
   범위 밖이면 — **한 줄로 끝나면 그 자리에서 고치고, 크면 백로그에 올리고 진행한다.**
4. **묻는 것은 셋뿐이다:** 어느 쪽을 골라도 말이 되는데 결과물이 갈릴 때 ·
   사용자의 앞선 확정을 뒤집게 될 때 · 되돌리기 어려울 때(데이터 유실·마이그레이션·저장 형식).
   **그 밖은 묻지 않는다.**

### 지도 — 어디에 무엇이 있나

| 목적 | 문서 |
|---|---|
| **판단 기준(최상위)** | `CLAUDE.md` |
| **지금 할 일** | **이 문서** |
| **실기기 확인** | `docs/device_walkthrough.md` |
| 규약 원문 | `docs/conventions.md` |
| 계층·단일 소스·확장점 | `docs/architecture_2026-07.md` |
| 확장 한계·성능 | `docs/scalability_performance_2026-07.md` |
| 화면 문구 | `docs/text_style_guide_2026-07.md` |
| 사용자가 확정한 것 | `docs/judgment_confirmations_2026-08.md` |
| 백로그 본문(닫힌 문서의 4장) | `docs/remaining_work_2026-07.md` |
| 그 밖 설계 정본 | `docs/*.md` — 손댈 영역의 것만 |

---

## 왜 이 문서가 생겼나 — 실측

사용자 질문(2026.08.19): *"개발이 끝이 안 나는데 원인이 뭐고 어떻게 해결할 수 있을까?"*

| 재는 것 | 실측 |
|---|---|
| 백로그 **250건** 중 사용자가 연 것 | **1건** (나머지는 콜드 검토 20 · 점검 18 · 감사 12 · 앞 판/착수 대조…) |
| 최근 20판의 산출 | 앱 코드 **4,220줄** vs 문서 4,235 + 도구 2,448 = **6,683줄** |
| 앱 코드를 한 줄도 안 바꾼 판 | **20판 중 6판** |
| `remaining_work` 크기 | 5일 만에 16,342 → **21,691줄** |
| 앱에서 화면(`ui/`) 비중 vs 그걸 보는 시험 | 코드의 **48%** / 시험의 **8%** |
| **실기기 확인 통과** | **7건 · 마지막 2026-08-01 · 그 뒤 52판** |

**뿌리는 하나다.** 실기기 확인이 멈추자 화면 부채가 쌓였고, 세션은 그 부채를 갚을 수 없으니
**부채를 안 만드는 일**(도구·검사·문서)로 흘렀다. 화면을 자동으로 못 보니 검사를 만들었고,
검사는 자기가 볼 수 있는 것 — **자신과 다른 도구** — 을 검사하기 시작했다.
그 활동이 백로그를 자가발전시켰고(249/250), 문서를 키웠고, 커진 문서가
**부채를 은폐했다**(인수인계 판은 줄곧 "열다섯"이라 적었는데 실제로는 124건이었다).

**그래서 고친 것은 둘이다:** 판의 정의(1장)와 문서 크기(이 문서).
**고치지 않기로 한 것도 적어 둔다** — 콜드 검토·자기 등재·검사 신설은 사용자가 이번에
자르지 않았다. 다시 자가발전이 보이면 그때 다시 잰다.
