# 지금 상태 — 세션 진입점

> **이 문서 하나가 "지금 무엇을 하는가"의 단일 소스다.** 500줄을 넘기지 않는다.
> 넘으면 그것이 자른다는 신호다 — 이 문서는 2026.08.19에 **21,691줄짜리
> `remaining_work_2026-07.md`를 대체하며** 생겼고, 그 문서가 커진 방식이 곧
> 이 프로젝트가 느려진 방식이었다(아래 「왜 이 문서가 생겼나」).
>
> **경위·이력은 여기 안 쌓는다.** 그건 `git log`가 갖고 있고, 2026.08.19 이전은
> `docs/remaining_work_2026-07.md`(닫힘)와 `docs/session_log_2026-07.md`(닫힘)에 있다.

---

## 0. 지금 무엇을 하는가

### ✅ 이번 판이 낸 것 (2026.08.25, 여덟 번째 파일 검토) — **사용자 요청: 내보낸 엑셀 파일 검토 → "근본적 수정"**

여덟 번째 파일(`NovelCharacter_20260825_130126.xlsx` — 34시트·211명·이미지 2,055장)을 전수로
뜯었다. **앞 판의 수리 다섯은 파일에서 전부 확인됐다**(세력 관계 코드 열 · 이미지경로 파일명
전환 최대 3,197자·절대경로 0 · 코드 열 녹색 삼각형 억제 · AI 쿨다운 미전송 · 안내 문구).
새로 나온 것은 **둘**이고 둘 다 뿌리에서 닫았다. 경위는 `git log`가 든다.

| 무엇이었나 | 무엇을 했나 |
|---|---|
| **③(0이 빠진 생일) 정리가 짝을 두고 갔다** — `character_field_values`만 올리고 `field_value_entries`를 안 건드려, 라이브러리에 `5-30`·`6-7`이 사용횟수 0으로 남고 **실제로 쓰이는 `05-30`·`06-07`은 행이 아예 없었다**. '필드 데이터' 시트가 스스로 적은 *"실제로 쓰인 값이 모이는 시트"*가 거짓이 된 자리 | `LegacyValueFormats` **④** + [BirthDateEntryRepair](../app/src/main/java/com/novelcharacter/app/data/maintenance/BirthDateEntryRepair.kt)(순수) — 엔트리 값을 저장 모양으로 올리고 **옛 표기를 별칭으로 남긴다**(앱의 '값 이름 변경'과 같은 처분). 저장 모양이 이미 있으면 병합한다(유니크 색인). `source`는 안 올린다 — 안 쓰는 값이 '미사용 정리'에서 빠지면 안 된다. 라이브러리가 **아예 모르는** 값은 `field_library_harvest_pending`으로 다음 실행의 수확에 맡긴다 |
| **세력 자동 관계 87건의 `factionId`가 비어 있었다** — 소속 클리크와 정확히 일치하고(엑실리온 66·카노스 15·월아 6) 생성일이 소속 행과 같은 밀리초 구간인데 연결만 없다. **왕복마다 굳는다**: 내보내기가 빈 칸을 쓰고 가져오기가 빈 칸을 '연결 해제'로 읽는다 | `LegacyValueFormats` **⑤** + [FactionAutoRelationRelink](../app/src/main/java/com/novelcharacter/app/data/maintenance/FactionAutoRelationRelink.kt)(순수) — 생성자가 넣는 모양 전부(유형·강도·양방향·빈 설명·순서 0) **+ 소속 생성 시각 ±60초** + 후보 세력이 정확히 하나일 때만 잇는다. **수동 관계를 잡으면 탈퇴가 사용자의 관계를 지우는 유실 경로가 열리므로**(`FactionRepository`의 금지) 규칙을 순수 계층에 두고 시험이 고정한다 |

**⑤는 새 발견이 아니라 미결이었다** — 2026.08.24 판이 이미 87건을 세어 *"사용자 몫"*으로
남기며 *"코드에 살아 있는 유실 경로는 없다"*고 적었다. **그 판단이 틀렸다**: `removeMember`·
`departMember`·`deleteAllByFaction`·`countByFaction`이 전부 `factionId`를 키로 도는데,
그중 `countByFaction`은 **세력을 지우기 전 고지에 "자동 관계 0건"이라 적는다**(R-4가 막으려는
거짓 안심). 가져오기가 경고한다는 것은 *파일에서 들어오는* 값의 이야기이고, **이미 DB에 든
행**은 아무도 보지 않았다.

**1회 플래그를 v3로 올렸다** — 항목이 늘 때 플래그를 안 올리면 이미 마친 설치가 **새 항목만
조용히 건너뛴다**(v2가 ③을 위해 올린 그 이유 그대로). 그 규약을 object의 KDoc 머리에 박았다.

**수확 예약을 이번 실행이 아니라 다음 실행에 건 것은 일부러다** — `seedFieldValueLibraryIfNeeded`가
`onCreate`에서 뒤에 불리지만 **플래그를 코루틴 밖에서 먼저 읽고**, 설령 읽는다 해도 두
코루틴이 같은 디스패처에서 경쟁해 수확이 ③보다 먼저 돌 수 있다(그러면 옛 표기를 다시 걷는다).

**산출을 사용자의 실제 파일로 확인했다** — 그 파일의 값으로 두 순수 오브젝트를 돌려
생일 엔트리 **3건이 이름 변경**(`4-28`→`04-28` · `5-30`→`05-30` · `6-7`→`06-07`, 병합 0)이고
세력 재연결이 **정확히 87건**(가문원 81 · 월아 6)이며 **수동 관계 48건은 한 건도 안 걸리는**
것을 봤다. 그중 `동료` 5건이 결정적이다 — '테스트' 세계관 세력 `1`의 자동관계유형이 마침
`동료`라, 소속 축이 없었으면 사용자의 관계가 걸렸을 자리다.

**오진을 피하려고 뺀 것도 적는다** — 등급 수치 겹침(스텔라 크로니클 셋)과 `테스트` 세계관의
순서 0 겹침은 **앞 판들이 문을 고친 뒤 남은 데이터**라 새 결함이 아니다. 대결 이미지 축의
절대경로는 2026.08.24 확정 설계다. `관계 변화`의 '관계 유형'에 드롭다운이 없는 것은 형제 둘과
갈리지만, 그 검사가 `showError`라 **과거 시점의 옛 유형을 되돌려 적는 것을 막는다** — 뺀 쪽이
안전한 판단으로 보여 결함으로 세지 않았다.

**기준선(판 끝 실측):** 시험 4,039 → **4,061 OK**(+22) · 프로브 **569 동일** · 뷰 프로브
**77 동일** · 검사 **54종 실패 0** · **Room 마이그레이션 없음**(저장 형식·스키마 무변경 —
고치는 것은 이미 든 행의 값이다).

**실기기 몫 — 신규 둘**(`3-ㅇ-33`·`3-ㅇ-34`, S1 묶음 · 473 → **476단계**. 앞 판이
*"다음 회차에 정식으로 넣을 것"*이라 적어 둔 **미등재 둘도 함께 등재했다** — `12-ㅋ4`와
`12-ㅁ2`의 취소 문장. 손으로 적힌 채였으면 이번 재생성이 그것을 지웠다): ⓐ 업데이트 직후 1회 정리가 돌고 나서 **설정 > 로그**에 두 줄이
뜨는가(생일 엔트리 3건 · 세력 재연결 87건) ⓑ **세력 상세에서 엑실리온을 지우려 할 때 고지가
'자동 관계 66건'이라 말하는가** — 종전에는 0건이었다. ⓑ가 이 판의 본체다.

### 앞 판이 낸 것 (2026.08.25, 일곱 번째 파일 검토) — **사용자 요청: 내보낸 엑셀 파일 검토 → "진짜 문제인지 검토하고 전부 근본 수리"**

일곱 번째 파일(`NovelCharacter_20260825_102527.xlsx` — 34시트·211명·이미지 2,055장)을 전수로
뜯어 다섯을 보고했고, **사용자 지시로 그 다섯을 되짚어 하나를 오진으로 물리고 넷을 뿌리에서
고쳤다**(*"방편식 말고 근본적으로"*). 경위는 `git log`가 든다 — 4커밋.

| 무엇이었나 | 무엇을 했나 |
|---|---|
| **'세력 관계'에만 행의 코드가 없었다** — 자연키가 (세력1, 세력2, 유형)이라 시트에서 유형을 고치면 새 관계가 생기고 옛 관계가 남는다. 파일에 이미 그 모양이 있었다(`2 ↔ 1`이 '동맹'과 '동', 13초 차) | **스키마 v58** — `FactionRelationship.code` + `MIGRATION_57_58`(형제 `MIGRATION_41_42`와 같은 절차) + 시트에 회색 '코드' 열 + `matchRow`(코드 우선 → 자연키 폴백, **같은 두 세력의 코드일 때만**) + 미리보기 동행(R-33) + 월드패키지 DTO에도 코드(판올림 아님 — R-64). **앞 판이 안내 문구로 막아 둔 자리를 코드로 갈음했다** |
| **등급 표의 수치가 겹쳐도 아무 말이 없었다** — 체계를 고르면 라벨은 체계 것, 숫자는 이 필드가 쓰던 것이 남아 `C`가 `F`와 같은 점수가 된다. 필드 넷이 그 모양이었고 드롭다운 차례까지 무너졌다(`F,C,E,B,D,A…`) | `GradeTable.duplicateValues`(순수) — **판정만 두고 처분은 부르는 쪽이 정한다**(`Problem`이 아니다: 행은 멀쩡하고 표의 뜻이 무너진 것이라 저장을 막을 일이 아니다). 필드 편집·등급 체계 편집에 안내 줄(열자마자), 가져오기에 경고 한 줄 |
| **AI 쿨다운이 파일을 타고 이식됐다** — 아직 지나지 않은 쿨다운 둘이 실려 있었고 코덱이 되읽는다(다른 기기에서 프로바이더가 잠긴 채 시작) | `AiProviderCodec.encodeForTransfer`/`keepDeviceState` — 저장용 `encode`와 갈랐다. 모델·주소의 사실인 학습값 넷은 그대로 싣고 **벽시계 한 점인 쿨다운만** 뺀다 |
| **'이미지경로'가 절대경로 배열이었다** — 68장에 6,053자, 셀 상한까지 **368장**. 넘으면 잘린 JSON이 안 읽혀 그 열이 무용해진다. 항목당 42자가 기기 경로 접두였다 | **파일명으로 담는다**(`util/ImagePathCell`) — `DropdownListLimits`가 고른 쪽(*담는 방법을 바꾼다*). 가장 긴 칸 6,053 → **3,197자**, 상한 368 → **697장**. 옛 절대경로 표기도 그대로 읽는다. 확장성 문서 상한 표에 등재 |
| **전부 숫자인 코드에 엑셀이 [숫자로 변환]을 권했다** — 눌리면 16자리가 유효 15자리로 접혀 코드가 다른 글자가 된다(파일에 실제로 하나 있었다) | `addIgnoredErrors(NUMBER_STORED_AS_TEXT)` — 회색 글자 열 + **이름이 '코드'로 끝나는 열**(편집 가능한 참조 코드 열까지). 판정은 순수 계층이 들고 전수 시험이 훑는다 |

**오진 하나를 물렸다** — 보고에는 *"'대결 기록'의 이미지 축 참가자 **이름 칸**이 절대경로다"*가
있었는데, 파일을 다시 열어 보니 **이름 칸은 이미 파일명**이었다(경로는 코드 칸뿐이고 그것은
2026.08.24에 *"경로가 곧 정체"*로 확정된 설계다). 잘라 본 덤프를 잘못 읽은 것이었다.

**콜드 검토를 두 번 돌렸고 다섯을 냈다. 그중 넷이 이 판이 만든 것이었다** — ⓐ 코드로 찾은 행의 유형을 옮길 때
그 자리에 다른 관계가 있으면 유니크에 걸려 SQL 예외로 죽던 것(먼저 갈라 고지하고 어느 쪽도
지우지 않는다 · 미리보기 동행) ⓑ 안내가 *"배열로 읽을 수 없는 값은 기존 배정을 그대로 두고
알려 드립니다"*를 캐릭터·세계관·작품 셋 모두에 약속하는데 **그 처분이 캐릭터에만** 있던 것
(`readImagePathsCell` 하나로 모았다) ⓒ 버전을 올리면 CI가 교착에 빠지던 것 — 스키마 JSON은
손으로 못 적고 만드는 것은 Gradle 빌드인데 하네스가 그 빌드보다 먼저 돌고 실패하면 빌드가
시작되지 않는다. 한 판 뒤처짐만, 그것도 그 구간을 잇는 마이그레이션이 있을 때만 받도록 했다.
**둘째 회차가 둘을 더 냈고 둘 다 이 판이 만든 것이었다** — ⓓ 수치 겹침의 *행을 세는 규칙*을
화면 둘에 각각 적은 것(`GradeTable.duplicateValuesOf` 하나로 모았다 — 바로 앞 커밋에서
*"둘이 갈리면 한쪽이 낡는다"*고 적어 놓고 만든 자리다) ⓔ 프로바이더를 행마다 저장소에서
다시 읽던 것(들이기 전에 한 번만 뜬다 — *이 기기가 갖고 있던 값*은 스냅샷이라야 뜻이 선다).

**커밋 일곱이 전부 홀로 컴파일된다** — 중간 커밋마다 워크트리를 떠 프로브를 돌려 전부
**569**를 확인했다(CI가 스키마 JSON을 민 봇 커밋 하나 포함).

**CI가 낸 것 하나를 더 닫았다 — 버전을 올리면 첫 회차가 반드시 빨간불이던 것.**
스키마 부기 검사가 **쌍**인데(순수 하네스 · 에뮬레이터 계측 시험) 앞에서 순수 쪽만 완화했다.
*"에뮬레이터 쪽은 빌드를 막지 않으니 스스로 낫는다"*고 봤는데 **실측으로 틀렸다**: 봇의
스키마 커밋은 `GITHUB_TOKEN`으로 밀려 워크플로를 다시 트리거하지 않고, 재실행·수동 실행은
둘 다 `403 Resource not accessible by integration`이었다 — 또 한 번 푸시하는 것 말고 길이
없었다. 둘이 **같은 규칙**을 들게 했다(한 판 뒤처짐 + 그 구간을 잇는 마이그레이션 선언).

**기준선(판 끝 실측):** 시험 4,009 → **4,039 OK**(+30) · 프로브 **569 동일** · 뷰 프로브
**77 동일** · 검사 **54종 실패 0** · 마이그레이션 하네스 전부 통과(신규
`verify_room_migration_58.py` 26건 · `verify_room_migration_57.py`는 58.json이 들어와
전체 표 대조 43건을 돈다) · **Room 마이그레이션 있음(v57 → v58, `58.json` 커밋됨)**.

**실기기 몫 — 신규 넷:** ⓐ 필드 편집에서 등급 체계를 고를 때 수치 겹침 안내 줄이 뜨는가
(`스텔라 크로니클`의 전투 센스·마력 회복 속도·지략이 그 상태다) ⓑ 등급 체계 편집 창의 같은 줄
ⓒ 내보낸 파일의 '이미지경로'가 파일명으로 보이고, 그 파일을 다시 들여도 그림이 그대로인가
ⓓ 코드 칸에 녹색 삼각형이 사라졌는가. **ⓒ가 가장 중요하다** — 저장 형식이 아니라 교환 표기가
바뀐 자리라, 왕복 한 번을 실제로 밟아 봐야 한다.

### 앞 판이 낸 것 (2026.08.24, 여섯 번째 파일 검토) — **사용자 요청: 내보낸 엑셀 파일 검토 → 확정 둘 제대로 수리**

여섯 번째로 사용자가 내보낸 파일(`NovelCharacter_20260824_211305.xlsx`)을 전수로 뜯어 **둘을
찾아 사용자 승인 아래 제대로 닫았다** — 방편식 패치가 아니라 뿌리를 고쳤다(신규 시험 6건).

| 무엇이었나 | 무엇을 했나 |
|---|---|
| **세계관 없는 '작품' 고아 행** — 캐릭터의 '미분류'와 달리 전용 목록·안내가 없어 사실상 안 보였다 | `NovelDao.getUnassignedNovels` 신설 + 목록 툴바에 **'세계관 미배정만 보기'** 토글(특정 세계관 화면에서는 숨김) + 빈 화면 문구 필터별 분리(R-17) + 안내 시트 '작품' 섹션에 캐릭터 쪽 관례를 그대로 적용한 안내 한 줄(전용 시트까지는 아니다 — 작품은 원래 세계관별 시트가 없다) |
| **새 필드가 항상 순서 0을 받아 겹쳤다** — 필드 관리·작품·사건, 세 다이얼로그 전부 `displayOrder`를 안 채운 채 넘겼다(실측: `테스트` 세계관에서 사용자 필드 `fgbde`가 `birth_year`와 0을 공유) | `FieldOrderAssignment`(순수) + `FieldDefinitionDao.getMaxDisplayOrder*` 신설. 세 다이얼로그가 공유하는 단일 관문 `UniverseRepository.insertField`에서 새 필드(id==0)만 재배정 |

**콜드 검토가 하나를 냈다** — `insertField`가 트랜잭션 없이 최댓값을 조회해 겹치면 순서 충돌이 재발할 수 있었다. `withTransaction`으로 닫았다.

**기준선(판 끝 실측):** 시험 3,988 → **3,994 OK**(+6) · 프로브 **569 동일**(master worktree와
바이트 동일) · 뷰 프로브 **77 동일** · 검사 **54종 실패 0** · Room 마이그레이션 없음.

**실기기 몫 — 신규:** 작품 목록 '세계관 미배정만 보기'(`ui/**`). 토글·빈 문구·화면별 숨김 확인.

### 앞 판이 낸 것 (2026.08.24, 다섯 번째 파일 검토) — **사용자 요청: 대결 이미지 축 참가자 코드의 왕복 결함 보완**

**접었다** — 경위는 `git log`가 든다. '대결 기록' 36행 중 23행(이미지 축)의 참가자 코드가
내보낸 기기의 절대경로였고, 그 경로가 담긴 이미지 없이 다시 들어오면 아무 경고 없이
고아가 됐다. **설계 자체는 옳다**(13-4: *"경로가 곧 정체"*) — 빠진 것은 그 사실을 앞뒤에서
말해 주는 자리 둘이었다: 내보내기가 그 데이터를 참조 집합에 안 넣어 손실 고지가 침묵했고,
가져오기가 판을 받아들인 뒤 아무 말도 안 했다. `DuelImageParticipants.referencedPaths`
(순수)를 세워 zip 완전성·손실 고지에 합류시키고, 가져오기 결과에 고지 한 줄을 더했다.
**거부하지 않는다** — 판은 그대로 들어오고 고아 집계는 종전 그대로다.
**기준선:** 시험 3,982 → 3,988 OK · 프로브 둘 동일 · 검사 54종 실패 0 · 저장 형식 무변경.

### 앞의 두 판 (2026.08.24, AI API) — **사용자 요청: AI API 전수 검토 → 결함 수리 → 품질 항목 실행**

**접었다** — 경위는 `git log`가 든다. 전수 검토가 확정 결함 일곱을 냈고(크레딧 소진이
"키가 틀렸다"로 나가던 것 · `max_completion_tokens` 교정 미학습 · 모델 목록의 출력 상한
폐기 · 중복 판정이 멀쩡한 항목을 버리던 것 · 선택 적용이 무조건 결과를 비우던 것 · 재요청
두 경로의 선적용 실패 무시 · 사전 제거 고지가 재시도 출구에서 빠지던 것) 전부 닫았다.
문서가 「한계」로 적어 둔 결손(기간 누적 사용량)을 구현했고, 이어진 판이 진행
다이얼로그의 취소·결정형 진행도(R-26)와 단가 기반 비용 추정을 얹었다.
**기준선:** 시험 3,955 → 3,982 OK · 프로브 둘 동일 · 검사 54종 실패 0 · Room 마이그레이션 없음.
**실기기 몫 5단계 등재**(`12-ㅈ3`·`ㅊ3`·`ㅋ3`·`12-ㅁ2` — S17·S19).

### 앞 판이 낸 것 (2026.08.25) — **사용자 요청: 네 번째 파일 검토 → 전부 수리 → 콜드 검토 → 병합**

**접었다** — 경위는 `git log`가 든다(9커밋 + 콜드 검토 2회, 병합 뒤 1회 추가). 네 번째로
내보낸 파일(34시트·209명·이미지 1,443장)을 전수로 뜯어 **아홉을 찾아 아홉을 닫았다** —
무소속 캐릭터가 시맨틱 동기화 일곱 자리에서 전부 빠진 것 · 생일 포맷의 0 누락 · 세계관·작품
이미지가 `imageMode=custom`이 아니면 엑셀 첨부가 무효였던 것 · 안내 문구 오류 셋 ·
틀 고정 기본값 누락(spec 28 중 14) · 이미지 `char:` 토큰 미해독(1,437/1,443행) · 연표-출생연도
불일치 무고지. 병합 뒤 콜드 검토를 한 번 더 돌려(사용자 지시) 생일 판정 로직이 세 자리에
손으로 흩어져 있던 뿌리 결함을 마저 닫았다. **기준선:** 시험 3,903 → 3,955 OK · 프로브
둘 동일 · 검사 54종 실패 0. 실기기 몫 9단계 등재(`3-ㅇ-24`~`ㅇ-32`).

### 앞 판이 낸 것 (2026.08.24 늦게) — **사용자 요청: 또 다음 파일을 검토 → 콜드 검토 → 병합**

**접었다** — 경위는 `git log`가 든다(6커밋 + 콜드 검토). 세 판째 같은 방법으로 파일
(34시트·209명·이미지 1,443장)을 전수로 뜯어 **아홉을 찾아 일곱을 닫았다** — 관계 유형
드롭다운이 자기 시트의 값을 거부하던 것(135행 중 87행) · '대결 기록'만 다른 이름 규칙 ·
'전체 캐릭터'에서 미분류 7명의 필드 칸이 통째로 빈 것 · 역전된 수정일 · '#' 빠진 색 등.
판정을 순수 계층으로 꺼내는 쪽으로 고쳤다(`RelationshipTypeOptions` · `CharacterNameIndex` ·
`ColorHex` · `LegacyValueFormats`). **닫지 않고 사용자 몫으로 남긴 둘 중 ⓐ(자동관계 87개의
세력 연결이 끊긴 것)를 2026.08.25 여덟째 판이 뿌리에서 닫았다** — 그때는 *"코드에 살아 있는
유실 경로는 없다"*고 보아 넘겼는데, 실제로는 세력 삭제 고지가 그 87행을 못 세고 있었다.
**기준선:** 시험 3,882 → 3,903 OK(+21) · 프로브 둘 동일 · 검사 54종 실패 0.
실기기 몫 8단계 등재(`3-ㅇ-16`~`ㅇ-23`).

### 앞 판이 낸 것 (2026.08.24 이르게) — **사용자 요청: 다음 날 파일을 다시 검토 → 전부 수리 → 콜드 검토 → 병합**

**접었다** — 경위는 `git log`가 든다(4커밋 + 콜드 검토). 앞 판 수리가 반영된 파일을 다시
전수로 뜯어 **여덟을 찾아 여덟을 닫았고**(두 줄 `__birth` · `__alive` 소급 · 머리글 잘림 등),
콜드 검토가 여덟을 더 내 여섯을 닫았다 — 그중 하나가 연표 경로의 `__birth` 둘째 줄이라는
**진짜 뿌리**였다. 소급 둘(1회·멱등)은 사용자 지시로 확정됐다. 실기기 몫 8단계 등재
(`3-ㅇ-8`~`ㅇ-15`).

### 앞 판이 낸 것 (2026.08.23 늦게) — **사용자 요청: 내보낸 엑셀 파일을 검토 → 전부 수리 → 콜드 검토 → 병합**

**접었다** — 경위는 `git log`가 든다(3커밋 + 콜드 검토). 사용자가 내보낸 첫 파일을 전수로
뜯어 **일곱을 찾아 일곱을 닫았고**, 콜드 검토가 넷을 더 냈다. 이 판이 세운 방법이 그 뒤 세 판을
이끌었다 — *"엑셀을 손대는 판은 파일을 한 벌 떠서 여는 것으로 시작할 것."*


### 앞 판이 낸 것 (2026.08.23 이르게) — **사용자 요청: 남은 오류·문제점 전부 검토·해결**

**접었다** — 경위는 `git log`가 든다(26커밋). 앱 코드 12축을 전수 감사해 확정 41건을 닫고,
그 위에 콜드 검토로 4건을 더 닫았다. **주황(조용한 유실·거짓 고지가 실사용 경로에)이 전부
닫힌 판**이다 — 원리적으로 못 만나던 자리 · 되돌릴 자리 없는 파괴 · 반쪽으로 끝나던 것 ·
거짓 고지 · 연타·회전 · 엑셀 왕복 · 미분류를 지우던 화면 · 손가락 아래 성능.
수리는 자리마다 단일 소스를 세우는 쪽으로 했고, 규약 R-29의 전수 명령도 함께 고쳤다
(열거식이 낡은 채 초록을 내고 있었다 — 호출 자리 24곳이 전수에서 빠져 있었다).

✅ **그때 대기로 남긴 되돌리기 축은 2026.08.24 판에서 닫혔다** — 생존여부 마이그레이션의
소급 여부를 사용자가 확정해(*"다 진행시켜"*) `alive_sync_migrated_v2`로 한 번 더 돈다.

### 그 앞 판 (2026.08.22) — **사용자 요청: 심각도 중·저인 것들을 검토하고 해결**

**접었다** — 경위는 `git log`가 든다. 여덟 축 감사로 후보 88건을 모아 주황을 전부 닫았고
(말없는 유실 · 거짓 고지 · 엑셀 왕복 · 끝나지 않는 자리 · AI 유료 응답), 노랑 아래도 부류로
묶어 걷었다. 사용자 판정 둘(이름뱅크 전역 모수 · 인사이트 채움률)을 받아 반영했다.

✅ 그때 남긴 되돌리기 축(생존여부 소급)은 **2026.08.24 판에서 닫혔다** — 위 참조.

### 그 앞의 네 판 (2026.08.20~21)

**접었다** — 링크 묶음 태그 공유 단위(사용자 판정) · 이미지탭 AI 태그 크래시의 부류 전체
수리(R-69·R-70 신설) · 워크스루 활성 333단계 전수 검토(확정 결함 64건) · 엑셀 입출력·인앱
UI 결함 검토. 살아 있는 몫만 3장 백로그에 남아 있다.

### 🎯 다음 판 — **실기기가 먼저다**

**일곱 판의 엑셀 검토가 전부 닫혔다 — 그 자리는 비었다. 남은 것은 회신 하나뿐이다.**

**일곱 판이 같은 실측을 냈다:** 사용자가 내보낸 파일을 열어 본 것만으로
**7 → 8 → 9 → 9 → 1 → 2 → 4건**이 나왔다. 일곱 번 다 시험 3,800~4,009개·검사 54종이
전부 초록인 채였다. 자동 검증이 *"컴파일되는가"*만 본다는 1장의 말은 화면에만 걸리는 것이
아니라 **내보낸 산출물에도 그대로 걸린다** — **엑셀을 손대는 판은 파일을 한 벌 떠서 여는
것으로 시작할 것.**

**일곱 번째 판이 하나를 더 배웠다: 보고한 것을 다시 열어 보면 오진이 나온다.** 다섯 중
하나가 **잘라 본 덤프를 잘못 읽은 것**이었다(대결 이름 칸은 이미 파일명이었다). 수리에
들어가기 전에 **원본을 다시 여는 한 걸음**이 그것을 걸렀다 — 안 걸렀으면 멀쩡한 설계를
고칠 뻔했고, 그 설계는 앞 판이 확정한 것이었다.

**앞 판이 남긴 둘 중 하나는 이번에 닫혔다:** ⓑ 연표 '탄생' 연도 ≠ 캐릭터 출생연도를 앱이
말하지 않던 축은 정합성 검사의 셋째 항목으로 섰다(어시스턴트 카드 · 실기기 `3-ㅇ-32`).

**남은 하나는 사용자가 봐야 갈린다**(0장 참조):
ⓐ **자동관계 87개의 세력 연결이 끊긴 것** — 파일만으로는 *예전에 풀렸다*와 *지금도 풀린다*가
안 갈린다. 코드에 살아 있는 유실 경로는 없음을 확인했으니(가져오기는 미해석 세력을 KEEP으로
강등하고 경고한다), **앱에서 세력 상세를 열어 자동 관계로 보이는지**만 보면 된다. 자동이 아니면
그 87개는 세력을 지워도 안 따라 지워지므로 그때 다시 연다.

**그래서 다음 판의 몫은 화면이다.** 최근 네 판이 앱 코드를 넓게 고쳤고 그중 상당수가
*보이는 것*을 바꿨는데(초기화 고지 · 진행 막대 · 등급 반영 시트 · 배치 필드값 스피너 ·
보충 탭 · 비교표 · 연표 카드 · 실루엣 편집기 · 어시스턴트 카드 · **네 판의 내보낸 파일 전체**),
**자동 검증은 그것을 원리적으로 못 본다**. `docs/device_walkthrough.md` **S1(15분)이 필수**다.
이번 판은 그 S1에 **1회 소급 둘**을 얹었다(`3-ㅇ-24`·`ㅇ-25`) — 업데이트 직후 첫 실행에서만
볼 수 있는 자리라, 그것을 지나쳐 버리면 다음 기회가 없다.

**대기 중인 사용자 판정은 없다.** 위 ⓐ는 판정이 아니라 **회신**이다(1분이면 갈린다).

백로그 **11건**은 여전히 전부 무언가를 기다린다 — 기기 회신 3 · `B-258` 잔여 1 · 자료 대기 1 ·
보류 6(3장). **판정 대기는 0이다.**

| | 무엇 | 조건 |
|---|---|---|
| **실기기 워크스루** | `docs/device_walkthrough.md` **S1(15분)이 필수** | 사용자만 할 수 있다. **이것이 대기열의 뿌리다**(2장) |
| **세력 자동관계 회신** | 위 ⓐ — 앱에서 세력 상세를 열어 자동 관계로 보이는지 | 1분. 아니면 그 자리가 다시 열린다 |
| **순회표 걷어내기** | 아래 — 기능 판에 딸려 가는 몫이라 **혼자 서지 않는다** | 다음 기능 판이 열릴 때 |

**기기 회신이 오면 `B-18`·`B-72 ⓑ`·`B-214`가 열리고, 보류 6은 규모 신호나 방아쇠가 서야 열린다.**

#### 순회표 걷어내기 — **살아 있다. 다만 딸려 간다**

**표를 다시 만들지 말 것.** 분류는 `tools/walkthrough/steps.json`의 `axis`·`audit` 칸에
단계마다 박혀 있고 걷어내는 장치도 서 있다. **대조는 끝났다 — 다시 재지 말 것.**

| 무엇을 | 수 | 어떻게 |
|---|---:|---|
| 반증 68 → `시험+코드`로 | **63**은 파일:줄이 `audit.evidence`에 있다 | 그 줄을 **열어 읽고** `drop`으로. **줄 번호가 아니라 코드 한 토막을 `anchor`로** |
| 안 덮인 `코드` 52 → `코드`로 | **24**는 자리가 적혀 있다 | 나머지 28은 찾아야 한다 |
| `순수` 8 | — | 시험이 싸면 쓰고 걷는다 |
| `기기` 51 | — | **남긴다.** 진짜 기기 몫이다 |

**한 건씩 열어 읽을 것** — 코드 11을 그렇게 했더니 3이 오분류였다(27%).
**규약:** 지우지 않고 `drop` 칸을 달아 남긴다 · `verify.py`가 사유를 기계로 검산한다 ·
`시험`만으로 걷을 수 있는 단계는 **사실상 없다**(콜드 검토가 13건을 전부 `시험+코드`로
되돌렸다) · **줄 번호는 근거가 못 된다**(같은 판 안에서 낡은 전례가 있다).
**하지 말 것(사용자 지시):** 코드 보면 아는 것에 시험을 만들지 않는다 · 검증을 복잡하게
만들지 않는다 · **애매하면 남긴다**(잘못 빼면 조용한 구멍이다).

**Robolectric은 세우지 않는다**(앞 판 판정 유지 — 사용자 지시와 어긋나고, 비용이 모든
빌드에 붙고, 더 싼 수단이 B-268에서 통했다). `수명` 20은 **추출 패턴**으로 덮는다:
그 화면을 손대는 판이 올 때 *판정만* 순수 클래스로 꺼낸다(위젯 접근은 화면에 남긴다).

### ⏳ B-267 — 병합됨, 기기 확인 하나 남음

회전에 캐릭터 편집의 **동적 필드가 통째로** 사라지던 결함. 수리·병합 완료(PR #364 ·
`f6808f07`). **남은 것:** 워크스루 `0-ㅅ` — 작품에 속한 캐릭터에서 이름과 생일·나이를 고친 뒤 회전.

**실기기 워크스루는 계속 유효하다.** `docs/device_walkthrough.md` — **S1(15분)이 필수**다.

---

## 1. 판의 정의 — **사용자가 앱에서 볼 수 있는 변화가 없으면 판이 아니다**

**2026.08.19 사용자 판정.** 종전에는 도구·검사·문서만 바꾸는 세션도 한 판으로 셌고,
그것이 실측으로 **최근 20판 중 6판(30%)**이었다. 같은 기간 앱 코드 4,220줄에
도구·문서 6,683줄이 붙었다 — **메타작업이 산출물의 1.6배**였다.

- **한 판은 사용자가 앱에서 볼 수 있는 변화를 하나 이상 낸다.**
- **도구·검사·문서 작업은 독립 판으로 열지 않는다.** 기능 판에 딸려 간다.
- **예외는 사용자가 지시했을 때뿐이다**(전례: 이 문서를 만든 판, 콜드 검토 지시).
- **검사·도구는 동결에 가깝게 다룬다.** 새 검사는 *같은 부류의 앱 결함이 실제로 두 번 난 자리*에만.
  현재 54종이고, 그것들이 잡은 것의 대부분은 **다른 검사와 도구의 결함**이었다.

### 완료의 정의

- **병합은 CI 초록으로 완료된다.** 실기기 확인은 병합을 막지 않는다.
- **실기기 확인은 별도 큐**(`device_walkthrough.md`)에 쌓고 **회차 단위로 소화**한다.
  종전 규칙(*"화면 변경은 실기기 확인 전까지 완료로 보고하지 않는다"*)은
  갚을 수 없는 부채를 만들어 **세션이 화면을 피하게 했다** — 그래서 바꿨다.

---

## 2. 실기기 확인 — **미검증 · 마지막 통과 2026.08.01**

| | |
|---|---|
| 통과 | **7건** (마지막 2026-08-01) |
| 일부 확인 | 9건 |
| **미검증** | **아래 세는 법이 정본이다** — 값을 여기 적지 않는다 |
| 그 사이 지나간 판 | **58판** |

**→ 절차는 `docs/device_walkthrough.md`.** **133절 전수**(당시 미검증 124 + 일부 확인 9)를 읽고
**확인 항목 1,348개를 접었다.** 절은 하나도 안 버렸고, 줄인 것은 항목이다
(말투 확인 · "종전과 같은가"만 묻는 회귀 · 순수 계산 대조 · 겹쳐 보는 자리).
그 뒤 **준비물별 25묶음**으로 다시 짜고, 걷어내기가 시작됐다.

**수를 여기 적지 않는다 — 적으면 낡는다.** 세는 법:
```bash
python3 tools/walkthrough/verify.py    # 단계·절·묶음의 정본
grep -cE "^### 3-[0-9]+\..*⬜ 미검증" docs/remaining_work_2026-07.md   # 옛 절의 미검증 수
```

> ⚠️ **두 수는 같아질 수 없다**(콜드 검토 2026.08.21). `remaining_work_2026-07.md`는
> 2026.08.19에 **닫혔고**, 그 뒤 등재된 절(`3-145` 이후 — 지금 `3-145`·`3-147`·`3-148`·
> `3-149`)은 그 문서에 없다. 그래서 위 `grep`은 **옛 절만** 세고, 워크스루가 낳은 절은
> `verify.py`와 `steps.json`이 든다. 종전에는 표가 손으로 적은 수를 들고 있어 그 수와
> 자기가 시킨 세는 법의 출력이 어긋났고, 어긋남의 사유가 어디에도 없었다.

**S1(15분)이 필수**이고 나머지는 번호 순서를 지킨다(재료 의존이 있다).

---

## 3. 살아 있는 백로그 — **11건**

세는 법: `tools/check_backlog_markers.sh` (그 출력이 정본이다).
**본문은 `docs/remaining_work_2026-07.md` 4장에 그대로 있다** — 그 문서는 닫혔지만
백로그 표는 여전히 유효하고, 이 표는 그 색인이다.

심각도: 🔴 데이터가 상한다 / 🟠 조용한 유실·거짓 고지가 실사용 경로에 /
🟡 걸리지만 유실 없음 / 🟢 불편하나 틀리지 않음 / 🔵 실사용에서 거의 안 걸림

### 고를 수 있는 것 — **0건**

**남은 11건은 전부 무언가를 기다린다** — 기기 회신 3 · `B-258` 잔여 1 · 자료 대기 1 · 보류 6.
**판정 대기는 0이다**(2026.08.19 질문 판이 넷을 다 받아 넷을 다 닫았다 — 확정 20장).
그래서 세션이 혼자 열 수 있는 기능 판이 없다. **다음에 움직이는 것은 실기기 회신이다**(0장).

### 기기 회신을 기다리는 것 — 3건

| 행 | | 무엇 |
|---|---|---|
| `B-18` | 🟡 | `restoreOperation`이 항목마다 트랜잭션을 연다 — 한 덩어리로 묶을 상한을 **실기기 측정 후** 정한다(확정됨) |
| `B-72 ⓑ` | 🟡 | 내보내기·자동 백업의 DOM 전량 적재 — 규모 신호가 실기기에서 나와야 한다 |
| `B-214` | 🔵 | 관계도 `onDraw`를 안 쟀다 — 그리기 축은 기기에서만 보인다 |

### 판정을 먼저 받아야 하는 것 — **0건**

넷(`B-258`·`B-259`·`B-262`·`B-263 ⓑ`)을 2026.08.19에 한 통으로 물어 **넷 다 받았고 넷 다
그 자리에서 처리했다.** 원문은 `docs/judgment_confirmations_2026-08.md` **20장**.

### 사용자가 열어야 하는 것 — 1건

| 행 | | 무엇 | 여는 조건 |
|---|---|---|---|
| `B-258` ⓑⓒⓓ | 🟡 | **반만 닫힌 행.** ⓐ(무소속 사건에 전역 구역)만 처리했다. 남은 것은 **임자와 구역이 어긋난 값** — 창이 추가 구역으로 그릴 것인가(ⓑ) · 가져오기가 교정할 것인가(ⓒ) · 이미 들어온 것을 소급할 것인가(ⓓ) | 사용자가 고른다. **ⓓ는 되돌리기 축**(저장된 값이 바뀐다)이라 다시 물어야 한다 |

### 자료를 기다리는 것 — 1건

| 행 | | 무엇 | 재개 조건 |
|---|---|---|---|
| `B-27 ①` | 🟡 | 임포트 원문 보존 잔여 — 날짜로 자동 변환된 셀의 재포맷 | 사용자 자료(실제로 깨진 파일) 필요 |

### 보류 — 6건 (재개 조건이 아직 안 섰다)

| 행 | | 무엇 | 재개 조건 |
|---|---|---|---|
| `B-49` | 🟢 | 세계관·작품·세력 목록의 검색 부재 | 규모 신호(항목이 수십 개) |
| `B-53` | 🔵 | 폴더 왕복 2차 후보 셋 | 사용자가 우선순위 낮음으로 판정 |
| `B-57` | 🔵 | 의존 주입이 수동이다(`by lazy`) | 규모 신호 |
| `B-58` | 🔵 | 이미지 로딩이 수동 관리다 | 규모 신호 |
| `B-125` | 🟡 | 릴리스 빌드를 CI가 한 번도 밟지 않는다 | 방아쇠(릴리스 준비) |
| `B-209` | 🟡 | 통계 드릴다운이 스냅샷 필드값을 매번 훑는다 | 실측 0.4ms — 규모가 커지면 재개 |

---

## 4. 재현 기준선 — 손대기 전에 이 숫자부터 맞춘다

```bash
JARS_DIR=<스크래치패드> tools/setup_jvm_env.sh      # 세션마다 1회
JARS_DIR=<스크래치패드> tools/run_jvm_tests.sh      # 순수 JVM 시험
JARS_DIR=<스크래치패드> tools/probe_compile.sh /tmp/probe.txt
JARS_DIR=<스크래치패드> tools/probe_view_compile.sh /tmp/view.txt
for f in tools/check_*.sh; do "$f" || echo "FAIL $f"; done
for p in tools/verify_room_migration*.py tools/verify_reset_coverage.py; do python3 "$p"; done
```

**현행 값은 여기 적지 않는다 — 0장의 「기준선(판 끝 실측)」 한 줄이 정본이다.**
같은 수가 두 자리에 살면 한쪽이 반드시 낡는다(콜드 검토 2026.08.21: 이 자리가 실제로
`3533`·`541`을 든 채 0장과 갈려 있었고, 5장 착수 절차가 *"기준선을 재현한다(4장)"*이라
지시하므로 다음 세션이 자기 변경이 수십 건을 만들었다고 오독할 자리였다).
**손대기 전에 위 명령을 한 번 돌려 그 출력을 기준선으로 삼는다.**

- ℹ️ **프로브 541은 줄었다가 하나 는 값이고, 둘 다 스텁의 그림자다.** 검토 판이 프로브 스텁
  셋을 좁게 실타입화해(`Activity`·`ContentResolver`·`DocumentsContract.deleteDocument` —
  LruCache·B-190 전례) 종전 `Any?` 수신이 숨기던 미해석 33건이 걷혔고, 콜드 검토 수리가
  런처 콜백(스텁에서 `Any`)을 실타입 인자로 쓰는 자리 하나를 더해 같은 부류의 그림자가
  한 줄 늘었다(`ExcelTransferController` — 같은 줄이 기준선에 이미 있다).
  **손대기 전에 master에서 한 번 떠 보고 견줄 것**은 그대로다.

- ℹ️ `triage_unresolved.sh`는 master에서 종료코드 1이다 — 프로브가 뺀 타입을 그대로 이름으로
  보고하기 때문이고(`ExcelImporter`·`AppSettingsBindings`·`CappedScrollView`·`TaskProgressDialog`,
  2026.08.20부터 `AiKeyStore`), **결함이 아니라 범위의 그림자다.**
  **손대기 전에 master에서 한 번 떠 보고 견줄 것** — 안 그러면 남의 빨간불을 자기 것으로 읽는다.

- ⚠️ **`run_jvm_tests.sh`의 종료 코드는 시험 실패를 말하지 않는다** — 언제나 0이다.
  산출에서 `OK (` / `FAILURES!!!`를 볼 것.
- ⚠️ **프로브 기준선을 뜨기 전에 `git fetch origin master`를 먼저 한다.**
  낡은 `origin/master`로 뜨면 프로브가 거짓말을 한다(양방향으로).
- ⚠️ **문서의 숫자보다 실제 실행 결과를 믿을 것.**

---

## 5. 착수 절차

1. **이 문서를 읽는다.** 그리고 `docs/conventions.md`에서 **손댈 영역의 규약만** 읽는다.
2. **기준선을 재현한다**(4장).
3. **손댈 자리의 설계 문서가 코드와 맞는지 본다.** 어긋나면 고치고 착수한다.
   범위 밖이면 — **한 줄로 끝나면 그 자리에서 고치고, 크면 백로그에 올리고 진행한다.**
4. **묻는 것은 셋뿐이다:** 어느 쪽을 골라도 말이 되는데 결과물이 갈릴 때 ·
   사용자의 앞선 확정을 뒤집게 될 때 · 되돌리기 어려울 때(데이터 유실·마이그레이션·저장 형식).
   **그 밖은 묻지 않는다.**

### 지도 — 어디에 무엇이 있나

| 목적 | 문서 |
|---|---|
| **판단 기준(최상위)** | `CLAUDE.md` |
| **지금 할 일** | **이 문서** |
| **실기기 확인** | `docs/device_walkthrough.md` |
| 규약 원문 | `docs/conventions.md` |
| 계층·단일 소스·확장점 | `docs/architecture_2026-07.md` |
| 확장 한계·성능 | `docs/scalability_performance_2026-07.md` |
| 화면 문구 | `docs/text_style_guide_2026-07.md` |
| 사용자가 확정한 것 | `docs/judgment_confirmations_2026-08.md` |
| 백로그 본문(닫힌 문서의 4장) | `docs/remaining_work_2026-07.md` |
| 그 밖 설계 정본 | `docs/*.md` — 손댈 영역의 것만 |

---

## 왜 이 문서가 생겼나 — 실측

사용자 질문(2026.08.19): *"개발이 끝이 안 나는데 원인이 뭐고 어떻게 해결할 수 있을까?"*

| 재는 것 | 실측 |
|---|---|
| 백로그 **250건** 중 사용자가 연 것 | **1건** (나머지는 콜드 검토 20 · 점검 18 · 감사 12 · 앞 판/착수 대조…) |
| 최근 20판의 산출 | 앱 코드 **4,220줄** vs 문서 4,235 + 도구 2,448 = **6,683줄** |
| 앱 코드를 한 줄도 안 바꾼 판 | **20판 중 6판** |
| `remaining_work` 크기 | 5일 만에 16,342 → **21,691줄** |
| 앱에서 화면(`ui/`) 비중 vs 그걸 보는 시험 | 코드의 **48%** / 시험의 **8%** |
| **실기기 확인 통과** | **7건 · 마지막 2026-08-01 · 그 뒤 52판** |

**뿌리는 하나다.** 실기기 확인이 멈추자 화면 부채가 쌓였고, 세션은 그 부채를 갚을 수 없으니
**부채를 안 만드는 일**(도구·검사·문서)로 흘렀다. 화면을 자동으로 못 보니 검사를 만들었고,
검사는 자기가 볼 수 있는 것 — **자신과 다른 도구** — 을 검사하기 시작했다.
그 활동이 백로그를 자가발전시켰고(249/250), 문서를 키웠고, 커진 문서가
**부채를 은폐했다**(인수인계 판은 줄곧 "열다섯"이라 적었는데 실제로는 124건이었다).

**그래서 고친 것은 둘이다:** 판의 정의(1장)와 문서 크기(이 문서).
**고치지 않기로 한 것도 적어 둔다** — 콜드 검토·자기 등재·검사 신설은 사용자가 이번에
자르지 않았다. 다시 자가발전이 보이면 그때 다시 잰다.
