# 아키텍처 개관 — 이 저장소의 진입점 (2026-07)

> **이 문서의 자리:** 코드베이스가 수백 파일·수만 줄이 되도록 **진입점 문서가 없었다.**
> 새 작업자(사람이든 세션이든)가 처음 잡는 문서는 수천 줄짜리 인수인계 로그
> (`remaining_work_2026-07.md`)였고, 거기에는 "지금 무엇을 해야 하는가"는 있어도
> "이 앱이 어떻게 생겼는가"는 없다. 이 문서가 그 자리를 채운다.
>
> **범위:** 현행 구조의 지도와 확장점, 그리고 규약 색인. **성능·확장 한계는 다루지 않는다** —
> 그쪽은 `docs/scalability_performance_2026-07.md`가 단일 소스다.
>
> **읽는 순서:** ① 이 문서로 지형을 잡고 → ② 손댈 영역의 설계 문서를 읽고 →
> ③ `remaining_work_2026-07.md` 5장에서 지금 할 일을 고른다.

---

## 1. 현행 규모 — 세는 법

**값을 적지 않는다.** 이 표는 두 판 연속 낡았고(v1.2가 고친 수를 v1.4 이후 다시 벌어졌다),
그때마다 정정 문장이 하나씩 늘었다. 규모는 **근거로만 인용되는 수**이므로
값이 아니라 **세는 명령**을 싣는다(v9.19 규칙 — `remaining_work` 5-a).

| 항목 | 세는 법 |
|------|---------|
| 메인 소스 | `find app/src/main/java -name '*.kt' \| wc -l` · 줄은 `-exec cat {} + \| wc -l` |
| 테스트 소스 | 파일은 `find app/src/test -name '*.kt' \| wc -l` · 실행 건수는 `tools/run_jvm_tests.sh` 출력 |
| Room DB | `data/database/AppDatabase.kt` — `@Database(version=…)` · `@Entity` 개수 · `MIGRATION_*` 개수 |
| 사용자 문구 | `res/values/strings.xml` 단일 파일 · 검사는 `tools/check_text_style.sh` |

> **재현 기준선은 여기가 아니라 `remaining_work` 2장·5장이 든다.** 그쪽 수에는
> "손대기 전에 이 숫자부터 재현할 것"이라는 용도가 붙어 있어 숫자가 일을 한다.
> 같은 수를 두 문서가 들면 반드시 한쪽이 뒤처진다 — 이 문서에서 실제로 두 번 그랬다
> (1장과 4장이 갈렸고, 그 이력이 아래 문서 이력 v1.2에 남아 있다).

> **테스트 파일 수 ≠ 러너가 도는 파일 수 — 차이는 1파일·3건이다.**
> `AiPresetsConsistencyTest`(3건)는 `R`을 참조해 순수 하네스에서 돌 수 없어 목록에서
> 빠져 있다(CI 전용). 그래서 소스의 `@Test` 합과 실행 건수가 **3만큼** 다르다 —
> 이 차이를 결함으로 오인하지 말 것. 러너는 `$TESTS` 목록에 적힌 파일만 컴파일하므로
> **목록에 넣지 않은 새 테스트는 조용히 돌지 않는다**(5장 착수 시 확인 항목).

---

## 2. 계층 지도

```
app/src/main/java/com/novelcharacter/app/
├── data/            DB 계층 — 여기가 진실의 원본
│   ├── model/         엔티티 + 설정 값 객체(FieldStatsConfig·DisplayFormat·SemanticRole …)
│   ├── dao/           Room DAO
│   ├── repository/    트랜잭션 경계 · 휴지통 · 스냅샷 복원
│   └── database/      AppDatabase + 마이그레이션 (버전·개수는 1장 세는 법이 든다)
├── util/            ★ 순수 계층이 사는 곳 — 다만 전부가 순수하지는 않다(아래 ⚠️)
├── excel/           엑셀 왕복 (내보내기·가져오기·시트 규약)
├── share/           월드패키지(ZIP) 내보내기·들이기
├── ai/              프롬프트 조립 · 응답 해석 · 정책
├── backup/          자동 백업 워커
├── widget/          홈 위젯
└── ui/              화면 — character · stats · adapter · image …
```

> **파일 수를 적지 않는 것도 일부러다.** 이 지도의 값어치는 "무엇이 어디 사는가"이지
> "얼마나 큰가"가 아니고, 괄호 안 수는 한 세션 만에 넷이 어긋났다(util·ai·ui·image).
> 규모가 필요하면 세면 된다 — `find app/src/main/java/com/novelcharacter/app/<패키지> -name '*.kt' | wc -l`.
> (`ui/character`처럼 하위 패키지를 가진 곳은 **무엇을 세는지 기준부터 정할 것** —
> 종전 값 22는 `ui/character/batch/`를 뺀 최상위만 센 것이었는데 그 사실이 적혀 있지 않아
> 재현이 불가능했다.)

**의존 방향은 한 방향이다:** `ui → repository → dao → Room`, 그리고 모든 계층이 `util`을
바라본다. `util`은 아무도 바라보지 않는다 — 그래서 이 저장소에서 실행 검증이 가능한
거의 유일한 계층이다(4장).

> ⚠️ **`util/` = 순수 계층이 아니다. 디렉터리가 아니라 파일 단위로 갈린다.**
> 일부는 Android·Material에 의존한다(`AlertDialogExt`·`ProgressDialogs`·
> `CharacterImageLoader`·`*Prefs`·`ThemeHelper` …). 이들은 JVM 하네스가 컴파일하지 않으므로
> **`util/`에 넣었다는 사실만으로 "테스트된다"고 가정하지 말 것.**
> 세는 법: `grep -rl '^import android\.\|^import androidx\.' …/util | wc -l`
>
> **그런데 'Android 비의존'과 '테스트된다'도 같은 말이 아니다 — 종전 서술은 이 둘을 붙여
> 놓아 틀렸다.** 순수한 파일이라도 러너의 `$SOURCES` 목록에 없으면 **컴파일조차 되지 않는다.**
> 실제로 순수한데 목록에 없는 파일이 여럿 있고, 그중 `FieldRandomGenerator.kt`는
> **5-1장이 "새 필드 타입 착수 시 반드시 훑을 것"으로 지목한 확장점인데 테스트가 0건**이다.
> **판정법은 하나뿐이다 — `tools/run_jvm_tests.sh`의 `$SOURCES` 목록에 그 파일이 있는가**(4장).
> 순수함은 테스트의 필요조건이지 충분조건이 아니다.

### 데이터 모델 — 엔티티 (세는 법: `grep -c '@Entity' app/src/main/java/com/novelcharacter/app/data/model/*.kt`)

| 묶음 | 엔티티 |
|------|--------|
| 뼈대 | `Universe` → `Novel` → `Character` |
| 사용자 정의 필드 | `FieldDefinition` · `CharacterFieldValue` · `EventFieldValue` · `NovelFieldValue` · `FieldValueEntry`(값 라이브러리) · `GradeSystem`(세계관 등급 체계 — U-1) |
| 시간 | `TimelineEvent` · `TimelineCharacterCrossRef` · `TimelineEventNovelCrossRef` · `CharacterStateChange` |
| 관계 | `CharacterRelationship` · `CharacterRelationshipChange` · `Faction` · `FactionMembership` · `FactionRelationship` |
| 이미지 | `ImageMeta` · `ImageTag` |
| 부가 | `CharacterTag` · `NameBankEntry` · `SearchPreset` · `CharacterListPreset` · `UserPresetTemplate` · `RecentActivity` |
| 대결 (B-104) | `DuelAxis`(세계관 단위 축) · `DuelMatch`(한 판 — **참가자를 코드로 가리킨다**) · `DuelCounterVerdict`(층 B의 사용자 처분) |
| 안전망 | `TrashSnapshot` · `OperationLog` |

**`FieldDefinition`이 이 앱의 심장이다.** "도구를 만드는 도구"(CLAUDE.md 핵심 철학)라는 성격이
전부 여기서 나온다 — 필드의 타입·파싱 방식·통계 편입 여부·AI 정책·표시 형식이 모두
`FieldDefinition.config`(JSON)에 사용자 설정으로 들어간다. 그래서 **이 앱의 확장은 코드가
아니라 데이터로 일어난다.**

---

## 3. 단일 소스 지도 — 이 저장소의 핵심 자산

`util`의 순수 객체들은 "편의 함수 모음"이 아니라 **도메인 규칙의 유일한 정의 자리**다.
같은 규칙이 여러 화면에 복제되면 반드시 갈라진다는 것을 이 저장소는 여러 번 값비싸게 배웠다
(U-9: 수식 서식이 7곳에 복제돼 로케일과 오류 처리가 제각각이었다).

**규칙: 아래 표에 있는 판단을 호출부에서 다시 구현하지 않는다.**

| 단일 소스 | 무엇의 유일한 정의인가 | 어긋났을 때 생긴 일 |
|-----------|------------------------|---------------------|
| `FieldValueTokenizer` | 필드값 문자열 → 통계 토큰 분리 | 순위 탭이 자체 분리를 써서 인사이트와 수치가 달랐다(S-18) |
| `FieldValueResolver` | 별칭·표시 라벨 접기 | — (R-21로 스냅샷 파생으로 승격) |
| `FieldStatsConfig` / `StatsFieldPolicy` | '통계에 포함' 설정의 적용 규칙 | 패턴 감지가 설정을 무시했다(S-14) |
| `NumericBinning` + `FieldValueMatchSpec` | 수치 구간 + 드릴다운 매칭 | 라벨 문자열로 매칭해 항상 0명이었다(S-16 → R-20) |
| `ValueDistributions` | 분포 전량 + 잘라낸 개수 | 비율 분모가 상위 N 합이라 편향이 왜곡됐다(S-17 → R-19) |
| `FormulaLexer` | 수식 어휘 규칙 + **버려지는 구간** | 미지 함수가 조용히 버려져 그럴듯한 오답이 됐다(로드맵 5) |
| `FormulaEvaluator` / `FormulaValidator` / `FormulaDisplay` | 수식 평가 / 저장 시점 검증 / 표시 서식 | 서식·NaN 처리가 7곳에 복제(U-9) |
| `GradeValueResolver` | 등급 라벨 → 숫자 | — |
| `GradeSystemRef` | 등급 체계 참조·재정의·실효 표의 config 표현 + 강등·재배선 규칙 (원본을 고치는 쓰기·전파는 `GradeSystemRepository` 경유 — R-30. 전파가 공허한 신설 삽입·복원·프룬 경로는 예외이며, 직접 호출 자리는 R-30 위반 후보로 점검한다) | — |
| `SnapshotRefs` / `SnapshotRefResolver` | 휴지통 복원의 안정 식별자 (**`naturalKeyOf`가 "이 ref가 자연키로 성립하는가"의 단일 소스** — 해석과 색인 만들기가 같은 함수를 부른다. 전역 구역은 세계관 코드 자리가 `null`, 빈 문자열은 전역이 **아니다** — R-1) | 복원이 남의 엔티티에 붙었다(R-1) · 전역 자연키를 해석만 떨어뜨려 색인이 죽은 코드였다(B-133) |
| `FolderNameToken` / `FolderRoundtripPlanner` / `FolderExportPlanner` / `FolderRoundtripLedger` | 이미지 폴더 왕복의 이름·계획·장부 | 개명 경로가 토큰을 깨 중복 편입(C-1) |
| `CharacterRepresentativeImage` / `ImagePathMatch` | **어느 이미지가 이 캐릭터를 대표하는가**(사다리·시드 랜덤·쓰기 정합) / 경로 대조 + **앱 저장소 봉쇄 판정**(`isInside` — *이 경로를 기기 밖으로 내보내도 되는가*. R-39) | 같은 판정이 아홉 종류로 갈려 규칙이 넷이었다(B-103) · 저장 이미지를 읽는 일곱 자리가 전부 가드를 두는데 **AI 전송 준비기만 없어, 화면에는 플레이스홀더로 그려지는 앱 밖 파일이 제3자 서버로 나갔다**(B-141) |
| `DetachedImageRule` / `DetachedImageMarker` | **무엇이 "캐릭터에서 뗀 것"인가**(순수 판정) / 그 표식을 읽고 쓰는 손 | 붙는 자리 셋·푸는 자리 셋에 규칙을 나눠 적으면 "뗐다가 다시 붙였는데 서랍에 남아 있다"가 생기고 어느 쪽이 틀렸는지 알 길이 없다(B-107 D2). 불변식은 하나다 — **캐릭터가 쓰고 있는 이미지는 뗀 것이 아니다** |
| `ImageDeletionService` | **앱에서 이미지를 지우는 처분**(참조 제거 + 라이브러리 행 제거 + 파일 삭제, 한 트랜잭션) | `ImageManagerViewModel.deleteInternal`에 private으로 갇혀 있어 `_삭제승인`이 같은 일을 하려면 규칙을 한 벌 더 적어야 했다 — 링크 정리·대표 포인터·롤백 조건이 갈라질 자리다(B-107 D6) |
| `RepresentativeImageCell` | 엑셀 `대표이미지` 열의 쓰기·읽기 규약(해석 사다리 5단) | — |
| `AiImageAttach` / `AiImagePreparer` | **AI 요청에 어느 그림을 몇 장 붙이는가**(순수 — 대표 우선·시드 랜덤·직접 고르기) / 전송용 축소·인코딩(Android) | 고르기 규칙을 화면마다 적으면 짧은 값·서술형·보완 재요청의 근거가 갈린다. 비트맵과 한 파일에 두면 **그 규칙을 실행으로 검증할 길이 없다**(B-120 A-7) |
| `CharacterFieldValueOverflow` | 캐릭터 시트가 담지 못한 값의 판정 | — |
| `DefaultFieldPlan` / `FieldTypeCompatibility` | 전역 기본 필드(B-119)의 **심기·전파·강등 판정** — 자리 판정 `(entityType, key)` · 벌어짐(`SAME`/`OUTDATED`/`DIVERGED`)과 기본 선택 · **`typeChanges`가 "이 세계관에서 타입이 바뀌는가"의 단일 소스**(저장소의 *값 채우기*와 미리보기의 *세기*가 같은 함수를 부른다) / **타입이 바뀌어도 값이 살아남는가**(세는 규칙 — **판정은 한 벌이되 처분은 소비처가 정한다**: 단일 필드 편집은 초기화, 전파는 고지만. B-137) | 채우는 조건이 세는 조건보다 좁아 *"값 N개가 초기화됩니다"* 경고가 **정식 경로에서 통째로 죽어 있었다**(B-135). 고치자 그 경고가 처음으로 떴고, **그 문구가 거짓이었다**(B-137 — 전파는 값을 지우지 않는다) |
| `FieldScopeCell` | **시트의 세계관 칸이 빈 것이 무슨 뜻인가** — 빈 칸 = 전역(무소속) 구역 | 판정이 다섯 경로에 흩어져 **둘만 알고 있었다**: '필드 정의'는 갈래가 있고 오버플로 두 시트·값 라이브러리 미리보기는 없어서, 내보내기가 빈 칸으로 낸 전역 값을 가져오기가 `skippedRows`로 버렸다(B-130) |
| `FieldDefinitionPrune` | 덮어쓰기에서 **지울 정의를 고르는 판정**(구역마다 매칭 근거를 따로 본다) | *'백업에 없다'*와 *'백업이 그 구역을 말하지 않았다'*를 가르지 않아, 시트가 다루지도 않은 전역 구역의 정의가 **값과 함께 CASCADE로** 사라졌다. 파일 단위 안전장치 둘(시트 부재·매칭 0건)은 세계관 필드가 매칭돼 그대로 통과했다(B-130) |
| `CharacterFieldValueMerge` / `EventFieldValueMerge` / `NovelFieldValueMerge` | 부분 저장의 커버 집합 — 종류마다 하나씩, **규칙은 하나** | 사건 필드값이 무통보 폐기됐다(S-6) |
| `EntityFieldHeaders` | 한 시트가 전 세계관의 커스텀 필드를 동적 열로 실을 때의 헤더 규칙(연표·작품 시트) | — (확-3에서 사건 전용 이름을 승격) |
| `SortComparators` / `FieldFilterHelper` / `UnassignedFilter` | 목록 정렬·필터 | — |
| `ImportSource`(`ImportWorkbook`/`Sheet`/`Row`/`Cell`) | 가져오기가 워크북을 읽는 유일한 접근면 | DOM·스트리밍 두 경로가 갈리면 같은 파일이 다른 데이터가 된다(B-8) |
| `ResetPlan` | 앱 초기화가 비우는 테이블의 범위 | 26개 중 5개가 '모든 데이터 삭제' 뒤에도 살아남았다(S-13) |
| `DuelRecords` | **저장된 대결 기록(코드) ↔ 순수 계층(id)의 해석** — 참가자 코드 배정 · 처분의 저장 형식과 정규 키 · 사라진 참가자 세기 | 저장을 id로 하면 휴지통에서 되살아난 캐릭터의 판이 **남에게 붙는다**(R-1의 오배정). 해석을 저장소에 두면 이 앱의 어떤 로컬 검증도 그 규칙을 못 본다 — 그래서 pure다 |
| `DuelRating` / `DuelPairing` / `DuelCounterRelations` | 대결(B-104)의 세 계층 — **이 축에서 누가 얼마나 센가**(Bradley–Terry MAP · 확신 · 짝 집계) / **다음에 무엇을 붙일까**(정보량 순서 · 확신 기준 접기 · 표적 재확인 · 진행률) / **무엇이 상성인가**(잔차 천적 · 순환 · 흔들림 · 2단 적합). 설계 정본은 `docs/duel_system_design_2026-08.md` | 짝 집계를 세 곳이 따로 세면 갈라진다 — 그래서 `Fit.tallies` 하나가 낸다. 상성을 3-순환으로만 정의하면 **천적을 원리적으로 못 본다**(설계 3장) |
| `DuelSession` / `DuelRound` / `DuelCardGrid` / `DuelStandings` | 대결 **화면**의 판단 — *"지금 무엇을 물을 것인가"*(대기열 소비 · 떠 있는 짝 고정 · 되돌리면 다시 묻기) / ***"한 화면에 누구를 올리고 그 누름이 어떤 판이 되는가"*(k지선다 — B-115)** / ***"그 카드를 어느 자리에 놓는가"*(슬롯 넷의 2×2)** / *"순위표와 상성 상세가 무엇을 보이는가"*(동점 등수 · 근거 · 고지 · 후보와 처분의 대조) | **화면 안에 두면 어떤 로컬 검증도 못 본다**(「세션 착수 규칙」 4번). 대기열이 도는 결함은 *끝나지 않는 대결*로, 처분 대조가 어긋나면 *한 관계가 두 줄*로 나타나는데 둘 다 컴파일은 통과한다. **`DuelRound`가 여기 있는 까닭이 특히 분명하다** — 그것이 **1:1을 `k=2`로 흡수**해 갈래를 없앴으므로(설계 14-2), 그 동치가 깨지면 **k지선다를 켠 적 없는 사용자의 기록까지 조용히 달라진다.** `canReuse`도 같은 부류다: 없으면 배경 재계산마다 셋째·넷째 카드가 갈려 **고르려던 얼굴이 손가락 아래에서 바뀌는데**, 눈으로는 *"가끔 카드가 바뀌네"* 정도로만 보인다 |
| `DuelFieldLinks` | **축과 필드를 잇는 규칙** — 저장 형식(JSON·엑셀 글) · 값에서 수 읽기 · **사전식 예측**(1순위가 갈리면 거기서 끝) · 예측과 실제의 대조 · **프로필로 걸린 산출 필드 세기**(B-122) · **걸려 있는 산출과 *일하는* 산출을 가르는 자리**(`effectiveOutcomes` — B-167. 충돌 경고·프로필 막기·카드 감추기·순위표 대조 **넷이 이것 하나를 본다**) | 연결이 셋(영향·산출·프로필)인 것은 **방향과 쓰임이 전부 다르기** 때문이다 — 영향은 *필드 → 대결*(보이고 **예측에 쓴다**), 산출은 *대결 → 필드*(보이면 답을 미리 주는 셈이다), 프로필은 *표시 전용*(보이지만 **예측에 안 쓴다**). 해석을 화면·엑셀·DB 세 곳에 두면 *"S-1"*을 −1로 읽는 부류의 어긋남이 자리마다 다르게 생긴다 |
| `DuelSystemFields` | **캐릭터 표의 열을 축에 거는 규칙**(B-167) — 의사키 어휘(`sys:`) · 열마다 다른 **토큰 나누기**(이명·태그는 쉼표, 메모는 통째로) · **진짜 필드와 부딪칠 때의 승자**(`shadowed` — 사용자가 만든 것이 이긴다. R-40) | 막혀 있던 것이 *타입*이 아니라 **소속**이었다 — 링크 창이 `FieldDefinition` 행만 내는데 이명은 `Character.anotherName`이라 **목록에 낼 행 자체가 없었다.** 어휘를 `SemanticRole.linkedKey`의 `__`와 **갈라 연** 것이 요점이다: 그쪽은 *어느 커스텀 필드가 나이인가*이고 이쪽은 *필드가 아예 없다*라 **뜻이 정반대**이며, 접두를 나눠 쓰면 값을 `character_field_values`에서 찾을지 `characters`에서 찾을지가 갈려 조용히 빈 값이 된다 |
| `DuelCandidateFilter` | **대결 후보 필터의 해석·대조·참가자 조립**(B-168) — 필터를 **키로** 해석하는 사다리(키 → 시스템 열 → 구식 id · **진짜 필드가 이긴다**) · **해석 실패 = 후보 0**(닫히는 실패) · **참가자 = 후보 ∪ 전적 보유** · 엑셀 칸의 사실 셋(없는 열/빈 칸/읽을 수 없는 칸 — `fromSheetCell`) | 조건 자체는 `FieldFilter` 그대로이고 커스텀 대조도 `FieldFilterHelper`가 한다(언어 두 벌 금지 — 원칙 05). 이 파일이 더한 것은 **id가 못 미더운 자리의 해석**(축은 복원·엑셀을 탄다)과 **시스템 열의 대조**뿐이다. *전적 있는 비후보를 남기는* 조립을 화면마다 적으면 한 화면이 빼는 순간 그 판이 고아가 되어 **남의 점수까지 움직인다**(설계 9-3) |
| `RepresentativeWeighting` | **대표 이미지 추첨의 가중치**(B-104 소비처 ⓑ) — 점수를 되돌린 γ가 그대로 무게이고 세기는 지수 α(꺼둠/약하게/세게) · **한 눈금(400점)에서 끊어** 최악 비율을 `10^α`로 묶는다 · **기울일 것이 없으면 `null`** | 무게를 지어내지 않는 것이 요점이다 — γ는 이미 *"i가 j를 이길 확률"*로 정의된 수라 *"확률이 두 배면 두 배 자주"*가 되어 뜻을 따로 설명할 필요가 없다. **끊는 빗장이 이 기능의 존재 이유**다: 안 끊으면 1위의 무게가 수백 배가 되어 *확률*이 **고정**으로 변질되고, 그것이 사용자가 물리친 바로 그 안이다(`representative_image_design` D9의 갈음). **`null`을 내는 것도 계약이다** — 그래야 호출부가 종전 `randomIndex`를 그대로 타고 *"대결을 안 한 캐릭터는 지금과 완전히 같다"*가 약속이 아니라 코드의 성질이 된다 |
| `DuelImagePrune` | **걸러낼 이미지 후보의 판정**(B-104 소비처 ⓒ) — 하위권 %(비율 그대로) + 최소 판 수 · 동점은 함께 들고 함께 빠진다 · **제안 목록을 낼 뿐 아무것도 지우지 않는다** | 특례를 두지 않는 것이 판정이다 — *"하위 20%"*는 다섯 장이 있어야 한 장이 거기 든다는 뜻이고, 작은 집합에 억지로 한 장을 세우면 그림 둘뿐인 캐릭터에서 **절반이 후보**가 된다. 판 수 하한이 없으면 흔들리는 점수로 *"지우시죠"*라고 말하게 되는데 그것은 앱이 지어낸 근거다(원칙 02) |
| `DuelCardInfo` | **대결 카드가 무엇을 띄우는가** (B-122) — 구역 셋의 배분(트리오 · 프로필 · 영향) · **빈 값의 처분이 두 구역에서 반대인 것** · 상한 넷과 접힌 수 · 겹치는 필드를 한 구역에만 두기 · **읽을 키 세기** | 카드가 *'그림 위 스크림'*에서 *'그림 + 정보 패널'*로 갈리며 정할 것이 늘었는데, 화면에 두면 어떤 로컬 검증도 못 본다(「세션 착수 규칙」 4번). **가장 값어치 있는 판정은 "산출 필드는 프로필 경로로도 못 올라온다"**이다 — 고르는 창이 막지만 엑셀은 그 창을 지나지 않고, 그 우회가 열리면 물음과 답이 한 화면에 놓인다. 빈 값 규칙이 반대인 것도 일부러다: 영향은 줄이 남아야 **두 카드의 자리가 맞고**, 프로필은 빠져야 좁은 카드의 상한 넷이 헛돌지 않는다. **읽을 키를 화면이 따로 세면** 값이 있는데도 빈 줄이 뜨고, 그것은 *"값을 안 적었다"*와 구별되지 않는다 |
| `DuelMatchLog` | **기록 화면이 무엇을 짚어 주는가** — 승자 읽기(깨진 판을 무승부로 접지 않는다) · 사라진 참가자 보존 · 필드와 어긋난 판 표시 · 상한 고지 | 목록이 *고칠 자리*를 짚지 못하면 수만 줄 가운데 무엇을 볼지 알 수 없다(원칙 02). 그 판단은 화면에 두면 어떤 로컬 검증도 못 본다 |
| `ImageTagVocabulary` | **이미지 태그 AI가 쓰는 어휘**(빈도 상위 태그 + '어휘에 포함' 필드 값 + 상한·절단 집계) — 폴더 이름 제안기(A-1 계열)와 내용 일괄 태깅(A-9)이 **같은 함수**를 쓴다 | 두 벌이면 **같은 앱의 두 AI가 다른 어휘로 말한다** — 한쪽만 `흑발`을 알면 같은 이미지가 어느 경로로 태깅됐는지에 따라 표기가 갈린다. 2026.08.07(B-121)에 `ImageFolderTagSuggester`의 companion에서 옮겨 왔다 |
| `ImageBatchTagSuggester` | **이미지 내용을 보고 다는 태그**(A-9) — 배치 나누기 · 번호 봉인 프롬프트 · **응답 index 검증** · 어휘 접기 · 드롭·실패 집계 · **되받기를 앞 실행 위에 얹는 규칙**(`mergeRetry`)과 **다시 물을 값어치의 판정**(`retryablePaths`) | 한 요청에 여러 장을 실으므로 **응답의 태그가 남의 이미지에 붙을 수 있다.** 번호가 어긋나면 **배치를 통째로 접는 판정**이 이 파일에 있고, 흩어지면 *절반만 믿고 붙이는* 경로가 생긴다(R-1 — 오배정은 생략보다 나쁘다). **`AiService`가 이미지 거부 시 돌려주는 '성공'을 실패로 뒤집는 자리**이기도 하다 — 그림이 근거의 전부인 기능이라 글만 보고 나온 태그는 지어낸 것이다. **합치는 규칙이 여기 있는 이유**(B-140): 화면이 되받기를 갈아치우기로 처리해 *"끝난 배치의 제안은 살린다"*가 **화면 계층에서만 뒤집혀 있었다** — 규칙을 순수 계층에 두어야 시험이 닿는다(R-38) |
| `ImageFilterHelper` | 이미지 탭의 필터 매칭 — 기본(소유·상태) × 링크 × **태그 유무**(`TagFilter`) × 태그 OR × 검색어 | 축을 화면에 흩으면 *"왜 좁아졌는지"*를 목록이 말하지 못한다. **태그 유무를 `tags`와 갈라 둔 것은 없는 것에는 이름이 없기 때문**이다(칩으로 담을 수 없다. 2026.08.07 B-121 신설 — 일괄 태깅의 진입 동선이 *"태그 없는 것들을 한꺼번에"*다) |
| `DuelScoreIndex` | **대결 점수가 대결 밖에서 무엇으로 보이는가** (B-117) — 순위표의 줄을 밖이 쓰는 표로 옮기는 것 · **무기록 참가자를 값 없음으로 두는 판정** · 값 없음의 정렬 자리 · 분포 구간 · **점수 보유자끼리의 등수 재매김**(`rankWithinScored` — 2026.08.07에 `DuelAiContext`의 private 사본을 여기로 올렸다. 소비처가 둘이 되는 순간 각자 매기면 같은 캐릭터가 화면마다 다른 등수를 갖는다) | 설계 4장은 *"파생값을 저장하지 않는다 — 저장하면 두 자리가 갈라진다"*고 못 박았는데, **소비처가 셋이 되니 저장하지 않아도 갈라질 수 있었다**(각자 적합하면 옵션 하나에 점수가 달라진다). 저장 금지의 나머지 절반이 **출구를 하나로 묶는 것**이고 이 파일이 그 출구다. 무기록 판정을 여기 둔 것도 같은 이유 — 앵커 1500을 값으로 섞으면 **아무것도 안 한 캐릭터가 목록 한가운데에 박힌다**(코드가 하지 *않는* 일이라 시험으로만 지켜진다) |
| `DuelAiContext` | **대결이 AI 프롬프트에서 무엇으로 보이는가** (소비처 ⓔ) — 실을 축 고르기 · **등수 재매김을 부르는 것**(계산 자체는 `DuelScoreIndex.rankWithinScored`로 올랐다 — B-113이 같은 재매김을 필요로 했다) · 줄의 모양(`강함: 12명 중 3위 · 1523 ±37 (12판)`) · 상한과 뺀 수 고지 | 조립기가 둘이라(짧은 값 추천 · 서술형 작성) **각자 자르면 같은 캐릭터가 경로에 따라 다른 축을 받는다.** 등수를 다시 매기는 것이 이 파일의 핵심 판정이다 — 순위표 줄의 등수는 **한 판도 안 친 참가자까지 세는데** 그들의 앵커 1500이 표 한가운데 앉아, 그대로 옮기면 분모를 넘는 *"3명 중 4위"*가 나온다(설계 10-3. 자기 재검토가 잡았다). 프롬프트 문자열은 **어떤 로컬 검증도 읽지 않으므로** 시험이 유일한 방어선이다 |
| `DuelGradeAssign` + `DuelGradeRef` | **대결 순위가 어떻게 등급이 되는가** (B-113) — 컷 검증(단조 **비**감소 · 0% 구간 허용) · 배정 · **자연 단절 경계 제안**(±오차 겹침 조건) · 미리보기 3분류 / 그 약속의 config 표현(축 code · **누적** 컷 · 반영 흔적). 설계 정본은 `docs/feature_roadmap_2026-08.md` 4장 | 백분위의 **분자와 분모가 둘 다 점수 보유자**인 것이 이 계산의 전부다 — 순위표 줄의 등수를 그대로 나누면 100%를 넘고, 100%를 넘는 값은 **어떤 컷에도 걸리지 않아 말없이 최하 등급이 된다**(그래서 하네스가 `Entry.rank`에 일부러 쓰레기를 넣는다). 컷을 **누적**으로 저장하는 것도 판단이다 — 화면은 폭으로 보이지만 폭으로 저장하면 라벨 하나가 사라졌을 때 남은 폭을 누구에게 줄지 다시 계산해야 하고, 그 계산이 곧 합이 100이 아니게 되는 자리다. 경계 표식이 **자기 자신을 세지 않는** 것도 여기 — 세면 오차가 1이어도 전원이 '경계'가 되어 표식이 아무 말도 안 한다 |
| `AiProviderCodec` | **프로바이더 설정이 저장될 때 무엇이 실리는가** (B-132) — 필드별 JSON 키 · null과 "설정됨"의 구별 · 손상 항목만 건너뛰기 · 등록순 정렬 | `imagesUnsupported`가 모델·`hasLearnedFacts()`·R-23 초기화까지 배선되고 **직렬화에만 빠져 있었는데**, 저장소가 Context 의존이라 **어떤 로컬 검증도 그 누락을 못 봤다**(컴파일도 정적 검사도 통과한다). 증상은 조용했다 — 비전 미지원 모델에서 청크마다 400→재시도가 되풀이되고 사전 고지 코드 세 자리가 죽은 길이 됐다. 순수로 갈라 둔 값어치는 개별 필드가 아니라 **부류**다: 시험이 필드 목록을 손으로 적지 않고 `AiProviderConfig` 선언에서 읽으므로, **새 학습값을 더하고 코덱을 잊으면 그 자리에서 실패한다** |
| `AiProviderFallback` | **한도에 걸리면 어디로 이어가는가** (B-108) — 두드릴 **순서**(활성이 맨 앞 · 쿨다운은 맨 뒤 · 키 없으면 제외 · 최대 3곳) · **방아쇠**(429는 재시도 먼저, 크레딧 소진은 곧바로 전환, 나머지는 그대로) · **쿨다운 판정**(10분, 벽시계 어긋남 포함) · **전환 고지 문구** | 전환을 관문(`AiService`) 안에 두지 않으면 인앱 기능 여섯이 각자 폴백을 짜고 **한 벌만 빠뜨려도 그 기능에서만 조용히 종전처럼 죽는다**. 그 판정을 다시 관문 **안**에 인라인으로 두면 저장소·네트워크가 Context를 쥐고 있어 **어떤 로컬 검증도 못 본다**(B-132가 난 그 자리). 갈라 두면 순서·방아쇠·쿨다운 전부가 시험 대상이 된다. **가장 값진 잠금은 새 기능이 아니라 옛 기능에 있다** — *"아무것도 실패하지 않으면 종전과 글자 그대로 같은 곳이 답한다"*가 깨지면 한도에 걸린 적 없는 사용자의 요청이 다른 모델로 나가고 **화면에는 아무 표시도 안 난다**. 고지 문구가 여기 있는 이유는 R-44다 — 호출부가 각자 지으면 같은 사실을 여섯 가지로 말하고, 하나가 빠져도 아무도 모른다(`tools/check_ai_switch_notice.sh`가 그 등재를 본다) |
| `AppSettingsKeys` + `AppSettingsBindings` | **엑셀 '앱 설정' 시트가 무엇을 싣는가** (B-105) — 키·형식·**처분**(실음/비밀)의 순수 선언과, 그 키를 실제 저장소에 잇는 읽기·쓰기 한 벌. 싣지 **않는** 저장소도 사유와 함께 여기 등재된다 | 종전에는 이 사실이 내보내기의 나열과 가져오기의 `when` **두 곳**에 갈려 있었고, 그래서 설정이 늘지 않았다. 한 벌로 모으되 **선언만 순수하게 갈라 둔 것**이 요점이다 — 합치면 *어떤 키가 있고 무엇이 비밀인가*라는 왕복 계약까지 `Context`에 딸려 가 순수 JVM 시험이 원리적으로 못 본다(B-132가 난 자리). 갈린 두 벌이 어긋나는 것은 `tools/check_app_settings_catalog.sh`가 본다 — **그 검사가 없으면 이 분리가 곧 종전의 '두 곳'이다**. 제외 목록에 **사유**를 요구하는 것도 이 표의 몫이다: 이름만 나열하면 다음 사람이 왜 빠졌는지 몰라 다시 조사한다 |
| `AiProviderActivation` | **저장이 활성 프로바이더를 옮기는가** (B-150) — 새 항목인가 · 키가 있는가 · 쓸 수 있는 활성이 있는가의 세 축 판정과 **고지 대상 여부** | 종전 규칙 *"첫 프로바이더일 때만 활성"* 한 줄 때문에 **한도에 걸린 프로바이더를 갈아 끼울 수 없었다**(사용자 보고). 판정을 저장소 안에 두면 Context 의존이라 시험이 못 본다. **키 조건이 '옮길 때'에만 걸리는 것이 핵심**이다 — 활성이 아예 없을 때까지 키를 요구하면 첫 등록의 종전 동작이 조용히 사라지고, `NO_KEY`(*"키를 등록해 주세요"*)보다 나쁜 `NO_PROVIDER`(*"프로바이더가 없습니다"*)가 뜬다. **근거가 시험에 없으면 다음 사람이 두 갈래를 통일한다** |
| `joinCsv` + `splitCsv` (`excel/SheetSpec.kt`) | **목록 셀이 여러 값을 어떻게 담는가** (B-27 ② · R-47) — **감싸기 판정**(값이 쉼표나 따옴표를 품으면 `"…"`, 안의 따옴표는 겹쳐 쓴다) · **되쪼개기**(칸의 첫 글자일 때만 따옴표가 감싼 것이다) · **옛 파일 경로**(따옴표가 없으면 종전 구현 그대로, 어긋난 따옴표도 옛 규칙으로 되돌린다) · 전각 쉼표 수용(F4) | 붙이는 쪽과 쪼개는 쪽이 갈리면 **내보낸 파일이 스스로를 다시 읽지 못한다** — 그리고 그 파손은 이름에 쉼표가 든 사용자에게만, 왕복 한 번 뒤에 나타나 **되돌릴 값이 이미 없다.** 한자리에 두면 *감싸는 조건*과 *푸는 조건*이 문법적으로 한 벌이고, 옛 파일 처분도 한 번만 정하면 된다. 새 목록 열이 쌍을 빠뜨리는 것은 `tools/check_list_cell_csv.sh`가 본다(시험은 *규칙이 옳은가*를 재지만 이 결함의 모양은 **아무도 부르지 않는 것**이다) |
| `AiErrorText` | **AI 오류 문구의 조립 차례** (B-150) — 프로바이더 줄 → 분류 문구(+HTTP 코드) → 제공사 원문 | 프로바이더 줄이 **맨 앞**인 것이 판정의 내용이다 — 뒤로 밀면 길이가 들쭉날쭉한 제공사 원문에 가려 사실상 안 보이는데, **코드는 멀쩡해 보이고 문구도 다 들어 있어 눈으로는 통과한다.** 종전에는 조립이 `Context.getString`과 한 함수에 묶여 있어 어떤 로컬 검증도 읽지 못했다 |
| `FieldConfigTransfer` | **필드 config가 세계관 경계를 넘을 때 무엇을 잃는가** (B-113 · R-35) — 등급 체계 참조와 대결 축 참조를 **함께** 걷어내는 한 자리 | 자리마다 두 줄씩 쓰면 **다음 자리에서 한 줄만 쓰는 것이 자연스러워지고**, 그때 빠지는 쪽은 늘 새로 들어온 키다(R-29가 겪은 그 형태). 축 code는 전역 유니크라 남으면 **남의 세계관 축을 정확히 찾는다** — 못 찾는 것보다 나쁘다 |
| `RankingSource` | **통계 순위 화면이 무엇으로 줄 세울 수 있는가** (B-117) — 필드와 대결 축이 한 목록에 서는 차례 · 고른 것을 저장·복원하는 열쇠 | 스피너 하나에 두 부류가 담기므로 **차례가 곧 계약**이고, 화면이 위치로 되짚는다. 화면 안에서 인덱스를 셈하면 「세션 착수 규칙」 4번이 말한 *보이지 않는 자리*가 된다. 열쇠에 접두사를 붙이는 것도 여기 — 필드 key와 축 코드가 우연히 같으면 다음에 열 때 엉뚱한 것이 골라지고, **접두사 없는 옛 값도 받아야** 업데이트 한 번에 모든 사용자의 선택이 풀리지 않는다 |
| `DuelCategoryStats` | **차례가 없는 값이 무슨 말을 하는가** (B-112) — 범주 필드 판정 · 값 짝의 전적과 **예상 승수** · 기운 자리 판정 · 못 센 판 세기 | 승패를 그냥 세면 *"불이 물에 약하다"*와 *"물을 든 캐릭터가 세다"*가 **섞인다** — 모델의 예상과 견주는 뺄셈이 그 둘을 가르고, 그것이 이 계산의 존재 이유다. 눈금(`minSamples` 3 · `zThreshold` 2.0)을 `DuelCounterRelations`에서 그대로 가져오는 것도 같은 자리 — **두 화면이 같은 말을 다른 기준으로 하면 사용자가 어느 쪽을 믿을지 알 수 없다** |
| `DuelImageFit` | **참가자 그림을 어떻게 놓는가** — 위쪽을 살리는 크롭 · 카드 배치와 맞춤의 선택 | 값이 틀려도 앱이 죽지 않고 **그림만 조용히 어긋나게 놓인다** — 컴파일도 정적 검사도 통과하므로 실기기 회신 말고는 잡을 길이 없는 부류다(사용자 지적으로 실제로 발견됐다: *"얼굴이 잘린다"*) |
| `DuelAxisChoice` | **필드 편집 창의 대결 축 고르기가 무엇을 보여 주고 무엇을 고른 채로 열리는가** (B-134) — 스피너 항목 목록 · 초기 선택 · **댕글링 판정**(가리키던 축을 못 찾았는가) · 위치→축 되찾기 | **어댑터를 채우는 것과 선택을 정하는 것이 갈려 있으면 그 틈이 오배정이 된다**(R-1). 종전 배선은 *"못 찾으면 `setSelection`을 안 부른다"*였는데 **`AbsSpinner.setAdapter`가 항목이 있으면 그 자리에서 0번을 고르므로 안 부르는 것이 곧 0번**이었다 — 주석이 바로 옆에서 *"조용히 0번으로 옮기면 오배정이다"*라고 금지하던 그 일이 그대로 일어났다. **화면 계층에서는 원리적으로 안 잡힌다: 코드 어디에도 "0"이 적혀 있지 않다.** 댕글링에 **자리 표시용 항목 하나를 세우는 것**도 이 파일의 판정이다 — 불리언으로 두면 사용자가 *첫 축*을 고를 때 선택이 안 바뀌어 콜백이 아예 오지 않고, **고쳤는데 사유 줄이 남아 빠져나올 길이 없다** |
| `DuelEntry` | **홈에서 대결을 누르면 어디를 여는가** — 살아 있는 축이면 재개 · 세계관 하나면 그 축 목록 · 여럿이면 질문 · 없으면 만들 것이 먼저 | 대결은 이 앱의 도구 중 유일한 **반복 활동**이라 진입점이 재개를 못 하면 매번 세 걸음을 다시 밟는다(원칙 04). 틀려도 **앱이 죽지 않는다** — 엉뚱한 축이 열리거나 빈 화면이 뜰 뿐이라 컴파일·정적 검사가 못 본다 |
| `MembershipTimeline` | 소속 구간의 시간 규칙 — 만들 때의 가입 연도, 고칠 때의 구간 겹침 | 재가입이 이전 탈퇴보다 앞이어도 통과해, 같은 해에 나가 있으면서 들어와 있는 이력이 됐다(세션 로그 1-h장) |

---

## 4. 검증 체계 — Android 빌드가 불가한 환경에서

`dl.google.com`이 차단돼 **`./gradlew`는 무엇을 하든 실패한다.** 그래서 검증은 네 갈래다.

| 도구 | 무엇을 보는가 | 기준선 |
|------|---------------|--------|
| `tools/run_jvm_tests.sh` | 순수 계층을 **실제로 실행**한다(표준 kotlinc + JUnitCore) | 기준선은 `remaining_work` 5장이 든다 |
| `tools/check_text_style.sh` | 화면 문구의 말투·용어(가이드 기계 검출분) | 기준선은 `tools/text_style_baseline.txt`가 든다(그 파일이 단일 소스) — 새 위반은 즉시 실패 |
| `tools/check_resources.sh` | 리소스 중복·미정의 참조·XML 구문 | 통과 |
| `tools/check_dialog_validation.sh` | 자동 닫힘 버튼 안의 조기 return(R-27 위반) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_prefs_keys.sh` | 같은 prefs 키를 두 곳이 다른 타입으로 쓰는가(R-28 위반) | 충돌 0 — 새 충돌 즉시 실패 |
| `tools/check_image_pointer.sh` | 캐릭터 `imagePaths` 쓰기가 대표 포인터를 함께 고치는가(R-32 위반) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_restore_preview_parity.sh` | 복원 미리보기(`analyze*`)가 가져오기와 같은 `merge*`로 '변경/동일'을 판정하는가(R-33 위반) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_completion_rate.sh` | 필드 완성도 백분율을 `CompletionRate` 하나가 내는가 — 분모가 필드 수인 백분율이 단일 소스 밖에 있는가 · 등재된 소비처가 실제로 그것을 부르는가(R-34 위반) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_config_boundary.sh` | 세계관 경계를 넘는 config 복사가 **두 참조를 함께** 걷어내는가 — 경계를 아는 파일(`GradeSystemRef.demote`·`remapCode` 호출자)이 대결 몫도 걷어내는가(R-35 위반) | **fail-closed** — 목록 밖의 새 호출자는 통과하지 못한다 |
| `tools/check_no_nul_bytes.sh` | 소스에 날 제어문자(NUL)가 있는가 — 있으면 git이 binary로 보아 **diff가 사라지고 grep 기반 도구가 그 파일을 통째로 건너뛴다**(R-37 위반) | **fail-closed** — 추적 파일 + 추적되지 않은 새 파일까지 본다. 본성이 바이너리인 확장자만 예외이고, 목록 밖의 새 확장자는 통과하지 못한다 |
| `tools/check_backlog_markers.sh` | `remaining_work` 4장 백로그에서 **[A] 닫힌 행이 살아 있는 심각도(🔴🟠🟡🟢🔵)를 들고 있는가** + **[B] 머리 표식이 아예 없는 행이 있는가**(그 장 머리 규약) — [A]는 *"무엇이 급한가"*를 심각도로 훑는 콜드 세션이 행 본문을 끝까지 읽게 만들고, **[B]는 표를 셀 수 없게 만든다**(2026.08.10까지 표식 없는 행이 80개였고 그것이 인수인계 판의 *"이것이 전수다"*를 떠받쳤다 — B-182). **살아 있는 행 수를 심각도별로 함께 내며, 그 출력이 "남은 일이 몇 건인가"의 단일 소스다**(문서에는 값을 적지 않는다) | **fail-closed** — 탐지기 자기 시험을 매 실행 앞에 둔다(python3이 없으면 *"위반 없음"*과 구별되지 않는다). **[A]만으로는 [B]가 원리적으로 안 잡힌다** — 표식이 없으면 *"닫혔는데 심각도를 들었다"*에 해당할 수가 없다. 반만 닫힌 행은 본문의 **"이 행이 열려 있는 이유"**가 탈출구다(B-27) |
| `tools/check_propagate_value_parity.sh` | 기본 필드 전파 미리보기에서 값을 **채우는 조건**(저장소)과 **세는 조건**(`DefaultFieldPlan`)이 한 벌인가 — 저장소가 `typeChanges`를 실제로 부르는가 · *직전 템플릿의 타입*으로 가르던 옛 조건이 되살아났는가(B-135) · **저장소가 값 표에 쓰는가**(값 DAO에서 부를 수 있는 것은 `get*`뿐 — B-137) | 0건 동결 — 새 위반 즉시 실패. 저장소는 Room에 매달려 **순수 시험이 원리적으로 못 보는 자리**라 이 검사가 유일한 눈이다. **지키는 것이 둘이다:** *세는가*(B-135)와 *지우지 않는가*(B-137 — 미리보기가 수를 세어 보이므로 **더하고 싶어지는 자리**다) |
| `tools/check_ai_review_retention.sh` | AI 검토 시트가 **유료 응답을 버리지 않는가**(R-38), 그리고 **유료 실행이 말없이 끝나지 않는가**(B-144) — 항목은 스크립트 머리의 *무엇을 보는가*가 든다(**여기 두 벌로 적지 않는다** — 종전에 ①~④를 옮겨 적었다가 ⑤가 들어오며 낡았다) | 0건 동결 — 새 위반 즉시 실패. **화면 계층이라 순수 JVM도 프로브(`ui/**` 제외)도 원리적으로 못 본다.** 순수 시험은 *합치는 규칙이 옳은가*까지만 잴 수 있고, *화면이 그 규칙을 지나는가*는 이 검사만 본다. **잠그는 축이 둘이다** — *이미 결제한 응답*을 잃지 않는가(회전·되받기·적용 실패)와 *결과를 알 기회*를 잃지 않는가(빈 결과의 침묵). **보는 시트는 이미지 둘만이 아니다** — B-123이 **이름 추천 시트**를 항목으로 더했다(회전 보존 · 실행 엔진이 뷰 밖 · **호스트가 재생성된 시트에 콜백을 다시 붙이는가**). 그 자기 시험에서 **기존 패턴 둘이 느슨한 것이 드러나 함께 조였다**: `onSaveInstanceState`는 접두만 맞아도, `val pool`은 지역 변수여도 통과하고 있었다 — **새 항목은 반드시 깨뜨려 보고 붙일 것**(안 잡는 검사는 통과와 구별되지 않는다) |
| `tools/check_ai_image_rule.sh` | 이미지 지시가 **이미지와 한 몸으로 다니는가**(A-7 · B-139) — 프로토콜 코덱이 `request.system`을 직접 읽는가(반드시 `effectiveSystem()`이다) · 코덱이 그것을 실제로 쓰는가 · 이미지 절이 `imageSystemRule` 밖으로 새어 `buildSystemPrompt`에 도로 이어 붙었는가 | 0건 동결 — 새 위반 즉시 실패. **시험과 짝이다:** `AiImageRequestTest`는 *지금 있는* 세 프로토콜의 결과만 재므로 넷째가 `request.system`으로 들어오면 침묵하고, 이 검사는 모양만 보므로 이름을 바꾼 부활을 놓친다. 실수의 방향이 **빼는 쪽**이라 어느 쪽이 뚫려도 모델이 속지는 않는다 |
| `tools/check_ai_send_guard.sh` | 기기 밖으로 나가는 읽기가 **봉쇄 가드를 지나는가**(R-39 · B-141) — `AiImagePreparer`가 `ImagePathMatch.isInside`를 부르는가 · 뿌리에 기본값이 붙었는가 · **모든 호출 자리가 뿌리를 넘기는가**(호출 자리는 손으로 적지 않고 **훑어서 찾는다**) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_view_probe_targets.sh` | `View`를 직접 상속한 클래스가 **전부 커스텀 뷰 프로브 대상 목록에 있는가**(R-31 · B-147) — 이 표의 다른 검사와 달리 보는 것이 **코드가 아니라 도구 설정**이다. **실패의 모양이 침묵인 유일한 부류라 세웠다**: 빠진 뷰는 프로브를 *통과*하는 것이 아니라 *들어가지도 않아서*, 오류 수도 이름도 뜨지 않고 **빠뜨릴수록 결과가 깨끗해 보인다**(R-31이 글자 그대로 금지해 두었는데도 둘이 닷새를 빠져 있었다). 범위는 `View` 직접 상속뿐이고 그 경계의 근거는 스크립트 머리가 든다 — **세울 수 없는 것을 요구하면 검사가 늘 빨간불이 되고 다음 사람이 끈다** | 4건 전량 등재 — 새 커스텀 뷰가 미등재면 즉시 실패. 밖에 있는 것은 `ZoomableImageView` 하나(B-166 — 스텁이 먼저다) |
| `tools/differential_compile.sh` | 손댄 파일에 **새로 생긴** 컴파일 오류만 | 기준선 대조 |
| `tools/verify_room_migration*.py` | 마이그레이션 하네스(마이그레이션마다 하나씩 는다 — 종 수는 `ls tools/verify_room_migration*.py`로 센다)를 **실제 SQLite로** 실행 | 각 스크립트 출력이 든다 |
| `tools/verify_reset_coverage.py` | 엔티티 목록 ↔ `ResetPlan` ↔ `executeReset` 호출부 3자 대조 | 스크립트 출력이 든다(엔티티가 늘면 함께 는다) |

**이 구조가 강제하는 설계 규칙:** 판단 로직은 최대한 `util`의 순수 객체로 내려야 한다.
`ui`에 남은 로직은 **실행 검증이 불가능**하고 차분 컴파일의 잡음에 묻힌다(뷰바인딩 미해결만
기준선에 212종). 로드맵 5가 `FieldEditDialog`의 검증 로직을 `FormulaValidator`로 내린 것도
이 이유다.

> **차분 컴파일 함정:** 스크립트 내부 정규화 `sed`가 실제 출력(`파일:행:열: error:`)과 달리
> 콜론 하나가 빠져 **줄 번호를 지우지 못한다.** 그대로 비교하면 손댄 파일의 오류가 전부
> '신규'로 뜬다. 스크립트 헤더가 안내하는 수동 정규화를 반드시 따를 것.

> **⚠️ 하네스가 통과시키고 CI만 터지는 자리 — `android.util.Log`.**
> 로컬 하네스는 `tools/jvm-stubs/AndroidLogStub.kt`로 Log 자리를 채운다(0을 돌려주는 껍데기).
> 그런데 Gradle의 `testDebugUnitTest`가 쓰는 android.jar은 **모든 메서드가 예외를 던지는**
> 껍데기라, 순수 로직이 진단용으로 부르는 `Log.w` 하나가 테스트를 깨뜨린다.
> 로드맵 5에서 실제로 이것에 걸렸다 — `FormulaEvaluator`의 순환 참조·미존재 키 경로가
> `Log.w`를 부르는데, 그 경로를 처음으로 테스트한 순간 **로컬 전량 통과 / CI 4건 실패**가 났다.
> `app/build.gradle.kts`의 `testOptions { unitTests { isReturnDefaultValues = true } }`로 막았다.
>
> **일반화:** 하네스의 스텁은 "컴파일을 통과시키는 것"이 목적이라 **런타임 의미가 실제와 다르다.**
> 순수 계층에 Android API를 새로 부르는 코드를 넣을 때는 하네스 통과가 근거가 되지 못한다.

---

## 5. 확장점 — 무엇을 추가할 때 어디를 건드리는가

### 5-1. 새 필드 타입 (현재 7종: TEXT · NUMBER · SELECT · MULTI_TEXT · GRADE · CALCULATED · BODY_SIZE)

`FieldType` enum에 더하는 것으로 끝나지 않는다. 그리고 **분기 지점을 enum으로 세면 과소평가된다** —
실측하면 이렇다:

| 세는 방법 | 세는 명령(파일 수) |
|-----------|---------------------|
| `FieldType`을 참조 | `grep -rl 'FieldType' app/src/main/java --include=*.kt \| wc -l` |
| `FieldType.<상수>`로 분기 | `grep -rlE 'FieldType\.(TEXT\|NUMBER\|SELECT\|MULTI_TEXT\|GRADE\|CALCULATED\|BODY_SIZE)' app/src/main/java --include=*.kt \| wc -l` |
| **`"CALCULATED"` 같은 생문자열로 분기** | `grep -rlE '"(TEXT\|NUMBER\|SELECT\|MULTI_TEXT\|GRADE\|CALCULATED\|BODY_SIZE)"' app/src/main/java --include=*.kt \| wc -l` — **착수 시 직접 셀 것.** 등재 후 계속 늘었다(23 → 28, 2026-07-29 실측) |

타입 판정이 `fd.type == "CALCULATED"` 같은 **문자열 비교로 수십 개 파일에 흩어져 있다**
(위 셋째 명령이 현행 수를 든다). `FieldType`에 상수를 하나 더해도 그 자리들은 아무것도
모른다 — 새 타입은 그 화면들에서 조용히 빠지고, 그것이 원칙 02 위반(껍데기 구현)의 가장
흔한 발생 경로다.

착수 시 반드시 훑을 것: 입력 렌더(`DynamicFieldRenderer`) · 통계 적격성(`StatsFieldPolicy`) ·
분포 계산(`ValueDistributions`) · 드릴다운 매칭(`FieldValueMatchSpec`) · 엑셀 열 직렬화
(`FieldConfigColumns`·`CharacterFieldValueOverflow`) · 랜덤 생성(`FieldRandomGenerator`) ·
AI 정책(`FieldAiPolicy`).

> **구조 부채 B-55로 이미 등재돼 있다**(`remaining_work` 4장 — 그 항목이 이 5-1장을
> 되가리키므로 상호 참조가 양방향이다). 문자열 분기를 `FieldType`으로 좁히면 새 타입 추가가
> 컴파일러의 도움을 받는다(`when`의 exhaustive 검사). 지금은 전수 grep이 유일한 방어다.

### 5-2. 새 대상(entityType) — 지금은 `character` · `event` · `novel`

`FieldDefinition.entityType`이 축이다. **DAO 기본 인자가 `ENTITY_CHARACTER`라는 것이 함정이다** —
호출부가 대상을 넘기지 않으면 조용히 캐릭터 필드를 본다(로드맵 5가 `FieldEditDialog`에서
실제로 이 결함을 잡았다). 새 대상을 열 때는 **규약 R-29의 전수 명령**을 돌릴 것 —
`getFieldsByUniverse*`뿐 아니라 `getAllFieldsList`·`getFieldByKey`·`getGroupNames`·
`countFieldsByKeyExcluding`도 같은 기본값을 갖는다.

**한 종류를 여는 데 드는 자리**(확-3 작품 축이 실측한 것 — 세션 로그 1-t·1-u장):
값 테이블 + DAO(캐릭터·사건판과 **동형**으로) · 병합 규약(`*FieldValueMerge` — R-5) ·
휴지통 스냅샷 **둘**(소유자 스냅샷 + 세계관 스냅샷의 고아 값) · 삭제 고지 집계 ·
`ResetPlan` · 필드 관리 칩 + 편집 폼 + 값 라이브러리 · 엑셀(정의 시트 `대상`, 소유자 시트의
`필드:` 동적 열, **가져오기 순서**) · 통계(스냅샷·인사이트·드릴다운·하위 그룹·CALCULATED) ·
월드패키지 엔트리 + schemaVersion. **남은 것은 세계관·세력**이다(백로그 확-3).

> **순서가 열보다 위험하다.** 작품 축은 열을 다 만들고도 `importNovels`가 `importFieldDefinitions`
> 앞에 있어 빈 DB 복원에서 값이 통째로 버려질 참이었다 — 새 시트가 필드 열을 실으면
> **그 시트가 언제 처리되는지**부터 확인할 것.

### 5-3. 새 통계 분석

**적응적 통계 원칙**(CLAUDE.md)상 분석 항목은 필드 구성에서 **동적으로 생성**된다.
새 분석을 붙일 자리는 `StatsDataProvider`이며, 셋을 반드시 지킬 것:
계산 필드 병합(R-16) · 자동 선택에만 설정 적용(R-18) · 라벨이 아닌 타입드 스펙으로 매칭(R-20).

### 5-4. 새 왕복 포맷

현재 둘: **엑셀 왕복**(`excel/` — 시트 규약, 오버플로 시트, 헤더 별칭)과
**월드패키지**(`share/` — ZIP + manifest. 현행 버전은 `WorldPackageContents.CURRENT_SCHEMA_VERSION`이
든다). 새 포맷은 기존 둘의 규약을 재사용해야 한다 —
특히 안정 식별자(`EntityCode`)와 "빈 칸 = 해제" 규약, 그리고 **거부가 아니라 수용·교정**
(개발 의도 4번).

---

## 6. 규약 색인 (R-1부터 — 아래 표가 현행 범위다)

정의 원문은 전부 **`docs/conventions.md`**에 있다(행 번호는 갱신되므로 제목으로 찾을 것 —
각 규약이 `## R-N.` 절이다). **아래는 색인이며, 착수 전 해당 영역의 규약은 원문을 읽을 것.**

| # | 한 줄 | 영역 |
|---|-------|------|
| R-1 | 스냅샷도 안정 식별자를 병기한다 — 코드가 대상을 정하고 id는 확인 수단이다 | 복원 |
| R-2 | Gson 스냅샷에 추가하는 필드는 전부 nullable (기존 필드 타입 변경 금지) | 복원 |
| R-3 | 보관 한도가 방금 만든 백업을 파괴해서는 안 된다 | 휴지통 |
| R-4 | 파괴적 동작은 실행 전에 결과를 알리고 취소 경로를 남긴다 | 전역 |
| R-5 | 폼의 권한은 폼이 실제로 렌더한 것까지다 | 입력 |
| R-6 | 예약 시트명은 소유자만 가질 수 있다 | 엑셀 |
| R-7 | 시트의 정체는 이름이 아니라 헤더가 정한다 — 첫 열 하나로는 부족하다 | 엑셀 |
| R-8 | 스냅샷은 겹치지 않고 이어붙는다 | 휴지통 |
| R-9 | 정리도 복원도 '삭제 작업' 단위다 | 휴지통 |
| R-10 | payload 한 행은 읽을 수 있는 크기여야 한다 (CursorWindow) | 저장 |
| R-11 | 보류 판정은 참조 종류마다 키가 있어야 한다 | 복원 |
| R-12 | 스냅샷의 종류는 인스턴스가 아니라 호출이 정한다 | 휴지통 |
| R-13 | 집계의 셀 단위가 다르면 함수를 나눈다 | 통계 |
| R-14 | 목록에서 잘라낸 것은 개수로 존재를 알린다 | 전역 |
| R-15 | 머지된 카드가 약속한 범위 = 그 카드에서 뻗는 모든 경로의 범위 | 통계 |
| R-16 | 계산 필드는 저장 행이 없다 — 저장 값을 읽는 모든 경로가 그것을 알아야 한다 | 통계·엑셀 |
| R-17 | 빈 결과는 사유를 말한다 — "못 찾음"과 "없음"은 다르다 | 전역 |
| R-18 | 설정은 '자동 선택'에만 적용된다 — 사용자가 고른 것은 계산한다 | 통계 |
| R-19 | 상한은 표시 계층의 관심사다 | 통계 |
| R-20 | 라벨은 매칭 키가 아니다 | 통계 |
| R-21 | 계산기의 파생 상태는 인자에서만 나온다 | 통계 |
| R-22 | 서식이 곧 값의 정체성인 문자열은 로케일을 고정한다 | 엑셀·통계 |
| R-23 | 학습한 사실은 학습 대상이 바뀌면 함께 버린다 | AI |
| R-24 | 성립하지 않는 조합의 설정은 보이지 않는다 | UI |
| R-25 | 화면에 노출되는 설정에는 목적문이 붙는다 | 텍스트 |
| R-26 | 항목 순회형 대량 작업에는 결정형 진행도를, 조회형에는 가짜 진행도를 붙이지 않는다 | UI |
| R-27 | 검증 실패는 창을 닫지 않는다 — 알리는 것과 고칠 자리를 남기는 것은 다른 일이다 | UI·입력 |
| R-28 | 두 화면이 같은 prefs 파일의 같은 키를 공유하지 않는다 — 타입이 갈리면 ClassCastException (`tools/check_prefs_keys.sh`) | 저장·UI |
| R-29 | `entityType`으로 갈리는 기능은 조회·중복 판정·순서·쓰기가 **모두** 같은 종류를 본다 — DAO 기본값이 캐릭터라 잊으면 오류가 아니라 잘못된 정답이 나온다 | 필드·데이터 |
| R-30 | 물질화된 파생값의 원본을 고치는 경로는 파생값 재작성과 한 트랜잭션이다 — 등급 체계의 실효 표가 그 사례 | 필드·데이터 |
| R-32 | 목록을 가리키는 포인터는 그 목록을 고치는 **모든** 경로가 함께 고친다 — 빠뜨려도 오류가 나지 않고 포인터만 조용히 어긋난다. 판정은 `util.CharacterRepresentativeImage`, 쓰기는 `Character.withImagePaths` 하나다 (`tools/check_image_pointer.sh`) | 저장·데이터 |
| R-33 | 복원 미리보기의 판정과 가져오기의 쓰기는 **같은 함수**에서 나온다 — 모을 것은 셋이다(열 해석·읽기·적용). 갈리면 늘 *'바뀌는데 안 바뀐다'는 거짓 안심*이 되어 사용자가 되돌릴 기회를 잃는다 (`tools/check_restore_preview_parity.sh`) | 엑셀 왕복 |
| R-34 | **같은 이름의 지표는 한 함수가 낸다** — 분자와 분모를 각 화면이 정하지 않는다. 분자는 언제나 분모 집합과의 **교집합**이고, 임계값 같은 상수도 단일 소스이며, 설정으로 여는 계수는 계산 계층이 **인자로 받는다**(그래야 순수 하네스가 돈다) (`tools/check_completion_rate.sh`) | 통계·완성도 |
| R-35 | **세계관 경계를 넘는 config 복사는 그 경계 안에서만 뜻이 있는 참조를 전부 걷어낸다** — 지금 둘이다(`gradeSystem` · `duelGrade.axisCode`). 자리마다 두 줄씩 쓰면 다음 자리에서 한 줄만 쓰게 되므로 `FieldConfigTransfer.demoteAcrossUniverse` 하나로 모은다. **새 키가 더 위험하다** — 축 code는 전역 유니크라 못 찾는 것이 아니라 **남의 세계관 축을 정확히 찾는다**(R-1의 오배정). 읽는 쪽 소속 검사가 이중 방어이고, 복원은 경계가 아니므로 예외다 (`tools/check_config_boundary.sh`) | 필드·데이터 |
| R-36 | **엑셀 가져오기에서 없는 열과 빈 칸은 다른 사실이다** — 빈 칸은 *"지워라"*, 없는 열은 *"이 파일은 그것을 말하지 않는다"*다. `read*Row`가 `cols.containsKey`로 갈라 `null`을 담고 `merge*`가 `null`이면 기존 값을 지킨다. 시트에 열이 늘 때마다 새로 생기는 함정이며, 겨냥하는 것은 **뒤늦게 더해진 선택적 열**이다(정체를 이루는 열은 대상이 아니다) | 엑셀 왕복 |
| R-38 | **유료 AI 응답은 뷰 밖에 두고, 검토 중인 응답을 버리는 길을 만들지 않는다** — 다시 여는 데 **돈이 드는** 부류라 저장소의 *주입 유실 시 안전 종료*(일곱 자리)를 그대로 쓰면 안 된다. 응답·실행·취소 깃발·진행도가 ViewModel로 올라가고, 재생성된 검토 화면은 `findFragmentByTag`로 **다시 먹인다**(닫지도 새로 만들지도 않는다). 검토 상태(어느 항목을 껐는가)는 시트가 `onSaveInstanceState`로 지킨다 (`tools/check_ai_review_retention.sh`) | AI · UI |
| R-39 | **기기 밖으로 나가는 읽기는 봉쇄 가드를 지난다 — 막힌 것은 "못 읽음"과 다른 사유다** — 판정은 `ImagePathMatch.isInside` 하나이고 **실패 처분이 대조 함수들과 반대다**(모르면 막는다 — 소비처가 *바이트를 내보내도 되는가*라 "모르겠으면 통과"가 곧 유출이다). 뿌리는 **필수 인자**여야 다음 소비처에서 조용히 빠지지 않고, 막힌 장수는 **갈라 센다**(처방이 다르다 — 뭉뚱그리면 멀쩡한 파일을 두고 *"읽지 못했습니다"*라고 말한다) (`tools/check_ai_send_guard.sh`) | AI · 이미지 |
| R-40 | **의사키 어휘를 새로 열 때는 접두가 겹치지 않아야 하고, 진짜 키와 부딪칠 때의 승자를 정해야 한다** — `__`(`SemanticRole.linkedKey`)와 `sys:`(`DuelSystemFields`)는 **뜻이 정반대**라 접두를 나눠 쓰면 값을 어디서 찾을지가 갈린다. `FieldDefinition.key`는 **자유 입력**이라 의사키와 같은 키의 필드를 만들 수 있고, 승자를 정하지 않으면 중복 제거가 **차례에 따라 하나를 조용히 버린다**. 교환 형식에서 안전한 글자여야 하고(엑셀은 칸 첫 글자 `@`를 함수로 읽는다) **모르는 의사키와 모르는 진짜 키는 갈라 다룬다**(뒤쪽은 나중에 살아나고 앞쪽은 영영 살아나지 않는다). **검사 스크립트가 아니라 `DuelSystemFieldsTest`가 잠근다** — 잠글 것이 문자열 한 자리가 아니라 *두 어휘의 관계*다 | 대결 · 필드 |
| R-41 | **편집 화면의 폼 상태는 한 함수가 한 벌로 싣고, 새 상태 필드는 그 벌에 함께 등재한다** — 그릇 셋(회전 Bundle · 백스택 `pendingViewState` · 영구 Draft)이 `saveFormState` 하나를 지나고, 새 필드는 `saveFormState`·`collectDraft`·`applyDraft` 세 자리에 함께 오른다. 드래프트 새 칸은 **nullable**(Gson null 주입을 타입에 싣는다). 복귀 복원은 무음이고 **묻는 것은 세션을 넘은 드래프트뿐**(구별 재료는 조각 인스턴스의 생사). **지키는 것은 실기기뿐이다** — 세 그릇 전부 화면 계층이라 어떤 로컬 검증도 못 본다 | 캐릭터 편집 |
| R-44 | **관문이 사용자가 고르지 않은 것으로 바꿨으면 결과가 그렇게 말한다** — AI 요청은 `AiService.complete` 하나를 지나고, 한도에 걸리면 관문이 **다음 프로바이더로 이어간다**. 사용자는 켠 적이 없고 화면에도 표시가 없으므로, 고지가 빠지면 **자기가 고른 모델의 답인 줄 알고 다른 회사 글을 받는다**. 뼈대 셋: 판정·문구는 한자리(`AiProviderFallback.switchNote(Of)`) · 표식은 **관문이** 새긴다(`withProvider`·`withSwitchedFrom` — B-150이 실패 표식에서 배운 그대로) · 등재를 기계가 본다(`tools/check_ai_switch_notice.sh`). **셋째가 없으면 규약이 성립하지 않는다** — 순수 시험은 *문구가 옳은가*를 재지만 이 결함의 모양은 **아무도 그 함수를 부르지 않는 것**이다. 적용 범위는 '전환'이 아니라 **'관문이 바꾼 것' 전부**다(`temperatureOmitted`·`imagesOmitted`가 이미 같은 부류다) | AI |
| R-45 | **왕복하는 설정은 카탈로그 하나가 든다** — 엑셀 '앱 설정' 시트는 내보내기가 손으로 나열하고 가져오기가 `when (key)`를 손수 분기해 **설정 하나에 두 곳을 고쳐야 했고, 그래서 늘지 않았다**(저장소 스무 곳이 넘는 동안 실린 것은 11개). 뼈대 셋: 한 설정은 한 선언(`Binding`이 읽기·쓰기를 함께 든다) · **선언은 순수하고 입출력만 `Context`를 쥔다**(`AppSettingsKeys` ↔ `AppSettingsBindings` — 합치면 계약까지 `Context`에 딸려 가 순수 시험이 못 본다. `Binding`이 `Spec`을 받으므로 **선언 없는 바인딩은 컴파일되지 않는다**) · 반대쪽은 기계가 본다(`tools/check_app_settings_catalog.sh`). **비밀은 처분이 따로다** — `Disposition.SECRET`은 **기본 제외 + 별도 동의**이고, 동의를 항목 목록에 넣지 않는 것이 요점이다(거기 넣으면 '전체 백업'만으로 키가 실린다) | 엑셀 |
| R-46 | **경로 판단은 단일 소스가 들고, 실패 처분은 방향이 둘이다** — `canonicalPath` 한 줄이 저장소 곳곳에 복붙돼 있었고, 남아 있는 동안 새 벌이 또 늘었다. 판단은 `ImagePathMatch.canonical`(정규화)·`isInside`(봉쇄) 둘뿐이고, **실패 처분의 방향이 반대인 것이 규약의 본체다** — 정규화는 실패하면 **원본을 들고 가고**(버리면 그 경로가 보호를 잃는다), 봉쇄는 실패하면 **막는다**(모르면 통과가 곧 유출이다). 한 줄을 자리마다 적으면 이 방향이 자리마다 갈린다 — 실측에서 **여섯 자리가 반대로** 들고 있었다. 반대쪽은 `tools/check_image_path_single_source.sh`가 본다 | 이미지 |
| R-47 | **목록 셀은 결합·분해를 한 쌍이 든다** — 엑셀의 목록 셀(참여 캐릭터·관련 작품·태그·별칭·대결 참가자…)은 붙이는 쪽이 자리마다 `joinToString(", ")`이고 쪼개는 쪽이 예외 규칙 없는 `split(",")`이라 **이름·제목에 쉼표가 있으면 왕복 한 번에 두 값으로 갈렸고, 갈린 쪽은 되돌릴 방법이 없다.** 함수는 `joinCsv`/`splitCsv` 둘뿐이고 규칙은 **엑셀·CSV 그대로**다(방언을 만들지 않는 것이 사용자가 이 규약을 고른 이유다). **옛 파일 경로가 규약의 절반이다** — 따옴표가 없으면 종전 구현과 글자 그대로 같고, 어긋난 따옴표는 옛 규칙으로 되돌린다. 반대쪽은 `tools/check_list_cell_csv.sh`가 본다. R-31·R-43·R-44와 같은 계열이고 넷 다 실패의 모양이 **침묵**이다 | 엑셀 |
| R-48 | **유지하기로 한 용어는 뜻을 하나로 못박고, 그 못을 기계가 지킨다** — `매칭`은 금지어가 아니라 **표2(유지)**이고 뜻은 *엑셀 왕복에서 파일의 행과 앱의 레코드를 잇는 것* 하나다(판정 23번). **대결 영역에서는 쓰지 않는다** — 그쪽은 '대결'·'판'·'짝'이며, `check_text_style.sh` **R4 DUEL-TERM**이 `duel_` 접두 키와 경로에 `Duel`이 든 코틀린 파일의 한국어 리터럴을 본다. **유지와 금지는 한 벌이다**: 유지의 근거가 *"뜻이 일관되게 쓰이고 있다"*이므로 대결이 같은 말을 쓰기 시작하면 **그 근거가 사라진다.** R3(표1 전역 금지)와 **방향이 다르다** — R4는 한 영역에만 걸어 나머지에서는 그대로 쓰게 둔다. 종전에 이 약속을 지킨 것은 **주석 한 줄**뿐이었다 | 문구 |
| R-43 | **손으로 적은 대상 목록은 기계가 완전성을 본다 — 빠지면 실패가 아니라 침묵이다** — `run_jvm_tests.sh`의 `TESTS`는 손으로 적은 목록이라 **없는 시험은 실패하지 않고 그냥 돌지 않는다.** 2026.08.09에 새 시험 파일 둘(18건)이 목록 밖에 있었는데 **초록이 그대로 떴고**, 잡힌 것은 기준선 증가분의 손셈뿐이었다. R-31이 커스텀 뷰 프로브에서 배운 것과 같은 모양이라 처방도 같다(`tools/check_jvm_test_targets.sh`). **면제는 검사 쪽 목록이 아니라 스크립트 주석의 사유로 둔다** — 두 벌로 적으면 낡고, 낡은 면제는 다시 침묵이다. 적용 기준: *침묵하는 목록*에만 걸고 *소리치는 목록*(빠지면 컴파일이 깨지는 `SOURCES`)에는 안 건다 | 검증 도구 |
| R-37 | **소스에 날 제어문자를 넣지 않는다 — 값이 아니라 표기로 적는다** — 값은 옳아도 파일이 텍스트이기를 그만두면 **git diff가 사라지고 grep 기반 도구가 그 파일을 건너뛴다**(`triage_unresolved.sh`의 선언 목록에서 `PresetMerge`가 실제로 빠져 있었고, 그 도구는 `CLAUDE.md` v2.0이 CI 완화의 유일한 근거로 지목한 것이다). 이스케이프로 적는다. **도구에 `grep -a`를 박는 쪽은 기각** — 진짜 바이너리까지 열어 도구마다 예외 목록을 만든다 (`tools/check_no_nul_bytes.sh`) | 도구·검증 |
| R-31 | 다이얼로그 본문 스크롤에는 높이 상한이 있다 — 없으면 긴 내용이 잘린 채 끝까지 스크롤되지 않는다. 코드는 `util.cappedScrollView`, **XML 루트는 `ui.common.CappedScrollView`**(중첩 스크롤판 `CappedNestedScrollView`)이고 측정 규칙의 단일 소스는 `util.DialogScrollCap`이다 (`tools/check_dialog_scroll.sh` — 코드·XML 두 축) | UI |

---

## 7. 문서 지도 — 무엇을 언제 읽는가

| 목적 | 문서 |
|------|------|
| **판단 기준**(모든 설계·리뷰의 최상위) | `CLAUDE.md` — 개발 의도 4질문 · 5대 원칙 · 새 기능 체크리스트 |
| **지형 파악**(지금 이 문서) | `docs/architecture_2026-07.md` |
| **규약 원문**(착수 전 읽는 현행 규칙) | `docs/conventions.md` — 정의 원문. 한 줄 색인은 위 6장 |
| **확장 한계·성능** | `docs/scalability_performance_2026-07.md` |
| **지금 할 일 / 백로그 / 실기기 확인** | `docs/remaining_work_2026-07.md` — **5장 머리의 🧭가 시작점**이다: 지금 할 일은 `📌 세션 인수인계` 중 **취소선 없는 맨 위 판 하나**가 정한다(위에서부터 읽으면 옛 블록의 최우선 표식에 걸린다 — CLAUDE.md v1.9가 실증으로 기록한 함정) |
| **확정된 판정 — 사용자가 무엇을 정했는가** | `docs/judgment_confirmations_2026-08.md` — **착수 전에 읽는다. 여기 있는 것은 다시 묻지 않는다**(착수 규칙 3번). 백로그 4장 각 행에는 답만 짧게 적고 **원문·해석·파생은 전부 이 문서가 갖는다** |
| **지난 세션이 무엇을 왜 했는가** | `docs/session_log_2026-07.md` — 위 문서 1장이었다(2026.07.30 분리). 다른 문서의 `1-x장`은 전부 이곳이다 |
| **미이행 기능 색출** — 로드맵(1~4장)은 **닫혔고 5장의 미검증 후보만 살아 있다** | `docs/superficial_feature_audit_2026-07.md` |
| **실사용 데이터가 말한 것** | `docs/usage_reality_check_2026-07.md` (+ `_runbook`) |
| **화면 문구** | `docs/text_style_guide_2026-07.md` |
| **영역별 설계** | 엑셀 왕복 `excel_roundtrip_audit` · 엑셀 스트리밍 가져오기 `excel_streaming_import` · 이미지 폴더 왕복 `image_folder_roundtrip_design`(결정 근거 `image_external_management`, **확장 `image_folder_tag_ai`**) · AI `ai_integration`(**A-7 이미지 첨부가 2026.08.07에 섰다** — 이 앱이 이미지를 기기 밖으로 보내는 첫 기능이라, 종전에 앱 전체를 두고 하던 약속 문구 세 자리를 **그 기능의 범위로 좁혔다**. **A-5는 결번**)·`ai_control_and_ui_density` · 값 라이브러리 `field_value_library` · 필터·정렬 짝 `filter_sort_parity` · **체형(실루엣 재편) `body_visual_redesign`(**v1 범위 구현 완료 — 2026.08.02, 설계 10장 1~7단계. 그 문서 v1.25**. 남은 것은 **v2·v3 범위**(그 문서 7장)와 실기기 확인 3-33~3-35. ⚠️ `handover_body_reauthor_2026-08`은 **역할이 끝난 문서**다 — 2026.08.01 시점에 멈춰 있고 스스로 *"다음 세션이 처음 읽는 파일"*이라 선언하므로 **그림을 다시 만질 때만** 편다)** · **백업·내보내기 동선 `backup_export_redesign`(설계 확정 — 체형 다음 슬라이스)** · **사건 필드 추천 `event_field_recommend_2026-08`(v1.1 — 설계·구현 완료. B-62·B-68 해소, 실기기 3-32)** · **대표 이미지 `representative_image_design_2026-08`(B-103 — 설계·구현 완료 2026.08.03. 실기기 확인 3-91)** · **뗀 이미지 관리 `detached_image_management_design_2026-08`(B-107 — v1.1, 설계·구현 완료 2026.08.03. 실기기 확인 3-52. 폴더 왕복 규약을 바꿨고 `image_folder_roundtrip_design` 3장·`image_folder_tag_ai` 2-1의 표를 **갱신했다**)** · **복원 미리보기 정합 `restore_preview_parity_2026-08`(B-101+B-102 — 설계·구현 완료 2026.08.03. 범주 전수 census가 1장이다. 규약 **R-33** + `tools/check_restore_preview_parity.sh`로 잠갔다. 실기기 확인 3-53)** · **대결 `duel_system_design_2026-08`(B-104 — **조사·검토 이행 + 순수 계층 구현 완료 2026.08.04.** 확정 문서 6장이 B-104에만 붙인 조사 의무의 산출물이고 그 뒤의 설계 정본이다. **세 축 모두에서 갈음이 나왔다** — 점수는 Elo가 아니라 **Bradley–Terry MAP**, 짝 고르기는 확정된 3층에 **정보량 순서·확신 기준 접기·표적 재확인**을 채움, **상성의 정의를 3-순환에서 잔차로 넓힘**(층 A는 천적을 원리적으로 못 본다). **데이터 계층 구현 완료 2026.08.04 — DB 버전 49**(엔티티 셋 · DAO 셋 · 마이그레이션 48→49 · 휴지통 · `util/DuelRecords` · `DuelRepository`). **참가자를 DB id가 아니라 코드로 담고 `duel_matches`에 캐릭터 FK를 두지 않은 것**이 그 슬라이스의 결정이다 — 조사가 정한 캐릭터 삭제 처분이 id로는 성립하지 않는다(R-1의 오배정). **화면 계층·진입점 완료 2026.08.04.** **층 C(축↔필드 연결)·기록 화면·엑셀 왕복 완료 2026.08.04 — DB 버전 50** (사용자 요청: *"이 값이 어느 필드와 연관있는지를 연결"* · *"영향 필드가 여럿이면 순위"* · *"산출 필드는 따로"* · *"대결 기록을 엑셀이나 인앱에서도 편집"* · *"대전 선택창으로 나가는 버튼이 없다"* · *"세로로 긴 사진에서 얼굴이 잘린다"*). **연결이 둘인 것이 그 슬라이스의 결정이다** — 영향 필드는 *필드 → 대결*이라 대결 화면에 보이고, 산출 필드는 *대결 → 필드*라 **보이지 않는다**(보이면 물음과 답이 한 화면에 놓인다 — 설계 5장 ①이 점수를 두고 금지한 것과 같은 부류). 순위는 **사전식**이다(가중합은 근거 없는 숫자를 만든다). 시트 셋은 축·기록·상성이고 **순서가 규약**이다(정의가 기록보다 앞). **범주 집계 완료 2026.08.04 — B-112, 그 문서 8장**(차례가 없는 값을 걸어도 화면 표시가 전부였던 자리다. **승패를 그냥 세면 *불이 물에 약한 것*과 *물을 든 캐릭터가 센 것*이 섞이므로 모델의 예상 승수와 견준다** — 설계 3장이 상성을 50%가 아니라 예상과 견준 그 뺄셈이고, 눈금도 `DuelCounterRelations.Options`에서 그대로 가져왔다. 그 결과 **전승은 조용하고 범주 순환은 잡힌다** — 전자는 개체의 강함으로 완벽히 설명되고 후자는 1차원 점수가 원리적으로 만들 수 없다. 이것이 **소비처 ⓐ(모순 감지)를 닫았다**). **목록·통계 편입 완료 2026.08.05 — B-117, 그 문서 9장 · DB 버전 51**(캐릭터 목록을 *"강함 순"*으로 세울 수 있고, 통계 순위 탭의 기준 목록에 필드 다음으로 대결 축이 선다. **요점은 저장하지 않는 대신 계산의 출구를 하나로 묶은 것** — `scoresOf`가 순위표와 같은 진입점을 타고 하네스가 *"줄과 글자 그대로 같은가"*를 잰다. **`scalability_performance` 7장 4단계 3번이 여기서 처음 발동했고 답은 "목록은 더하되 계산은 더하지 않는다"**이다: 스냅샷에 싣는 것은 **축 목록**이지 판이 아니다. 이것이 **소비처 ⓓ를 닫았다**). **AI 컨텍스트 완료 2026.08.05 — 소비처 ⓔ, 그 문서 10장**(계약은 `ai_integration` A-6. 캐릭터 필드 추천과 서술형 작성이 **대결 우열을 프롬프트에 싣는다** — 프롬프트의 다른 섹션은 전부 사용자가 *적은* 것이지만 이것만 **둘씩 비교해 고른 것이 쌓인 서수 데이터**라, 없으면 추천이 사용자가 이미 정해 둔 우열을 모른 채 값을 지어낸다. ⓐ가 어긋난 값을 사후에 짚는다면 ⓔ는 **애초에 제안되지 않게 하는** 쪽이다. **등수는 다시 매긴다** — 그대로 옮기면 무기록 참가자의 앵커가 분모를 넘긴다. **DB 변경 없음.** 이것이 **소비처 ⓔ를 닫았다**). **프로필 필드 + 화면 재설계 완료 2026.08.07 — B-122, `feature_roadmap_2026-08` 3장 · DB 버전 52**(카드가 *'그림 위 스크림'*에서 **'그림 + 정보 패널'**로 갈리고, 연결이 **셋**이 됐다 — 프로필 필드는 *표시 전용*이라 **보이되 예측에 안 들어간다**. **B-114(무승부)를 함께 열었다** — 저장 형식은 처음부터 받고 있었고 화면만 없었으므로 같은 화면을 두 번 고치지 않는다. **GENDER 시맨틱 역할을 신설**해 필수 트리오(이름·성별·나이)가 라벨 추측 없이(R-20) 선다 — 아홉 번째이자 **첫 표식 전용 역할**이다. 판단은 `util/DuelCardInfo`로 내렸고, 카드 한 벌(`view_duel_card.xml`)을 두 번 include해 **k지선다(B-115)가 그릇만 바꾸면 되게** 뒀다. 규약 **R-36**이 함께 섰다 — 엑셀 `프로필필드` 열을 더하는 순간 그전 파일 전부가 *'열이 없는 파일'*이 됐고, 없는 열을 빈 칸으로 읽으면 다시 들이는 것만으로 연결이 지워진다). **후보 필터 완료 2026.08.08 — B-168, 그 문서 12장 · DB 버전 55**(*"이 대결에선 여성끼리만"* — 축이 조건을 직접 담고(`candidateFiltersJson` — 프리셋 참조는 무음 변경이라 기각) 필드는 **키가 정본**이다(`FieldFilter.fieldKey` — 복원 내성·시스템 열·엑셀 이식을 한 수에). **필터는 새로 붙일 짝만 좁힌다** — 전적 있는 비후보는 표식과 함께 순위표에 남고 `scoresOf` 소비처 넷은 안 바뀐다. 해석 실패는 후보 0으로 닫힌다). **소비처가 전부 닫혔다 — ⓑ·ⓒ 완료 2026.08.09, 그 문서 13장·13-8 · DB 버전 56**(빗장이 먼저였다: 이미지 축의 참가자 코드가 **경로**라 개명 한 번에 판이 남의 이미지에 붙는 자리였고, 규약 **R-42**가 그것을 막는다. 축은 세계관 단위로 두되 **한 축 안이 캐릭터 수만큼의 독립된 대결**이라 적합도 캐릭터마다 따로 돈다 — 한 번에 적합하면 캐릭터 사이의 점수 차가 *아무 판도 근거하지 않은 수*가 된다. 그 위에 소비처 둘이 얹혔다: **ⓑ 대표 이미지가 고정 1위가 아니라 점수 가중 확률 추첨**이 됐고(사용자 갈음 — *"대결 1위 그림이 대표가 되면 이미지 다양성이 아쉽잖아. 확률이면 모를까"*), **ⓒ 걸러낼 후보가 이미지 탭 칩**으로 섰다(**조용히 지우지 않는다 — 제안까지가 끝**). 기준 축은 축 편집 창의 스위치(`isBasisAxis` — 세계관당 하나, 유일성은 쓰기가 지킨다)이고 엑셀 `대결 축` 시트에 `기준축` 열이 붙었다. **두 소비처가 같은 진입점을 탄다**(`basisImageScores`) — 각자 훑으면 대표가 보는 순위와 정리가 보는 순위가 갈린다). **k지선다 완료 2026.08.09 — B-115, 그 문서 14장 · 마이그레이션 없음**(설계 2장이 *"v1 밖"*으로 미뤄 둔 둘 중 나머지이고 이로써 그 표가 비었다. 저장 형식은 v1부터 받고 있었다 — `groupId`. **뼈대는 1:1을 특수한 경우로 두지 않은 것**이다: 종전 `Winner.A/B/DRAW` 옆에 갈래를 세우면 같은 판단이 두 곳에 생기므로 **`k=2`를 규칙 안으로 흡수**했고(*고르면 `k−1`판 · 비슷함이면 `C(k,2)`판*), 쓰는 길도 하나로 합쳤다(`record` 제거 → `recordGroup`). **진 것들끼리는 적지 않는다** — 함께 떴다는 이유로 없는 답을 지어내면 순위가 사용자가 말하지 않은 것으로 움직인다. **셋일 때 넷째 슬롯은 사라지지 않고 자리를 지킨다** — 없애면 셋째가 혼자 두 배로 커지고 큰 카드는 그 자체로 눈을 끈다. **PL 적합은 실측이 없어 안 바꿨다**(B-176)). 실기기 확인 3-56·3-57·3-58·3-59·**3-60·3-61·3-85·3-86·3-87**)** |
| **신규 요청 7건 로드맵·설계 (2026.08.06~07)** | **`feature_roadmap_2026-08`** — 전역 기본 필드(B-119) · AI 이미지 첨부(B-120) · 일괄 태깅(B-121) · 대결 프로필+화면 재설계(B-122, B-114 흡수 — 목업 `mockups/duel_play_redesign_2026-08.html`) · 대결 등급 산정(B-113의 답) · **AI 이름 추천(B-123) · 이름은행 연계 개선(B-124)**. 실사용 표본(2026.08.07) 관찰은 그 문서 0-1장. **설계 완료 — 판정 Q1~Q7 전부 확정(2026.08.07, 확정 문서 8장). 구현은 확정 순서(그 문서 9장)대로 진행한다. **어디까지 닫혔는지는 여기 적지 않는다** — 그 표의 `~~취소선~~ ✅` 행을 세는 것이 원문이고, *지금 무엇을 할 차례인가*는 `remaining_work` 5장의 현행 📌 판이 든다. (종전 이 자리는 닫힌 것을 손으로 열거해 **세 슬라이스 뒤처진 채** 서 있었다 — *"다음은 ④ B-120"*이라 적힌 동안 실제로는 ⑥까지 닫혔고, 이 문서가 v1.6에서 규약 범위 표기를 없앤 것과 같은 이유로 값이 아니라 **읽는 법**으로 바꾼다.) 슬라이스별 앵커는 9-1 착수 지도**. 기존·예정 기능 개선 검토(5장)도 그 문서가 든다 |
| **점검 결과·수리 계획** | `app_inspection_round2` · `repair_plan` · `usability_review` · `design_intent` · **`plan_design_adversarial_review_2026-08`(전 문서 적대 검토 — 실증 발견·후보 목록·판정 대기)** |
| **절차서** | `room_migration_verification` — 마이그레이션 하네스를 어떻게 만들고 돌리는가(종 수는 `ls tools/verify_room_migration*.py`로 센다) |
| **브랜치·병합** | `docs/branch_merge_rules.md` |
| **끝난 것 / 낡은 것** | `docs/archive/` — 구현 완료된 계획서와 구시점 리뷰. **근거로 쓰지 말 것**(각 문서 머리의 보관 헤더가 무엇이 낡았는지 적어 둔다) |

> **루트의 `.md`는 `CLAUDE.md` 하나다.** 예전에는 루트에 계획서 2종과 리뷰 2종이 더 있었는데,
> 넷 다 현재형으로 말하면서 실제로는 끝났거나 전제가 낡아 있었다(`CODE_REVIEW.md`는
> "테스트 파일 미발견"이라 적혀 있었다 — 지금은 테스트가 있고 CI가 게이트다).
> `docs/archive/`로 옮기고 각각에 무엇이 낡았고 무엇이 이어받았는지를 헤더로 달았다.
>
> **여기에 건수를 적지 않는 것은 일부러다.** 같은 문장이 `app_inspection_round2`에서
> 두 판 연속 낡았다(924 → 942 → 981 → …). 근거로만 인용되는 수는 적지 않고 **세는 법**을
> 적는다 — `grep -rho '@Test' app/src/test | wc -l`.

---

## 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.43 | 2026.08.10 | **`매칭` 표2 확정 반영 — 6장 색인에 R-48 등재**(마이그레이션 없음 · 코드 무변경). **3장 단일 소스 표에는 올리지 않았다** — 이 규약이 세우는 것은 *코드의 단일 소스*가 아니라 **용어의 뜻 하나**이고, 그것을 지키는 자리는 코틀린 객체가 아니라 검사 스크립트(R4)다. 3장에 올리면 *"어느 함수가 든다"*를 찾는 사람이 빈손으로 돌아온다. **R3와 나란히 읽히도록 색인 문구에 방향 차이를 적었다** — R3는 전역 금지, R4는 **영역 한정**이고, 둘을 같은 것으로 읽으면 `매칭`을 전역에서 지우려 든다(그것이 바로 이번에 재확정으로 뒤집힌 오독이다) |
| v1.42 | 2026.08.10 | **B-27 ② + B-38 반영 — 목록 셀의 쉼표**(**마이그레이션 없음** — DB를 건드리지 않았고 바뀐 것은 셀 안의 표기 규약뿐이다). 3장 단일 소스 표에 **`joinCsv` + `splitCsv`**, 6장 색인에 **R-47**을 등재했다. **함께 R-46도 등재했다 — 색인에 아예 없었다**(2026.08.10 B-106 ⓐ가 규약은 세우고 이 표에 올리지 않았다. R-47을 넣으면서 바로 옆자리가 비어 있는 것을 발견했고, **같은 표를 고치는 김이라 함께 채웠다**). **표에 올린 근거:** 결합과 분해는 갈릴 수 있는 두 자리인데 갈리면 **내보낸 파일이 스스로를 다시 읽지 못하고**, 그 파손은 *이름에 쉼표가 든 사용자에게만* 왕복 한 번 뒤에 나타나 **되돌릴 값이 이미 없다.** **옛 파일 처분이 이 항목의 절반이라는 것도 표에 적었다** — 저장·교환 형식 변경이라 되돌리기 어려운 축이고, 빠른 길을 옛 구현 그 자체로 두는 것이 그 처분이다. 가릴 수 없는 자리 하나(옛 파일에서 값 전체가 감싸인 `"전체"`)는 CSV 자체의 모호함이라 시험이 명시로 잠갔다 |
| v1.41 | 2026.08.09 | **B-108 반영 — 한도에 걸리면 다음 프로바이더가 이어받는다**(**마이그레이션 없음** — 프로바이더 설정은 Room이 아니라 SharedPreferences + `AiProviderCodec`이고, 새 칸 둘은 구버전 JSON에 없으면 `priority=0`·쿨다운 없음으로 읽혀 **옛 목록 순서 그대로**다). 3장 단일 소스 표에 **`AiProviderFallback`**, 6장 색인에 **R-44**를 등재했다. **표에 올린 근거가 이 판의 요점이다** — 전환 판정은 두 번 숨을 수 있었고 둘 다 나쁘다: 호출부 여섯에 두면 **한 벌만 빠뜨려도 그 기능에서만 조용히 종전처럼 죽고**, 관문 안에 인라인으로 두면 저장소·네트워크가 Context를 쥐어 **어떤 로컬 검증도 못 본다**(B-132가 난 그 자리). **가장 값진 잠금이 새 기능이 아니라 옛 기능에 있는 것**도 등재할 사실이다 — 순서가 활성을 앞에 두지 않으면 한도에 걸린 적도 없는 요청이 다른 모델로 나가는데 **화면에는 아무 표시도 안 난다**. **등재 문구와 실측이 갈린 자리가 하나 있었다** — 확정 7-1 ①이 *"429는 재시도 / 크레딧 소진은 전환"*이라 갈랐는데 그대로 읽으면 429가 끝내 전환하지 않아 **멀쩡한 둘째 키를 두고 실패를 받는다**(요청 원문이 없애 달라고 한 바로 그 상황이다). ①이 경계한 것은 *갈아치우고 **돌아오지 않는 것***이므로 **재시도를 먼저 세우고 10분 쿨다운으로 돌아오게** 해 세 문장이 동시에 참이 되게 했다 — 뒤집은 것이 아니라 읽기를 고른 것이고 근거는 `ai_integration`에 남겼다. **R-44는 R-31·R-43과 같은 계열이다** — 손으로 지키는 등재를 기계가 보는 부류이고, 셋 다 실패의 모양이 **침묵**이라 다른 검사가 원리적으로 못 본다 |
| v1.40 | 2026.08.09 | **B-104 소비처 ⓑ·ⓒ 반영 — 대결이 대표 그림을 뽑고 정리를 제안한다**(DB 버전 **56** — `duel_axes.isBasisAxis`, NOT NULL + DEFAULT 0 + 하네스 30건). 3장 단일 소스 표에 **`RepresentativeWeighting`·`DuelImagePrune`**, 6장 색인에 **R-43**을 등재하고 7장 대결 행을 갱신했다(**소비처가 전부 닫혔다**). **`RepresentativeWeighting`을 표에 올린 근거가 이 판의 요점이다** — 무게를 지어내지 않고 적합의 γ를 그대로 쓰는 것이 이 파일의 계약이고, **끊는 빗장**과 **`null`을 내는 조건** 둘이 그 계약을 지킨다: 안 끊으면 *확률*이 **고정**으로 변질되어 사용자가 물리친 안으로 되돌아가고, `null`을 안 내면 *"대결을 안 한 캐릭터는 지금과 완전히 같다"*가 깨진다(무게가 전부 1.0이어도 뽑는 방법이 나머지 연산에서 누적합으로 갈려 **같은 시드가 다른 그림을 고른다**). **NOT NULL + DEFAULT 0은 형제 칸들과 정반대다** — `candidateFiltersJson`·`sortDuelAxisCode`가 nullable인 것은 *없는 값*과 *빈 값*을 갈라야 해서이고, 이 칸은 **스위치라 중간 상태가 없다.** **세계관당 하나를 스키마가 아니라 쓰기가 지키는 것**도 등재할 사실이다 — *"켜진 것이 없음"*이 정상이라 유일 인덱스가 부분 인덱스여야 하는데 그 문법이 Room 대조와 어긋난다. 그래서 저장소를 지나지 않는 경로 둘(엑셀 가져오기·휴지통 복원)을 따로 막았고, **복원은 반대로 판정했다**(되살린 축이 살아 있는 기준을 훔치지 않고 그 사실을 고지한다). **R-43은 이 문서가 처음 싣는 "검증 도구" 규약이다** — 지키는 대상이 앱 코드가 아니라 **검증 도구 자신의 완전성**이고, 실패의 모양이 침묵이라 다른 어떤 검사도 원리적으로 못 본다 |
| v1.39 | 2026.08.09 | **B-123 반영 — AI 이름 추천**(DB 변경 없음, 마이그레이션 없음). **3장 단일 소스 표에는 올리지 않았다** — 새 순수 파일(`ai/CharacterNameAiSuggester`)의 소비처가 시트 하나뿐이라 *여러 자리가 같은 규칙을 써야 한다*는 그 표의 조건에 아직 닿지 않는다(B-124가 `NameBankMatch`·`NameBankPickOrder`를 올리지 않은 것과 같은 기준이다. **같음의 판정은 이 기능도 `NameBankMatch`를 쓴다** — 새 규칙을 만들지 않았다). 고친 것은 **6장 도구 표의 `check_ai_review_retention.sh` 행**이다: 그 검사가 이제 **이미지 시트 둘 + 이름 추천 시트**를 본다. **여기서 배울 것은 검사기의 자기 시험이다** — 새 항목 넷을 달고 하나씩 깨뜨려 보니 **둘이 안 잡혔다**(`onSaveInstanceState`는 접두만 맞아도, `val pool`은 지역 변수여도 통과). 규약(R-38)은 그대로이고 **넓힌 것은 그 규약을 지키는 기계의 사정 범위**다 — 규약으로만 적어 두면 다음 사람이 새 시트를 옛 모양으로 만든다(B-147이 커스텀 뷰 등재에서 실증한 그것). 설계·구현 기록은 `feature_roadmap_2026-08` 7장(7-6·7-7), 계약은 `ai_integration` **A-8** |
| v1.38 | 2026.08.08 | **B-168 반영 — 대결 후보 필터**(DB 버전 **55** — nullable 순수 추가 + 하네스 21건). 3장 단일 소스 표에 **`DuelCandidateFilter`**, 6장 색인에 **R-41**(B-169+B-170 — 편집 화면 폼 상태 한 벌)을 등재하고 7장 대결 행을 갱신했다. **`DuelCandidateFilter`를 표에 올린 근거가 이 판의 요점이다** — 조건 언어(`FieldFilter`)와 커스텀 대조(`FieldFilterHelper`)는 기존 것을 그대로 쓰고(두 벌 금지), 이 파일이 소유하는 것은 **id가 못 미더운 자리의 키 해석**과 **"참가자 = 후보 ∪ 전적 보유"의 조립**이다. 그 조립을 화면마다 적으면 한 화면이 비후보를 빼는 순간 그 판이 고아가 되어 **남의 점수까지 움직인다**(설계 9-3 — 저장하지 않는 설계에서 출구를 하나로 묶는 B-117의 그 규칙이 참가자 집합에도 적용된 것). **R-41은 이 문서가 처음 싣는 "화면 전용" 규약이다** — 지키는 것이 기계도 시험도 아니라 실기기뿐이라는 사실이 색인에 함께 실린다(어떤 로컬 검증도 세 그릇을 못 본다) |
| v1.37 | 2026.08.08 | **B-167 반영 — 시스템 열을 대결 축에**(DB 변경 없음, 마이그레이션 없음). 3장 단일 소스 표에 **`DuelSystemFields`**를 등재하고 6장 색인에 **R-40**을 올렸다. **새 파일을 세운 근거는 `DuelFieldLinks`를 넓히지 않은 이유와 같다** — 그쪽은 *걸린 것을 어떻게 견주는가*이고 이쪽은 *무엇을 걸 수 있는가*라, 합치면 **엑셀 왕복·예측 규칙이 열 목록과 함께 낡는다.** 적어 둘 사실 셋: ⓐ **막혀 있던 것이 타입이 아니라 소속이었고 그 구별이 처방을 정했다** — 걸러내기를 고치는 일이었다면 한 줄이었겠지만 **목록에 낼 행 자체가 없어** 줄의 타입을 갈라야 했다(`DuelFieldLinkAdapter`가 `FieldDefinition` 대신 `Choice`를 받는다. 가짜 `FieldDefinition`을 지어 끼우면 **그 가짜가 저장 경로로 새어 없는 필드가 DB에 생긴다**) ⓑ **어휘를 여는 판은 기존 어휘와의 관계를 규약으로 남겨야 한다** — `__`와 `sys:`는 겉모습이 같고 뜻이 정반대라, 적어 두지 않으면 다음 사람이 *둘 다 의사키인데*로 합친다(R-40) ⓒ **커스텀 뷰 프로브가 자기 구멍을 보였다** — 대상 목록에 `DuelFieldLinks.kt`가 있는데 그 파일의 새 의존을 따라가지 못해 **B-147이 없앤 가짜 오류 1건이 그대로 되살아났다.** `check_view_probe_targets.sh`는 `View` 상속 클래스의 등재만 보므로 **이 부류는 기계가 못 잡는다** — 프로브 대상 목록은 *파일*이 아니라 **닫힌 의존 집합**이라야 뜻이 있다. 절대값 0을 기대할 수 있게 된 덕에 눈에 띈 것이고, base 대 cur로만 보던 시절이었으면 묻혔다. **마무리 콜드 검토가 3장 행 하나를 더 넓혔다** — `DuelFieldLinks`에 **`effectiveOutcomes`**(걸려 있는 산출과 *일하는* 산출을 가르는 자리). 첫 구현은 그 판정을 **네 자리에 흩어 놓고 순위표에서만 걸러내** 일하지도 않는 연결이 **카드에서 이명을 감추고 없는 충돌을 경고했다** — B-131이 *"두 벌로 적는 구조가 원인이고 빠진 줄은 증상"*이라 적은 그 부류이며, **이번에는 그 두 벌을 판 자신이 만들었다.** → **새 판정을 들여오는 슬라이스는 그 판정의 소비처를 세어야 한다** (`grep '\.outcomes'`가 여덟 자리를 냈고, 첫 구현은 그중 하나만 고쳤다) |
| v1.36 | 2026.08.08 | **B-147 반영 — 코드가 아니라 *검증 도구*를 고친 슬라이스다**(앱 동작 무변경, DB 무변경, **앱 소스 변경 0줄**). 6장 도구 표에 **`tools/check_view_probe_targets.sh`**를 등재했다. **이 표의 다른 검사와 성질이 다르다는 것을 행에 적었다** — 나머지는 전부 *코드가 규약을 어겼는가*를 보는데, 이것은 ***도구 설정이 코드를 다 덮고 있는가***를 본다. 3장 단일 소스 표에 오를 것은 없다(판정 로직이 늘지 않았다). 적어 둘 사실 셋: ⓐ **실패의 모양이 침묵인 부류는 검출이 아니라 *열거*로만 잡힌다** — 커스텀 뷰 프로브의 대상 목록에서 빠진 뷰는 *통과*하는 것이 아니라 *들어가지도 않아서*, 오류 수가 늘지 않고 이름도 안 뜬다. **빠뜨릴수록 깨끗해 보이므로** 출력을 아무리 잘 읽어도 못 찾고, `) : View(`를 세어 목록과 대조하는 수밖에 없다. v1.32(B-146+B-145)가 *"네 겹이 전부 초록인 것이 결함 없음이 아니다"*를 적은 그 부류의 세 번째 사례이고, 셋 다 **값은 옳고 도구의 시야만 깨진** 자리였다 ⓑ **규약에 적는 것과 지켜지는 것은 다른 명제다** — R-31이 *"누락하면 조용히 검사에서 빠진다"*고 경고한 아래에서 둘이 닷새를 빠져 있었다. v1.30이 *"금지 문구가 있다는 것과 금지가 지켜진다는 것은 다른 명제"*라 적은 것과 같은 결론이며, 그때처럼 **처방은 문구가 아니라 기계**다 ⓒ **가짜 오류 하나가 가드 하나를 죽이고 있었다** — `DuelAxis` → `DuelFieldLinks` 미등재로 프로브가 늘 1건을 들고 있었는데, **오류가 있으면 코틀린이 코드 생성까지 가지 않아** 스크립트 말미의 *"오류 0인데 클래스 파일도 0"* 가드(2026-08-01의 함정 대비)가 **영영 뜰 수 없었다**. 없애니 절대값 0 · 클래스 215개가 됐다 — **잔여 노이즈를 "실제 결함이 아니다"로 분류해 두고 넘어가면, 그것이 무엇을 함께 가리고 있는지는 아무도 안 센다** |
| v1.35 | 2026.08.08 | **B-144 반영**(일괄 AI 태깅이 빈손으로 끝났을 때의 고지 — DB·단일 소스 무변경). 6장 도구 표의 **`check_ai_review_retention.sh` 행만 고쳤다** — 그 검사가 잠그는 축이 **둘이 됐다**: *이미 결제한 응답을 잃지 않는가*(회전·되받기·적용 실패)와 ***결과를 알 기회*를 잃지 않는가**(빈 결과의 침묵). 코드 쪽은 화면 한 줄이라 3장 단일 소스 표에 오를 것이 없다. **함께 고친 것이 이 판의 실제 소득이다 — 그 행이 항목을 ①~④로 옮겨 적고 있었고 이미 낡아 있었다**(B-163이 ⑤를 더했는데 여기는 넷에 멈춰 있었다. 하루 전 같은 검사의 *자기 목차*가 같은 이유로 낡은 것을 앞 판이 잡았는데, **이쪽은 그 사본이라 함께 낡았다**). 값을 지우고 **읽는 법**을 적었다 — 항목의 원문은 스크립트 머리의 *무엇을 보는가*다. 이 저장소가 `R-1~R-27`·`4종`·`16종`에서 **네 번 배운 것과 같은 처방**이고(값이 아니라 세는 법), *다른 문서에 옮겨 적은 목록*이 그중 가장 조용히 낡는 부류다 — **원문이 늘 때 사본은 아무것도 실패시키지 않는다** |
| v1.34 | 2026.08.08 | **B-141 반영**(AI 전송 준비기의 앱 저장소 봉쇄 가드 — DB 변경 없음). 3장 단일 소스 표의 **`ImagePathMatch` 행을 넓혔다** — 경로 *대조*에 **봉쇄 판정(`isInside`)**이 더해졌다. **새 파일을 세우지 않은 이유**는 둘 다 canonical 경로를 견주는 같은 부류라 떼어 놓으면 실패 처분이 따로 낡기 때문이고, 실제로 **한 파일 안에서 처분이 갈린다**(대조는 모르면 원본을 들고 가고, 봉쇄는 모르면 막는다) — 그 갈림을 파일 머리 규약과 함수 KDoc 양쪽에 적었다. 6장 색인에 **R-39**를 등재하고 도구 표에 `tools/check_ai_send_guard.sh`를 올렸다. **곁다리로 R-38이 6장 색인에 빠져 있던 것을 함께 채웠다** — 규약 원문(`conventions.md`)에는 있는데 색인에만 없어, *색인으로 규약을 세는* 경로에서 통째로 안 보였다(같은 표를 손대는 김에 고쳤다 — 착수 규칙 2번의 *범위 안*). **적어 둘 사실 둘:** ⓐ **순수 계층으로 가르는 것이 여기서는 편의가 아니라 규약의 일부다** — 준비기는 `BitmapFactory`·`Base64`를 써 순수 JVM 하네스가 **원리적으로 못 닿으므로**, 판정을 그 안에 적으면 경계 문자·`..` 이탈·심볼릭 링크 어느 것도 잴 수 없다(B-139가 `imageSystemRule`을 가른 것과 같은 이유) ⓑ **`Context`를 드는 대신 뿌리를 필수 인자로 받는 것**이 이 저장소가 두 판 연속 택한 모양이다 — 가드가 *기억해서 부르는 것*이 되는 순간 다음 소비처에서 조용히 빠지고, **빠진 것은 아무것도 실패시키지 않는다**(요청은 200으로 돌아온다) |
| v1.33 | 2026.08.08 | **B-136+B-140 반영**(AI 검토 시트의 유료 응답 보존 — DB 변경 없음). 3장 단일 소스 표의 **`ImageBatchTagSuggester` 행에 둘을 더했다**: **`mergeRetry`**(되받기를 앞 실행 위에 얹는 규칙)와 **`retryablePaths`**(다시 물을 값어치의 판정). **새 파일을 세우지 않고 기존 행을 넓힌 이유**는 둘 다 *번호가 어긋난 배치를 어떻게 처분하는가*의 뒷면이라 봉인 판정과 같은 자리에 있어야 하기 때문이다 — 떼어 놓으면 *접는 규칙*과 *되받는 규칙*이 따로 낡는다. 6장 도구 표에 **`tools/check_ai_review_retention.sh`**를 등재했다. **적어 둘 것은 이 슬라이스가 드러낸 계층 사고다**: 순수 계층이 `Result`에 *"끝난 배치의 제안은 살린다"*고 **선언까지 해 두었는데 화면이 그것을 뒤집고 있었다.** 규칙을 옳게 적어 두는 것과 그 규칙이 지켜지는 것은 다른 일이고, **화면 계층은 순수 JVM도 프로브(`ui/**` 제외)도 원리적으로 못 본다** — 그래서 시험(*규칙이 옳은가*)과 검사(*화면이 그 규칙을 지나는가*)를 갈라 두 벌로 잠갔다(B-135가 저장소에서 마주친 것과 같은 사각이며, 이번에는 그 사각이 **화면 쪽**이었다). **함께 등재하는 사실 하나** — 재생성된 시트의 처분이 **부류마다 다르다**: 이 저장소의 일곱 자리는 *주입 유실 시 안전 종료*가 옳고(다시 열면 그만), **유료 응답을 든 시트는 닫는 것이 곧 돈을 버리는 것**이라 `findFragmentByTag`로 다시 먹여야 한다(R-38) |
| v1.32 | 2026.08.07 | **B-146+B-145 반영 — 코드가 아니라 *검증 도구*를 고친 슬라이스다**(앱 동작 무변경, DB 무변경). 4장 검사 도구 표에 **`tools/check_no_nul_bytes.sh`**, 6장 색인에 **R-37**을 등재했다. **이 슬라이스를 대기열 맨 앞에 둔 근거가 그 성질이다** — 둘 다 *다른 모든 작업의 검출력*을 되돌리는 일이라 먼저 하면 이후 슬라이스가 그만큼 안전해진다. **B-146**: `PresetMerge.kt`의 날 NUL 하나가 그 파일을 git·grep 양쪽에서 텍스트가 아니게 만들어 `triage_unresolved.sh`의 선언 목록에서 **통째로 빠져 있었다**(고치니 589 → 590, 늘어난 하나가 정확히 `PresetMerge`다). **B-145**: 마이그레이션 54 하네스가 `PRAGMA foreign_keys=ON`을 켜 놓고 **자식 표를 하나도 싣지 않아** 자기가 켠 설정이 뜻하는 검사를 한 번도 못 했다(검사 19 → 36건). 적어 둘 사실 셋: ⓐ **네 겹이 전부 초록인 것이 "결함 없음"이 아니라는 명제의 가장 순수한 사례다** — 두 건 다 *값은 옳고 도구의 시야만 깨진* 부류라, 시험·정적 검사·CI 어느 것도 원리적으로 볼 수 없었다 ⓑ **하네스가 "안전하다"고 말할 때 무엇에 기대어 안전한지 적어야 한다** — 이 마이그레이션은 스스로 안전한 것이 아니라 *Room이 `onUpgrade` 동안 FK를 꺼 둔다*는 사실에 얹혀 있다. 그래서 **두 설정에서 각각** 재고 ON 쪽 유실을 수치로 남겼다(자식 7행이 오류 없이 전량 소실) ⓒ **손으로 적은 목록은 그 목록이 불완전할 때 아무 말도 하지 않는다** — B-145의 원인이 정확히 그것이라, 자식 표 집합을 `data/model` 선언에서 세어 대조하는 검사를 붙였다(다섯째 자식이 생기면 하네스가 실패한다) |
| v1.31 | 2026.08.07 | **B-150+B-132+B-151 반영**(활성 프로바이더가 옮겨 가지 않던 것 — 사용자 보고. DB 변경 없음). 3장 단일 소스 표에 셋을 등재했다: **`AiProviderCodec`**(설정 직렬화) · **`AiProviderActivation`**(저장이 활성을 옮기는가) · **`AiErrorText`**(오류 문구의 조립 차례). **셋을 한꺼번에 올리는 근거는 같다** — 전부 **Context 의존 클래스 안에 살던 판정**이라 이 저장소의 어떤 로컬 검증도 읽지 못했고, 셋 다 틀려도 컴파일과 정적 검사를 통과했다(「세션 착수 규칙」 4번이 말한 그 축이되 화면이 아니라 **저장·문구 계층**이다). 적어 둘 사실 둘: ⓐ **"두 줄이면 고친다"고 등재된 결함이 두 줄로 안 끝나는 경우가 있다** — B-132는 실제로 두 줄이었지만, 그 두 줄이 빠진 **이유**(직렬화를 아무도 못 보는 자리에 둔 것)를 그대로 두면 다음 학습값에서 같은 사고가 난다. 그래서 시험이 필드 목록을 **선언에서 읽게** 했다 ⓑ **표식을 호출부에 맡기지 않는다** — `AiErrorMessages.of`에 인자를 더하는 쪽이 값싸 보였으나 자리가 8곳이 되고, **빠뜨린 자리는 종전과 글자 그대로 같은 문구**를 내 고쳤다고 믿는 채로 안 고쳐진다(B-131이 *"두 벌로 적는 구조가 원인이고 빠진 줄은 증상"*이라 적은 그 부류다). 관문 하나가 새기게 하니 표식 없는 실패는 `NO_PROVIDER` 하나뿐이고, **그 사실 자체가 누락의 판별 기준**이 된다 |
| v1.30 | 2026.08.07 | **B-131+B-134+B-138 반영**(대결 등급 산정의 저장 배선 — DB 변경 없음). 3장 단일 소스 표에 **`DuelAxisChoice`**(축 고르기의 항목·초기 선택·댕글링 판정)를 등재했다. **이 파일을 표에 올리는 근거는 `DuelImageFit`·`DuelEntry`와 같은 부류다** — 틀려도 앱이 죽지 않고, 컴파일도 정적 검사도 통과한다. 다만 이쪽은 결과가 더 나쁘다: **다른 축의 순위가 이 필드에 배정되는 오배정**(R-1)이고, 사용자는 그것을 *채워진 값*으로 본다. 적어 둘 사실 둘: ⓐ **프레임워크의 기본 동작이 곧 코드다** — `AbsSpinner.setAdapter`가 스스로 0번을 고르므로 *"`setSelection`을 안 부른다"*는 선택 없음이 아니라 **0번 선택**이었다. 주석은 그 옆에서 정확히 그것을 금지하고 있었고, **금지 문구가 있다는 것과 금지가 지켜진다는 것은 다른 명제다** ⓑ **순수 계층 시험은 호출부가 무엇을 넘기는지 영원히 못 본다** — `DuelGradeAssignTest`는 B-138이 죽여 놓은 100% 가지를 처음부터 잠그고 **통과하고 있었다**(시험이 옳은 값을 직접 넘겨서다). 순수 함수에 *"모르면 이 값을 넘겨라"*류의 폴백 인자가 있으면 **호출부가 폴백만 넘기고 있지 않은지 grep으로 셀 것**(호출부 셋 중 둘이 그랬다) |
| v1.29 | 2026.08.07 | **B-130 반영**(전역 구역이 엑셀 왕복에 반쪽만 들어가 있던 것 — DB 변경 없음). 3장 단일 소스 표에 **`FieldScopeCell`**(빈 세계관 칸의 뜻)과 **`FieldDefinitionPrune`**(덮어쓰기 삭제의 근거 판정)을 등재했다. **둘을 표에 올리는 이유가 이 슬라이스의 요점이다** — 설계(로드맵 1-9)는 *"내보내기·가져오기·미리보기 셋이 같은 판정"*이라 적었는데 **구현은 둘뿐이었고**, 문서·주석이 *"내보내기가 그렇게 싣는다"*고 단언해 **그 단언을 믿으면 결함이 보이지 않았다**. 판정을 함수 하나로 모아야 *어디가 빠졌는지*를 셀 수 있다(R-33·R-34와 같은 처방). **`FieldDefinitionPrune`이 표에 오르는 근거는 다르다** — 그것은 계산이 아니라 **되돌릴 수 없는 삭제의 근거**이고, `CharacterFieldValue`가 `onDelete = CASCADE`인 데다 이 경로는 휴지통을 지나지 않아 **틀리면 값이 영구히 사라진다**. 적어 둘 사실 하나: **한 축(전역 구역)이 여러 경로에 걸릴 때 "하나 고쳤으니 됐다"가 가장 비싸다** — B-119가 연 축이 엑셀 내보내기·오버플로 가져오기 둘·미리보기 하나에 도달하지 않았고, 그 넷은 서로 다른 함수였다 |
| v1.28 | 2026.08.07 | **B-119 확장 반영**(전역 필드의 무소속 구역 — DB 54, **첫 표 재건**). 3장 단일 소스 표는 행을 더하지 않았다 — 전역 구역의 정의는 `FieldDefinition.universeId` KDoc(null의 뜻·유니크가 못 지키는 것·쓰기 경로가 심기 로직뿐임)과 `DefaultFieldTemplateRepository`(GLOBAL_SCOPE_NAME·globalFields의 멱등 폴백)가 들며, **둘 다 이미 표에 있는 자산의 확장**이다. 적어 둘 사실 하나: **재건 마이그레이션이 생겼으므로 "마이그레이션은 전부 순수 추가"라는 전제를 어디에도 새로 적지 말 것** — 54가 그 전제를 이미 깨뜨렸고, 다음 재건은 `verify_room_migration_54.py`가 본보기다(칸 단위 오배정 검사·id 보존·FK 생존) |
| v1.27 | 2026.08.07 | **B-121 구현 반영**(이미지 내용 일괄 AI 태깅 — 신규 7건 순서 ⑤). 3장 단일 소스 표에 셋을 등재했다: **`ImageTagVocabulary`**(두 태그 AI가 같은 어휘로 말하게 하는 자리 — `ImageFolderTagSuggester`의 companion에서 옮겨 왔다) · **`ImageBatchTagSuggester`**(번호 봉인과 **응답 index 검증**이 사는 자리) · **`ImageFilterHelper`**(태그 유무 축 `TagFilter`가 붙었다). **`ImageFilterHelper`를 이제야 표에 올리는 이유는 축이 하나 늘었기 때문이다** — 태그 유무를 `tags`(어느 태그인가)와 갈라 둔 것은 **없는 것에는 이름이 없어 칩으로 담을 수 없어서**이고, 그 구별이 흐려지면 *"태그 없는 것만"*이 *"필터를 안 걸었다"*와 섞인다. **`ImageBatchTagSuggester`가 표에 오르는 근거는 조립기가 둘이라서가 아니라 판정이 하나여야 해서다** — 한 요청에 여러 장을 실으면 응답의 태그가 남의 이미지에 붙을 수 있고, *어긋난 배치를 통째로 접는다*는 판정이 흩어지면 **절반만 믿고 붙이는 경로**가 생긴다(R-1) |
| v1.26 | 2026.08.07 | **B-122 구현 반영**(대결 프로필 + 화면 재설계 + B-114 흡수 — 신규 7건 순서 ②. DB 버전 **52**). 3장 단일 소스 표에 **`DuelCardInfo`**(카드가 무엇을 띄우는가)를 등재하고 **`DuelFieldLinks` 행을 함께 고쳤다** — 연결이 둘에서 **셋**이 됐고 새로 들어온 프로필은 *보이되 예측에 안 들어가는* 셋째 쓰임이다. 6장 색인에 **R-36**(없는 열과 빈 칸은 다른 사실이다)을 등재했다. **`DuelCardInfo`를 표에 올리는 이유는 빈 값의 처분이 두 구역에서 반대이기 때문이다** — 영향 필드는 줄이 남아야 두 카드의 자리가 맞고, 프로필은 빠져야 좁은 카드의 상한 넷이 헛돌지 않는다. 하나로 통일하는 것이 자연스러워 보이는 자리라 **시험이 없으면 다음 사람이 반드시 합친다.** **R-36을 규약으로 올린 근거도 이 슬라이스가 만들었다** — `프로필필드` 열을 더하는 순간 그때까지 내보낸 **모든 파일**이 *'그 열이 없는 파일'*이 됐고, 같은 자리를 보니 형제 칸 둘도 이미 같은 상태였다(아무도 겪지 않은 것은 그 열들이 시트와 함께 태어났기 때문일 뿐이다). **화면은 실기기 확인 전까지 완료가 아니다** — 3-61이 그 자리이며, 카드 구조가 통째로 바뀌었으므로 이 슬라이스에서 가장 못 미더운 축이다 |
| v1.25 | 2026.08.07 | **B-113 구현 반영**(대결 등급 산정 — 신규 7건 순서 ①). 3장 단일 소스 표에 **`DuelGradeAssign`+`DuelGradeRef`**(순위 → 등급의 계산과 config 표현)와 **`FieldConfigTransfer`**(경계 강등의 한 자리)를 등재하고, **`DuelScoreIndex`·`DuelAiContext` 두 행을 함께 고쳤다** — `rankWithinScored`가 그쪽 private에서 이쪽으로 올라왔기 때문이다(소비처가 둘이 되는 순간 각자 매기면 같은 캐릭터가 화면마다 다른 등수를 갖는다). 4장 검사 표에 `tools/check_config_boundary.sh`, 6장 색인에 **R-35**를 등재했다. **검사를 '0건 동결'이 아니라 'fail-closed'로 적은 것은 성질이 달라서다** — 이쪽은 위반 패턴을 세는 것이 아니라 *목록 밖의 새 호출자*를 막는다 |
| v1.24 | 2026.08.07 | **콜드 세션 시뮬레이션 검토가 잡은 둘을 정정**(병합 전 점검 — 신규 7건 로드맵 세션). ① 7장 신규 7건 행이 *"게이트는 Q1~Q7의 답"*·*"순서 초안"*으로 낡아 있었다 — 게이트는 2026.08.07에 열렸고 순서는 확정이다. **"완료 표시는 네 자리"(v9.92 관행 — 설계 정본·인수인계·아키텍처 지도·5장 블록) 중 이 지도만 빠져 있던 것**이다 ② *지금 할 일* 행이 여전히 **"(5장이 시작점)"**이라고만 말했다 — `CLAUDE.md` v1.9가 *그 문구가 콜드 세션을 이미 끝난 작업으로 보낸다*를 실증으로 기록하고 자기 지도를 🧭로 고쳤는데, **같은 지도의 사본인 이 문서 7장은 옛 문구 그대로**였다. 🧭·취소선 판별로 맞췄다 |
| v1.23 | 2026.08.06 | **7장 지도에 `feature_roadmap_2026-08` 등재**(신규 요청 5건의 설계 정본 — 코드 무변경 세션이라 2~6장은 손대지 않았다). 별도 행으로 세운 것은 이것이 **영역 하나가 아니라 다섯 영역(필드·AI·이미지·대결 둘)에 걸친 로드맵 문서**라 영역별 설계 행에 끼우면 찾는 경로가 갈리기 때문이다. **지도에 올리는 이유는 v1.13이 적은 그대로다** — 등재하지 않으면 진입점이 최신 설계를 모른다. 행에 **"구현 대기 · 착수 게이트는 그 문서 10장"**을 함께 적었다 — 등재된 행이 낡는 쪽 함정(v1.17 ②)을 피하려면 상태가 행에 있어야 한다 |
| v1.22 | 2026.08.05 | **B-104 소비처 ⓔ(AI 컨텍스트) 반영**(DB 변경 없음). 3장 단일 소스 표에 **`DuelAiContext`** 등재, 7장 대결 행 갱신(**소비처 ⓔ가 닫혔다** — 남은 것은 **ⓑ·ⓒ 둘뿐이고 그 둘은 같은 빗장 하나를 공유한다**. 실기기 확인 추가 없음 — 이 판은 화면을 하나도 바꾸지 않았다). **이 파일을 표에 올리는 이유는 조립기가 둘이기 때문이다** — 짧은 값 추천과 서술형 작성이 각자 자르면 같은 캐릭터가 경로에 따라 다른 축을 받는다. **그리고 그 안의 핵심 판정은 "등수를 다시 매기는 것"이다:** 순위표 줄의 등수는 한 판도 안 친 참가자까지 세는데 그들의 **앵커 1500이 점수 순 정렬에서 표 한가운데 앉으므로**, 그대로 옮기면 분모를 넘는 *"3명 중 4위"*가 나온다. **자기 재검토가 잡았고, 고치는 근거는 새 규칙이 아니라 설계 9-3이 통계 순위 탭에 이미 세워 둔 *"등수만 이 표의 것이다"*였다** — **같은 문서 안의 결론도 다른 소비처에 옮길 때 다시 적용해야 한다는 것이 이 판이 남기는 교훈이다.** 프롬프트 문자열은 **어떤 로컬 검증도 읽지 않으므로**(컴파일도 정적 검사도 통과한다) 시험이 유일한 방어선이라는 점도 함께 적어 둔다 |
| v1.21 | 2026.08.05 | **B-117 목록·통계 편입 반영**(DB 버전 **51** — `character_list_presets.sortDuelAxisCode`, nullable 순수 추가). 3장 단일 소스 표에 **`DuelScoreIndex`·`RankingSource`** 등재, 7장 대결 행 갱신(**소비처 ⓓ가 닫혔다** — 남은 것은 ⓑ·ⓒ·ⓔ, 실기기 확인에 **3-59** 추가). **`DuelScoreIndex`를 표에 올리는 이유가 이 판의 요점이다** — 설계 4장은 *"파생값을 저장하지 않는다. 저장하면 두 자리가 갈라진다"*고 못 박았는데, **소비처가 셋이 되니 저장하지 않아도 갈라질 수 있었다**(순위표·목록·통계가 각자 적합하면 옵션 하나에 같은 캐릭터의 점수가 달라진다). **저장 금지는 규칙의 절반일 뿐이고 나머지 절반이 "출구를 하나로 묶는 것"이다.** **`RankingSource`가 표에 오르는 이유는 다르다** — 그것은 계산이 아니라 **화면이 위치로 되짚는 목록의 차례**이고, 화면 안에서 셈하면 「세션 착수 규칙」 4번이 말한 *보이지 않는 자리*가 된다. **함께 등재하는 판정 하나:** `scalability_performance` 7장 4단계 3번이 처음 발동했고 답은 **"목록은 더하되 계산은 더하지 않는다"**였다 — 스냅샷에 싣는 것을 *날것이냐 파생이냐*로 묻지 말고 **무엇을 실으면 순회가 따라오는가**로 물을 것 |
| v1.20 | 2026.08.04 | **B-112 범주 집계 반영**(DB 변경 없음). 3장 단일 소스 표에 **`DuelCategoryStats`** 등재, 7장 대결 행 갱신(**소비처 ⓐ가 닫혔다** — 남은 것은 ⓑ~ⓔ, 실기기 확인에 **3-58** 추가). **이 계산을 단일 소스 표에 올리는 이유는 그것이 *무엇을 세는가*가 아니라 *무엇과 견주는가*이기 때문이다** — 승패를 그냥 세면 *"불이 물에 약하다"*와 *"물을 든 캐릭터가 세다"*가 **섞이고**, 그 둘을 가르는 것은 모델의 예상 승수와의 뺄셈뿐이다(설계 3장이 상성을 50%가 아니라 예상과 견준 것과 같은 자리). **눈금을 새로 만들지 않은 것도 같은 부류다** — `minSamples` 3과 `zThreshold` 2.0을 `DuelCounterRelations.Options`에서 그대로 가져왔고, 그러지 않으면 **두 화면이 같은 말(*"이것은 상성이다"*)을 다른 기준으로 하게 된다.** **하네스가 못 박은 계약 둘이 이 등재의 값어치다:** *물이 불을 늘 이긴다*는 **잡히지 않고**(개체의 강함으로 완벽히 설명되므로 조용한 것이 옳다) `불>물>풀>불`은 **잡힌다**(1차원 점수가 원리적으로 못 만드는 모양이라 잔차가 남는다). **전자를 시험으로 적은 것이 요점이다** — 표에 12승 0패가 적힌 채 *"예상과 비슷하다"*고 말하니 결함처럼 보이고, 시험이 없으면 다음 사람이 z를 걷어내 이 표를 *누가 센가*로 되돌린다(그 답은 순위표가 이미 한다) |
| v1.19 | 2026.08.04 | **B-104 층 C·기록 화면·엑셀 왕복 반영**(사용자 요청 여섯. DB 버전 **50**). 3장 단일 소스 표에 **`DuelFieldLinks`·`DuelMatchLog`·`DuelImageFit`** 등재, 7장 대결 행 갱신. **`DuelFieldLinks`를 표에 올린 이유가 이 슬라이스의 요점이다** — 축↔필드 연결은 화면(값을 띄운다)·엑셀(사람이 고친다)·DB(저장 형식) 세 곳이 함께 읽는데, 해석을 각자 두면 *"S-1"*을 −1로 읽는 부류의 어긋남이 자리마다 다르게 생긴다. **`DuelImageFit`은 성격이 다른 등재다** — 그 계산이 틀려도 앱은 죽지 않고 **그림만 조용히 어긋나게 놓인다**(사용자 지적으로 발견된 결함이고, 「세션 착수 규칙」 4번이 말한 *보이는 것을 못 보는* 축의 전형이다) |
| v1.18 | 2026.08.04 | **B-104 데이터 계층 반영**(세션 로그 1-cb. DB 버전 **49**). 2장 데이터 모델에 **대결 묶음**(`DuelAxis`·`DuelMatch`·`DuelCounterVerdict`) · 3장 단일 소스 표에 **`DuelRecords`**(저장된 기록과 순수 계층 사이의 해석) · 7장 대결 행을 **데이터 계층 완료**로. **단일 소스 표에 `DuelRecords`를 올린 이유가 이 슬라이스의 요점이다** — 판은 참가자를 **코드로** 가리키고(id로 담으면 복원이 재발급한 id 때문에 판이 남의 캐릭터에 붙는다 — R-1) 순수 계층은 `Long` id로 도는데, **그 해석을 저장소에 두면 이 저장소의 어떤 로컬 검증도 규칙을 볼 수 없다.** 특히 *"사라진 참가자에게도 id를 주되 참가자 목록에는 넣지 않는다"*(고아를 세는 방법)는 걸러 넘기면 조용히 0이 되는 부류라, pure로 두고 실행으로 고정했다 |
| v1.17 | 2026.08.04 | **7장 지도의 빈자리 둘을 메웠다**(병합 전 점검 — 세션 로그 1-bz). ① **`judgment_confirmations_2026-08.md`가 지도에 없었다** — `CLAUDE.md` 지도에는 있는데 **이 문서에는 없어서**, *지형 파악* 진입점으로 들어온 세션은 *"무엇이 이미 확정됐는가"*에 닿을 길이 없었다. 착수 규칙 3번이 *"사용자 확정을 뒤집지 말 것"*을 질문 기준으로 삼는데 **확정이 어디 있는지를 지도가 말하지 않으면 그 규칙이 지켜질 수 없다**(CLAUDE.md v1.8이 자기 지도에 등재하며 적은 것과 같은 이유). ② **체형 행이 *"설계 마무리 — 코드 구현 대기"*로 낡아 있었다** — v1 범위는 **2026.08.02에 닫혔고**(설계 10장 1~7단계), 그 행이 가리키던 `handover_body_reauthor_2026-08`은 2026.08.01 시점에 멈춘 채 스스로 *"다음 세션이 처음 읽는 파일"*이라 선언한다. **그대로 두면 착수 세션이 이미 끝난 작업을 후보로 올린다** — 행을 *v1 완료 + 남은 것은 v2·v3*로 고치고, 그 인수인계에는 역할 종료 헤더를 달았다. **v1.13이 *"지도에 새 설계를 등재하지 않으면 진입점이 최신 설계를 모른다"*고 적은 것의 짝이다** — 이번 것은 **등재된 행이 낡는** 쪽이다 |
| v1.16 | 2026.08.04 | **R-34 등재**(B-100 완성도 가중) — 6장 규약 색인과 검사 도구 표 양쪽. *"같은 이름의 지표는 한 함수가 낸다"*이며, 어기면 `tools/check_completion_rate.sh`가 막는다. **이 문서가 규약 색인이므로 R-34도 여기서 찾을 수 있어야 한다**(아래 v1.15가 R-33에 대해 적은 것과 같은 이유). 등재의 계기는 필드 완성도가 **열 자리에서 분자·분모·임계값이 갈려 있던 것**이고, 그중 분자의 갈림은 `CharacterFieldValueMerge`가 **일부러 남기는** 보존 값을 세어 **완성도를 100% 넘게 만들 수 있었다**. (곁다리: 이 표에 **`v1.15`가 둘**이다 — 슬라이스 밖이라 고치지 않았고 **B-109**가 이미 '같은 번호가 둘인 자리'로 등재해 두었다.) |
| v1.15 | 2026.08.03 | **B-107 뗀 이미지 관리 구현 반영**(세션 로그 1-bu). 3장 단일 소스 표에 `DetachedImageRule`/`DetachedImageMarker`(뗀 것 판정)와 `ImageDeletionService`(삭제 처분) 등재 · 7장 지도에서 B-107을 **구현 완료**로 옮기고, **역참조 둘이 "바꿀 예정"에서 "갱신됐다"로 바뀐 사실**을 함께 적었다(`image_folder_roundtrip_design` 3장·`image_folder_tag_ai` 2-1 — 그 표들은 이제 `_분리됨`·`_삭제승인`을 담은 현행이다). 예약 폴더가 **넷에서 여섯**이 됐다 |
| v1.14 | 2026.08.03 | **B-103 대표 이미지 구현 반영**(세션 로그 1-bs). 3장 단일 소스 표에 `CharacterRepresentativeImage`/`ImagePathMatch`(대표 판정·경로 대조)와 `RepresentativeImageCell`(엑셀 열 규약) 등재 · 4장 검증 도구에 `check_image_pointer.sh` · 6장 색인에 **R-32**(포인터는 목록을 고치는 모든 경로가 함께 고친다) · 7장 지도에서 B-103을 **구현 완료**로 옮기고 **B-107 행에 "선행이 끝나 지금이 착수 자리"를 적었다** — 그 행이 순서 근거를 담고 있는데 선행 상태가 바뀐 것을 말하지 않으면 다음 세션이 다시 대조해야 한다 |
| v1.15 | 2026.08.03 | 7장 영역별 설계에 **`restore_preview_parity_2026-08` 등재**(B-101+B-102 복원 미리보기 정합). **이 문서가 규약 색인이므로 R-33도 여기서 찾을 수 있어야 한다** — 그 규약은 `analyze*`와 `import*`가 같은 함수를 부르게 하고, 어기면 `tools/check_restore_preview_parity.sh`가 막는다. 선행 B-87(세력 소속·세력 관계 매처)이 **규약으로 올라가지 않아 나머지 열다섯이 그대로 갈려 있었던 것**이 이 등재의 이유다 |
| v1.13 | 2026.08.03 | 7장 영역별 설계에 **설계 문서 둘 등재** — `representative_image_design_2026-08`(B-103 대표 이미지)와 `detached_image_management_design_2026-08`(B-107 뗀 이미지 관리). 둘 다 **설계 확정·구현 대기**다. **B-107 행에 상호 참조를 함께 적었다** — 그 설계가 **폴더 왕복 규약을 바꾸므로**(`_삭제승인` 신설 · `_미배정/` 재정의) `image_folder_roundtrip_design` 3장과 `image_folder_tag_ai` 2-1에 **역참조를 걸었고**, 이 지도가 그 사실을 알아야 반대쪽에서 들어온 세션이 찾는다. 지도에 새 설계를 등재하지 않으면 **진입점이 최신 설계를 모른다** — 이번 인수인계 점검이 그것을 잡았다 |
| v1.12 | 2026.08.01 | 7장 영역별 설계에 **`backup_export_redesign` 등재**(백업·내보내기 동선 재편 — 실기기 확인 중 사용자가 실제로 막힌 백업 동선의 재편, 사용자 확정으로 체형 다음 슬라이스) + 체형 행 상태를 "이식 목업 완료 — 검증 대기" → "설계 마무리 — 코드 구현 대기"로(판정 6건 확정 반영) |
| v1.11 | 2026.08.01 | 7장 지도 갱신 — 점검 결과 행에 **`plan_design_adversarial_review_2026-08` 등재**(전 문서 적대 검토, 세션 로그 1-ab), 체형 행을 "설계 확정 — 구현 대기" → **"이식 목업 완료 — 사용자 검증 대기"**로, 이어받는 진입점(`handover_body_reauthor_2026-08`)을 지도에 명기(그 문서가 지도 밖이라 3홉을 밟아야 닿던 것 — 검토 F-2) |
| v1.10 | 2026.07.31 | 7장 영역별 설계에 **체형(실루엣 재편) `body_visual_redesign`** 등재 — 설계 확정·구현 대기(사용자 확정 다음 작업) |
| v1.9 | 2026.07.31 | **문서 감사 반영** — 2장 지도(DB 버전·파일 수 값 제거, 세는 법 위임), 3장 GradeSystemRef 행(R-30 예외 명시 — 프룬·신설 삽입 경로), 4장 하네스·리셋 행(종수·건수 → 스크립트 출력 위임), 5-1 실측표(값 → 세는 명령 3종 병기), 5-4 manifest 버전(코드 참조로), 7장 절차서 행(하네스 종수 제거) |
| v1.8 | 2026.07.30 | **U-1 세계관 등급 체계 반영**(세션 로그 1-y장) — 2장 데이터 모델에 `GradeSystem` 등재(DB v46), 3장 단일 소스 표에 `GradeSystemRef` 등재(참조·재정의·실효 표의 config 표현 — 물질화 구조라 `GradeValueResolver` 소비자는 무변경), 6장 색인에 **R-30** 등재(물질화된 파생값의 원본 편집은 파생값 재작성과 한 트랜잭션) |
| v1.7 | 2026.07.30 | **확-3 작품 축이 열려 5-2 확장점을 갱신**(종류가 셋이 됐다 — `character`·`event`·`novel`, 세션 로그 1-u장). 전수 대상이 `getFieldsByUniverse*`만이 아니라는 것을 명시했다(`getAllFieldsList`·`getFieldByKey`·`getGroupNames`·`countFieldsByKeyExcluding`도 같은 기본값을 갖는다). **한 종류를 여는 데 드는 자리를 목록으로** 남겼다 — 다음 종류(세계관·세력)의 견적이 추측이 아니라 실측에서 나오게. 아울러 **"순서가 열보다 위험하다"**를 경고로 넣었다(작품 시트가 필드 정의보다 먼저 처리돼 값이 버려질 참이었다). 3장 단일 소스 표에 `NovelFieldValueMerge`·`EntityFieldHeaders` 등재, 2장 데이터 모델에 `NovelFieldValue` 등재. **엔티티 수 '26'은 값을 지우고 세는 법으로 바꿨다** — 이 문서가 1장·7장에서 이미 두 번 쓴 처방이고, 실제로 v45가 표를 하나 더한 순간 낡았다 |
| v1.6 | 2026.07.30 | **6장 색인에 규약 R-29 등재**(`entityType`으로 갈리는 기능은 조회·중복 판정·순서·쓰기가 모두 같은 종류를 본다 — P5, 세션 로그 1-p장). 아울러 **R-28 행이 셀 하나가 빠진 채였다** — 3열 표에 2열로 들어가 렌더가 어긋났다(신설 시 영역 칸을 빼먹었다). `저장·UI`로 채웠다: 바로 옆에 행을 더하면서 깨진 행을 두는 것이 더 나쁘다 |
| v1.5 | 2026.07.30 | **7장 문서 지도에 `docs/session_log_2026-07.md` 등재**(`remaining_work` 1장이 분할됐다 — 근거는 그 문서 5-a 3번). 지도 행에 **"다른 문서의 `1-x장`은 전부 이곳"**을 함께 적었다: 절 번호를 보존한 분리라 참조 문자열만으로는 어느 문서인지 알 수 없다. 아울러 **`R-1~R-27` 범위 표기 2자리를 없앴다**(6장 제목·7장 지도) — R-28이 등재되며 낡았고, 삼자 일치 자체는 {1..28}로 정상이었으니 **틀린 것은 상한을 적었다는 사실**이다. 이 문서가 1장·2장에서 이미 두 번 쓴 처방(값이 아니라 세는 법 / 표가 곧 현행)을 규약 범위에도 적용했다 — **표가 현행 범위다.** 3장 단일 소스 표의 `1-h장` 참조도 새 문서를 가리키도록 정정 |
| v1.4 | 2026.07.29 | **7장 각주의 테스트 건수를 없앴다** — "지금은 924건이 돈다"는 `app_inspection_round2`가 같은 자리에서 두 판 연속 낡은 것과 **같은 문장**이었고, 여기서도 이미 스테일이었다(실측 984). 갱신이 아니라 제거를 골랐다: 이 자리의 수는 *근거로만 인용되는 수*라 값이 아니라 "테스트가 있다·CI가 게이트다"가 요지이고, **고칠 수 없는 것은 낡지도 않는다.** 대신 세는 법(`grep -rho '@Test' app/src/test \| wc -l`)을 적었다. *재현 기준선으로 쓰이는 수*(`remaining_work` 2장·5장)는 용도가 달라 그대로 둔다. 근거: `remaining_work` 5-a |
| v1.3 | 2026.07.29 | **규약 색인이 가리키는 곳을 `docs/conventions.md`로 변경.** 6장 색인 자체는 그대로이고 **정의 원문의 위치만** 바뀌었다(`remaining_work` 1장 → 별도 문서, 근거는 그 문서 5-a). 색인이 이미 "행 번호가 아니라 제목으로 찾으라"고 안내하던 덕에 링크 한 줄 교체로 끝났다 — 각 규약이 이제 `## R-N.` 절이라는 사실만 덧붙였다. 7장 문서 지도에 새 문서 등재. 검증: 색인 번호 집합 = 원문 정의 집합 = 저장소 전체 참조 집합 = {1..27} 삼자 일치 |
| v1.2 | 2026.07.29 | **앞 커밋이 절반만 고친 수치 정정 + 계층 설명의 사실 오류 정정.** ① 4장 러너 기준선이 **896으로 남아 있었다**(1장만 924로 고쳐졌다 — 같은 문서 안에서 두 수치가 갈렸다) ② 규모 실측 현행화(328파일·87,108줄 → **332·88,420**, util 59→60·excel 22→24·ui 123→124) ③ **마이그레이션 "44개" → 43개**(v1→v44는 43단계다) ④ **`util/`을 "순수 계층 — Android 비의존"이라 단정하던 것 정정** — 60개 중 **22개가 Android 의존**이며 JVM 하네스가 컴파일하지 않는다(디렉터리가 아니라 `$SOURCES` 목록이 판정한다) ⑤ 테스트 파일 71 vs 러너 실행 70의 차이 명시(`AiPresetsConsistencyTest`는 CI 전용) ⑥ B-55 생문자열 분기 23→**28** 재실측. 규약 **R-27**(검증 실패는 창을 닫지 않는다) 등재 + `tools/check_dialog_validation.sh` 검증 체계 표 등재 |
| v1.1 | 2026.07.29 | 수치 현행화(테스트 896건·70파일) · 단일 소스 표에 `ImportSource`·`ResetPlan` 등재 · 검증 체계 표에 python 하네스 4종 추가 · **문서 지도의 누락 8종 등재**(특히 `excel_streaming_import` — 바로 다음 작업의 설계 단일 소스인데 지도에 없었다) |
| v1.0 | 2026.07.28 | 최초 작성 — 진입점 부재를 메운다. 현행 규모 실측(328파일·87,108줄·DB v44·엔티티 26), 계층·데이터 모델 지도, 단일 소스 표, 검증 체계, 확장점 4종, 규약 R-1~R-26 색인, 문서 지도 |
