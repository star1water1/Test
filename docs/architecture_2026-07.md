# 아키텍처 개관 — 구조·단일 소스·확장점 (2026-07)

이 문서는 현행 코드 구조와 확장점, 규약 색인의 지도다.
작업 시작과 공통 절차는 [AGENTS.md](../AGENTS.md), 현재 작업·대기 조건은
[status.md](status.md)에서 확인하고, 이 문서는 해당 영역의 절만 읽는다.
규약 정의는 `docs/conventions.md`, 성능·확장 한계는 `docs/scalability_performance_2026-07.md`가 정한다.

---

## 1. 현행 규모 — 세는 법

**값을 적지 않는다.** 이 표는 두 판 연속 낡았고(v1.2가 고친 수를 v1.4 이후 다시 벌어졌다),
그때마다 정정 문장이 하나씩 늘었다. 규모는 **근거로만 인용되는 수**이므로
규모 수치는 현재 코드에서 세는 명령으로 확인한다.

| 항목 | 세는 법 |
|------|---------|
| 메인 소스 | `find app/src/main/java -name '*.kt' \| wc -l` · 줄은 `-exec cat {} + \| wc -l` |
| 테스트 소스 | 파일은 `find app/src/test -name '*.kt' \| wc -l` · 실행 건수는 `tools/run_jvm_tests.sh` 출력 |
| Room DB | `data/database/AppDatabase.kt` — `@Database(version=…)` · `@Entity` 개수 · `MIGRATION_*` 개수 |
| 사용자 문구 | `res/values/strings.xml` 단일 파일 · 검사는 `tools/check_text_style.sh` |

검증 범위는 `AGENTS.md`의 「검증」, 명령과 최근 기록값은 `docs/status.md` 4장을 따른다.
이 문서에 별도의 착수 절차나 실행 기준선을 두지 않는다.

> **테스트 파일 수 ≠ 러너가 도는 파일 수 — 차이는 1파일·3건이다.**
> `AiPresetsConsistencyTest`(3건)는 `R`을 참조해 순수 하네스에서 돌 수 없어 목록에서
> 빠져 있다(CI 전용). 그래서 소스의 `@Test` 합과 실행 건수가 **3만큼** 다르다 —
> 이 차이를 결함으로 오인하지 말 것. 러너는 `$TESTS` 목록에 적힌 파일만 컴파일하므로
> **목록에 넣지 않은 새 테스트는 조용히 돌지 않는다**(5장 착수 시 확인 항목).

---

## 2. 계층 지도

```
app/src/main/java/com/novelcharacter/app/
├── data/            DB 계층 — 여기가 진실의 원본
│   ├── model/         엔티티 + 설정 값 객체(FieldStatsConfig·DisplayFormat·SemanticRole …)
│   ├── dao/           Room DAO
│   ├── repository/    트랜잭션 경계 · 휴지통 · 스냅샷 복원
│   └── database/      AppDatabase + 마이그레이션 (버전·개수는 1장 세는 법이 든다)
├── util/            ★ 순수 계층이 사는 곳 — 다만 전부가 순수하지는 않다(아래 ⚠️)
├── excel/           엑셀 왕복 (내보내기·가져오기·시트 규약)
├── share/           월드패키지(ZIP) 내보내기·들이기
├── ai/              프롬프트 조립 · 응답 해석 · 정책
├── backup/          자동 백업 워커
├── widget/          홈 위젯
└── ui/              화면 — character · stats · adapter · image …
```

> **파일 수를 적지 않는 것도 일부러다.** 이 지도의 값어치는 "무엇이 어디 사는가"이지
> "얼마나 큰가"가 아니고, 괄호 안 수는 한 세션 만에 넷이 어긋났다(util·ai·ui·image).
> 규모가 필요하면 세면 된다 — `find app/src/main/java/com/novelcharacter/app/<패키지> -name '*.kt' | wc -l`.
> (`ui/character`처럼 하위 패키지를 가진 곳은 **무엇을 세는지 기준부터 정할 것** —
> 종전 값 22는 `ui/character/batch/`를 뺀 최상위만 센 것이었는데 그 사실이 적혀 있지 않아
> 재현이 불가능했다.)

**의존 방향은 한 방향이다:** `ui → repository → dao → Room`, 그리고 모든 계층이 `util`을
바라본다. `util`은 아무도 바라보지 않는다 — 그래서 이 저장소에서 실행 검증이 가능한
거의 유일한 계층이다(4장).

> **그 '아무도'가 2026.08.19까지는 참이 아니었다** (B-251 ⓓ). `util/ConsistencyChecker`가
> `ui.stats.StatsSnapshot`을 import 해 **`util` → `ui`, 이 방향의 유일한 역방향**이었다.
> **대가는 문장이 아니라 검증이었다:** 프로브가 `ui`를 통째로 빼므로 그 파일은 **목록에
> 있으면서도 타입 검사되지 않았고**(오류 51 — 전부 그 한 줄의 그림자), 순수 하네스도 실을 수
> 없어 **시험이 0건**이었다. 스냅샷을 `util/StatsSnapshot.kt`로 옮기니 둘 다 닿는다
> (프로브 615 → 564 · 시험 +19).
> **남은 역방향은 `util/AlertDialogExt` 하나이고**(`ui.common.CappedScrollView`) 그쪽은
> Android 뷰에 묶인 파일이라 성격이 다르다 — B-251 ⓑ가 *열지 않기로* 닫은 갈래다.
> **세는 법:** `grep -rn '^import com.novelcharacter.app.ui' app/src/main/java/com/novelcharacter/app/util/`

> ⚠️ **`util/` = 순수 계층이 아니다. 디렉터리가 아니라 파일 단위로 갈린다.**
> 일부는 Android·Material에 의존한다(`AlertDialogExt`·`ProgressDialogs`·
> `CharacterImageLoader`·`*Prefs`·`ThemeHelper` …). 이들은 JVM 하네스가 컴파일하지 않으므로
> **`util/`에 넣었다는 사실만으로 "테스트된다"고 가정하지 말 것.**
> 세는 법: `grep -rl '^import android\.\|^import androidx\.' …/util | wc -l`
>
> **그런데 'Android 비의존'과 '테스트된다'도 같은 말이 아니다 — 종전 서술은 이 둘을 붙여
> 놓아 틀렸다.** 순수한 파일이라도 러너의 `$SOURCES` 목록에 없으면 **컴파일조차 되지 않는다.**
> 실제로 순수한데 목록에 없는 파일이 여럿 있고, 그중 `FieldRandomGenerator.kt`는
> **5-1장이 "새 필드 타입 착수 시 반드시 훑을 것"으로 지목한 확장점인데 테스트가 0건**이다.
> **판정법은 하나뿐이다 — `tools/run_jvm_tests.sh`의 `$SOURCES` 목록에 그 파일이 있는가**(4장).
> 순수함은 테스트의 필요조건이지 충분조건이 아니다.

### 데이터 모델 — 엔티티 (세는 법: `grep -c '@Entity' app/src/main/java/com/novelcharacter/app/data/model/*.kt`)

| 묶음 | 엔티티 |
|------|--------|
| 뼈대 | `Universe` → `Novel` → `Character` |
| 사용자 정의 필드 | `FieldDefinition` · `CharacterFieldValue` · `EventFieldValue` · `NovelFieldValue` · `FieldValueEntry`(값 라이브러리) · `GradeSystem`(세계관 등급 체계 — U-1) |
| 시간 | `TimelineEvent` · `TimelineCharacterCrossRef` · `TimelineEventNovelCrossRef` · `CharacterStateChange` |
| 관계 | `CharacterRelationship` · `CharacterRelationshipChange` · `Faction` · `FactionMembership` · `FactionRelationship` |
| 이미지 | `ImageMeta` · `ImageTag` |
| 부가 | `CharacterTag` · `NameBankEntry` · `SearchPreset` · `CharacterListPreset` · `UserPresetTemplate` · `RecentActivity` |
| 대결 (B-104) | `DuelAxis`(세계관 단위 축) · `DuelMatch`(한 판 — **참가자를 코드로 가리킨다**) · `DuelCounterVerdict`(층 B의 사용자 처분) |
| 안전망 | `TrashSnapshot` · `OperationLog` |

**`FieldDefinition`이 이 앱의 심장이다.** "도구를 만드는 도구"(CLAUDE.md 핵심 철학)라는 성격이
전부 여기서 나온다 — 필드의 타입·파싱 방식·통계 편입 여부·AI 정책·표시 형식이 모두
`FieldDefinition.config`(JSON)에 사용자 설정으로 들어간다. 그래서 **이 앱의 확장은 코드가
아니라 데이터로 일어난다.**

---

## 3. 단일 소스 지도 — 이 저장소의 핵심 자산

`util`의 순수 객체들은 "편의 함수 모음"이 아니라 **도메인 규칙의 유일한 정의 자리**다.
같은 규칙이 여러 화면에 복제되면 반드시 갈라진다는 것을 이 저장소는 여러 번 값비싸게 배웠다
(U-9: 수식 서식이 7곳에 복제돼 로케일과 오류 처리가 제각각이었다).

**규칙: 아래 표에 있는 판단을 호출부에서 다시 구현하지 않는다.**

| 단일 소스 | 무엇의 유일한 정의인가 | 어긋났을 때 생긴 일 |
|-----------|------------------------|---------------------|
| `FieldValueTokenizer` | 필드값 문자열 → 토큰 분리(통계·폼 자동완성·값 라이브러리·검색) | 순위 탭이 자체 분리를 써서 인사이트와 수치가 달랐다(S-18). **2026.08.11(B-178)에 쉼표 규칙을 `CsvTokens`에 넘겼다** — 손으로 쪼개는 자리가 화면 둘·AI 하나에 더 있었고, 그중 하나는 *붙이기만 새 규칙*이라 값을 망가뜨릴 참이었다 |
| `CsvTokens` | **쉼표로 갈리는 값의 규칙 — 앱 전체의 단일 소스**(엑셀 목록 셀 R-47 + 인앱 필드값 + **식별자 셀**). 감싸기·되쪼개기·전각 정규화가 여기 하나이고, **읽기는 축이 둘이다** — *따옴표를 어디까지 인정하는가*(`quotedOnlyIfNeeded`)와 *전각을 고쳐 읽는가*(`normalizeFullWidth`). **둘이 한 인자에 묶여 있던 것이 B-261이다** | 같은 개념이 두 규칙이었다(B-178). **정의가 `util`에 있는 것이 요점** — `excel/`에 두면 `util`이 `excel`을 바라보게 되어 의존 방향이 뒤집힌다. `joinCsv`/`splitCsv`는 위임으로 남아 호출부는 무변경. **인정 조건이 갈린다**: 셀은 CSV 관용 그대로, 인앱 값은 *우리가 감쌌을 값*일 때만. **2026.08.16(B-207)에 마지막 예외가 들어왔다** — `Character.aliases`만 맨 `split(",")`이라 같은 이명이 대결 카드에서는 두 조각, 통계·AI 문맥에서는 세 조각이었다. 소비처가 넷이어도 **고칠 자리는 속성 하나**였고(넷이 전부 그 속성을 읽는다), 잠그는 것은 값이 아니라 **두 경로의 답이 같은가**다(`DuelSystemFieldsTest`) |
| `MultiValueInput` | **다중값 입력칸의 채우기·되쪼개기 짝**(태그·이명·별칭·SELECT 선택지·대결 후보 필터 값). `format`/`parse` 둘뿐이고 규칙의 정의는 [`CsvTokens`]가 그대로 든다(이 둘은 위임) | **규칙이 이미 있었는데도 결함이 살아 있던 자리다**(B-186). `CsvTokens`가 규칙을 들고 `FieldValueTokenizer.splitMulti`가 *되쪼개는 쪽*을 열어 두었는데 **채우는 쪽에는 이름이 없었다** — 자리마다 `joinToString(", ")`이라, 한 칸의 두 반쪽이 **다른 파일에 갈라져 있었다**(태그: 채우기 `CharacterEditFragment` ↔ 되쪼개기 `CharacterSaveCoordinator`). 그래서 *사용자가 아무것도 고치지 않아도* 엑셀에서 들여온 `"주인공, 남자"` 태그 하나가 **열었다 저장하는 것만으로** 둘이 됐다. **이 객체가 드는 것은 규칙이 아니라 *짝*이고, 그것이 이 행이 표에 있는 이유다** — 이름 없는 반쪽은 자리마다 다시 적힌다. 모드는 좁은 쪽(`quotedOnlyIfNeeded`)이라 옛 값의 뜻이 바뀌지 않고, **바뀌는 것은 칸에 보이는 글자이지 저장된 값이 아니다**(R-63)
| `BodyGenerationEditState` | **🎲 축·프리셋 편집 창이 회전을 넘겨 지키는 것**(B-260 · R-65) — 한 벌([`GenerationPreset`])과 **칸의 날 글자**를 함께 들고, `encode`/`decode`가 그 왕복을 든다. `aligned()`가 담긴 칸을 한 벌의 모양에 맞춘다 | **담는 벌을 순수로 내린 이유가 [`BodyEditorState`]와 같다** — 창 안에서 `Bundle`에 칸을 넣고 빼면 왕복을 증명할 수단이 없다(회전은 실기기에서만 보인다). 내리니 `data class` 동등성 하나로 **새 칸의 누락까지** 잡힌다. **값이 아니라 날 글자인 것이 이 표에 있는 이유다**: 지키려는 것은 *저장 가능한 한 벌*이 아니라 **적던 중인 것**이라, `"12."`를 값 타입으로 담으려면 눌러 담는 자리가 생기고 그러면 회전 한 번이 사용자가 적던 값을 조용히 바꾼다 |
| `StandardYearLink` | **표준연도 연동의 키·값과 *"이 캐릭터가 연동 중인가"*의 판정**(B-251 ⓓ). `__std_year_link` 상태변화의 값이 `none`이면 해제, **없으면 연동**(끈 적이 없으면 켜져 있다 — 그 기본값이 `isUnlinked(null)` 한 자리에만 적혀 있다). 순수 오브젝트라 `ConsistencyCheckerTest`가 잰다 | 종전에는 상수 다섯이 `StandardYearSyncHelper`의 companion에 살았다 — 그 클래스는 Repository 둘을 받아 **Room 없이는 컴파일되지 않으므로**, 순수한 소비처(`ConsistencyChecker`)까지 그 불순함을 짊어져 **순수 하네스에 실리지 못했다**(선언과 배선을 가르는 `AppSettingsKeys`↔`AppSettingsBindings`의 전례를 이쪽만 안 따르고 있었다). 그리고 *"해제란 무엇인가"*가 **두 자리에 따로** 적혀 있었다 — `isLinked`는 `!= none`으로, `ConsistencyChecker`는 `== none`으로. 지금은 같은 뜻이지만 **한쪽만 고치면 정합성 검사기가 조용히 연동 해제 캐릭터를 모순으로 신고한다** |
| `WorldPackageImages` | **월드패키지 이미지 엔트리의 이름과 복원 판정**(B-234 · R-64) — `entryName(엔티티접두사, i)`가 `images/{접두사}{i}.jpg`를 짓고, `isRestorable`이 **잘린 장을 결번으로 접는다**(내보내기가 `image_failures.json`에 적어 보낸 이름). `truncatedSet`이 그 목록을 읽는 자리다 | **이름을 짓는 규약이 둘이었다** — 내보내기는 접두사에 `images/`를 미리 붙여 들고 다녔고 가져오기는 부를 때마다 붙였다(같은 결과를 내지만 한쪽만 바뀌면 장이 통째로 안 잡힌다). 더 큰 것은 **잘린 장**이다: `copyTo`가 중간에 던지면 `closeEntry()`가 쓴 만큼으로 엔트리를 정상 종료해 **zip은 온전하고 내용만 잘린 장**이 되는데, 임포터는 결번이라야 유실로 세므로 **받는 기기가 그것을 멀쩡한 그림으로 복원**했다. 받는 쪽 바이트 검증을 안 고른 것은 성능이다 — 그 비용은 *정상 경로의 장마다* 붙는다. 소비처 둘이 `Context`·Room·`ZipFile`에 묶여 순수 하네스도 프로브도 못 보므로 **부르는가는 `tools/check_world_package_images.sh`가 본다** |
| `SheetResolver` | **예약 시트 해석의 단일 판정**(B-217 · R-33 / R-7) — 시트가 어느 spec의 것인가(정확명·접미사 밀림 복구·캐릭터 시트 지문 배제·정확명 폴백). 미리보기(`analyze*`)·가져오기(`findSheet`)·삭제 가드(`canRestore`)가 같은 함수를 본다. [ImportWorkbook] 추상 위의 순수 오브젝트라 `SheetResolverTest`가 DOM·스트리밍 양 경로에서 잰다 | 판정이 DB에 묶인 서비스 안 private일 때 미리보기 열아홉 자리가 정확명 `getSheet`로 우회했다 — 예약명을 빼앗긴 레거시 백업에서 가져오기는 밀린 시트를 되찾아 읽는데 미리보기는 "시트 없음"이라 말하고, 이름을 차지한 캐릭터 시트를 데이터 시트로 읽어 엉뚱한 건수를 예고했다. OVERWRITE 잔여 정리(prune 둘)도 정확명만 봐서 매칭까지 해 놓고 "시트 없음"으로 정리를 건너뛰었다. 우회 재발은 `check_restore_preview_parity.sh` ④가 막는다 |
| `FactionIndex` | **세력 참조 해석의 단일 판정**(B-87 이래 · 4-3 규약) — 코드 우선 → 이름 폴백, 동명은 **세계관으로만** 좁히고 그래도 여럿이면 모호 선언(`FactionLookupResult`). 세력 열이 있는 시트 전부(관계·소속·관계 시트의 미리보기·가져오기)가 이 하나를 본다. **아래 둘(작품·캐릭터)이 이것과 동형이고, 이것이 먼저 선 원본이다**. 순수 오브젝트라 `FactionRefResolverTest`가 잰다 | 이름만으로 first-match 하면 **다른 세계관의 동명 세력**에 무경고로 연결되고, 그 소속으로 자동 관계까지 생성돼 오염이 번졌다 |
| `NovelTitleIndex` | **작품 제목 해석의 단일 판정**(B-224 · 1차 감사 4-3) — 하나면 힌트 없이 확정 · 여럿이면 세계관으로 좁힘 · 그래도 여럿이면 **모호 선언**(`NovelTitleLookup`). 연표(미리보기·가져오기)와 상태변화(미리보기·가져오기) 넷이 같은 함수를 본다. `FactionIndex.resolve`와 **동형**이고(그쪽이 세력 축의 같은 판정 — 먼저 선 원본), 순수 오브젝트라 `NovelRefResolverTest`가 잰다 | 해석이 `allNovels.find`(first-match)로 호출부마다 흩어져 있어, 동명 작품이 있으면 사건이 **아무 작품에나** 붙고 그 작품의 세계관이 **사건 소속으로까지** 번졌다(무근거 배정 · 경고 없음). 상태변화 쪽은 같은 first-match가 **동명이인 해소의 기준**이라, 엉뚱한 작품을 기준으로 사람을 골랐다 |
| `CharacterRefLadder` | **캐릭터 참조 해석의 단일 판정**(B-232 · R-33) — 코드가 답하면 이름은 보지 않고(`codeFirst`), 동명이인은 '작품' 힌트로만 좁히며(`nameWithNovelHint`), 좁혀지지 않으면 **모호 선언**(`CharLookupResult.Ambiguous`). 미리보기(`analyze*`)와 가져오기(`import*`)가 **짝끼리 같은 사다리**를 부르고, 모호는 `skippedCount`로 센다(B-102 ⓑ). `FactionIndex`·`NovelTitleIndex`와 동형이고, 순수 오브젝트라 `CharacterRefResolverTest`가 잰다. **사다리가 둘인 것은 시트마다 해소 힌트가 달라서다** — 합치지 않고 짝을 맞춘다(`tools/check_restore_preview_parity.sh` ⑤가 그 짝을 기계로 본다) | 미리보기가 `findCharacterByName`(전역 `LIMIT 1`)으로 동명이인 중 **아무나** 골라 그 행을 '신규/갱신'으로 예고하는 동안, 짝 가져오기는 같은 행을 **거부**했다 — 유실은 없고 **예고한 숫자가 실행되지 않았다**(다섯 자리) |
| `FieldValueCellPlan` | **필드값 한 칸의 처분 단일 판정**(B-187 · R-33 ⑧) — *값 있음+기존 없음 → 신규 / 다름 → 변경 / 같음 → **동일**(쓰지 않는다) / 빈칸+열 있음+기존 있음 → 비움 / 그 밖 → 없음.* 가져오기 **열다섯 자리**와 미리보기 범주 **셋**이 이것을 부른다 | 여섯 자리에 손으로 적혀 있었고 **미리보기에는 한 자리도 없었다** — 미리보기가 세기 시작하면 그것이 일곱째 벌이 된다. 기계는 `check_field_value_plan.sh`(**쓰기 쪽**에서 잡는다 — 없는 코드는 스캐너가 못 보므로). 덤이 아니라 같은 처방의 결과로 **무편집 왕복이 표를 다시 쓰지 않게 됐다**(목표 규모 36,510회) |
| `DefaultFieldTemplateIndexes` + `guardDefaultFieldSlot` | **'기본 필드'의 정체 해석과 *자리 충돌 게이트*** (B-252 · R-33 ⑨) — 정체는 *코드 우선 → (대상, 필드키)*이고, 자리를 옮기는 편집이 **남의 자리와 부딪치면 키·대상만 되돌린다**(유니크 `(대상, 필드키)`). 미리보기·가져오기가 같은 색인·같은 게이트를 지난다 | **게이트가 가져오기에만 있었다** — 미리보기는 `merge* != existing` 하나로 판정해 **되돌려질 행을 '변경'이라 예고**했다(설계 1-2가 등급 체계의 `rename` 게이트에서 고친 것과 같은 부류). **스캐너 ①이 이것을 원리적으로 못 본다** — 그 그물은 *손으로 짠 비교식*을 보는데 이 결함은 **빠진 게이트**다. 판정이 순수라 `DefaultFieldSlotGuardTest`가 재고, **같은 처방이 R-53도 갚았다**: 게이트를 DAO로 세우면 행별 조회가 하나 더 붙을 자리였는데 색인으로 내리니 미리보기 둘·가져오기 셋이 함께 0이 됐다(그 자리는 `check_preview_row_queries.sh`가 **스스로 적어 둔 사각**이다 — 클래스 수준 private 함수 속의 DAO 호출) |
| `analysisUniverses()` (+ `analysisCreatedUniverses`) | **복원 미리보기가 보는 세계관 목록 — `DB + 이 파일이 만들 것`** (B-254 · R-33 ⑨). 캐릭터 시트 훑기 · 필드값 세 시트 · 값 라이브러리 · 작품·연표의 필드 열이 전부 이 하나를 지난다 | 짝인 `import*`는 `getAllUniversesList()` 하나로 답이 맞는다 — `importUniverses`가 **먼저 심기 때문**이다. **같은 코드를 써도 서는 자리가 다르면 답이 갈린다**는 것이 이 행의 요점이고, **빈 DB 복원에서 미리보기의 캐릭터 예고가 통째로 비었다**(파일에 200명이 있어도 미분류 시트 하나만 세어진다). **등재가 지목한 한 자리가 실측 일곱이었다.** 기계는 `check_restore_preview_parity.sh` **스캐너 ⑦**(수리 전 8자리 확인), 예외는 명단이 아니라 **자리에** 적는다(`// DB 전용 허용(사유)`) |
| `SheetResolver.locateHeader` + `dataRows` | **헤더 행이 어디인가, 데이터는 어디서 시작하는가** (B-231 ⓑ · R-56 ⓑ) — 맨 앞 `HEADER_SEARCH_ROWS`행에서 첫 열이 spec 헤더인 첫 행을 고르고, 데이터 루프는 **그 다음 행**부터 돈다 | 종전에는 `getRow(0)`과 `1..lastRowNum`이 **각각 서른 자리·마흔일곱 자리**에 흩어져 *헤더는 0행*을 상수로 박고 있었다. 표 위에 제목·메모를 얹은 파일은 시트가 통째로 거부됐고(개발 의도 4번), **찾아 주기 시작하면 그 상수들이 전부 틀린 것이 된다.** **가장 위험한 것은 찾는 쪽이 아니라 찾은 뒤다** — 헤더를 3행에서 찾고 루프가 1행부터 돌면 제목 줄이 데이터가 되는데 **오류도 경고도 없다**. `sheetHasDataRow`는 한 걸음 더 나쁘다: 제목 줄이 데이터로 세어지면 **헤더만 있는 시트가 '복원 재료 있음'이 되어 덮어쓰기가 기존 데이터를 지우고 한 건도 넣지 못한다**(유실). 판정이 [ImportSheet] 추상 위 순수라 **DOM·스트리밍이 같은 함수를 보고**(`SheetResolverTest`가 양 경로에서 잰다), 스트리밍은 `readLeadingRows`로 창만큼만 읽는다 — 없으면 `getRow(1)` 하나가 시트를 통째로 적재해 **스트리밍이 DOM보다 무거워진다.** 기계는 `check_header_row_report.sh`의 축 셋(수리 전 47자리 확인) |
| `BodyEditorState` | **실루엣 편집기가 회전을 넘겨 지키는 것의 전수**(B-201 · R-41-a) — 끌어 만든 수치·*이 자리에서 만든 값*의 집합(`touched`)·🎲 직전으로 한 단계·축/컵/기준 캐릭터 선택, 그리고 **열 때 뜬 스냅샷**(원문·해석 수치·되쓸 자리·고지 깃발). 시트는 `encode`/`decode` 한 줄씩만 들고 호스트는 `bindSilhouetteEditor` 한 자리에서 꽂는다 | 시트의 입력이 전부 호스트가 꽂는 맨 `var`라 **회전 한 번에 편집이 통째로 사라지는데 시트는 멀쩡히 떠 있었다**([적용]은 `onApply`가 null이라 아무 데도 닿지 않는다). 담는 자리를 순수로 내린 것이 요점이다 — 화면 안에 두면 왕복을 증명할 수단이 없다(R-41 본문이 *'기계를 못 세운다'*고 적어 둔 그 이유). 기계는 `check_silhouette_rebind.sh` 다섯 축 |
| `CharacterFieldColumns` | **캐릭터 시트 열 머리 → 필드 해석 사다리**(B-187) — `이름(필드키)` → key → 이름(유일할 때만) → 자동 생성/미해석, 그 뒤 **단사 보장**(한 필드에 두 열이면 앞 열만) | 사다리가 `buildColumnFieldMap` 안에 있었고 **그 함수는 해석하면서 동시에 DB에 필드를 만든다** — 그래서 미리보기가 부를 수 없었다. **미리보기가 부를 수 없는 함수는 그 자체가 R-33 위반의 예고다.** 자동 생성을 모형화하지 않으면 '필드 정의' 시트가 없는 파일에서 미리보기가 "0건"이라 말하며 가져오기는 수천 건을 쓴다 |
| `collectEntityFieldColumns` | **작품·연표의 `필드:` 열 수집**(B-187) — 내보내기 규칙의 역함수(정확 일치) 우선, 실패 시 추측 파싱. 가져오기 둘 + 미리보기 둘이 함께 쓴다 | 글자까지 같은 코드가 **두 자리**에 있었고 미리보기가 세면 넷이 된다. 정확 일치를 최우선으로 두는 이유는 이름이 괄호로 끝나는 필드(`규모(명)`)를 세계관 한정으로 오인하면 **열 전체가 버려지기** 때문이다 |
| `ImageZipReport` | **기기 밖으로 나가는 산물의 이미지 수록 집계와 고지 판정**(B-225) — 사유별 계수(담김·파일 없음·저장소 밖·읽기 실패)에 더해 **참조 목록 자체를 못 읽은 항목 수**(`unreadableRefCount` — 장이 아니라 **항목**)를 들고, 어느 문구를 낼지(`noticeKinds`)와 어느 사유를 늘어놓을지(`lossReasons`)까지 **순수하게** 정한다. 엑셀 백업 ZIP(`ImageZipHelper`)과 월드패키지(`WorldPackageExporter`)가 **같은 벌**을 쓰고, 자원 매핑만 `ImageNoticeRes`가 든다. 시험은 `ImageBackupIntegrityTest` | 벌이 엑셀 쪽에만 있어 **월드패키지는 계수 자체가 없었다** — 한 장의 I/O 오류가 그 엔티티의 남은 장까지 무음 탈락시키고 아무 데도 나타나지 않았다(감사 5장의 비대칭). 그리고 고지 우선순위가 `ExcelExporter`의 **사적 함수** 안에 있어 어느 시험도 못 닿았고, **손상된 참조는 분자에서도 분모에서도 빠져** 비율이 100%가 되는 바람에 *'완전한 백업입니다'*가 그대로 나갔다 — 그 백업만 믿고 원본 기기를 지우면 이미지가 영구 소멸한다 |
| `CharacterValueLedger` | **가져오기 한 번 동안 `(캐릭터, 필드)` 값의 현재 상태**(B-72 ②) — 캐릭터 시트와 '캐릭터 필드값' 시트가 **같은 장부 하나**를 본다. 조회 단위를 *값*에서 *캐릭터*로 내린 것이 이 객체의 전부다 | 종전에는 두 시트가 각자 값마다 DB를 물었다 — 캐릭터 시트는 필드가 **열**이라 조회 수가 **(행 × 필드 열)**에 비례했다(×30 = 57만 회. `scalability` 3-9). **두 시트가 같은 항목을 다룰 수 있으므로 장부도 하나여야 한다** — 둘로 나누면 앞 시트가 쓴 값을 뒤 시트가 못 보고 *있는 값에 insert*를 쳐 `(characterId, fieldDefinitionId)` **유니크 색인 위반으로 가져오기가 통째로 실패한다.** **`isLoaded`가 따로 있는 것이 계약이다** — *값이 0건인 캐릭터*와 *아직 안 물어본 캐릭터*가 갈리지 않으면 헛조회가 돌거나 기존 값을 못 보고 덮어쓴다. **DB를 직접 건드리는 자리는 `forget`으로 명시해 내리고**(`deleteValuesNotInUniverse` 하나), 그 짝으로 **`load`가 이미 실린 것을 덮어쓰지 않는다** — 덮어쓰면 그 명시가 없는 자리에서도 조용히 되돌아간다. **`reset()`은 앞뒤 둘 다에서 부른다 — 몫이 다르다**: 앞은 실패·취소로 끝을 못 본 가져오기의 찌꺼기를 다음 실행이 보지 않게 하고, 뒤는 **놓아주기**다. 이 서비스는 화면과 수명이 같고(`ExcelTransferController` → `ExcelImporter` → 이 객체) 담는 것이 id가 아니라 **행 전체**라, 안 놓으면 가져오기가 끝난 뒤에도 그대로 남아 **봉우리가 아니라 바닥을 올린다** — 곧이어 도는 내보내기·자동 백업의 봉우리가 그만큼 위에서 시작한다(B-72가 지키는 바로 그 경로다). 짝은 `tools/check_value_ledger_sync.sh`가 기계로 본다 |
| `ImportLookupIndex` | **가져오기가 코드·자연키로 엔티티를 되찾는 규칙**(B-210) — 시트 루프가 행마다 `…ByCode`·`…ByNaturalKey`로 DB를 묻던 자리를 **표 한 번 읽기**로 바꾼다. 캐릭터·사건·작품·세계관 넷은 여러 시트가 함께 보므로 **가져오기 수명**이고(서비스 필드), 나머지는 시트 함수의 지역 변수다 | 손코딩 맵이 **세 가지로 각자 다르게 틀렸다.** ① `associateBy`는 **뒤엣것**을 남기는데 대체하는 질의는 `ORDER BY` 없는 `LIMIT 1`이라 사실상 **먼저 실린 것**을 준다 — 같은 키가 둘인 파일에서 *합쳐지는 상대가 바뀐다*. 그래서 `load`는 **덮어쓰지 않고 뒤에 붙이고** 호출부는 **id 오름차순으로 싣는다**. ② 갱신을 안 하면 같은 파일의 뒷 행이 앞 행이 만든 엔티티를 못 보고 **있는 것에 insert를 쳐** 대결 축·상성처럼 유니크 인덱스가 선 표에서는 **가져오기가 통째로 죽는다** — insert·update 뒤에 `put`. ③ **키가 바뀌면 옛 키를 끊어야 한다** — 캐릭터·세계관의 이름과 코드는 시트에서 고칠 수 있고, SQL로 옛 이름을 물으면 당연히 없다. **자연키는 반드시 타입 있는 `data class`로 짓는다**: `listOf(10L, 1993, null)`은 코틀린이 `1993`을 `Long`으로 올려 `Int` 칸의 키와 **영영 같지 않은데 컴파일도 예외도 없다** — 모든 조회가 빗나가 가져오기가 *"기존 행이 없다"*고 보고 전부 새로 만든다. **넷째는 색인이 스스로 막을 수 없어 호출부의 규약이다: 싣기가 쓰기보다 앞서야 한다** — `load`가 뒤에 붙이므로 쓰기가 먼저면 나중 적재분이 그 뒤로 가 ①이 깨진다(첫 행이 충돌 해결 행이거나 코드 칸이 빈 행이면 실제로 그 경로다). 그래서 `ExcelImportService`의 `remember*` 넷은 전부 `ensure…Index()`를 먼저 부른다. 시험은 `ImportLookupIndexTest` |
| `ImportIdentityIndexes` (`StateChangeIndexes` · `RelationshipIndexes` · `RelationshipChangeIndexes` · `FactionIdentityIndexes` · `DuelAxisIndexes` · `DuelMatchIndexes` · `FieldDefinitionIndexes` + 자연키 일곱) | **그 색인을 *어떤 키로 짓고 어떤 순서로 싣는가*** (B-236). 바로 위 `ImportLookupIndex`가 *색인 그 자체*라면 이쪽은 **그 색인의 계약**이다 — 가져오기(`import*`)와 복원 미리보기(`analyze*`)가 **같은 클래스**를 세운다 | **미리보기만 행마다 DB를 묻고 있었다**(B-236 — `analyze*` 아홉 자리. 가져오기는 B-210에서 내렸다). 고치면서 순서 규약을 미리보기 쪽에 **다시 적으면 같은 사실이 두 자리에 살고, 이 저장소가 여러 번 확인했듯 한쪽이 반드시 낡는다.** 그리고 그 낡음의 모양이 고약하다: R-33이 요구하는 것은 *같은 답*인데, **순서가 갈리면 같은 `merge*`를 써도 비교 상대가 달라져** 미리보기가 '변경/동일'을 가져오기와 다르게 말한다 — 오류도 고지도 없이. **순서 규약이 종류마다 다른 것이 요점이다**: 대부분은 `LIMIT 1` = rowid라 **id 오름차순**이지만, **관계만 `displayOrder ASC, createdAt DESC`**다(호출부가 쌍의 목록에서 `find { 유형이 같다 }`로 고르므로 그 순서가 곧 *어느 관계에 이력이 붙는가*다). **자연키를 `ExcelImportService`의 private에서 끌어낸 것도 이 이유다** — 거기 있으면 순수 시험이 원리적으로 닿지 못한다(미리보기의 *계수*는 DB에 묶여 못 재지만 **키 모양과 싣는 순서는 순수다**). 시험은 `ImportIdentityIndexesTest`, 구조는 `tools/check_preview_row_queries.sh`가 본다 |
| `PreviewCreations` (`PreviewIdMinter` · `ANALYSIS_CREATED_NOVEL_ID`) | **복원 미리보기가 *이 파일이 앞서 만든 항목*에 매기는 임시 id의 규칙** (B-233 · R-33 셋째 짝). 음수 **연번**이며 '가져오기가 새로 만들어 배정할 작품' 표지값 **아래**에서 시작한다 | 미리보기는 DB만 보고 등재하지 않아, 같은 대상을 두 번 쓴 파일을 **신규 2**로 셌다(가져오기는 신규 1 + 갱신 1). 등재를 시작하면 **id 규칙이 이 기능의 유일한 실패 지점**이 된다 — 전부 같은 값이면 `ImportLookupIndex.put`이 *같은 행이 옮겨 온 것*으로 보아 **앞 신규의 키를 끊어** 같은 증상이 다른 원인으로 되살아나고, 표지값과 겹치면 미리보기가 *새로 만들 작품*과 *방금 만든 항목*을 같은 것으로 본다. 두 규칙이 `ExcelImportService`의 private에 있으면 순수 시험이 원리적으로 닿지 못하므로 여기로 내렸다 — `PreviewCreationsTest`가 **양방향으로** 잠근다(규칙을 되돌리면 실제로 빨간불) |
| `ImageLinkGroupPlanner` | **"이미지" 시트가 배정한 링크 묶음을 무엇에 쓰는가**(B-239) — 가드(`(시트가 든 id + 지금 그 토큰의 식구).toSet().size >= 2` — 1장짜리 묶음은 만들지 않는다) · **처리 순서**(넣은 순서) · **쓰기를 흉내 내는 겹의 갱신 규칙**. 순수 오브젝트라 `ImageLinkGroupPlannerTest`가 잰다 — 그 시험의 뼈대는 **옛 토큰별 루프를 정답지로 다시 적어 무작위 상태에서 최종 DB를 대조하는 것**이다 | 종전에는 토큰마다 `getByGroup`을 물어 판정했다(왕복 × 토큰 수). **읽기 전 일괄 조회로 '그냥' 바꿀 수 없는 자리라 이 클래스가 있다** — 이 루프는 제 안에서 `setGroup`으로 쓰고 **그 쓰기가 뒤 토큰의 답을 바꾼다**(이미지가 X에서 Y로 옮겨 가면 Y를 처리한 뒤의 X 조회는 그것을 식구로 세지 않는다). 겹 없이 일괄 조회만 얹으면 가드의 답이 뒤집혀 **1장짜리 묶음이 생기거나 생겨야 할 묶음이 안 생긴다 — 유실도 오류도 고지도 없는 조용한 행동 변화다.** 그래서 **B-238이 같은 함수 앞쪽에서 쓴 *읽기 전 색인*이 여기서는 본보기가 되지 못한다** |
| `ImageTagApplyPlanner` | **AI 태그 검토 시트가 고른 태그를 무엇에 쓰는가**(B-241) — 이미 가진 태그와의 대조 · **쓰기를 흉내 내는 겹의 갱신 규칙** · 계수(`tagCount`는 더해진 태그 수, `touchedPaths`는 태그가 붙은 **서로 다른** 경로 수). 이미지판·폴더판 두 화면 진입점이 같은 이것을 쓴다. 순수 오브젝트라 `ImageTagApplyPlannerTest`가 잰다 — 뼈대는 **옛 경로별 루프를 정답지로 다시 적은 무작위 대조**다(4,000판) | 종전에는 경로마다 `getTagsByImageList`를 물어 대조했다(왕복 × 이미지 수 — **폴더판은 고른 폴더 아래 전량을 돌아 사용자가 크기를 정한다**). 읽기를 앞으로 모으면 **같은 이미지가 두 번 도는 경우에 답이 갈린다** — 앞 차례의 `insertAll`을 뒤 차례가 못 보고 같은 태그를 다시 `fresh`로 센다. **행은 `(imageId, tag)` 유니크라 늘지 않으므로 DB를 봐도 안 보이고 오류도 나지 않는다** — 화면 계수만 커지는 거짓 고지다(개발 의도 2번). `ImageLinkGroupPlanner`와 같은 부류이고, 갈림의 잣대도 같다: **루프가 제가 읽는 것을 쓰는가** |
| `ImageAdoptionPlanner` + `ImageAdoption` | **입양(insert-or-get)을 목록 단위로 접는 판정과 그 통로**(B-244). 무엇이 새 행이고 무엇이 **승격 대상**인가 · 중복 경로를 접는 규칙(입력 순서 보존) · 삽입이 경합에 진 자리(`rowId == -1`)를 다시 물어 채우는 규칙. 판정은 순수 오브젝트라 `ImageAdoptionPlannerTest` 14건이 잰다(옛 경로별 루프를 정답지로 다시 적은 무작위 4,000판 + 경합 2,000판 대조). `ImageAdoption`은 그 답을 DB에 옮기는 손이고 `SqlInChunks`(R-54)를 지난다 | 종전에는 `ImageMetaDao.adopt`를 **경로마다** 불렀다 — 행이 이미 있으면 **왕복 셋**이고 `@Transaction`이라 **경로마다 트랜잭션이 하나씩** 열렸다(호출부 11곳 · 실측). **DAO가 아니라 `util`에 둔 것이 이 행의 설계 판단이다** — `IN (:paths)`를 쓰므로 `SqlInChunks`를 지나야 하는데 그것은 `util`이고 **`data/dao/`가 `util`을 import 하는 전례가 0이다**(실측). 계층을 뒤집는 대신 통로를 `util`에 두고 DAO에는 통로가 필요 없는 조각(`insertAll`)만 더했다. **틀렸을 때의 모양이 셋 다 조용하다:** 승격을 빠뜨리면 자동 반납이 **사용자가 태그를 붙인 행을 지우고**(지워진 뒤엔 증거가 없다), 중복 경로의 id가 갈리면 태그·링크가 엉뚱한 행에 붙으며(행은 `path` 유니크라 DB는 멀쩡해 보인다), 경합을 빠뜨리면 그 경로만 id 없이 지나간다 |
| `BackupChunkFormat` | **백업 다섯 형식의 틀 짜기와 읽기 — 형식의 단일 소스**(B-230 ⓐ · B-248). **쓰는 것은 둘(NCB3 기기 키 · NCP2 패스프레이즈), 읽는 것은 다섯**(+ NCB2 · NCP1 · v1 레거시 — R-58: 옛 매직을 읽는 길은 남긴다). 매직 넷·청크 크기·IV/태그 길이·PBKDF2 계수와 **청크를 쓰고 읽는 짝**이 여기 하나다. **NCB3·NCP2의 청크는 `헤더 전체 ∥ 색인(8B) ∥ 마지막인가(1B)`를 AAD로 문다**(B-248) — GCM은 *각 청크가 진짜인가*만 답하므로, 묶기 전에는 **잘림·재배열·중복·헤더 조작이 하나도 안 걸렸다.** `javax.crypto`만 쓰고 `android.*`를 하나도 import 하지 않는 것이 이 파일의 존재 이유이며, `BackupEncryptor`에는 **AndroidKeyStore에서 키를 얻는 일만** 남았다. 순수라 `BackupChunkFormatTest`가 세 형식을 통째로 잰다 | **426줄에 감사도 시험도 0이었다.** 닿지 못한 이유가 로직이 아니라 **파일 머리의 `android.security.keystore` import 둘**이라는 것이 이 행의 요점이다 — 그 둘이 있으면 순수 하네스가 컴파일조차 못 하고, 그래서 *복호화 결함 하나가 전 백업 복원 불가*인 코드가 어떤 로컬 검증도 받지 못했다(`DropdownListSheet`·`ImportLookupIndex`가 먼저 낸 처방을 그대로 썼다). **갈라 놓고 나서 실제로 결함이 나왔다:** `encryptChunks`가 `read()` 한 번의 반환값을 청크 길이로 삼고 있었는데, **청크에는 길이 칸이 없어** 읽는 쪽이 `chunkSize+16`을 통째로 집는다 — 짧은 읽기가 중간에 하나만 끼면 그 뒤 청크의 IV까지 삼켜 **그 지점부터 파일 전체가 복원 불가**가 된다. `read()`는 버퍼를 채운다고 약속하지 않고 FUSE로 얹힌 외부 저장소가 실제로 짧게 준다. **쓰는 쪽만 `readFully`를 안 쓰고 있던 비대칭이 표식이었고**, 무엇보다 이 파손은 **백업을 뜨는 시점에 증상이 없다**  **B-248이 그 위에 하나를 더 걷었다 — 그쪽은 *청크가 전부 있는가*를 아무도 안 보고 있었다:** 뒤가 잘린 파일을 `readChunks`가 EOF로 읽고 **정상 종료해 짧은 평문을 냈다**(사고로 안 이어지던 것은 하류 zip이 잡아 주기 때문이고 **이 계층의 보장이 아니다**). **총 청크 수 대신 *마지막 표시*를 묶은 것이 그 판의 판단이다** — 총수는 흘려 쓰는 함수가 미리 알 수 없어 *선언한 수와 실제 수가 갈리는* 새 실패 축을 만든다. **잘림은 태그 불일치와 갈라 던진다**(`TruncatedBackupException`) — 화면이 태그 불일치를 *암호가 틀렸다*로 읽어 **맞는 암호를 넣고도 영영 되묻는** 자리가 있었다. 그것도 추측이 아니라 *마지막이 아닌 것으로 되물어 열리는가*로 **증명하고** 던진다 |
| `SqlInChunks` | **`IN (:목록)` 질의에 넘길 목록을 상한 아래로 가르는 통로 — 값과 나누는 법의 단일 소스**(B-242 · R-54). `LIMIT`(=900)이 적히는 자리가 여기 하나이고, **저장소의 옛 청크 상수는 이제 하나도 없다**(B-247이 마지막 넷을 없앴다 — 이름이 남으면 다음 사람이 그것도 세어야 하고, 이 저장소는 그 비용을 두 판 연속 치렀다. 세는 법은 R-54의 ⚠️가 든다). **호출부도 전부 통로를 지난다** — 손으로 적던 `.chunked(900)` 리터럴 스물하나를 B-245가, `.chunked(<상수>)` 마흔을 B-247이 옮겼다. 돌려받는 것에 따라 셋으로 갈린다 — 행은 잇고(`flat`), 없으면 돌고(`each`), **영향 건수는 더한다(`sum`)**. 목록이 둘인 질의는 `sizeFor(예약분)`으로 몫을 밝힌다. 순수(Android 무의존)라 `SqlInChunksTest`가 잰다 | **관례로만 두었더니 다시 벌어졌다.** B-51 ①에서 `removeTagsFromImages`가 선택분 전량을 한 질의에 실어 상한을 넘겼고 **그 예외를 자기 `catch`가 삼켜 조용한 유실**이 됐다 — `chunked(900)`은 그 사고의 산물인데, B-242가 기계로 세니 **청크 없는 자리가 24곳**이고 값 `900`은 **일곱 자리에 따로** 적혀 있었다. **값만 모으는 것으로는 안 되는 것이 이 행의 요점**이다: 새로 쓰는 호출부가 통로를 안 지나는 것은 여전히 아무도 못 보므로 `tools/check_in_clause_chunk.sh`가 짝으로 선다(기준선 0). **통로가 손으로 적던 것보다 빠른 것도 설계다** — 한 덩이에 들어가면 쪼개지 않고 원본을 그대로 넘긴다(`.chunked(900).flatMap{}`은 그 흔한 경우에도 리스트 둘을 새로 만들었다). 느린 통로는 성능을 이유로 우회당하고, **그 우회가 곧 다음 B-51이다** |
| `FactionStanding` | **'지금 소속'인가**(B-171) — `isCurrent`·`current`·`hasCurrent`·`countCurrent`·`currentFactionIds`. **SQL 쌍둥이(`SQL_CURRENT`)까지 이 객체가 든다** — Room 질의는 코틀린 함수를 부를 수 없어 같은 술어가 DAO에 글자로 한 벌 더 살고, 그 일치는 `tools/check_faction_standing.sh`가 본다(R-50) | **소유자가 아예 없었다** — 착수 시점에 판정이 통계 넷 · 세력 관리 둘 · 캐릭터 AI 문맥 · 엑셀 가져오기 · DAO 질의 셋에 각자 적혀 있었고, 대결 카드가 여덟째로 적을 참이었다(원칙 05 · B-117이 배운 그 부류). **`FactionMembership.isActiveAtYear`와 묻는 것이 다르다** — 그쪽은 *그 해에 소속이었나*(연표·시점 슬라이더), 이쪽은 *지금인가*(대결·통계·세력 관리). 합치면 **가입 연도를 적어 둔 멤버가 그 해 이전 시점에서 통째로 빠지고** *지금*이 무엇인지는 여전히 아무도 못 정한다. **엑셀로만 생기는 모순 조합**(`탈퇴연도`만 있고 `탈퇴유형`이 빈 행)의 처분은 미판정이다(B-206) |
| `DuelAxisTransfer` | **대결 축이 세계관 경계를 넘는 규칙**(B-116) — 무엇이 새로 나야 하는가(`id`·`code`·`universeId`·`displayOrder`, 전부 **인자**라 서명에 적혀 있다) · 무엇이 내려가는가(`isBasisAxis`) · 무엇이 그대로 오는가(연결 셋) · **없는 키 세기**(연결 + 후보 필터) | **`code`가 전역 유니크라 복사가 오배정을 만든다** — 삽입이 죽거나, 죽지 않는 경로에서는 `duelGrade.axisCode`를 든 남의 필드 config가 그 축을 **정확히 찾는다**(R-1 · R-35). **`isBasisAxis`는 켜진 채 들어오면 `saveAxis`가 받는 세계관의 기존 기준을 조용히 내린다** — 축을 하나 가져왔을 뿐인데 대표 그림이 바뀐다(2026.08.11에 같은 모양을 한 번 겪었다). **후보 필터의 `fieldId`는 지운다** — `DuelCandidateFilter.resolve`에 *키가 없으면 id로* 폴백이 있어 남의 id가 우연히 실재하면 엉뚱한 필드로 **정확히** 해석된다 |
| `FieldValueResolver` | 별칭·표시 라벨 접기 | — (R-21로 스냅샷 파생으로 승격) |
| `FieldStatsConfig` / `StatsFieldPolicy` | '통계에 포함' 설정의 적용 규칙 | 패턴 감지가 설정을 무시했다(S-14) |
| `NumericBinning` + `FieldValueMatchSpec` | 수치 구간 + 드릴다운 매칭. **구간 생성·값 추출(`numericValuesOf`)·구간 세기(`autoDistribution`)가 전부 여기 하나다** | 라벨 문자열로 매칭해 항상 0명이었다(S-16 → R-20). **2026.08.12(B-39)에 값 추출과 세기까지 넘겼다** — 화면이 자체 8등분을 들어 같은 필드가 인사이트(5)와 레거시 상세(8)에서 다른 모양이었고, 호출부가 `toFloatOrNull()`을 직접 쓰던 자리는 **줄바꿈 없는 공백(U+00A0)에서 갈려** 그 값이 분포에서만 빠지고 드릴다운 목록에는 떴다(일반 공백은 `parseFloat`가 이미 건너뛰므로 **그 문자로만 드러난다**). **구간은 모집단의 것이라** 값 하나씩 부르는 `getFieldValues`에서는 만들 수 없다 — ~~B-196이 열려 있는 이유다~~ **그래서 B-196의 접기가 `getFieldValues`가 아니라 `foldNumericDistribution`(모집단을 가진 자리)에 앉았다** (2026.08.17 닫힘). **2026.08.17(B-197)에 여집합이 들어왔다** — `NumericPartOutsideRanges`는 *어느 구간에도 안 드는 값*을 가리키고, **경계 판정을 되풀어 적지 않고 `NumericPartRange` 갈래에 되물어** 낸다(상한 포함 규칙이 바뀌어도 여집합이 저절로 따라온다). 여집합을 라벨(`구간 밖`)로 싣지 않은 이유가 이 표의 취지 그대로다 — 라벨 일치는 NUMBER·CALCULATED에서만 **우연히** 맞고 **BODY_SIZE 파트에서는 조용히 0명**이 된다. **다만 B-196의 접기는 이 타입을 쓰지 않는다** — 그쪽은 *이미 파싱된 통계 키*를 접으므로 어느 키가 어느 구간에 들었는지를 아는 자리이고, 스펙이 `Values(그 키 집합)`라 더 정확하다(라벨·카테고리가 살아 있는 필드에서도 어긋나지 않는다) |
| `ValueDistributions` | 분포 전량 + 잘라낸 개수 | 비율 분모가 상위 N 합이라 편향이 왜곡됐다(S-17 → R-19) |
| `DisplayCap` / `CrossTableFold` | **통계 탭이 한 번에 그리는 항목 수의 상한**(B-199 · B-194 · B-212) — 상수 셋(명단 묶음 50 · 패턴 카드 축별 8 · 교차표 행·열 각각 12)과 세 가지 셈(앞에서 자르기 · **축별 정원** · 묶음 단위 펼치기), 그리고 교차표를 **합을 잃지 않고 접는** 계산. 화면 문구는 이 상수들로 채운다(R-14 — 문구에 숫자를 박으면 상한을 옮길 때 거짓이 된다) | 통계 상세의 명단·카드·표가 **상한 없이 전량을 그렸다.** 명단은 전부 캐릭터 축이라 ×30에서 목록 하나가 6,420개의 뷰였고(재활용 없는 `LinearLayout`), 교차표만은 **비용이 곱**이라 `(값 종수 × 값 종수)`로 늘어 규모가 아니라 **차수**의 문제였다. **셋 다 처방이 자르기가 아니라 접기인 것이 요점이다** — 명단은 *다음 한 묶음*씩 끝까지 펼 수 있고(*"나머지 전부"*로 두면 한 번의 탭이 상한 이전으로 되돌린다), 교차표는 넘친 행·열을 '기타'로 **합쳐** 총합을 보존하며(버리면 표·차트·요약이 서로 다른 말을 한다 — R-19), 패턴 카드는 **축별**이라 캐릭터 축이 사건·작품 축의 자리를 못 뺏는다(통째로 하나면 상한이 *"접었다"*가 아니라 *"그 축을 없앴다"*가 된다). **순수 계층에 둔 것은 시험 때문이다** — 자리가 Fragment라 그대로 두면 로컬 검증이 0이다. 시험은 `DisplayCapTest`·`CrossTableFoldTest`, 상한 등재는 `scalability` 4장 |
| `GraphForceLayout` / `DisplayCap.rankedCap` | **관계도의 배치 계산과 그리는 노드 수의 상한**(B-213) — 힘의 **누적 순서**(세력 쌍은 `(i, j)` 사전순 · 엣지는 입력 순서)와 세력 쌍을 **세력별 묶음에서** 뽑는 규칙, 그리고 *무엇을 남기는가*(연결 수 내림차순)와 *어떤 순서로 그리는가*(**입력 순서**)를 가른 상한 | **이 앱에서 캐릭터 수에 대해 비용이 2차식인 유일한 계산이고**, 그것을 가둔다고 4장이 적어 둔 "요약 모드"가 **상한이 아니라 술어**(*연결 3개 이상 전부*)여서 남는 수가 관계에 비례해 함께 늘었다 — ×30에서 841명, 밀도 세 배에서 4,660명(`scalability` 3-12). **순수 계층에 둔 것은 시험 때문이다** — 계산이 `RelationshipGraphView` 안에 있어 **로컬 검증이 하나도 닿지 않았다**(뷰 프로브는 컴파일만 보고, 순수 시험·실클래스패스 프로브는 `ui` 계층을 통째로 뺀다). **누적 순서가 계약인 것이 이 행의 요점이다** — 만드는 쌍의 집합이 같아도 순서가 달라지면 300회 되먹임이 그 차이를 키워 **좌표 최대 차 37,660**이 났다. `GraphForceLayoutTest`가 **걷어낸 종전 구현을 하네스 안에 들고** 좌표를 대조한다(`ExportWorkbookParityTest`와 같은 idiom). 상한 쪽은 *남길 것*과 *그릴 순서*를 갈라야 한다 — 초기 배치가 노드 순서로 원을 그리므로 골라낸 순서를 그대로 쓰면 **남은 노드의 좌표까지 바뀐다**. 상한 등재는 `scalability` 4장 |
| `PatternAxis` / `PatternThresholds` | 패턴 감지의 **축**(제목 접두·개수 단위·조언 문구·모집단의 뜻)과 **기준**(유형별 민감도 + 받아들이는 범위 `*_RANGE`). 감지는 `detectFieldPatterns` 하나가 축을 인자로 받아 캐릭터·사건·작품을 함께 돈다 | **사건·작품 필드가 패턴 감지에서 통째로 빠져 있었다**(B-36 — 축마다 함수를 따로 두는 구조라 캐릭터판만 자랐다). **기준은 전부 개발자 상수였다**(B-70 — 특히 '공백 100년'은 수천 년 역법 사용자에게 아무 뜻이 없었다). 범위를 창과 저장소가 각자 들면 **창이 통과시킨 값을 저장소가 조용히 접어** 적은 것과 저장된 것이 갈린다 — 그래서 `*_RANGE`가 단일 소스이고, **처분만 방향이 다르다**(사람 앞에서는 되돌려 묻고 R-27, 파일에서 온 값은 접되 말한다) |
| `PromptTemplates` / `PromptTokens` / `PromptTemplateValidator` | **AI에 보내는 메시지 양식 열넷**(2026.08.20 사용자 요청) — 기본 양식·자리표 어휘·필수 자리표(`PromptTemplates`), 자리표를 값으로 펼치는 규칙(`PromptTokens` — *빈 자리는 줄째로, 빈 절은 머리까지*), 사용자가 고친 글의 거절 사유(`PromptTemplateValidator`). 응답 형식 지시(`responseFormat`)도 여기 하나다 | 종전에는 프롬프트 열넷이 각 서제스터의 `companion object`에 **하드코딩된 문자열**이었다 — 앱이 무엇을 보내는지 볼 수도 고칠 수도 없었다(원칙 01이 정면으로 겨누는 자리). **잠근 자리와 연 자리를 가른 것이 이 셋의 요점이다**: 응답 형식과 `[추천할 필드]` 절은 배열 이름 하나만 달라져도 읽는 쪽이 **0건**을 보고 사용자는 *"형식 오류"*라는 거짓 진단과 함께 돈을 낸 요청을 잃으므로 **필수 자리표**로 잠갔고(자리는 사용자가, 내용은 앱이), 규칙 본문은 지워도 앱이 결손을 세어 말하므로 열었다. **펼친 값을 다시 훑지 않는 것도 계약이다** — 다시 훑으면 캐릭터 메모에 적어 둔 `{{…}}`가 프롬프트 구조를 바꾼다(`PromptTokensTest`가 잠근다). 부수로 하나가 고쳐졌다: 근거 강도 지시의 하드코딩된 `16.`은 사용자가 규칙 수를 바꾸는 순간 반드시 어긋나므로 **번호 없는 이름표 블록**이 됐고, 그 덕에 사건 축의 사본이 사라져 **두 축이 한 벌**을 쓴다 |
| `FormulaLexer` | 수식 어휘 규칙 + **버려지는 구간** | 미지 함수가 조용히 버려져 그럴듯한 오답이 됐다(로드맵 5) |
| `FormulaEvaluator` / `FormulaValidator` / `FormulaDisplay` | 수식 평가 / 저장 시점 검증 / 표시 서식 | 서식·NaN 처리가 7곳에 복제(U-9) |
| `BodyAnalysisConfig.GenerationPreset` | **🎲 생성 축·프리셋이 무엇인가**(B-93) — 축 넷의 이름·목표값과 **자기 구간의 끝**, 프리셋 넷, 그리고 값이 설 수 있는 범위·순서 규칙(`Limits`). 저장 자리는 `bodyAnalysis.generation`(세계관 단위 = 필드 config) | **축을 사용자에게 열면 *생성기가 겨누는 자리*와 *요약이 가르는 자리*가 곧바로 두 벌이 된다** — 종전에는 경계 상수 넷이 `BodySilhouetteSpec`에, 라벨 문자열 여섯이 `axisSummary` 안에 따로 있었다. 지금은 **구간이 목록에서의 자리에서 파생되고**(앞 축의 끝 → 자기 끝) 요약도 같은 배열을 읽어, 고른 축과 돌아오는 요약이 원리적으로 갈릴 수 없다. `genreTargetIdeals`(목표 비율)도 여기서 파생되며 **프리셋을 이름이 아니라 자리로 찾는다** — 라벨 조회는 사용자가 이름을 바꾸는 순간 예외로 죽고 그 경로는 읽기 카드가 캐릭터마다 지난다. `Limits`가 순수 계층에 있는 이유는 **편집 창이 화면 계층이라 순수 시험이 안 닿기 때문**이다: 잣대가 갈리면 *창은 거부하는데 엑셀로는 들어오는* 값이 생기고 그 값은 고칠 자리가 화면에 없다. **2026.08.18(B-202)에 `Limits`가 하나를 더 들었다 — 생성기가 산출값을 자르는 범위(`*_REACH`)다.** 종전에 그 수는 `BodyGenerator`의 날 `coerceIn(45.0, 110.0)`뿐이라, *구간이 그 창 밖에 놓였는가*를 **창이 잴 방법이 아예 없었다.** 같은 자리에서 `emptyBands()`가 *들어갈 정수 cm가 없는 축*을 답하는데, 그 물음의 잣대가 **요약이 가르는 자리 그대로**(`(앞 축의 끝, 자기 끝]` — 아래 끝이 열려 있다)여야 하고 **키는 이 한 벌이 실제로 내는 것**(`heightSpans()`)이어야 한다. 판정을 순수로 내리면 *아무도 안 부르는 죽은 코드*가 되기 쉬워, 부르는 화면이 있는지까지 검사가 본다(R-62) |
| `BodyTargetRatio` | **목표 비율의 이상값이 어디서 오는가**(B-94) — 기준 넷(자동/장르/이상 몸/작품 평균) 중 **바닥 하나**를 고르고, 키별 직접 고정을 그 위에 얹고, **실제로 쓴 기준과 고른 기준을 함께** 돌려준다(`Basis`) | **부르는 자리가 둘이다** — `BodyAnalysisHelper.analyze`와 **읽기 카드의 즉석 전환**(확정 8번 ㄱ2). 전환마다 분석을 통째로 돌리는 것은 규칙을 다시 매기고 태그를 다시 세는 일이라 값이 안 나오고, 그래서 **카드가 자기 안에서 이상값을 다시 매기고 싶은 유혹이 정확히 그 자리에 있다** — 그렇게 갈리면 B-92의 컵 상수·15판의 축 라벨과 같은 두 벌이 된다. **`requested`와 `source`를 함께 드는 것도 계약이다**: 재료가 없어 떨어졌을 때 하나만 들면 화면이 거짓말을 하거나(고른 것만) 사용자가 자기 선택이 왜 사라졌는지 모른다(쓴 것만). **기본값 `AUTO`는 옛 겹 그대로**라 손대지 않은 필드가 한 글자도 안 바뀐다 |
| `GradeValueResolver` | 등급 라벨 → 숫자 | — |
| `GradeSystemRef` | 등급 체계 참조·재정의·실효 표의 config 표현 + 강등·재배선 규칙 (원본을 고치는 쓰기·전파는 `GradeSystemRepository` 경유 — R-30. 전파가 공허한 신설 삽입·복원·프룬 경로는 예외이며, 직접 호출 자리는 R-30 위반 후보로 점검한다) | — |
| `ui/common/GradeSystemEditor` | **등급 체계를 만들고 고치는 창** — 세계관 편집과 필드 편집이 한 벌로 쓴다 (B-71). 검증(`GradeTable`)·**라벨 개명 추적**·전파 고지가 한 자리에 있다 | 베끼면 그중 한 벌만 고쳐지는 날이 온다. 특히 개명 추적은 **재정의가 개명을 따라가는 유일한 근거**라, 두 벌이 되면 한쪽 화면에서만 재정의가 끊긴다 |
| `UnassignedHistoryScope` | **필드 key를 바꿀 때 미분류 캐릭터의 상태변화 이력을 함께 옮길 것인가** (B-13) — 보관 값이 가리키는 세계관으로 가린다(B-128과 같은 단서) | 이력은 문자열 `fieldKey` 하나뿐이라 행만으로는 어느 세계관 것인지 알 수 없다. 둘째 잣대를 만들면 같은 물음에 두 답이 생긴다(원칙 05) |
| `SnapshotRefs` / `SnapshotRefResolver` / `RestoreTally.pendingKeyOf` | 휴지통 복원의 안정 식별자 (**`naturalKeyOf`가 "이 ref가 자연키로 성립하는가"의 단일 소스** — 해석과 색인 만들기가 같은 함수를 부른다. 전역 구역은 세계관 코드 자리가 `null`, 빈 문자열은 전역이 **아니다** — R-1) | 복원이 남의 엔티티에 붙었다(R-1) · 전역 자연키를 해석만 떨어뜨려 색인이 죽은 코드였다(B-133) · **보류 판정이 해석과 다른 근거를 써서 미리보기는 '유실 없음', 복원은 유실이었다**(B-154 — `noteFieldDef`가 `universeCode`만 봤다) · **코드 없는 구버전 참조가 보류 키를 아예 못 가져 거짓 경고를 냈다**(B-25 — `pendingKeyOf`가 *코드가 있으면 코드, 없으면 `(타입, 옛 id)`*의 단일 소스다. **한 항목이 두 키를 낸다** — 어느 쪽으로 찾는가는 *참조하는 쪽*이 정하기 때문이다(참조당하는 쪽에 코드가 없거나, **참조하는 쪽 payload에 refs가 통째로 없거나**). **id를 보존하며 되살리는 다섯에만 심는다** — 등급 체계·대결 축은 언제나 새 id를 받으므로 거기 보류를 주면 거짓 안심이 된다) |
| `FolderNameToken` / `FolderRoundtripPlanner` / `FolderExportPlanner` / `FolderRoundtripLedger` | 이미지 폴더 왕복의 이름·계획·장부 | 개명 경로가 토큰을 깨 중복 편입(C-1) |
| `CharacterRepresentativeImage` / `ImagePathMatch` | **어느 이미지가 이 캐릭터를 대표하는가**(사다리·시드 랜덤·쓰기 정합) / 경로 대조 + **앱 저장소 봉쇄 판정**(`isInside` — *이 경로를 기기 밖으로 내보내도 되는가*. R-39) | 같은 판정이 아홉 종류로 갈려 규칙이 넷이었다(B-103) · 저장 이미지를 읽는 일곱 자리가 전부 가드를 두는데 **AI 전송 준비기만 없어, 화면에는 플레이스홀더로 그려지는 앱 밖 파일이 제3자 서버로 나갔다**(B-141) |
| `DetachedImageRule` / `DetachedImageMarker` | **무엇이 "캐릭터에서 뗀 것"인가**(순수 판정) / 그 표식을 읽고 쓰는 손 | 붙는 자리 셋·푸는 자리 셋에 규칙을 나눠 적으면 "뗐다가 다시 붙였는데 서랍에 남아 있다"가 생기고 어느 쪽이 틀렸는지 알 길이 없다(B-107 D2). 불변식은 하나다 — **캐릭터가 쓰고 있는 이미지는 뗀 것이 아니다** |
| `ImageDeletionService` | **앱에서 이미지를 지우는 처분**(참조 제거 + 라이브러리 행 제거 + 파일 삭제, 한 트랜잭션) | `ImageManagerViewModel.deleteInternal`에 private으로 갇혀 있어 `_삭제승인`이 같은 일을 하려면 규칙을 한 벌 더 적어야 했다 — 링크 정리·대표 포인터·롤백 조건이 갈라질 자리다(B-107 D6) |
| `RepresentativeImageCell` | 엑셀 `대표이미지` 열의 쓰기·읽기 규약(해석 사다리 5단) | — |
| `AiImageAttach` / `AiImagePreparer` | **AI 요청에 어느 그림을 몇 장 붙이는가**(순수 — 대표 우선·시드 랜덤·직접 고르기) / 전송용 축소·인코딩(Android) | 고르기 규칙을 화면마다 적으면 짧은 값·서술형·보완 재요청의 근거가 갈린다. 비트맵과 한 파일에 두면 **그 규칙을 실행으로 검증할 길이 없다**(B-120 A-7) |
| `FieldValueOverflow` (+ `CharacterFieldValueOverflow`·`NovelFieldValueOverflow`·`EventFieldValueOverflow`) | 그 시트가 **열로 담지 못한 값**의 판정 — 셋이 한 규칙을 쓰고 나머지는 이름만 다른 입구다 | 시트마다 따로 적으면 갈리고, 갈림은 *어느 시트에도 안 나가는 값*으로만 드러난다(B-65) |
| `FieldValueTypeMismatch` | **저장된 값이 지금 제 필드의 타입으로 읽히는가**(B-156) — 데이터 건강도가 *타입이 안 맞게 된 값*을 세고 목록으로 내는 판정. `DataHealthStats.issueCount`는 그 옆에서 **무엇을 "발견 사항"으로 세는가**의 단일 소스다(입력 현황은 빼고 중복 태그·작품 미배정은 넣는다) | **바로 아래 `FieldTypeCompatibility`와 묻는 시제가 다르다** — 그쪽은 *미래형*(저 타입으로 바꾸면 살아남는가, config를 모른다), 이쪽은 *현재형*(지금 제 타입으로 읽히는가, 필드 정의 전체를 안다). 합치면 **등급 값 전부**(라벨은 정수가 아니다)와 **체형 값 전부**(그쪽은 언제나 `false`)가 불일치로 잡혀 정상 데이터가 통째로 경고가 된다. **두 답이 같은 NUMBER에서만 위임한다** — 거기서 갈리면 전파가 *"값 N개가 맞지 않게 됩니다"*라 센 값을 건강도가 못 찾아, 사용자가 **고지받은 것을 고치러 갈 수 없다**. `issueCount`는 종전에 Fragment 안의 덧셈이라 잠글 시험이 없었고, 그래서 **입력량은 세고 진짜 결함(중복 태그·작품 미배정)은 안 세고** 있었다(B-59 · R-15) |
| `DefaultFieldPlan` / `FieldTypeCompatibility` | 전역 기본 필드(B-119)의 **심기·전파·강등 판정** — 자리 판정 `(entityType, key)` · 벌어짐(`SAME`/`OUTDATED`/`DIVERGED`)과 기본 선택 · **`typeChanges`가 "이 세계관에서 타입이 바뀌는가"의 단일 소스**(저장소의 *값 채우기*와 미리보기의 *세기*가 같은 함수를 부른다) / **타입이 바뀌어도 값이 살아남는가**(세는 규칙 — **판정은 한 벌이되 처분은 소비처가 정한다**: 단일 필드 편집은 초기화, 전파는 고지만. B-137) | 채우는 조건이 세는 조건보다 좁아 *"값 N개가 초기화됩니다"* 경고가 **정식 경로에서 통째로 죽어 있었다**(B-135). 고치자 그 경고가 처음으로 떴고, **그 문구가 거짓이었다**(B-137 — 전파는 값을 지우지 않는다) |
| `FieldScopeCell` | **시트의 세계관 칸이 빈 것이 무슨 뜻인가** — 빈 칸 = 전역(무소속) 구역 | 판정이 다섯 경로에 흩어져 **둘만 알고 있었다**: '필드 정의'는 갈래가 있고 오버플로 두 시트·값 라이브러리 미리보기는 없어서, 내보내기가 빈 칸으로 낸 전역 값을 가져오기가 `skippedRows`로 버렸다(B-130) |
| `FieldDefinitionPrune` | 덮어쓰기에서 **지울 정의를 고르는 판정**(구역마다 매칭 근거를 따로 본다) | *'백업에 없다'*와 *'백업이 그 구역을 말하지 않았다'*를 가르지 않아, 시트가 다루지도 않은 전역 구역의 정의가 **값과 함께 CASCADE로** 사라졌다. 파일 단위 안전장치 둘(시트 부재·매칭 0건)은 세계관 필드가 매칭돼 그대로 통과했다(B-130) |
| `CharacterFieldValueMerge` / `EventFieldValueMerge` / `NovelFieldValueMerge` | 부분 저장의 커버 집합 — 종류마다 하나씩, **규칙은 하나** | 사건 필드값이 무통보 폐기됐다(S-6) |
| `EntityFieldHeaders` | 한 시트가 전 구역의 커스텀 필드를 동적 열로 실을 때의 헤더 규칙(연표·작품 시트) — 이름·구역 한정자·`(쉼표 구분)` 접미사·**열을 세울 수 있는가**(`plan`) | — (확-3에서 사건 전용 이름을 승격). **2026.08.11(B-177·B-65)**: 쓰기는 새 머리 하나, **읽기는 둘**(옛 머리 폴백이 없으면 이미 내보낸 파일의 그 열이 통째로 버려진다). **2026.08.16(B-222 ②)**: `MULTI_SUFFIX`가 **고정 열에도 적용됐다** — '필드 데이터' 시트의 `별칭(콤마구분)`이 `별칭 (쉼표 구분)`이 되어, 쉼표를 쪼개는 열의 안내가 시트를 가리지 않고 한 말이 됐다(머리 글자는 `FieldValueSheetMapper.ALIAS_HEADER` 하나가 들고 spec·가져오기 둘·안내문이 그것을 읽는다). **고정 열의 폴백은 자리가 다르다** — 동적 열은 `expectedHeaders`/`parseFallback`이 들지만, 고정 열은 **`ExcelHeaderAliases` 등재 한 줄**이 든다. 그 한 줄이 읽기 사다리와 *'인식하지 못해 무시했습니다'* 경고를 **함께** 고치는 것이 요점이다(가져오기 쪽에만 폴백을 두면 옛 파일마다 *읽히면서 못 읽었다고 말하는* 경고가 붙는다) |
| `EntityFieldColumnResolver` | 그 헤더의 역함수 중 **구역 해석** — 이 열이 어느 구역의 필드인가 | 작품·사건 두 경로가 같은 `when`을 각자 들고 있었고 둘 다 *"이 행의 세계관 필드"*만 받아, 세계관을 옮겨 생긴 값이 왕복에서 사라졌다(B-65) |
| `SortComparators` / `FieldFilterHelper` / `UnassignedFilter` | 목록 정렬·필터. **`FieldFilterHelper`는 *필터가 어느 필드를 가리키는가*의 단일 소스이기도 하다**(B-11) — **키 → (시스템 열) → id 사다리**(`DuelCandidateFilter.resolve`와 같은 순서)와 **같은 대상 판정**(`sameTarget` — 칩을 갈아 끼우고 지우는 동일성) | **정렬은 키로 병합돼 있는데 필터만 id였다** — 한 화면 안에서 두 잣대가 갈려, 전역 뷰가 세계관 A·B의 같은 키를 한 번에 못 걸렀다(B-11). **키가 죽었을 때 id로 떨어뜨리지 않는 것이 계약이다**(id는 재발급되므로 구제하면 남의 필드를 거른다) · **별칭·토큰 해석은 필드마다 다시 한다**(값 라이브러리가 필드 단위라, 한 번만 해석해 돌려쓰면 다른 세계관의 정규값이 조용히 빠진다) |
| `FieldSuggestionEntries` | **폼이 권하는 값의 잣대**(B-129) — 값 라이브러리 엔트리에서 자동완성에 세울 것을 고른다(숨김 제외 · 사용 횟수 내림차순, 동률이면 값 순) | **잣대가 SQL에만 있었다**(`FieldValueEntryDao.getForUniverse`). 그 질의는 `fd.universeId = :universeId`로 묶여 있어 **전역 구역(무소속)에서 원리적으로 빈 목록**이고, 무소속 작품 폼이 필드를 그리기 시작하자 *필드는 있는데 제안만 없는* 자리가 됐다 — 고장과 구분되지 않는다. **순수 계층으로 올린 값은 둘이다:** ⓐ 전역 구역이 열린다(엔트리는 필드 id로만 매여 구역이 개입할 자리가 없다) ⓑ **질의가 하나 없어진다** — 폼은 허용 목록 판정 때문에 `entriesForFields`로 **같은 엔트리를 이미 읽어 두고 있었다**(숨김 포함. 제안이 그 계약에서 숨김을 빼는 쪽이다). 사건 편집·캐릭터 폼은 아직 옛 길이라 여기가 갈릴 수 있는 자리다 |
| `PresetLimit` | 프리셋 개수의 **권고** 한도 — 목록·검색 두 저장소와 엑셀 가져오기가 함께 본다 | 같은 수 20이 **두 벌**이었고 경로마다 법(인앱 하드 차단)과 권고(엑셀 수용)로 갈려, 21번째를 원하는 사용자에게 인앱 확장 경로가 없었다(B-75 — 확정 19번 ㄱ1로 권고 통일) |
| `PresetNameConflict` | 프리셋 **이름 겹침의 판정** — 겹쳤는가 · 기본 프리셋인가 · 자기 자신인가, 그리고 '다른 이름으로 저장'의 제안 이름. 두 프리셋 화면(캐릭터 목록 · 전역 검색)이 **저장·개명 네 자리**에서 함께 본다(창 자체도 한 벌이다 — `ui/common/PresetNameDialog.kt`) | 판정을 화면마다 짜면 *한쪽은 물어보고 한쪽은 안 물어보는* 상태가 된다 — **바로 위 `PresetLimit` 행이 같은 두 화면에서 실제로 겪은 그 모양이다**(B-191 · R-60). 겹침의 잣대는 **DB 유니크 색인과 같아야** 하므로(앞뒤 공백만 다듬는다) 다듬기도 여기 한 자리다 |
| `FieldValueFixRoute` | **타입이 안 맞는 값을 고치러 가는 길** — 어느 종류가 어느 화면으로 가고 무엇을 싣는가, 그리고 **인자 이름**(`focusEventId`·`focusNovelId`·`focusFieldId`·`focusFieldName`). 보내는 화면(통계 건강도)과 받는 화면 셋이 전부 이 상수를 지나고, 자원인 `nav_graph.xml`만 예외라 **그 선언이 있는지를 검사가 본다**(R-61) | **이 부류의 실패는 예외가 아니라 침묵이다** — 이름이 갈리면 화면은 그냥 뜨고 아무 일도 안 일어난다(누른 사람은 자기 부탁이 없던 일이 된 줄 모른다). 인자는 타입 없는 문자열이고 짝은 다른 파일 다른 계층이라 컴파일도 순수 시험도 원리적으로 못 본다. **목적지를 enum으로 내린 것이 그 갈래를 시험에 넣은 자리**이고(`R.id.*`는 순수가 못 본다), 화면에는 자원 id로의 번역 한 줄만 남는다(B-198) |
| `ImportSource`(`ImportWorkbook`/`Sheet`/`Row`/`Cell`) | 가져오기가 워크북을 읽는 유일한 접근면 | DOM·스트리밍 두 경로가 갈리면 같은 파일이 다른 데이터가 된다(B-8) |
| `ResetPlan` | 앱 초기화가 비우는 테이블의 범위 | 26개 중 5개가 '모든 데이터 삭제' 뒤에도 살아남았다(S-13) |
| `DuelRecords` | **저장된 대결 기록(코드) ↔ 순수 계층(id)의 해석** — 참가자 코드 배정 · 처분의 저장 형식과 정규 키 · 사라진 참가자 세기 · **코드를 무엇으로 견주는가**(`CodeMatch` — B-175) | 저장을 id로 하면 휴지통에서 되살아난 캐릭터의 판이 **남에게 붙는다**(R-1의 오배정). 해석을 저장소에 두면 이 앱의 어떤 로컬 검증도 그 규칙을 못 본다 — 그래서 pure다. **대조까지 여기 있는 것은 축마다 코드의 뜻이 다르기 때문**이다(캐릭터 축은 개명되지 않는 `code`, 이미지 축은 **경로**). 정규화는 **비교 시점에만** 하고 저장·표시는 원본 표기다(R-42 · R-51) |
| `DuelImageRoster` | **이미지 축에서 판·처분이 누구 몫인가**(B-104 이미지 축 · B-175) — `owners`(어느 그림이 누구 것인가) · `claimOf`(내 것 / 캐릭터를 넘음 / 주인 없음) · `build`·`split`·`splitOf` · **`crossCharacterMatchesOf`(넘는 판을 *고르는* 쪽 — B-208)** | 이미지 대결은 **캐릭터마다 따로 논다**(사용자 확정 — 설계 13-2). 몫 가르기를 자리마다 적으면 **고르는 화면의 진행률과 순위표의 판 수가 어긋나고**, 실제로 순위표는 몫을 아예 가르지 않아 남의 캐릭터 판을 *"지워진 참가자의 판"*이라 고지했다(거짓 고지 — 개발 의도 2번). **성격이 다른 둘을 갈라 세는 것이 이 객체의 요점**이다: 지워진 그림이 낀 판(내 대결의 일부였다 → 적합의 고아)과 남의 그림이 낀 판(누구의 대결도 아니다 → 따로 고지). **세는 쪽 옆에 고르는 쪽을 둔 것도 같은 뿌리다**(B-208) — 순위표는 그 판을 *수*로 말하고 기록 화면은 *줄*로 보이는데, 한쪽이 제 자리에서 조건을 하나 빠뜨려도(범위·지워진 그림) 오류가 나지 않고 **갈린 수만 사용자가 본다** |
| `WorldPackageDuels` | **월드패키지의 대결 재배선** — 내보낼 판을 참가자 소속으로 거르고(범위 밖은 세어 고지), 가져올 판의 **참가자 코드·이미지 경로를 재발급 표로 다시 잇는다**(정규 키 재생성 · 기준 축 유일성 포함) | 가져오기가 code를 **재발급**하므로 표를 따라가지 않으면 판이 **이미 있던 남의 캐릭터에 붙는다**(R-1). 세력 간 관계와 달리 **이름 폴백이 없다** — 판은 사람이 읽는 이름이 없고 동명이인 해석은 그 자체가 오배정이라 **해석 실패는 언제나 제외 + 계수**다. 배선(`WorldPackageImporter`)은 Room을 물어 순수 JVM이 닿지 않으므로, 묻어 두면 이 위험 전체가 시험 없이 남는다 |
| `DropdownListLimits` / `DropdownListSheet` | **드롭다운을 명시 목록으로 실을 수 있는가**의 판정과, 못 실을 때의 **보관처**(B-221). 판정은 순수(수식 255자 · 값 안의 쉼표·따옴표 · 상자 본문 한도 255/225)이고, 보관은 POI를 물어 숨김 시트 한 열에 **이어 붙인다**(`목록 데이터` — 예약명) | **한도를 넘긴 파일은 엑셀이 *복구 경고*와 함께 그 열의 유효성 검사를 통째로 제거한다** — 조용한 유실은 아니지만 사용자가 보기엔 *앱이 만든 파일이 깨진 것*이고 드롭다운은 없다. 사용자가 늘리는 목록(작품·세력·세계관·축)이라 **목표 규모에서 반드시 걸린다**(작품 60개면 넘는다). **깨는 것이 길이만이 아닌 것이 이 행의 요점** — 쉼표는 구분자라 값 하나를 둘로 쪼개고, 그 둘은 **짧아도** 깨진다. 이어 붙이는 이유는 스트리밍이 되돌아간 행을 버리기 때문이고(`ExportWorkbooks`), 그 성질은 `DropdownListSheetTest`가 두 구현에 같은 쓰기를 시켜 **파일로 되읽어** 잠근다 |
| `ExportWorkbooks` | **내보내기 워크북의 생성·해제** — DOM(`XSSFWorkbook`)과 스트리밍(`SXSSFWorkbook`) 두 구현 · 임시 파일 자리 · 임시 파일까지 함께 놓는 해제 | 어느 구현으로 썼는지는 **사용자에게 알리지 않는다**(가져오기의 `ImportWorkbook`과 같은 전제 — *두 경로의 결과가 같다*). 그 전제를 `ExportWorkbookParityTest`가 잠그고, 그래서 실기기 측정이 스트리밍의 대가를 문제로 지목하면 **인자 하나로 되돌릴 수 있다**(R-49 · scalability S7) |
| `DuelRating` / `DuelPairing` / `DuelCounterRelations` | 대결(B-104)의 세 계층 — **이 축에서 누가 얼마나 센가**(Bradley–Terry MAP · 확신 · 짝 집계) / **다음에 무엇을 붙일까**(정보량 순서 · 확신 기준 접기 · 표적 재확인 · 진행률) / **무엇이 상성인가**(잔차 천적 · 순환 · 흔들림 · 2단 적합). 설계 정본은 `docs/duel_system_design_2026-08.md` | 짝 집계를 세 곳이 따로 세면 갈라진다 — 그래서 `Fit.tallies` 하나가 낸다. 상성을 3-순환으로만 정의하면 **천적을 원리적으로 못 본다**(설계 3장) |
| `DuelSession` / `DuelRound` / `DuelCardGrid` / `DuelStandings` | 대결 **화면**의 판단 — *"지금 무엇을 물을 것인가"*(대기열 소비 · 떠 있는 짝 고정 · 되돌리면 다시 묻기) / ***"한 화면에 누구를 올리고 그 누름이 어떤 판이 되는가"*(k지선다 — B-115)** / ***"그 카드를 어느 자리에 놓는가"*(슬롯 넷의 2×2)** / *"순위표와 상성 상세가 무엇을 보이는가"*(동점 등수 · 근거 · 고지 · 후보와 처분의 대조) | **화면 안에 두면 어떤 로컬 검증도 못 본다**(「세션 착수 규칙」 4번). 대기열이 도는 결함은 *끝나지 않는 대결*로, 처분 대조가 어긋나면 *한 관계가 두 줄*로 나타나는데 둘 다 컴파일은 통과한다. **`DuelRound`가 여기 있는 까닭이 특히 분명하다** — 그것이 **1:1을 `k=2`로 흡수**해 갈래를 없앴으므로(설계 14-2), 그 동치가 깨지면 **k지선다를 켠 적 없는 사용자의 기록까지 조용히 달라진다.** `canReuse`도 같은 부류다: 없으면 배경 재계산마다 셋째·넷째 카드가 갈려 **고르려던 얼굴이 손가락 아래에서 바뀌는데**, 눈으로는 *"가끔 카드가 바뀌네"* 정도로만 보인다 |
| `DuelFieldLinks` | **축과 필드를 잇는 규칙** — 저장 형식(JSON·엑셀 글) · 값에서 수 읽기 · **사전식 예측**(1순위가 갈리면 거기서 끝) · 예측과 실제의 대조 · **프로필로 걸린 산출 필드 세기**(B-122) · **걸려 있는 산출과 *일하는* 산출을 가르는 자리**(`effectiveOutcomes` — B-167. 충돌 경고·프로필 막기·카드 감추기·순위표 대조 **넷이 이것 하나를 본다**) | 연결이 셋(영향·산출·프로필)인 것은 **방향과 쓰임이 전부 다르기** 때문이다 — 영향은 *필드 → 대결*(보이고 **예측에 쓴다**), 산출은 *대결 → 필드*(보이면 답을 미리 주는 셈이다), 프로필은 *표시 전용*(보이지만 **예측에 안 쓴다**). 해석을 화면·엑셀·DB 세 곳에 두면 *"S-1"*을 −1로 읽는 부류의 어긋남이 자리마다 다르게 생긴다 |
| `DuelSystemFields` | **캐릭터 표의 열을 축에 거는 규칙**(B-167) — 의사키 어휘(`sys:`) · 열마다 다른 **토큰 나누기**(이명·태그는 쉼표, 메모는 통째로) · **진짜 필드와 부딪칠 때의 승자**(`shadowed` — 사용자가 만든 것이 이긴다. R-40) | 막혀 있던 것이 *타입*이 아니라 **소속**이었다 — 링크 창이 `FieldDefinition` 행만 내는데 이명은 `Character.anotherName`이라 **목록에 낼 행 자체가 없었다.** 어휘를 `SemanticRole.linkedKey`의 `__`와 **갈라 연** 것이 요점이다: 그쪽은 *어느 커스텀 필드가 나이인가*이고 이쪽은 *필드가 아예 없다*라 **뜻이 정반대**이며, 접두를 나눠 쓰면 값을 `character_field_values`에서 찾을지 `characters`에서 찾을지가 갈려 조용히 빈 값이 된다 |
| `DuelCandidateFilter` | **대결 후보 필터의 해석·대조·참가자 조립**(B-168) — 필터를 **키로** 해석하는 사다리(키 → 시스템 열 → 구식 id · **진짜 필드가 이긴다**) · **해석 실패 = 후보 0**(닫히는 실패) · **참가자 = 후보 ∪ 전적 보유** · 엑셀 칸의 사실 셋(없는 열/빈 칸/읽을 수 없는 칸 — `fromSheetCell`) | 조건 자체는 `FieldFilter` 그대로이고 커스텀 대조도 `FieldFilterHelper`가 한다(언어 두 벌 금지 — 원칙 05). 이 파일이 더한 것은 **id가 못 미더운 자리의 해석**(축은 복원·엑셀을 탄다)과 **시스템 열의 대조**뿐이다. *전적 있는 비후보를 남기는* 조립을 화면마다 적으면 한 화면이 빼는 순간 그 판이 고아가 되어 **남의 점수까지 움직인다**(설계 9-3) |
| `RepresentativeWeighting` | **대표 이미지 추첨의 가중치**(B-104 소비처 ⓑ) — 점수를 되돌린 γ가 그대로 무게이고 세기는 지수 α(꺼둠/약하게/세게) · **한 눈금(400점)에서 끊어** 최악 비율을 `10^α`로 묶는다 · **기울일 것이 없으면 `null`** | 무게를 지어내지 않는 것이 요점이다 — γ는 이미 *"i가 j를 이길 확률"*로 정의된 수라 *"확률이 두 배면 두 배 자주"*가 되어 뜻을 따로 설명할 필요가 없다. **끊는 빗장이 이 기능의 존재 이유**다: 안 끊으면 1위의 무게가 수백 배가 되어 *확률*이 **고정**으로 변질되고, 그것이 사용자가 물리친 바로 그 안이다(`representative_image_design` D9의 갈음). **`null`을 내는 것도 계약이다** — 그래야 호출부가 종전 `randomIndex`를 그대로 타고 *"대결을 안 한 캐릭터는 지금과 완전히 같다"*가 약속이 아니라 코드의 성질이 된다 |
| `DuelImagePrune` | **걸러낼 이미지 후보의 판정**(B-104 소비처 ⓒ) — 하위권 %(비율 그대로) + 최소 판 수 · 동점은 함께 들고 함께 빠진다 · **제안 목록을 낼 뿐 아무것도 지우지 않는다** | 특례를 두지 않는 것이 판정이다 — *"하위 20%"*는 다섯 장이 있어야 한 장이 거기 든다는 뜻이고, 작은 집합에 억지로 한 장을 세우면 그림 둘뿐인 캐릭터에서 **절반이 후보**가 된다. 판 수 하한이 없으면 흔들리는 점수로 *"지우시죠"*라고 말하게 되는데 그것은 앱이 지어낸 근거다(원칙 02) |
| `DuelCardInfo` | **대결 카드가 무엇을 띄우는가** (B-122) — 구역 셋의 배분(트리오 · 프로필 · 영향) · **빈 값의 처분이 두 구역에서 반대인 것** · 상한 넷과 접힌 수 · 겹치는 필드를 한 구역에만 두기 · **읽을 키 세기** | 카드가 *'그림 위 스크림'*에서 *'그림 + 정보 패널'*로 갈리며 정할 것이 늘었는데, 화면에 두면 어떤 로컬 검증도 못 본다(「세션 착수 규칙」 4번). **가장 값어치 있는 판정은 "산출 필드는 프로필 경로로도 못 올라온다"**이다 — 고르는 창이 막지만 엑셀은 그 창을 지나지 않고, 그 우회가 열리면 물음과 답이 한 화면에 놓인다. 빈 값 규칙이 반대인 것도 일부러다: 영향은 줄이 남아야 **두 카드의 자리가 맞고**, 프로필은 빠져야 좁은 카드의 상한 넷이 헛돌지 않는다. **읽을 키를 화면이 따로 세면** 값이 있는데도 빈 줄이 뜨고, 그것은 *"값을 안 적었다"*와 구별되지 않는다 |
| `QuotePicker` | **명대사 여럿 중 어느 것이 뜨는가** (사용자 요청 2026.08.20) — 생일 자리의 되돌아감(생일 전용 → 없으면 일반) · 평소 자리에서 생일 전용을 빼는 것 · **그날그날 무작위이되 같은 날은 고정**인 고르기 | **주사위가 아니라 셈인 것이 이 행의 요점이다.** 고르기를 `(날짜, 캐릭터)`의 함수로 두지 않으면 같은 날 **생일 축하 창과 대결 카드가 다른 대사**를 띄우고, 회전·재진입에도 대사가 갈려 무작위가 아니라 고장으로 읽힌다. 씨앗을 밖에서 받는 것도 일부러다 — 안에서 `Calendar`를 부르면 *"내일은 다른 대사가 뜨는가"*를 시험이 물을 수 없다. **계약이 "같은 씨앗 + 같은 목록 = 같은 답"**이라(목록 차례가 답을 정한다) DAO가 어디서든 `sortOrder ASC, id ASC`로 내야 성립한다 |
| `BirthdayCelebration` | **오늘 축하 창의 쪽에 무엇이 담기는가** (사용자 요청 2026.08.20) — 월·일 없는 줄의 처분 · 지워진 캐릭터의 처분 · 작품 제목·대표 그림·대사 붙이기 | `DuelCardInfo`와 같은 자리다(화면에 두면 어떤 로컬 검증도 못 본다). **오늘치 판정을 스스로 하지 않고 [`BirthdayHelper`]에서 받는 것**이 요점 — 윤년 2/29 규칙이 두 벌이 되면 축하 창과 위젯·알림이 **다른 날 다른 사람**을 말한다. **받는 것이 id가 아니라 맞은 행인 것도 같은 근거다**(콜드 검토 2026.08.20): `__birth`는 DB가 강제하는 유일 행이 아니라 엑셀이 둘째 행을 만들 수 있고, id로 행을 다시 고르면 **오늘 맞지 않은 행**의 날짜를 적는다. **나이를 세지 않는 것도 판정이다**: 표준연도 연동을 안 건 캐릭터·표준연도 없는 작품·무소속이 전부 정답이 없어, 지어내면 축하 자리에 틀린 숫자가 뜬다 |
| `DuelMatchLog` | **기록 화면이 무엇을 짚어 주는가** — 승자 읽기(깨진 판을 무승부로 접지 않는다) · 사라진 참가자 보존 · 필드와 어긋난 판 표시 · 상한 고지 | 목록이 *고칠 자리*를 짚지 못하면 수만 줄 가운데 무엇을 볼지 알 수 없다(원칙 02). 그 판단은 화면에 두면 어떤 로컬 검증도 못 본다 |
| `ImageTagVocabulary` | **이미지 태그 AI가 쓰는 어휘**(빈도 상위 태그 + '어휘에 포함' 필드 값 + 상한·절단 집계) — 폴더 이름 제안기(A-1 계열)와 내용 일괄 태깅(A-9)이 **같은 함수**를 쓴다 | 두 벌이면 **같은 앱의 두 AI가 다른 어휘로 말한다** — 한쪽만 `흑발`을 알면 같은 이미지가 어느 경로로 태깅됐는지에 따라 표기가 갈린다. 2026.08.07(B-121)에 `ImageFolderTagSuggester`의 companion에서 옮겨 왔다 |
| `ImageBatchTagSuggester` | **이미지 내용을 보고 다는 태그**(A-9) — 배치 나누기 · 번호 봉인 프롬프트 · **응답 index 검증** · 어휘 접기 · 드롭·실패 집계 · **되받기를 앞 실행 위에 얹는 규칙**(`mergeRetry`)과 **다시 물을 값어치의 판정**(`retryablePaths`) | 한 요청에 여러 장을 실으므로 **응답의 태그가 남의 이미지에 붙을 수 있다.** 번호가 어긋나면 **배치를 통째로 접는 판정**이 이 파일에 있고, 흩어지면 *절반만 믿고 붙이는* 경로가 생긴다(R-1 — 오배정은 생략보다 나쁘다). **`AiService`가 이미지 거부 시 돌려주는 '성공'을 실패로 뒤집는 자리**이기도 하다 — 그림이 근거의 전부인 기능이라 글만 보고 나온 태그는 지어낸 것이다. **합치는 규칙이 여기 있는 이유**(B-140): 화면이 되받기를 갈아치우기로 처리해 *"끝난 배치의 제안은 살린다"*가 **화면 계층에서만 뒤집혀 있었다** — 규칙을 순수 계층에 두어야 시험이 닿는다(R-38) |
| `ImageFilterHelper` | 이미지 탭의 필터 매칭 — 기본(소유·상태) × 링크 × **태그 유무**(`TagFilter`) × 태그 OR × 검색어 | 축을 화면에 흩으면 *"왜 좁아졌는지"*를 목록이 말하지 못한다. **태그 유무를 `tags`와 갈라 둔 것은 없는 것에는 이름이 없기 때문**이다(칩으로 담을 수 없다. 2026.08.07 B-121 신설 — 일괄 태깅의 진입 동선이 *"태그 없는 것들을 한꺼번에"*다) |
| `DuelScoreIndex` | **대결 점수가 대결 밖에서 무엇으로 보이는가** (B-117) — 순위표의 줄을 밖이 쓰는 표로 옮기는 것 · **무기록 참가자를 값 없음으로 두는 판정** · 값 없음의 정렬 자리 · 분포 구간 · **점수 보유자끼리의 등수 재매김**(`rankWithinScored` — 2026.08.07에 `DuelAiContext`의 private 사본을 여기로 올렸다. 소비처가 둘이 되는 순간 각자 매기면 같은 캐릭터가 화면마다 다른 등수를 갖는다) | 설계 4장은 *"파생값을 저장하지 않는다 — 저장하면 두 자리가 갈라진다"*고 못 박았는데, **소비처가 셋이 되니 저장하지 않아도 갈라질 수 있었다**(각자 적합하면 옵션 하나에 점수가 달라진다). 저장 금지의 나머지 절반이 **출구를 하나로 묶는 것**이고 이 파일이 그 출구다. 무기록 판정을 여기 둔 것도 같은 이유 — 앵커 1500을 값으로 섞으면 **아무것도 안 한 캐릭터가 목록 한가운데에 박힌다**(코드가 하지 *않는* 일이라 시험으로만 지켜진다) |
| `DuelAiContext` | **대결이 AI 프롬프트에서 무엇으로 보이는가** (소비처 ⓔ) — 실을 축 고르기 · **등수 재매김을 부르는 것**(계산 자체는 `DuelScoreIndex.rankWithinScored`로 올랐다 — B-113이 같은 재매김을 필요로 했다) · 줄의 모양(`강함: 12명 중 3위 · 1523 ±37 (12판)`) · 상한과 뺀 수 고지 | 조립기가 둘이라(짧은 값 추천 · 서술형 작성) **각자 자르면 같은 캐릭터가 경로에 따라 다른 축을 받는다.** 등수를 다시 매기는 것이 이 파일의 핵심 판정이다 — 순위표 줄의 등수는 **한 판도 안 친 참가자까지 세는데** 그들의 앵커 1500이 표 한가운데 앉아, 그대로 옮기면 분모를 넘는 *"3명 중 4위"*가 나온다(설계 10-3. 자기 재검토가 잡았다). 프롬프트 문자열은 **어떤 로컬 검증도 읽지 않으므로** 시험이 유일한 방어선이다 |
| `DuelGradeAssign` + `DuelGradeRef` | **대결 순위가 어떻게 등급이 되는가** (B-113) — 컷 검증(단조 **비**감소 · 0% 구간 허용) · 배정 · **자연 단절 경계 제안**(±오차 겹침 조건) · 미리보기 3분류 / 그 약속의 config 표현(축 code · **누적** 컷 · 반영 흔적). 설계 정본은 `docs/feature_roadmap_2026-08.md` 4장 | 백분위의 **분자와 분모가 둘 다 점수 보유자**인 것이 이 계산의 전부다 — 순위표 줄의 등수를 그대로 나누면 100%를 넘고, 100%를 넘는 값은 **어떤 컷에도 걸리지 않아 말없이 최하 등급이 된다**(그래서 하네스가 `Entry.rank`에 일부러 쓰레기를 넣는다). 컷을 **누적**으로 저장하는 것도 판단이다 — 화면은 폭으로 보이지만 폭으로 저장하면 라벨 하나가 사라졌을 때 남은 폭을 누구에게 줄지 다시 계산해야 하고, 그 계산이 곧 합이 100이 아니게 되는 자리다. 경계 표식이 **자기 자신을 세지 않는** 것도 여기 — 세면 오차가 1이어도 전원이 '경계'가 되어 표식이 아무 말도 안 한다 |
| `AiProviderCodec` | **프로바이더 설정이 저장될 때 무엇이 실리는가** (B-132) — 필드별 JSON 키 · null과 "설정됨"의 구별 · 손상 항목만 건너뛰기 · 등록순 정렬 | `imagesUnsupported`가 모델·`hasLearnedFacts()`·R-23 초기화까지 배선되고 **직렬화에만 빠져 있었는데**, 저장소가 Context 의존이라 **어떤 로컬 검증도 그 누락을 못 봤다**(컴파일도 정적 검사도 통과한다). 증상은 조용했다 — 비전 미지원 모델에서 청크마다 400→재시도가 되풀이되고 사전 고지 코드 세 자리가 죽은 길이 됐다. 순수로 갈라 둔 값어치는 개별 필드가 아니라 **부류**다: 시험이 필드 목록을 손으로 적지 않고 `AiProviderConfig` 선언에서 읽으므로, **새 학습값을 더하고 코덱을 잊으면 그 자리에서 실패한다** |
| `AiProviderFallback` | **한도에 걸리면 어디로 이어가는가** (B-108) — 두드릴 **순서**(활성이 맨 앞 · 쿨다운은 맨 뒤 · 키 없으면 제외 · 최대 3곳) · **방아쇠**(429는 재시도 먼저, 크레딧 소진은 곧바로 전환, 나머지는 그대로) · **쿨다운 판정**(10분, 벽시계 어긋남 포함) · **전환 고지 문구** | 전환을 관문(`AiService`) 안에 두지 않으면 인앱 기능 여섯이 각자 폴백을 짜고 **한 벌만 빠뜨려도 그 기능에서만 조용히 종전처럼 죽는다**. 그 판정을 다시 관문 **안**에 인라인으로 두면 저장소·네트워크가 Context를 쥐고 있어 **어떤 로컬 검증도 못 본다**(B-132가 난 그 자리). 갈라 두면 순서·방아쇠·쿨다운 전부가 시험 대상이 된다. **가장 값진 잠금은 새 기능이 아니라 옛 기능에 있다** — *"아무것도 실패하지 않으면 종전과 글자 그대로 같은 곳이 답한다"*가 깨지면 한도에 걸린 적 없는 사용자의 요청이 다른 모델로 나가고 **화면에는 아무 표시도 안 난다**. 고지 문구가 여기 있는 이유는 R-44다 — 호출부가 각자 지으면 같은 사실을 여섯 가지로 말하고, 하나가 빠져도 아무도 모른다(`tools/check_ai_switch_notice.sh`가 그 등재를 본다) |
| `AppSettingsKeys` + `AppSettingsBindings` | **엑셀 '앱 설정' 시트가 무엇을 싣는가** (B-105) — 키·형식·**처분**(실음/비밀)의 순수 선언과, 그 키를 실제 저장소에 잇는 읽기·쓰기 한 벌. 싣지 **않는** 저장소도 사유와 함께 여기 등재된다 | 종전에는 이 사실이 내보내기의 나열과 가져오기의 `when` **두 곳**에 갈려 있었고, 그래서 설정이 늘지 않았다. 한 벌로 모으되 **선언만 순수하게 갈라 둔 것**이 요점이다 — 합치면 *어떤 키가 있고 무엇이 비밀인가*라는 왕복 계약까지 `Context`에 딸려 가 순수 JVM 시험이 원리적으로 못 본다(B-132가 난 자리). 갈린 두 벌이 어긋나는 것은 `tools/check_app_settings_catalog.sh`가 본다 — **그 검사가 없으면 이 분리가 곧 종전의 '두 곳'이다**. 제외 목록에 **사유**를 요구하는 것도 이 표의 몫이다: 이름만 나열하면 다음 사람이 왜 빠졌는지 몰라 다시 조사한다 |
| `AiProviderActivation` | **저장이 활성 프로바이더를 옮기는가** (B-150) — 새 항목인가 · 키가 있는가 · 쓸 수 있는 활성이 있는가의 세 축 판정과 **고지 대상 여부** | 종전 규칙 *"첫 프로바이더일 때만 활성"* 한 줄 때문에 **한도에 걸린 프로바이더를 갈아 끼울 수 없었다**(사용자 보고). 판정을 저장소 안에 두면 Context 의존이라 시험이 못 본다. **키 조건이 '옮길 때'에만 걸리는 것이 핵심**이다 — 활성이 아예 없을 때까지 키를 요구하면 첫 등록의 종전 동작이 조용히 사라지고, `NO_KEY`(*"키를 등록해 주세요"*)보다 나쁜 `NO_PROVIDER`(*"프로바이더가 없습니다"*)가 뜬다. **근거가 시험에 없으면 다음 사람이 두 갈래를 통일한다** |
| `joinCsv` + `splitCsv` (`excel/SheetSpec.kt`) | **목록 셀이 여러 값을 어떻게 담는가** (B-27 ② · R-47) — **감싸기 판정**(값이 쉼표나 따옴표를 품으면 `"…"`, 안의 따옴표는 겹쳐 쓴다) · **되쪼개기**(칸의 첫 글자일 때만 따옴표가 감싼 것이다) · **옛 파일 경로**(따옴표가 없으면 종전 구현 그대로, 어긋난 따옴표도 옛 규칙으로 되돌린다) · 전각 쉼표 수용(F4) | 붙이는 쪽과 쪼개는 쪽이 갈리면 **내보낸 파일이 스스로를 다시 읽지 못한다** — 그리고 그 파손은 이름에 쉼표가 든 사용자에게만, 왕복 한 번 뒤에 나타나 **되돌릴 값이 이미 없다.** 한자리에 두면 *감싸는 조건*과 *푸는 조건*이 문법적으로 한 벌이고, 옛 파일 처분도 한 번만 정하면 된다. 새 목록 열이 쌍을 빠뜨리는 것은 `tools/check_list_cell_csv.sh`가 본다(시험은 *규칙이 옳은가*를 재지만 이 결함의 모양은 **아무도 부르지 않는 것**이다) |
| `AiErrorText` | **AI 오류 문구의 조립 차례** (B-150) — 프로바이더 줄 → 분류 문구(+HTTP 코드) → 제공사 원문 | 프로바이더 줄이 **맨 앞**인 것이 판정의 내용이다 — 뒤로 밀면 길이가 들쭉날쭉한 제공사 원문에 가려 사실상 안 보이는데, **코드는 멀쩡해 보이고 문구도 다 들어 있어 눈으로는 통과한다.** 종전에는 조립이 `Context.getString`과 한 함수에 묶여 있어 어떤 로컬 검증도 읽지 못했다 |
| `FieldConfigTransfer` | **필드 config가 경계를 넘을 때 무엇을 잃는가** — 축이 **둘**이고 합치지 않는다: `demoteAcrossUniverse`(세계관 — 등급 체계 참조와 대결 축 참조를 **함께** 걷어낸다. B-113 · R-35) · `demoteAcrossEntityType`(**종류** — 이 종류에서 뜻이 없는 설정을 걷는다. B-63). 걷는 쪽과 **세는 쪽**(`droppedKeys`)이 한 함수라 미리보기가 예고한 것과 심기는 것이 갈리지 않는다 | 자리마다 두 줄씩 쓰면 **다음 자리에서 한 줄만 쓰는 것이 자연스러워지고**, 그때 빠지는 쪽은 늘 새로 들어온 키다(R-29가 겪은 그 형태). 축 code는 전역 유니크라 남으면 **남의 세계관 축을 정확히 찾는다** — 못 찾는 것보다 나쁘다 |
| `RankingSource` | **통계 순위 화면이 무엇으로 줄 세울 수 있는가** (B-117) — 필드와 대결 축이 한 목록에 서는 차례 · 고른 것을 저장·복원하는 열쇠 | 스피너 하나에 두 부류가 담기므로 **차례가 곧 계약**이고, 화면이 위치로 되짚는다. 화면 안에서 인덱스를 셈하면 「세션 착수 규칙」 4번이 말한 *보이지 않는 자리*가 된다. 열쇠에 접두사를 붙이는 것도 여기 — 필드 key와 축 코드가 우연히 같으면 다음에 열 때 엉뚱한 것이 골라지고, **접두사 없는 옛 값도 받아야** 업데이트 한 번에 모든 사용자의 선택이 풀리지 않는다 |
| `DuelCategoryStats` | **차례가 없는 값이 무슨 말을 하는가** (B-112) — 범주 필드 판정 · 값 짝의 전적과 **예상 승수** · 기운 자리 판정 · 못 센 판 세기 | 승패를 그냥 세면 *"불이 물에 약하다"*와 *"물을 든 캐릭터가 세다"*가 **섞인다** — 모델의 예상과 견주는 뺄셈이 그 둘을 가르고, 그것이 이 계산의 존재 이유다. 눈금(`minSamples` 3 · `zThreshold` 2.0)을 `DuelCounterRelations`에서 그대로 가져오는 것도 같은 자리 — **두 화면이 같은 말을 다른 기준으로 하면 사용자가 어느 쪽을 믿을지 알 수 없다** |
| `DuelImageFit` | **참가자 그림을 어떻게 놓는가** — 위쪽을 살리는 크롭 · 카드 배치와 맞춤의 선택 | 값이 틀려도 앱이 죽지 않고 **그림만 조용히 어긋나게 놓인다** — 컴파일도 정적 검사도 통과하므로 실기기 회신 말고는 잡을 길이 없는 부류다(사용자 지적으로 실제로 발견됐다: *"얼굴이 잘린다"*) |
| `DuelAxisChoice` | **필드 편집 창의 대결 축 고르기가 무엇을 보여 주고 무엇을 고른 채로 열리는가** (B-134) — 스피너 항목 목록 · 초기 선택 · **댕글링 판정**(가리키던 축을 못 찾았는가) · 위치→축 되찾기 | **어댑터를 채우는 것과 선택을 정하는 것이 갈려 있으면 그 틈이 오배정이 된다**(R-1). 종전 배선은 *"못 찾으면 `setSelection`을 안 부른다"*였는데 **`AbsSpinner.setAdapter`가 항목이 있으면 그 자리에서 0번을 고르므로 안 부르는 것이 곧 0번**이었다 — 주석이 바로 옆에서 *"조용히 0번으로 옮기면 오배정이다"*라고 금지하던 그 일이 그대로 일어났다. **화면 계층에서는 원리적으로 안 잡힌다: 코드 어디에도 "0"이 적혀 있지 않다.** 댕글링에 **자리 표시용 항목 하나를 세우는 것**도 이 파일의 판정이다 — 불리언으로 두면 사용자가 *첫 축*을 고를 때 선택이 안 바뀌어 콜백이 아예 오지 않고, **고쳤는데 사유 줄이 남아 빠져나올 길이 없다** |
| `DuelEntry` | **홈에서 대결을 누르면 어디를 여는가** — 살아 있는 축이면 재개 · 세계관 하나면 그 축 목록 · 여럿이면 질문 · 없으면 만들 것이 먼저 | 대결은 이 앱의 도구 중 유일한 **반복 활동**이라 진입점이 재개를 못 하면 매번 세 걸음을 다시 밟는다(원칙 04). 틀려도 **앱이 죽지 않는다** — 엉뚱한 축이 열리거나 빈 화면이 뜰 뿐이라 컴파일·정적 검사가 못 본다 |
| `MembershipTimeline` | 소속 구간의 시간 규칙 — 만들 때의 가입 연도, 고칠 때의 구간 겹침 | 재가입이 이전 탈퇴보다 앞이어도 통과해, 같은 해에 나가 있으면서 들어와 있는 이력이 됐다(세션 로그 1-h장) |

---

## 4. 검증 체계 — Android 빌드가 불가한 환경에서

Android SDK·Gradle 의존성을 사용할 수 있으면 실제 Gradle 빌드·시험으로 검증한다.
`dl.google.com` 접근 제한 등으로 로컬 빌드가 어려운 환경에서는 아래 하네스로 해당 범위를
확인하고, 앱 코드 PR의 실제 빌드·시험은 CI에서 확인한다. 준비 조건·명령·결과 해석은 `docs/status.md` 4장을 따른다.

| 도구 | 무엇을 보는가 | 기준선 |
|------|---------------|--------|
| `tools/run_jvm_tests.sh` | 순수 계층을 **실제로 실행**한다(표준 kotlinc + JUnitCore) | 명령·최근 기록값은 `docs/status.md` 4장 |
| `tools/check_text_style.sh` | 화면 문구의 말투·용어(가이드 기계 검출분) | 기준선은 `tools/text_style_baseline.txt`가 든다(그 파일이 단일 소스) — 새 위반은 즉시 실패 |
| `tools/check_resources.sh` | 리소스 중복·미정의 참조·XML 구문 | 통과 |
| `tools/check_ai_tag_consumers.sh` | **AI 이미지 태그 설정(기조·어휘)을 싣는 자리가 등재와 같은가**(B-165). 위 `check_text_style.sh`가 *문체*를 보는 것과 갈린다 — 이쪽은 **문구가 사실인가**의 자리이고, 그것은 코드와 문구를 **함께** 봐야만 보인다. 한국어를 읽지는 않는다(읽을 수 없다): *자리의 집합*을 보고 소비처가 늘거나 줄면 큰소리로 실패해 사람을 문구로 보낸다(`check_view_probe_targets.sh`의 논리). 운반 역할 셋(`AiPromptSettings`·`AiSettingsFragment`·`AppSettingsBindings`)은 **역할로** 갈라 뒀으므로 그 밖의 새 자리는 반드시 걸린다. 되돌아가는 한 가지 모양(*"이미지 자체는 보내지 않습니다"*의 부활)도 함께 잠갔다 | 소비처 둘(`suggestFolderTags`·`suggestImageTags`) 일치 |
| `tools/check_dialog_validation.sh` | R-27 위반 둘 — **[A] 자동 닫힘 버튼 안의 조기 return**(낱말 `return@setPositiveButton`) + **[B] 가드-래핑**(저장을 `if`로 감싸 실패 경로가 아무것도 보존하지 않는 형태. 2026.08.10 · B-76). **[A]만으로는 [B]가 원리적으로 안 잡힌다** — 같은 유실이 낱말 없이도 나기 때문이다(`if (name.isNotEmpty()) save()`는 비면 아무 말 없이 닫힌다). 실패 분기가 알림뿐인 것도 [B]로 센다(창이 닫히는 것은 매한가지다) | **[A] 0건 동결** · **[B] `tools/dialog_validation_baseline.txt`로 동결**(줄이는 갱신만 정상 — `--rebaseline`). **열쇠는 `파일\|바깥 함수\|조건`이고 줄 번호를 안 쓴다** — 쓰면 그 자리 위의 한 줄이 기준선을 통째로 어긋나게 한다. 판정기 자기 시험을 매 실행 앞에 둔다(구조를 읽으므로 판정기가 틀리면 어디에도 안 드러난다) |
| `tools/check_prefs_keys.sh` | 같은 prefs 키를 두 곳이 다른 타입으로 쓰는가(R-28 위반) | 충돌 0 — 새 충돌 즉시 실패 |
| `tools/check_image_pointer.sh` | 캐릭터 `imagePaths` 쓰기가 대표 포인터를 함께 고치는가(R-32 위반) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_restore_preview_parity.sh` | 복원 미리보기(`analyze*`)가 가져오기와 같은 `merge*`로 '변경/동일'을 판정하는가(R-33 위반) — **스캐너 여덟.** ⑧은 *짝이 있기는 한가*를 묻는다(B-256): 함수 자체가 없으면 앞의 일곱은 볼 것이 없어 울리지 않는다. **⑧은 fail-closed다** — `importAll` 본문을 못 뜨면 *위반 없음*이 아니라 **뜨는 법이 깨졌다**로 빨간불이다(콜드 검토) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_preview_row_queries.sh` | 복원 미리보기(`analyze*`)가 **행 루프 안에서 DB를 묻는가**(B-236). 바로 위 정합 검사와 축이 갈린다 — 그쪽은 *같은 답을 내는가*, 이쪽은 *한 번에 묻는가*다. **두 갈래를 본다:** ① 루프 뒤의 `db.…Dao()` ② `analyze*` 안에 선언된 **지역 함수** 속의 그것(선언은 루프 앞, 호출은 루프 안 — `analysisNovelIdByTitle`이 정확히 그 모양으로 숨어 있었고 캐릭터 시트 6,420행 × 2회였다) | 0건 동결 — 새 위반 즉시 실패. 탐지기 자기 시험을 매 실행 앞에 둔다. **수리 전 코드에 돌려 21건을 실제로 잡는 것을 확인했다.** 못 보는 자리는 스크립트 머리에 적었다(클래스 수준 헬퍼 · 조회 *횟수*) |
| `tools/check_import_row_queries.sh` | 가져오기(`ExcelImportService.kt`)가 **일괄판이 있는 단수 조회**를 쓰는가(B-238 — `getByPath`·`getTagsByImageList`). 바로 위 미리보기 검사의 **짝이되 규칙이 다르다**: `analyze*`는 세기만 하므로 루프 안의 *모든* `db.`가 위반이지만, `import*`는 **행마다 쓰는 것이 일**이라 같은 규칙을 걸면 거짓 양성만 쌓인다 — 그래서 **단수/일괄 짝이 있는 읽기만 이름으로 든다**(새 일괄판을 만들면 스크립트의 `BANNED` 표에 짝을 등재할 것). **루프를 찾지 않는 것이 설계다** — 그 메서드는 루프 밖이라도 쓸 이유가 없으므로 이름으로 막으면 B-236이 물렸던 *지역 함수 사각지대*가 원리적으로 생기지 않는다 | 0건 동결 — 새 위반 즉시 실패. 탐지기 자기 시험을 매 실행 앞에 둔다. **수리 전 코드(`origin/master`)에 돌려 2건을 실제로 잡는 것을 확인했다.** 못 보는 자리는 스크립트 머리에 적었다(목록 밖 단수 조회 · DAO 내부의 단수 호출 · 조회 *횟수*) |
| `tools/check_completion_rate.sh` | 필드 완성도 백분율을 `CompletionRate` 하나가 내는가 — 분모가 필드 수인 백분율이 단일 소스 밖에 있는가 · 등재된 소비처가 실제로 그것을 부르는가(R-34 위반) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_sheet_rotation_wiring.sh` | 🔄 **창 전수의 회전 처분** — `DialogFragment` 33개를 훑어, **주입 `var`**를 든 창이 셋 중 하나를 갖췄는지 본다: ⓐ 주입이 없다(인자 번들·자기 뷰모델·`setFragmentResult` — R-65가 가장 안전하다고 적은 길) · ⓑ 주입이 비면 안전 종료 · ⓒ 호스트가 `findFragmentByTag`로 다시 꽂기(R-41-a). 셋 다 아니면 회전한 창이 **빈 채로 멀쩡히 떠 있고 [적용]이 아무 데도 안 닿는다** | 0건 동결 · **fail-closed**(창을 하나도 못 찾으면 위반). 이 부류가 네 번 났다(B-201 · B-260 · FieldEditDialog 세 호출부 · 목록 컨트롤·검색 필터) |
| `tools/check_generation_edit_rotation.sh` | 🎲 축·프리셋 편집 창의 회전 보존 배선 여덟 축 — `DialogFragment`인가 · 담는가 · 푸는가(못 읽으면 안전 종료) · **든 칸 전수가 담기는가**(선언에서 뜬다) · 폼이 *담긴 것*에서 서는가 · 듣는 자리가 `onCreate`인가 · 보내는 쪽이 보내는가 · **이른 결과를 들고 기다렸다 꺼내는가**(R-65 · B-260. **넷째·다섯째 축은 같은 그물을 쓴다** — 선언에서 뜬 칸 전수가 *담기는가*와 *담긴 것에서 대입되는가*이고, 다섯째가 부정형이던 첫 판은 한 축만 다른 꼴로 되돌리면 초록이었다) | 0건 동결 — 회전은 실기기에서만 보이고 **CI도 컴파일만 본다**(담기를 그만둔 창은 완벽하게 컴파일된다). 넷째 축은 fail-closed다 |
| `tools/check_world_package_images.sh` | 월드패키지 이미지 엔트리의 이름이 `WorldPackageImages.entryName` 하나에서 나오는가 · **가져오기가 잘린 장 판정을 부르고 그 결과로 갈래를 가르는가** · 내보내기가 `image_failures.json`을 싣고 `ImageTally.fail(…)`로 적는가(R-64 · B-234. 축 셋 + 콜드 검토 보강 둘 — *부르기만 하고 안 쓰면 부르지 않은 것과 같다*. 판정 변수의 이름은 손으로 적지 않고 **대입에서 뜬다**) | 0건 동결 — 소비처 둘이 순수 하네스·프로브 밖이라 *부르는가*를 잴 길이 이것뿐이다(`check_body_reach_bounds.sh` 축 ③과 같은 논리) |
| `tools/check_config_boundary.sh` | 세계관 경계를 넘는 config 복사가 **두 참조를 함께** 걷어내는가 — 경계를 아는 파일(`GradeSystemRef.demote`·`remapCode` 호출자)이 대결 몫도 걷어내는가(R-35 위반) | **fail-closed** — 목록 밖의 새 호출자는 통과하지 못한다 |
| `tools/check_no_nul_bytes.sh` | 소스에 날 제어문자(NUL)가 있는가 — 있으면 git이 binary로 보아 **diff가 사라지고 grep 기반 도구가 그 파일을 통째로 건너뛴다**(R-37 위반) | **fail-closed** — 추적 파일 + 추적되지 않은 새 파일까지 본다. 본성이 바이너리인 확장자만 예외이고, 목록 밖의 새 확장자는 통과하지 못한다 |
| `tools/check_backlog_markers.sh` | `remaining_work` 4장 백로그에서 **[A] 닫힌 행이 살아 있는 심각도(🔴🟠🟡🟢🔵)를 들고 있는가** + **[B] 머리 표식이 아예 없는 행이 있는가**(그 장 머리 규약) — [A]는 *"무엇이 급한가"*를 심각도로 훑는 콜드 세션이 행 본문을 끝까지 읽게 만들고, **[B]는 표를 셀 수 없게 만든다**(2026.08.10까지 표식 없는 행이 80개였고 그것이 인수인계 판의 *"이것이 전수다"*를 떠받쳤다 — B-182). **살아 있는 행 수를 심각도별로 함께 내며, 그 출력이 "남은 일이 몇 건인가"의 단일 소스다**(문서에는 값을 적지 않는다) | **fail-closed** — 탐지기 자기 시험을 매 실행 앞에 둔다(python3이 없으면 *"위반 없음"*과 구별되지 않는다). **[A]만으로는 [B]가 원리적으로 안 잡힌다** — 표식이 없으면 *"닫혔는데 심각도를 들었다"*에 해당할 수가 없다. 반만 닫힌 행은 본문의 **"이 행이 열려 있는 이유"**가 탈출구다(B-27) |
| `tools/check_propagate_value_parity.sh` | 기본 필드 전파 미리보기에서 값을 **채우는 조건**(저장소)과 **세는 조건**(`DefaultFieldPlan`)이 한 벌인가 — 저장소가 `typeChanges`를 실제로 부르는가 · *직전 템플릿의 타입*으로 가르던 옛 조건이 되살아났는가(B-135) · **저장소가 값 표에 쓰는가**(값 DAO에서 부를 수 있는 것은 `get*`뿐 — B-137) | 0건 동결 — 새 위반 즉시 실패. 저장소는 Room에 매달려 **순수 시험이 원리적으로 못 보는 자리**라 이 검사가 유일한 눈이다. **지키는 것이 둘이다:** *세는가*(B-135)와 *지우지 않는가*(B-137 — 미리보기가 수를 세어 보이므로 **더하고 싶어지는 자리**다) |
| `tools/check_ai_review_retention.sh` | AI 검토 시트가 **유료 응답을 버리지 않는가**(R-38), 그리고 **유료 실행이 말없이 끝나지 않는가**(B-144) — 항목은 스크립트 머리의 *무엇을 보는가*가 든다(**여기 두 벌로 적지 않는다** — 종전에 ①~④를 옮겨 적었다가 ⑤가 들어오며 낡았다) | 0건 동결 — 새 위반 즉시 실패. **화면 계층이라 순수 JVM도 프로브(`ui/**` 제외)도 원리적으로 못 본다.** 순수 시험은 *합치는 규칙이 옳은가*까지만 잴 수 있고, *화면이 그 규칙을 지나는가*는 이 검사만 본다. **잠그는 축이 둘이다** — *이미 결제한 응답*을 잃지 않는가(회전·되받기·적용 실패)와 *결과를 알 기회*를 잃지 않는가(빈 결과의 침묵). **보는 시트는 이미지 둘만이 아니다** — B-123이 **이름 추천 시트**를 항목으로 더했다(회전 보존 · 실행 엔진이 뷰 밖 · **호스트가 재생성된 시트에 콜백을 다시 붙이는가**). 그 자기 시험에서 **기존 패턴 둘이 느슨한 것이 드러나 함께 조였다**: `onSaveInstanceState`는 접두만 맞아도, `val pool`은 지역 변수여도 통과하고 있었다 — **새 항목은 반드시 깨뜨려 보고 붙일 것**(안 잡는 검사는 통과와 구별되지 않는다) |
| `tools/check_ai_image_rule.sh` | 이미지 지시가 **이미지와 한 몸으로 다니는가**(A-7 · B-139) — 프로토콜 코덱이 `request.system`을 직접 읽는가(반드시 `effectiveSystem()`이다) · 코덱이 그것을 실제로 쓰는가 · 이미지 절이 `imageSystemRule` 밖으로 새어 `buildSystemPrompt`에 도로 이어 붙었는가 | 0건 동결 — 새 위반 즉시 실패. **시험과 짝이다:** `AiImageRequestTest`는 *지금 있는* 세 프로토콜의 결과만 재므로 넷째가 `request.system`으로 들어오면 침묵하고, 이 검사는 모양만 보므로 이름을 바꾼 부활을 놓친다. 실수의 방향이 **빼는 쪽**이라 어느 쪽이 뚫려도 모델이 속지는 않는다 |
| `tools/check_ai_send_guard.sh` | 기기 밖으로 나가는 읽기가 **봉쇄 가드를 지나는가**(R-39 · B-141) — `AiImagePreparer`가 `ImagePathMatch.isInside`를 부르는가 · 뿌리에 기본값이 붙었는가 · **모든 호출 자리가 뿌리를 넘기는가**(호출 자리는 손으로 적지 않고 **훑어서 찾는다**) | 0건 동결 — 새 위반 즉시 실패 |
| `tools/check_view_probe_targets.sh` | `View`를 직접 상속한 클래스가 **전부 커스텀 뷰 프로브 대상 목록에 있는가**(R-31 · B-147) — 이 표의 다른 검사와 달리 보는 것이 **코드가 아니라 도구 설정**이다. **실패의 모양이 침묵인 유일한 부류라 세웠다**: 빠진 뷰는 프로브를 *통과*하는 것이 아니라 *들어가지도 않아서*, 오류 수도 이름도 뜨지 않고 **빠뜨릴수록 결과가 깨끗해 보인다**(R-31이 글자 그대로 금지해 두었는데도 둘이 닷새를 빠져 있었다). 범위는 `View` 직접 상속뿐이고 그 경계의 근거는 스크립트 머리가 든다 — **세울 수 없는 것을 요구하면 검사가 늘 빨간불이 되고 다음 사람이 끈다** | 4건 전량 등재 — 새 커스텀 뷰가 미등재면 즉시 실패. 밖에 있는 것은 `ZoomableImageView` 하나(B-166 — 스텁이 먼저다) |
| `tools/differential_compile.sh` | 손댄 파일에 **새로 생긴** 컴파일 오류만 | 기준선 대조 |
| `tools/check_event_crud_notify.sh` | 사건 CRUD가 **실패를 알리는가**, 그리고 **그 고지가 닿는가**(B-29) — 축이 둘이다: ⓐ `CharacterViewModel`·`TimelineViewModel`의 `insertEvent`·`updateEvent`·`updateEventAndShiftOthers`·`deleteEvent`가 **catch 블록 안에서** 고지하는가 ⓑ 사건 편집 창을 띄우는 화면 셋이 **결과 채널을 관측하는가** | 0건 동결. **ⓑ가 이 검사의 존재 이유다** — ⓐ만 보면 *"고지를 달았다"*와 *"고지가 사용자에게 닿는다"*를 가릴 수 없고, `CharacterEditFragment`가 정확히 그 상태였다(`viewModel.result.observe`가 없어 같은 조작이 뜬 화면에 따라 알려지기도 침묵하기도 했다). 화면 계층이라 순수 JVM도 프로브(`ui/**` 제외)도 원리적으로 못 본다. **catch 블록 경계를 중괄호 깊이로 따라가는 것이 이 검사의 설계 판단이다** — 함수 단위로 보면 **성공 고지가 실패 고지의 부재를 덮는다**(세 함수가 성공도 함께 알린다. 첫 판이 실제로 그렇게 통과했다). 자기 시험 본보기에 **여러 줄 시그니처**를 반드시 남겨 둘 것 — 대상 셋이 전부 그 모양이고, 그것을 안 닮은 본보기 탓에 첫 판이 `deleteEvent` 하나만 검사하면서 초록을 냈다(같은 함정이 바로 아래 검사에 살아 있다 — B-183) |
| `tools/check_harvest_population.sh` | 값 라이브러리 **수확이 상태변화 이력을 읽는가**(B-60 · 확정 20번 ㄱ1) — 읽으면 위반이다 | 0건 동결. **집계(`recountUsageForFieldsOrThrow`)는 처음부터 현재 값 세 표만 셌는데 수확은 이력까지 담아 두 쪽이 서로 다른 모집단을 봤다.** 증상이 침묵이라 몇 달을 살아남았다 — 이력에만 있는 값은 엔트리로 들어오고도 `usageCount`가 **영원히 0**이라, '미사용 자동수집 정리'가 **살아 있는 값을 지우자고 권한다.** 컴파일·타입도 순수 JVM도 못 본다(양쪽 다 DAO에 매달려 있다). **함수 단위로 갈라 보는 것이 이 검사의 유일한 설계 판단이다** — 같은 파일의 **전파**는 이력을 반드시 읽으므로(표기를 바꾸면 이력도 따라가야 한다) 파일 전체 금지는 옳은 코드를 위반이라 말한다 |
| `tools/check_pending_key.sh` | 복원 미리보기의 **보류 키를 심는 쪽과 묻는 쪽이 같은 함수를 쓰는가**(B-25) — ① `tally.note` 전 자리가 `RestoreTally.pendingKeyOf`로 키를 만드는가 ② `collectOperationCodes`가 옛 id 키를 코드 키와 **함께** 심는가 ③ **id를 보존하지 않는 타입**(등급 체계·대결 축)에는 심지 않는가 ④ **동료 검사**(`in pendingPeerCodes`)도 같은 모양으로 묻는가 | 0건 동결. **이 배선은 어떤 순수 시험도 못 잡는다 — 실측했다**: 심는 줄을 통째로 지워도 `run_jvm_tests` 전수가 초록이다(`collectOperationCodes`가 private + Room이라 원리적으로 못 닿는다). 실패의 모양이 **침묵**이라 R-31·R-43·R-44·R-47과 같은 계열이고, 어긋나는 방향이 둘이라는 것이 이 검사의 설계 판단이다 — **한쪽이 빠지면 거짓 경고**(예고는 '찾을 수 없음', 실제는 이어진다), **없어야 할 곳에 심으면 거짓 안심**(예고는 '유실 없음', 실제는 잃는다). 뒤쪽이 더 나쁘므로 축을 따로 뒀다. **첫 실행이 자기 오검출로 빨간불이었고 그것이 검사의 모양을 정했다** — `when` 갈래를 함수 본문 전체에서 자르면 마지막 갈래가 `when` 밖 코드까지 삼킨다 |
| `tools/check_dao_sql_twins.sh` | **살아 있는 SQL의 리터럴이 코틀린 상수의 값과 같은가**(B-266 · R-68) — 축 넷: 표식이 가리키는 상수의 *현재 값*과 대조 · 값이 상수와 겹치는데 표식 없는 리터럴 · 낡은 표식·면제 · **대상 폴더 밖의 살아 있는 SQL**(앞의 셋은 볼 것이 없어 하나도 안 울리는 자리다). **쌍둥이를 명단이 아니라 값으로 찾고**(그래서 등재가 센 한 자리가 열여섯이 됐다) 상수는 `const val`과 **enum 항목 코드**를 함께 센다. 보는 것은 `data/dao/`의 `@Query`와 `data/maintenance/`의 `execSQL`·`query("…")`이고, **마이그레이션 SQL은 얼어붙은 기록이라 뺀다** | 기준선 **0** · 표식 15. 자기 시험 **여덟**을 **이 스크립트 자신을 가짜 트리에 대고 돌려** 친다(복사본이 아니다). 그 시험은 전부 **SAF 질의를 하나씩 이고 도는데**, `contentResolver.query(uri, …)`를 SQL로 오인하면 곧바로 전부 빨간불이다(콜드 검토가 실측한 거짓 양성). 면제 표식은 **값을 지목해야** 유효하다 |
| `app/src/androidTest/**` (`AppDatabaseMigrationTest`) | 마이그레이션을 **실제 Room으로** 밟는다(B-9) — 커밋된 최신 스키마가 현행 엔티티와 맞는가 · 커밋된 이웃 버전 사이가 실제로 도는가 · `ALL_MIGRATIONS` 사슬에 구멍이 없는가 | 에뮬레이터 필요 — `.github/workflows/migration-test.yml`(API 26 = minSdk)가 유일한 실행 자리다. **하네스와 축이 다르다:** 하네스는 소스만 읽어 *적어야 할 SQL을 적었는가*를 보고, 이쪽은 *그 결과 스키마가 Room이 기대하는 것과 같은가*를 본다 — 어긋나면 Room이 **DB 열기를 거부**하고 증상은 *"업데이트한 사용자의 앱이 실행되자마자 죽는다"*이다. **하네스를 대체하지 않는다:** 계측 시험은 **커밋된 스키마 사이만** 걸을 수 있는데 v1~v55의 스키마는 그때 빌드와 함께 사라져 **앞으로도 생기지 않는다** |
| `tools/verify_room_migration*.py` | 마이그레이션 하네스(마이그레이션마다 하나씩 는다 — 종 수는 `ls tools/verify_room_migration*.py`로 센다)를 **실제 SQLite로** 실행 | 각 스크립트 출력이 든다 |
| `tools/verify_reset_coverage.py` | 엔티티 목록 ↔ `ResetPlan` ↔ `executeReset` 호출부 3자 대조 | 스크립트 출력이 든다(엔티티가 늘면 함께 는다) |

**이 구조가 강제하는 설계 규칙:** 판단 로직은 최대한 `util`의 순수 객체로 내려야 한다.
`ui`에 남은 로직은 **실행 검증이 불가능**하고 차분 컴파일의 잡음에 묻힌다(뷰바인딩 미해결만
기준선에 212종). 로드맵 5가 `FieldEditDialog`의 검증 로직을 `FormulaValidator`로 내린 것도
이 이유다.

> **차분 컴파일 함정:** 스크립트 내부 정규화 `sed`가 실제 출력(`파일:행:열: error:`)과 달리
> 콜론 하나가 빠져 **줄 번호를 지우지 못한다.** 그대로 비교하면 손댄 파일의 오류가 전부
> '신규'로 뜬다. 스크립트 헤더가 안내하는 수동 정규화를 반드시 따를 것.

> **⚠️ 하네스가 통과시키고 CI만 터지는 자리 — `android.util.Log`.**
> 로컬 하네스는 `tools/jvm-stubs/AndroidLogStub.kt`로 Log 자리를 채운다(0을 돌려주는 껍데기).
> 그런데 Gradle의 `testDebugUnitTest`가 쓰는 android.jar은 **모든 메서드가 예외를 던지는**
> 껍데기라, 순수 로직이 진단용으로 부르는 `Log.w` 하나가 테스트를 깨뜨린다.
> 로드맵 5에서 실제로 이것에 걸렸다 — `FormulaEvaluator`의 순환 참조·미존재 키 경로가
> `Log.w`를 부르는데, 그 경로를 처음으로 테스트한 순간 **로컬 전량 통과 / CI 4건 실패**가 났다.
> `app/build.gradle.kts`의 `testOptions { unitTests { isReturnDefaultValues = true } }`로 막았다.
>
> **일반화:** 하네스의 스텁은 "컴파일을 통과시키는 것"이 목적이라 **런타임 의미가 실제와 다르다.**
> 순수 계층에 Android API를 새로 부르는 코드를 넣을 때는 하네스 통과가 근거가 되지 못한다.

---

## 5. 확장점 — 무엇을 추가할 때 어디를 건드리는가

### 5-1. 새 필드 타입 (현재 7종: TEXT · NUMBER · SELECT · MULTI_TEXT · GRADE · CALCULATED · BODY_SIZE)

`FieldType` enum에 더하는 것으로 끝나지 않는다. 그리고 **분기 지점을 enum으로 세면 과소평가된다** —
실측하면 이렇다:

| 세는 방법 | 세는 명령(파일 수) |
|-----------|---------------------|
| `FieldType`을 참조 | `grep -rl 'FieldType' app/src/main/java --include=*.kt \| wc -l` |
| `FieldType.<상수>`로 분기 | `grep -rlE 'FieldType\.(TEXT\|NUMBER\|SELECT\|MULTI_TEXT\|GRADE\|CALCULATED\|BODY_SIZE)' app/src/main/java --include=*.kt \| wc -l` |
| **`"CALCULATED"` 같은 생문자열로 분기** | **이제 0이다** (B-55 해소, 2026.08.13 — 아래 참조). 되돌아왔는지는 `tools/check_field_type_branch.sh`가 본다 |

**2026.08.13에 전부 enum으로 좁혔다 (B-55 · 규약 R-52).** 종전에는 타입 판정이
`fd.type == "CALCULATED"` 같은 **문자열 비교로 스물아홉 파일에 흩어져** 있었고, `FieldType`에
상수를 하나 더해도 그 자리들은 아무것도 몰라 **새 타입이 조용히 빠졌다**(원칙 02 위반의 가장
흔한 발생 경로 — S-15가 실제 사례다).

**지금의 구조는 이렇다:**
- **저장은 여전히 문자열이다**(`FieldDefinition.type`). 엑셀·월드패키지·프리셋 JSON이 이 값을
  글자로 싣고 그 파일들은 앱보다 오래 살기 때문이며, enum 컬럼으로 바꾸면 모르는 글자가
  들어온 순간 행이 통째로 죽는다.
- **좁히는 자리는 하나다** — `FieldDefinition.fieldType`(`FieldType?` 게터). 모르는 글자는
  `null`이고, 각 분기가 그 상태를 명시적으로 답한다.
- **분기는 `when`이다.** 집합(`in setOf(...)`)은 새 타입이 늘어도 조용하지만 `when`은 컴파일을 깬다.

**그래서 새 타입을 더할 때 할 일이 바뀌었다** — 훑을 목록을 사람이 들고 다니는 것이 아니라,
`FieldType`에 상수를 한 줄 더하고 **컴파일러가 깨뜨리는 자리를 하나씩 답하면 된다.**
착수 시 특히 눈여겨볼 곳(컴파일러가 잡아 주지만 판단이 필요한 자리): 입력 렌더
(`DynamicFieldRenderer`·`DynamicFieldFormBuilder`) · 통계 분석 방법 표
(`FieldStatsConfig.StatsType.forFieldType`) · 수치 정렬 판정(`FieldValueSorter.isNumericSortType`) ·
값 카탈로그 적격성(`FieldValueTokenizer`) · 엑셀 가져오기의 값 검증 표(`ExcelImportService`) ·
타입 변경 호환성(`FieldTypeCompatibility`).

> **컴파일러가 못 잡는 자리 둘**(그래서 검사가 따로 있다):
> ① 엑셀 '타입' 열의 드롭다운 — 이제 `FieldType.entries`에서 뽑으므로 자동으로 따라온다.
> ② 새로 적히는 생문자열 — `tools/check_field_type_branch.sh`가 막는다(생문자열 · `.name` 비교 두 축).
>
> **일부러 남긴 문자열 둘**: 마이그레이션(이미 일어난 일의 재생이라 그때 글자를 적어야 한다) ·
> `a.type != b.type` 류(타입 분기가 아니라 *글자가 달라졌는가*). 사유 원문은 규약 **R-52**다.

### 5-2. 새 대상(entityType) — 지금은 `character` · `event` · `novel`

`FieldDefinition.entityType`이 축이다. **DAO 기본 인자가 `ENTITY_CHARACTER`라는 것이 함정이다** —
호출부가 대상을 넘기지 않으면 조용히 캐릭터 필드를 본다(로드맵 5가 `FieldEditDialog`에서
실제로 이 결함을 잡았다). 새 대상을 열 때는 **규약 R-29의 전수 명령**을 돌릴 것 —
`getFieldsByUniverse*`뿐 아니라 `getAllFieldsList`·`getFieldByKey`·`getFieldsByKey`·
`getGlobalFieldsList`·`getGlobalFieldByKey`·`countFieldsByKeyExcluding`도 같은 기본값을 갖는다.
**이 목록을 손으로 대조하지 말 것** — R-29의 전수 명령 ②가 DAO에서 직접 뽑는다(이름을 적으면
낡는다: 종전 목록은 사라진 함수 둘을 들고 실재하는 셋을 빠뜨린 채 서 있었다).

**한 종류를 여는 데 드는 자리**(확-3 작품 축이 실측한 것 — 세션 로그 1-t·1-u장):
값 테이블 + DAO(캐릭터·사건판과 **동형**으로) · 병합 규약(`*FieldValueMerge` — R-5) ·
휴지통 스냅샷 **둘**(소유자 스냅샷 + 세계관 스냅샷의 고아 값) · 삭제 고지 집계 ·
`ResetPlan` · 필드 관리 칩 + 편집 폼 + 값 라이브러리 · 엑셀(정의 시트 `대상`, 소유자 시트의
`필드:` 동적 열, **가져오기 순서**) · 통계(스냅샷·인사이트·드릴다운·하위 그룹·CALCULATED) ·
월드패키지 엔트리 + schemaVersion. **남은 것은 세계관·세력**이다(백로그 확-3).

> **순서가 열보다 위험하다.** 작품 축은 열을 다 만들고도 `importNovels`가 `importFieldDefinitions`
> 앞에 있어 빈 DB 복원에서 값이 통째로 버려질 참이었다 — 새 시트가 필드 열을 실으면
> **그 시트가 언제 처리되는지**부터 확인할 것.

> **AI 추천 경로도 종류마다 연다 (2026.08.11, B-43).** 지금 서 있는 축은 **캐릭터·사건 둘**이고
> 작품은 아직 없다(확-3 잔여). 여는 데 드는 자리는 **셋뿐이다**: ⓐ `ai/`에 그 종류의 프롬프트
> (`FieldPromptSource` 구현 — 컨텍스트 블록만 새로 쓰고 `[추천할 필드]` 절은
> `CharacterFieldAiSuggester.appendTargetSection`을 부른다) ⓑ 화면의 ✨ 진입 + 검토 창
> ⓒ `FieldEditDialog.AI_CAPABLE_ENTITY_TYPES`에 한 줄. **실행 규칙(청킹·부분 실패 격리·결손 0
> 보장·토큰 집계)은 베끼지 않는다** — 그쪽이 갈리면 한쪽 축에서만 유료 응답이 조용히 사라진다.
> 세는 법: `ls app/src/main/java/com/novelcharacter/app/ai/*AiSuggester.kt`.

### 5-3. 새 통계 분석

**적응적 통계 원칙**(CLAUDE.md)상 분석 항목은 필드 구성에서 **동적으로 생성**된다.
새 분석을 붙일 자리는 `StatsDataProvider`이며, 아래를 반드시 지킬 것(수는 적지 않는다 — "넷"이라 적어 둔 동안 다섯이 됐다):
계산 필드 병합(R-16) · 자동 선택에만 설정 적용(R-18) · 라벨이 아닌 타입드 스펙으로 매칭(R-20) ·
**스냅샷 순수 헬퍼는 `perSnapshot` 메모이즈를 지난다**(S6 2차 — 통계 적재는 계산 10개를
async로 동시에 돌리므로, 스냅샷만 받는 헬퍼를 캐시 밖에 두면 호출부 수만큼 다시 짓는다.
돌려받은 값은 **공유 사본이라 읽기만 한다** — 변조는 `StatsMemoParityTest`가 잡는다.
실측·경위는 `scalability_performance_2026-07.md` 3-13) ·
**값 분포·계수는 건별이 아니라 접힌 표 위에서 센다**(S6 4차 — `valueCountsOf`(def별
원문→건수) + `foldStatsKeyCounts`. 파싱 def·config는 호출부가 정하므로 R-15 그룹 파싱과
그대로 겹쳐 쓴다. 값 행마다 `getFieldValues`를 다시 부르는 계수 루프를 새로 적으면
그 소비처만 건별 비용으로 돌아간다 — S6 5차가 패턴·인사이트·타입 판정까지 같은 표로
옮겼고, (key,type) 그룹은 `mergedRawCounts`로 병합한다(단일 def 그룹은 공유 표 **그
객체**를 받으므로 역시 읽기만 한다). 대조는 `StatsFoldParityTest`·`StatsScanParityTest`,
실측·경위는 3-15·3-16).

### 5-4. 새 왕복 포맷

현재 둘: **엑셀 왕복**(`excel/` — 시트 규약, 오버플로 시트, 헤더 별칭)과
**월드패키지**(`share/` — ZIP + manifest. 현행 버전은 `WorldPackageContents.CURRENT_SCHEMA_VERSION`이
든다. **v6부터 세계관의 대결(축·판·상성)을 담고**, 참가자 코드·이미지 경로의 재배선은
`WorldPackageDuels`가 단일 소스다 — 재발급 표에 없는 코드는 **원문을 남기지 않고 제외 + 계수**한다.
**이미지 엔트리의 이름과 복원 판정은 `WorldPackageImages`가 든다** — B-234부터 `image_failures.json`이
*내보내는 중 잘린 장*을 함께 실어, 받는 기기가 그것을 결번으로 접는다. **그 엔트리는 판올림이 아니다**
— 부재가 곧 어제까지의 동작이라 옛 앱이 그대로 읽는다(R-64)).
**내보내기 워크북은 두 구현이다** — `ExportWorkbooks`가 DOM·스트리밍을 내주고 둘의 산출이
같다는 것을 `ExportWorkbookParityTest`가 잠근다(R-49).

> **워크북에 시트를 새로 만들면 갈 곳이 넷이다**(B-221에서 실측 — 넷째를 빠뜨려 검사가 잡았다):
> ⓐ `RESERVED_SHEET_NAMES`(없으면 가져오기가 *"인식되지 않아 무시되었습니다"*로 **자기 산출물을
> 낯설다고 말한다**) ⓑ `SheetTabColors`의 그룹 하나(`ExportPresentationSpecTest`가 예약명 전수를
> 요구한다 — 숨김 시트라 색이 안 보여도 배정은 있어야 한다) ⓒ 내보내기 요약 계수(사용자가
> 볼 시트 수와 어긋난다) ⓓ **스트리밍에서도 되는가** — 행 번호가 증가만 하는가.

새 포맷은 기존 둘의 규약을 재사용해야 한다 —
특히 안정 식별자(`EntityCode`)와 "빈 칸 = 해제" 규약, 그리고 **거부가 아니라 수용·교정**
(개발 의도 4번).

---

## 6. 규약 색인 (R-1부터 — 아래 표가 현행 범위다)

정의 원문은 전부 **`docs/conventions.md`**에 있다(행 번호는 갱신되므로 제목으로 찾을 것 —
각 규약이 `## R-N.` 절이다). **아래는 색인이며, 착수 전 해당 영역의 규약은 원문을 읽을 것.**

| # | 한 줄 | 영역 |
|---|-------|------|
| R-1 | 스냅샷도 안정 식별자를 병기한다 (4-4의 확장) | 복원 |
| R-2 | Gson 스냅샷에 추가하는 필드는 전부 nullable | 복원 |
| R-3 | 보관 한도가 방금 만든 백업을 파괴해서는 안 된다 (4-6의 확장) | 휴지통 |
| R-4 | 파괴적 동작은 실행 전에 결과를 알리고 취소 경로를 남긴다 | 전역 |
| R-5 | 폼의 권한은 폼이 실제로 렌더한 것까지다 | 입력 |
| R-6 | 예약 시트명은 소유자만 가질 수 있다 (4-5를 규칙으로) | 엑셀 |
| R-7 | 시트의 정체는 이름이 아니라 헤더가 정한다 — 단, 첫 열 하나로는 부족하다 | 엑셀 |
| R-8 | 스냅샷은 겹치지 않고 이어붙는다 (B-1) | 휴지통 |
| R-9 | 정리도 복원도 '삭제 작업' 단위다 (B-14, R-3의 확장) | 휴지통 |
| R-10 | payload 한 행은 읽을 수 있는 크기여야 한다 | 저장 |
| R-11 | 보류 판정은 참조 종류마다 키가 있어야 한다 (R-1·R-8의 짝) | 복원 |
| R-12 | 스냅샷의 종류는 인스턴스가 아니라 호출이 정한다 (B-2/B-21) | 휴지통 |
| R-13 | 집계의 셀 단위가 다르면 함수를 나눈다 (B-4) | 통계 |
| R-14 | 목록에서 잘라낸 것은 개수로 존재를 알린다 (B-5) | 전역 |
| R-15 | 머지된 카드가 약속한 범위 = 그 카드에서 뻗는 모든 경로의 범위 (로드맵 3) | 통계 |
| R-16 | 계산 필드는 저장 행이 없다 — 저장 값을 읽는 모든 경로가 그것을 알아야 한다 | 통계·엑셀 |
| R-17 | 빈 결과는 사유를 말한다 — "못 찾음"과 "없음"은 다르다 | 전역 |
| R-18 | 설정은 '자동 선택'에만 적용된다 — 사용자가 고른 것은 계산한다 (로드맵 4) | 통계 |
| R-19 | 상한은 표시 계층의 관심사다 (R-14의 실행 규칙) | 통계 |
| R-20 | 라벨은 매칭 키가 아니다 (S-16) | 통계 |
| R-21 | 계산기의 파생 상태는 인자에서만 나온다 (S-18 수리의 선행 조건) | 통계 |
| R-22 | 서식이 곧 값의 정체성인 문자열은 로케일을 고정한다 (로드맵 4 재공격) | 엑셀·통계 |
| R-23 | 학습한 사실은 학습 대상이 바뀌면 함께 버린다 (P-A 설계) | AI |
| R-24 | 성립하지 않는 조합의 설정은 보이지 않는다 (P-A 설계 — 기존 판단의 승격) | UI |
| R-25 | 화면에 노출되는 설정에는 목적문이 붙는다 (텍스트 개혁 — 기존 판단의 승격) | 텍스트 |
| R-26 | 항목 순회형 대량 작업에는 결정형 진행도를, 조회형 로딩에는 가짜 진행도를 붙이지 않는다 (폴더 왕복 설계 — 사용자 확정 2026.07.28) | UI |
| R-27 | 검증 실패는 창을 닫지 않는다 — 알리는 것과 고칠 자리를 남기는 것은 다른 일이다 (B-28, 2026.07.29) | UI·입력 |
| R-28 | 두 화면이 같은 prefs 파일의 같은 키를 공유하지 않는다 (2026.07.30, 문서 감사 중 발견) | 저장·UI |
| R-29 | entityType으로 갈리는 기능은 조회·판정·순서·쓰기가 모두 같은 종류를 본다 (P5, 2026.07.30) | 필드·데이터 |
| R-30 | 물질화된 파생값의 원본을 고치는 경로는 파생값 재작성과 한 트랜잭션이다 (U-1, 2026.07.30) | 필드·데이터 |
| R-32 | 목록을 가리키는 포인터는 그 목록을 고치는 모든 경로가 함께 고친다 (B-103, 2026.08.03) | 저장·데이터 |
| R-33 | 복원 미리보기의 판정과 가져오기의 쓰기는 같은 함수에서 나온다 (B-101/B-102, 2026.08.03) | 엑셀 왕복 |
| R-34 | 같은 이름의 지표는 한 함수가 낸다 — 분자와 분모를 각 화면이 정하지 않는다 (B-100, 2026.08.04) | 통계·완성도 |
| R-35 | 세계관 경계를 넘는 config 복사는 그 경계 안에서만 뜻이 있는 참조를 전부 걷어낸다 (B-113, 2026.08.07) | 필드·데이터 |
| R-36 | 엑셀 가져오기에서 없는 열과 빈 칸은 다른 사실이다 (B-122, 2026.08.07) | 엑셀 왕복 |
| R-38 | 유료 AI 응답은 뷰 밖에 두고, 검토 중인 응답을 버리는 길을 만들지 않는다 (B-136+B-140, 2026.08.08) | AI · UI |
| R-39 | 기기 밖으로 나가는 읽기는 봉쇄 가드를 지난다 — 그리고 막힌 것은 "못 읽음"과 다른 사유다 (B-141, 2026.08.08) | AI · 이미지 |
| R-40 | 의사키 어휘를 새로 열 때는 접두가 겹치지 않아야 하고, 진짜 키와 부딪칠 때의 승자를 정해야 한다 (B-167, 2026.08.08) | 대결 · 필드 |
| R-41 | 편집 화면의 폼 상태는 한 함수가 한 벌로 싣고, 새 상태 필드는 그 벌에 함께 등재한다 (B-169+B-170, 2026.08.08) | 캐릭터 편집 |
| R-42 | 경로를 자연키로 담은 표는 그 경로를 바꾸는 모든 자리가 함께 옮긴다 (B-104 이미지 축, 2026.08.09) | 이미지·대결 |
| R-44 | 관문이 사용자가 고르지 않은 것으로 바꿨으면 결과가 그렇게 말한다 (B-108, 2026.08.09) | AI |
| R-45 | 왕복하는 설정은 카탈로그 하나가 든다 — 선언·읽기·쓰기가 갈리면 늘지 않는다 (B-105, 2026.08.09) | 엑셀 |
| R-46 | 경로 판단은 단일 소스가 들고, 실패 처분은 방향이 둘이다 — 복붙은 그 방향을 자리마다 뒤집는다 (B-106 ⓐ, 2026.08.10) | 이미지 |
| R-47 | 목록 셀은 결합·분해를 한 쌍이 든다 — 붙이는 쪽만 고치면 파일이 스스로를 못 읽는다 (B-27 ② + B-38, 2026.08.10) | 엑셀 |
| R-48 | 유지하기로 한 용어는 뜻을 하나로 못박고, 그 못을 기계가 지킨다 — 대결 영역의 매칭 금지 (판정 23번, 2026.08.10) | 문구 |
| R-55 | **낡을 수 있는 값을 일괄로 떠서 넘길 때, "낡아도 같다"의 근거가 SQL의 모양이면 그 모양을 기계가 지킨다** — R-53이 *"앞에서 한 번에 물어라"*라 하고 R-54가 그 목록을 통로에 태우지만, **앞에서 뜬 값이 루프 도중에 낡으면 어떻게 하는가**는 둘 다 답하지 않는다. 가르는 물음은 *"쓰기가 다른 행을 바꾸는가"*가 아니라 **"읽은 값으로 코틀린이 분기하는가"**다 — 분기하면 겹이 필요하고(`ImageLinkGroupPlanner`의 `>= 2` 가드), SQL의 `WHERE`로 그대로 흘러가면 필요 없다(판정을 DB가 **지금 상태에서** 한다). 근거는 값이 흘러 드는 파라미터의 KDoc에 한 벌만 적고, 그 근거가 기대는 SQL의 모양은 기계가 본다 — R-50과 같은 계열이다(Room `@Query`는 코틀린이 아니라 타입 검사도 순수 시험도 원리적으로 닿지 않는다). 검사는 `tools/check_group_dissolve_contract.sh`. 원문은 `docs/conventions.md` R-55 | 성능·DB |
| R-57 | 색인이 NULL을 통과시키는 자리는, 무엇이 대신 막는지를 쓰는 줄에 적는다 (B-230 ⓑ, 2026.08.16) | 저장소·데이터 무결성 |
| R-58 | 저장 형식을 판올림하면 새 매직을 세우고, 옛 매직을 읽는 길은 남긴다 — 그리고 그 길을 손으로 짠 옛 바이트로 잰다 (B-248, 2026.08.16) | 저장·교환 형식 |
| R-59 | 하네스에 있고 기기에 없는 것은, 가려서 잰다 (B-250, 2026.08.17) | 검증 하네스·런타임 |
| R-60 | 사용자가 적는 이름이 유니크 키인 표에는 REPLACE로 넣지 않는다 — 겹침은 물어서 가르고, 덮어써도 id는 지킨다 (B-191, 2026.08.17) | 데이터 무결성·다이얼로그 |
| R-70 | 기기와 하네스가 같은 이름의 다른 구현을 쓰는 API는 우리 쪽에서 못박는다 (2026.08.21, 부류 전수 감사) | 검증·플랫폼 |
| R-69 | 정규식은 뜻을 표기로 적는다 — 약칭은 기기와 데스크톱에서 다른 글자를 가리킨다 (2026.08.21, 사용자 보고 크래시 + 전수 실측) | 검증·플랫폼 |
| R-68 | SQL 안의 글자가 코틀린 상수의 쌍둥이면 그 짝을 자리에 적는다 — 잇는 것이 없으면 둘은 반드시 갈린다 (B-266, 2026.08.19) | 데이터·SQL |
| R-67 | 선언을 다른 패키지로 옮겼으면 같은 패키지에 있던 소비처를 따로 센다 — import 목록에는 그들이 없다 (B-251 ⓓ, 2026.08.19) | 검증·도구 |
| R-66 | 성능 수리는 재기 전까지 수리가 아니다 — 그리고 벤치가 실제 경로에 닿는지를 먼저 증명한다 (B-251 ⓓ, 2026.08.19) | 성능 |
| R-65 | 창이 돌려주는 값은 콜백이 아니라 결과로 건넨다 — 되살리는 배선은 없는 것이 가장 안전하다 (B-260, 2026.08.18) | 화면·회전 보존 |
| R-64 | 부분 실패로 남은 산출물은 받는 쪽이 알아볼 수 있게 나간다 — 그리고 그 표시는 판올림이 아니다 (B-234, 2026.08.18) | 월드패키지·교환 형식 |
| R-63 | 다중값 입력칸도 결합·분해를 한 쌍이 든다 — 여는 것만으로 값이 갈려서는 안 된다 (B-186, 2026.08.18) | 입력칸·쉼표 |
| R-62 | 막지 않는 경고는 저장 시점이 아니라 적는 동안 나온다 — 그리고 그 판정이 읽는 상수는 산출이 쓰는 상수여야 한다 (B-202, 2026.08.18) | 화면 경고·검증 |
| R-61 | 화면 사이로 넘기는 요청은 이름을 한 자리에 두고, 읽은 자리가 지운다 (B-198, 2026.08.18) | 화면 간 이동·인자 |
| R-54 | IN (:목록)에 넘기는 목록은 통로를 지난다 — 상한을 넘으면 질의가 통째로 죽는다 (B-242, 2026.08.16) | 저장소·성능 |
| R-51 | 같은 값이 축마다 다른 것을 뜻하면 대조 방식을 부르는 쪽이 밝힌다 — 기본값에 기대면 그 판단이 없다 (B-175, 2026.08.12) | 대결 |
| R-52 | 타입으로 갈라지는 자리는 enum이 지킨다 — 문자열 비교는 새 타입을 조용히 빠뜨린다 (B-55, 2026.08.13) | 필드·타입 |
| R-53 | 행마다 묻는 자리는 짝의 한 쪽만 고쳐지지 않는다 — 미리보기와 가져오기는 같은 색인을 세운다 (B-236, 2026.08.15) | 데이터·성능 |
| R-50 | 판정이 두 언어에 살면 그 일치를 기계가 본다 — SQL 쌍둥이는 주석으로 묶이지 않는다 (B-171, 2026.08.12) | 대결·세력 |
| R-56 | 헤더 행을 집는 자리는 하나이고, 실패는 언제나 사유와 함께 나간다 (B-231, 2026.08.16) | 엑셀 가져오기 |
| R-49 | 스트리밍으로 쓰는 시트는 되돌아가지 않는다 — 행의 서식은 그 행을 쓰는 자리에서 (B-72, 2026.08.11) | 내보내기 |
| R-43 | 손으로 적은 대상 목록은 기계가 완전성을 본다 — 빠지면 실패가 아니라 침묵이다 (B-104 소비처 ⓑ·ⓒ, 2026.08.09) | 검증 도구 |
| R-37 | 소스에 날 제어문자를 넣지 않는다 — 값이 아니라 표기로 적는다 (B-146, 2026.08.07) | 도구·검증 |
| R-31 | 다이얼로그 본문 스크롤에는 높이 상한이 있다 (B-91, 2026.08.02 · XML 축 B-98, 2026.08.03) | UI |

---

## 7. 문서 지도

| 목적 | 정본 |
|------|------|
| 작업 절차·현재 상태 | `AGENTS.md` · `docs/status.md` |
| 규약·구조·성능 | `docs/conventions.md` · `docs/architecture_2026-07.md` · `docs/scalability_performance_2026-07.md` |
| 사용자 확정·화면 문구 | `docs/judgment_confirmations_2026-08.md` · `docs/text_style_guide_2026-07.md` |
| 실기기 확인 | `docs/device_walkthrough.md` |
| AI·필드 값 | `docs/ai_integration.md` · `docs/field_value_library.md` |
| 이미지 왕복·분류 | `docs/image_folder_roundtrip_design_2026-07.md` · `docs/image_folder_tag_ai_2026-07.md` |
| 대결·체형 | `docs/duel_system_design_2026-08.md` · `docs/body_visual_redesign_2026-07.md` |
| 백업·복원·엑셀 | `docs/backup_export_redesign_2026-08.md` · `docs/restore_preview_parity_2026-08.md` · `docs/excel_roundtrip_audit_2026-07.md` |
| Room 마이그레이션 | `docs/room_migration_verification.md` |
| 백로그 근거·옛 세션·완료 계획 | `docs/remaining_work_2026-07.md` · `docs/session_log_2026-07.md` · `docs/archive/README.md` |

관련 절만 읽고, 현재 작업 여부는 `docs/status.md`에서 확인한다.

## 문서 이력

회차별 수정 기록은 [정리 전 원문](https://github.com/star1water1/Test/blob/ac7057af4014717743a2547dab5d0db176c4fce7/docs/architecture_2026-07.md)과 Git 이력에서 확인한다. 이 문서에는 현행 구조와 색인만 유지한다.
