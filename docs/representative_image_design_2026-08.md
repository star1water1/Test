# 캐릭터 대표 이미지 (B-103) — 설계

> **확정된 뼈대는 사용자 판정이다**(`docs/judgment_confirmations_2026-08.md` 1장 1번):
> **ㄱ1** 파일 기준 저장 · **ㄴ3** 화면 진입마다 랜덤 · **ㄷ1** 삭제 시 알리고 랜덤 복귀 ·
> **ㄹ1** 엑셀에 실음. 이 문서는 그 넷을 **어떻게 만들지**를 정한다 —
> 확정 문서가 스스로 적어 둔 대로 *"확정은 무엇을 할지가 정해졌다는 뜻이지
> 어떻게 만들지가 정해졌다는 뜻이 아니다"*.
>
> **사용자 요청 원문은 둘이었다**(2026.08.02): ① 다가오는 생일 배너의 사진도 랜덤화
> ② 캐릭터마다 대표 이미지를 고정 — **랜덤보다 우선해서** 뜬다.
>
> **착수 순서상 이것이 B-104(대결)보다 먼저다**(확정 문서 4장) — 대결이 쌓는 이미지 축
> 승자가 대표의 **기본값 후보**이므로, 대표가 먼저 서야 얹을 자리가 생긴다(D9).

---

## 1. 현행 실측 (2026.08.03, 파일:심볼 기준 — 코드로 확인한 사실만)

### 1-1. "어느 이미지가 이 캐릭터를 대표하는가"를 정하는 자리 — 아홉 종류, 열아홉 지점

| 자리 | 지금 무엇을 고르는가 | 주기 |
|---|---|---|
| `CharacterAdapter:247~262·300` (목록 카드) | `ImageIndexPrefs`에 **영속된 랜덤 인덱스**(`idx % paths.size`) | **화면 진입 1회 재추첨**(`CharacterListFragment:587`) |
| `BirthdayBannerAdapter:92~98` (생일 배너) | 자체 해석 후 `paths[0]` | 없음 — 언제나 0번 |
| `CharacterDetailFragment:778~841` (상세 이미지) | 전량 ViewPager + **랜덤 시작 위치**(`:838~840`) | 화면 진입마다 |
| `CharacterDetailFragment:1055` | `CharacterImageLoader.firstImagePath()` | 없음 |
| `RankingAdapter:99~104` (순위 카드) | `paths.firstOrNull()` | 없음 |
| `StatsDataProvider:2524·2749` (드릴다운 목록) | `images.firstOrNull()` ×2 | 없음 |
| `AssistantProviders:102·134·177·290·342·546·588` | `firstImagePath()` ×7 (`AffectedCharactersSheet`가 이 값을 받는다) | 없음 |
| `DuplicateCharacterDialog:194` | `firstImagePath()` | 없음 |
| `UniverseViewModel:305·324·429` / `:344` (세계관·작품 카드 연동) | `randomOrNull()` ×3 · `firstOrNull()` ×1 | 호출마다 |

→ **아홉 종류가 네 가지 규칙을 쓴다**: 언제나 0번 · 영속 랜덤 인덱스 · 호출마다 랜덤 ·
진입마다 랜덤 시작. **B-103 등재가 알고 있던 것은 이 중 넷뿐이다.**

### 1-2. 등재가 몰랐던 것 셋 — "전제는 늘 틀린다"의 이 문서 판

1. **`ImageIndexPrefs`가 등재에 없다.** 캐릭터 목록은 **이미 랜덤이고, 이미 "화면 진입마다"다.**
   따라서 사용자가 고른 **ㄴ3은 새 동작이 아니라 나머지 여덟 종류를 목록에 맞추는 것**이다.
2. **상세 화면도 이미 진입마다 랜덤이다**(시작 위치). 등재는 상세를 "그 함수를 쓰게 할 곳"으로만 적었다.
3. **`RankingAdapter`가 등재 목록에 없다.** 세는 법(`ui` 하위 `firstImagePath`·`imagePaths` grep)이
   실제로는 `RankingEntry.imagePaths`라는 **한 겹 우회**를 못 잡는다 — 순위 카드는 스냅샷 필드를 거친다.

> **그리고 이 랜덤은 한 번 죽은 적이 있다.** 세션 로그 **1-12**(사용자 실기기 보고, 2026.07.28):
> 성능 수리 **P2-1**이 *"방출마다 clearAll = 전 목록 재랜덤 + O(N) 쓰기"*를 없애면서
> **재추첨 호출부가 통째로 사라져** "최초 1회 추첨 후 영구 고정"이 됐다. 지금 살아 있는 것은
> `observeData()` 진입부의 1회 재추첨이다.
> **이 설계는 바로 그 자리를 다시 손댄다 — 회귀 위험이 가장 큰 지점이고,
> 그때의 제약(같은 화면 안에서 이미지가 튀지 않을 것 · 방출마다 O(N) prefs 쓰기 금지)은
> 이 설계도 그대로 지켜야 한다.**

### 1-3. `imagePaths`를 다시 쓰는 자리 — 여섯 갈래, 열여섯 지점

ㄱ1(파일 기준 저장)이 실제로 요구하는 것은 칸 하나가 아니라 **이 목록 전수**다.

| 갈래 | 지점 | 경로에 무슨 일이 일어나는가 |
|---|---|---|
| 이미지 관리 | `ImageManagerViewModel:361`(삭제) `:601`(이동·개명) `:699`(이동 되돌리기) `:914`(되살리기) `:1018`(정리) | 사라짐 / 바뀜 / 바뀜 / 새로 붙음 / 사라짐 |
| 폴더 왕복 | `OrganizeFolderService:401·430`(편입) `:422·467`(제외) | 붙음 / 사라짐 — **폴더 왕복은 경로를 바꾼다는 것이 이미 확인된 사실이다** |
| 엑셀 가져오기 | `ExcelImportService:3277`(갱신) `:3296`(신규) — 재매핑은 `:293 remapImagePaths` | 다른 기기의 경로가 이 기기 경로로 **재매핑** |
| 월드패키지 | `WorldPackageImporter:362` — 재매핑은 `:226 restoreImagePaths` | ZIP 엔트리가 새 경로로 풀림 |
| 폼 저장 | `CharacterSaveCoordinator:99~113` | **목록 전체 교체** |
| 신규 생성 | `NameBankViewModel:263` · `ImageManagerViewModel:831` | 대표 없음이 정상 |
| 휴지통 | 스냅샷 `TrashRepository:730` · 복원 `:1600`(`data.character.copy(...)`) | payload에 실려 함께 오간다 |

> **`CharacterSaveCoordinator`가 이 목록에서 가장 위험하다.** 이 함수는 기존 엔티티를
> `copy()`하지 않고 **폼 스냅샷으로 `Character`를 새로 조립한다**(`:99`). 새 칸을 여기에
> 적지 않으면 **캐릭터를 한 번 저장할 때마다 대표가 조용히 지워진다.**
> **같은 병이 이 저장소에서 이미 한 번 났다** — `partSlots`가 저장할 때마다 지워지던 것
> (세션 로그 **1-ar**). 값·해석·소비가 다 있는데 아무도 밟지 않던 경로였다.

### 1-4. 곁가지 사실

- `Character`(`data/model/Character.kt`)에는 `imagePaths` 하나뿐 — 모드도 포인터도 없다. DB `version = 46`.
- `Universe`/`Novel`에는 `imageMode` + `imageCharacterId`(+`imageNovelId`)가 있고,
  화면 문구는 이미 **"대표 이미지 설정"**(`image_mode_label`)이라는 말을 쓴다 → **용어를 새로 짓지 않는다.**
- 캐릭터 이미지 순서를 바꾸는 화면은 **없다**(`CharacterImageStripController`: 탭=뷰어, 롱프레스=삭제).
  등재가 인덱스 방식의 약점으로 든 "순서 편집에 밀린다"는 **아직 일어나지 않은 위험**이고,
  실제로 일어난 위험은 **경로가 바뀌는 것**이다(1-3의 여섯 갈래 중 넷).
- `runCatching { File(p).canonicalPath }.getOrNull() ?: p`가 **여덟 곳에 복붙**돼 있다
  (`ImageOwnershipGuard`·`CharacterImageAutoLinker`·`StorageAnalyzer`·`OrganizeFolderService`·
  `ImageManagerViewModel` 등). 경로 대조의 단일 소스가 없다.

---

## 2. 진단 — 확정된 답 넷이 각각 무엇을 요구하는가

| 확정 | 겉보기 작업 | 실제로 요구하는 것 |
|---|---|---|
| **ㄱ1** 파일 기준 저장 | 칸 하나 추가 | **1-3의 열여섯 지점과 한 트랜잭션**(R-30 부류). 포인터는 목록을 가리키므로 목록이 바뀌면 함께 바뀌어야 한다 |
| **ㄴ3** 진입마다 랜덤 | 랜덤 넣기 | **주기의 통일.** 랜덤은 이미 있고 자리마다 규칙이 다르다(1-1) |
| **ㄷ1** 삭제 고지 | 토스트 하나 | **두 갈래.** 앱 안 삭제는 **사전 고지 + 취소 경로**(R-4), 앱 밖 삭제·폴더 왕복은 사전 고지가 원리적으로 불가능하므로 **사후 발견 고지** |
| **ㄹ1** 엑셀에 실음 | 열 하나 추가 | **왕복 해석 사다리.** 경로는 기기마다 다르고 외부 편집이 들어온다(개발 의도 4번 — 거부가 아니라 수용·교정) |

---

## 3. 결정 (D1~D9)

### D1. 저장 — 칸 하나. **모드 칸은 두지 않는다**

```kotlin
// data/model/Character.kt
val representativeImagePath: String = ""   // 빈 문자열 = 지정 없음(랜덤)
```

- **왜 `Universe.imageMode` 선례를 따르지 않는가.** 세계관·작품은 이미지의 **출처가 여럿**이라
  (자기 이미지 / 캐릭터 / 작품) 모드가 필요하다. 캐릭터의 이미지 출처는 **자기 것 하나뿐**이고
  선택지는 *고정했나 / 안 했나*뿐이다. 칸을 둘로 나누면 `mode=select`인데 포인터가 빈 상태가
  **표현 가능해지고**, 그 순간 두 칸이 서로를 부정한다(R-29가 경계하는 "잘못된 정답" 부류).
- 마이그레이션 **46→47**: `ALTER TABLE characters ADD COLUMN representativeImagePath TEXT NOT NULL DEFAULT ''`.
  하네스 `tools/verify_room_migration_47.py`를 함께 만든다(46판과 동형).
  > **⚠️ 번호를 선점하지 않는다 (2026.08.03 — B-107 설계가 낸 것).**
  > **B-107**(뗀 이미지 관리 — `docs/detached_image_management_design_2026-08.md`)도
  > 칸을 더하므로 마이그레이션이 필요하다. **먼저 착수하는 쪽이 47을 갖고 뒤가 48이다.**
  > 착수 세션은 `AppDatabase`의 현행 `version`을 먼저 읽고 번호를 정할 것 —
  > 이 문서의 `47`은 **작성 시점의 다음 번호**이지 예약이 아니다.
- **읽을 때는 `String?`로 받는다**(R-2) — 휴지통 payload·월드패키지는 Gson이 Unsafe로 객체를
  만들므로 **구버전 데이터에는 non-null 선언에도 null이 주입된다.** D2의 함수가 null과 빈 문자열을
  같게 다루면 그것으로 끝난다. `CharacterSnapshotPayloadTest`에 구버전 payload 사례를 더한다.

### D2. 판정 단일 소스 — `CharacterRepresentativeImage` (util, 순수)

```kotlin
object CharacterRepresentativeImage {
    enum class Source { PINNED, RANDOM, NONE }
    data class Pick(
        val path: String?,        // 표시할 경로 (없으면 null)
        val source: Source,
        val pinnedMissing: Boolean // 지정은 있는데 목록에 없다 — ㄷ1의 사후 고지 신호
    )
    fun pick(imagePathsJson: String?, representativePath: String?, seed: Long, characterId: Long): Pick
}
```

**사다리**
1. 지정한 대표가 **그 캐릭터의 목록 안에 있으면** 그것 (`PINNED`)
2. ~~*(비어 있는 칸 — B-104가 채운다. D9)*~~ → **이 칸은 서지 않는다** (2026.08.09 사용자 갈음 — D9 참조)
3. 시드 랜덤 (`RANDOM`) — **B-104가 여기에 *가중치*로 얹혔다** (2026.08.09 구현 완료)
4. 목록이 비었으면 없음 (`NONE`)

> **구현이 이 서명을 한 자리 넓혔다** — `pick`/`pickFrom`/`path`가 `weights: Map<String, Double>? = null`을
> 받는다(정규 경로 → 무게). **기본값 null이 종전 그대로**라 무게를 안 드는 호출부는 한 글자도
> 바뀌지 않았다. 무게가 없거나 전부 같으면 `randomIndex`를 그대로 타므로 **위 사다리의 성질도
> 그대로다**(D9의 ✅ 항 참조).

- **대조는 정규화 경로로 한다.** `ImagePathMatch.canonical(p)`를 util에 세우고 새 코드가 그것만 쓴다
  (1-4의 여덟 곳 복붙을 이 슬라이스에서 전부 걷어내지는 않는다 — 범위 밖이므로 **백로그 등재**. 5장).
- 순수 함수이므로 **순수 JVM 테스트 대상**이다(아키텍처 4장: 판단 로직은 `util`로 내린다).

### D3. 랜덤 주기 — **시드 하나 = 화면 진입 한 번.** 맵도 prefs도 쓰지 않는다

```
index = floorMod(mix(seed, characterId), paths.size)
```

| 요구 | 이 방식이 어떻게 만족하는가 |
|---|---|
| 화면 진입마다 다른 이미지 (ㄴ3) | 진입 때 새 시드를 뽑는다 |
| 같은 화면 안에서는 고정 | 시드가 같으면 결과가 같다 — 스크롤 재바인드·검색·필터·정렬·저장에 **튀지 않는다** |
| P2-1 제약 (O(N) 쓰기 금지) | **쓰기가 0이다.** 지금은 진입마다 prefs 전량 읽기(`loadAll`) + bind마다 쓰기가 있다 |
| 스테일 인덱스 위험 | 상태를 저장하지 않으므로 **없다** |

- **시드는 화면이 소유한다**(`onViewCreated`에서 1회 생성해 어댑터에 넘긴다).
  전역 싱글턴을 두지 않는 이유: 두 화면이 동시에 살아 있을 때 한쪽의 진입이 **다른 쪽 그림을 바꾼다.**
- **`ImageIndexPrefs`의 캐릭터 몫은 사라진다.** 작품·세계관 몫은 이 슬라이스 밖이라 **그대로 둔다**
  (백로그 등재 — 5장).
- **"화면 진입"의 정의:** 그 화면의 **뷰 생성**(`onViewCreated`). 회전 복원도 새 진입이다 —
  1-12의 수리가 이미 그 단위를 쓰고 있고, 사용자에게도 *"나갔다 들어오면 바뀐다"*가 설명 가능한 유일한 선이다.

### D4. 표시 자리 아홉 종류를 전부 D2로 바꾼다

| 자리 | 시드 소유자 | 바뀌는 것 |
|---|---|---|
| 캐릭터 목록 | `CharacterListFragment` | 인덱스 prefs → D2. 고정된 캐릭터는 언제나 그 장 |
| 생일 배너 | `HomeDashboardFragment` | `paths[0]` → D2. **사용자 요청 ①이 여기서 이뤄진다** |
| 상세 ViewPager | `CharacterDetailFragment` | 랜덤 시작 위치 → **대표가 있으면 대표에서 시작**, 없으면 랜덤(현행 유지) |
| 순위 카드 | 통계 화면 | `firstOrNull` → D2 (`RankingEntry`에 포인터 동승 — D8) |
| 통계 드릴다운 ×2 | 통계 화면 | 같음 |
| 어시스턴트 ×7 | 각 화면 | `firstImagePath()` → D2 |
| 중복 다이얼로그 | 그 다이얼로그 | 같음 |
| 세계관·작품 카드 연동 ×4 | 각 목록 | **연동 캐릭터의 대표를 존중한다** — 지금은 무조건 랜덤 |
| `CharacterDetailFragment:1055` | 그 화면 | 같음 |

> **`CharacterImageLoader.firstImagePath()`는 지운다.** 남겨 두면 다음 사람이 그것을 부르고,
> 그 화면만 다시 0번을 보여 준다 — 등재가 *"모으지 않으면 손댄 화면에서만 듣는다"*고 적은 그대로다.

### D5. 쓰기 정합 — 1-3의 열여섯 지점, 처분은 셋뿐

| 무슨 일이 일어나는가 | 대표를 어떻게 하는가 | 해당 지점 |
|---|---|---|
| 경로가 **바뀐다** | **따라서 고쳐 쓴다** | 이동·개명·되돌리기 · 폴더 왕복 편입 · 엑셀 재매핑 · 월드패키지 복원 |
| 경로가 **사라진다** | **비운다 + 고지**(ㄷ1) | 삭제 · 정리 · 폴더 왕복 제외 |
| 목록이 **통째로 바뀐다** | 대표가 새 목록에 없으면 비운다 + 고지 | 폼 저장(`CharacterSaveCoordinator`) |

- **전부 `imagePaths` 쓰기와 같은 트랜잭션에서 한다.** 빠뜨려도 오류는 나지 않고,
  포인터만 조용히 어긋난다 — **R-30이 등급 체계에서 배운 것과 같은 모양**이다
  (*"물질화는 원본 편집 경로의 목록을 함께 관리하는 일이다"*).
- **구현 세션은 이것을 규약으로 승격한다(제안 R-32):**
  *"목록을 가리키는 포인터는 그 목록을 고치는 모든 경로가 함께 고친다."*
  승격 시 갱신할 곳 셋 — `conventions.md` 원문 · 아키텍처 6장 색인 · 세션 로그 근거.

> **B-107이 이 목록을 열여섯 → 열여덟로 넓힌다 (2026.08.03).**
> 새로 드는 둘: **`_삭제승인/` 삭제 실행**(폴더 받아오기) · **편집창의 명시 삭제**(2지 선택).
> 둘 다 *"경로가 사라진다"* 갈래이므로 처분은 같다 — **비운다 + 고지.**
> **B-103이 먼저인 편이 싸다** — B-107을 먼저 하면 이 설계가 그 둘까지 다시 훑어야 하는데,
> 반대 순서면 B-107은 이미 선 함수를 부르기만 하면 된다.

### D6. 고지 두 갈래 (ㄷ1)

**ⓐ 사전 — 앱 안에서 지울 때 (R-4: 파괴적 동작은 실행 전에 결과를 알리고 취소 경로를 남긴다)**

이미지 관리는 이미 소유자 목록을 갖고 있다(`ImageManagerViewModel`의 owner 집계).
확인 창이 그 사실을 말한다.

> 이 이미지는 캐릭터 %1$s의 대표 이미지입니다. 지우면 대표 지정이 풀리고 랜덤 표시로 돌아갑니다.
> 삭제하시겠습니까?

(둘 이상이면 `캐릭터 %1$d명의 대표 이미지입니다`.) 편집 화면 스트립의 롱프레스 삭제도 같은 문장을 쓴다.

> **고지 자리가 셋으로 는다 (2026.08.03 — B-107 설계가 낸 것).**
> **`_삭제승인/` 폴더 받아오기**가 세 번째 삭제 경로다(B-107 D6). 그 확인창도 대표가 섞여
> 있으면 말해야 한다 — 폴더로 지우면 조용히 사라지는 자리가 생기면 ㄷ1이 반쪽이 된다.
> 그리고 **편집 화면의 롱프레스 창은 B-107 D7에서 2지 선택창이 된다**(캐릭터에서만 빼기 /
> 앱에서 삭제). **둘은 같은 창이다** — 창을 둘로 띄우면 조작 마찰이고 한쪽만 말하면 거짓이라,
> **한 창이 두 줄을 함께 말한다**(무엇을 지우는지 + 그것이 대표라는 것).

**ⓑ 사후 — 앱 밖 삭제·폴더 왕복·복원 (사전 고지가 불가능한 자리)**

D2가 `pinnedMissing`을 내면 **상세 화면에 인라인 고지**를 띄우고 **그 자리에서 고칠 경로**를 준다
(변수 제어: 검증 → 알림 → 바로잡을 경로).

> 대표로 지정한 이미지를 찾을 수 없어 랜덤 표시로 돌아갔습니다. 다른 이미지를 대표로 지정할 수 있습니다.

- **포인터를 조용히 지우지 않는다.** 사용자가 정한 것이고, **폴더 왕복은 파일을 되돌려 놓을 수 있다** —
  잠깐 안 보인다고 지우면 되돌아왔을 때 지정이 사라져 있다.
- 앱 **안**의 삭제는 파일이 영구히 사라지는 것을 아는 자리이므로 ⓐ에서 **비운다**(D5).

### D7. 지정 조작 — ☆ 한 번 (원칙 04)

| 자리 | 조작 | 왜 여기인가 |
|---|---|---|
| 상세 화면 이미지 위 ☆ | 탭 = 지금 보고 있는 그 장을 대표로. 다시 탭 = 해제 | 큰 그림을 보면서 고르는 자리다. **1탭** |
| 편집 스트립 썸네일 코너 ☆ | 같음 | 롱프레스(=삭제)와 탭(=뷰어)을 **건드리지 않는다** — 기존 제스처를 뺏지 않는다 |

- 대표인 장에는 ☆가 채워진 상태로 남아 **목록에서도 어느 것이 대표인지 보인다**
  (원칙 04: 일일이 확인하지 않으면 존재를 알 수 없는 데이터가 있어서는 안 된다).
- 새 설정 스위치가 아니므로 R-25 목적문 의무 대상은 아니다. 대신 **도움말 한 줄**을 상세 화면 `?`에 넣는다:
  *"대표로 지정한 이미지는 목록·배너·통계에서 언제나 그 장이 보입니다. 지정하지 않으면 화면에 들어올 때마다 무작위로 한 장이 보입니다."*

### D8. 엑셀·월드패키지 왕복 (ㄹ1)

**캐릭터 시트에 `대표이미지` 열 하나**(`이미지경로` 바로 뒤. `SheetSpec:469` 다음).
`CHARACTER_FIXED_HEADERS`(`SheetSpec:65`)와 `ExcelHeaderAliases`(`:32` 근처)에 함께 등재한다 —
**등재하지 않으면 커스텀 필드 열로 오인된다.**

- **내보내기:** 그 행의 이미지 중 **파일명**. 같은 행 안에서 파일명이 겹치면 **전체 경로**로 떨어진다.
  (`이미지경로`가 내부 경로 원문을 싣는 것과 달리, 이 열은 사람이 읽고 고칠 수 있어야 값어치가 있다.)
- **가져오기 사다리** — 거부가 아니라 수용·교정(개발 의도 4번):
  1. 그 행의 경로와 **정확히 일치**
  2. **재매핑된** 경로와 일치(`remapImagePath` 재사용 — 다른 기기에서 온 파일)
  3. 그 행 안에서 **유일한 파일명**
  4. **1부터 세는 번호**("2" → 두 번째 이미지)
  5. 그래도 못 찾으면 **경고 등재 + 기존 값 유지** — 버리지 않는다
- **빈 칸 = 해제**(앱의 기존 왕복 규약과 같다).
- 파일명 겹침 처리는 **이미 있는 판단을 따른다** — `ImageMetaRowResolver`가 "이미지" 시트에서
  같은 문제를 같은 방식으로 다룬다(겹치면 파일명만으로 확정하지 않고 알린다).
- **월드패키지:** `Character` 엔티티를 통째로 직렬화하므로 자동으로 실린다. 셋만 손댄다 —
  ① `WorldPackageContents`의 `allPresent(...)` 캐릭터 행에 추가(R-2)
  ② `WorldPackageImporter:362`에서 `imagePaths`와 **같은 재매핑**
  ③ **`CURRENT_SCHEMA_VERSION`은 올리지 않는다** — 4·5 두 번의 상향은 전부 **새 엔트리 파일 추가**였고,
  필드 추가로 올리면 구버전 앱이 **패키지 전체를 거부한다**(`schemaVersion > CURRENT` 검사).
  잃는 것이 "대표 한 칸"에서 "패키지 전체"로 커진다.
- **통계 스냅샷:** `RankingEntry`(`StatsDataProvider:508·3195`)와 드릴다운 스냅샷이
  `imagePaths`를 이미 나르므로 포인터도 **같이 나른다**(캐릭터당 짧은 문자열 하나. 순회 계산은 늘지 않는다).

### D9. B-104(대결)를 위해 비워 두는 칸 — **② 대신 ③의 가중치가 됐다 (2026.08.09 사용자 갈음)**

> **이 절의 원안은 폐기됐다. 아래 갈음이 현행이다.**
>
> 원안은 *대결 1위를 사다리 ②(①과 ③ 사이)에 놓는 것*이었다. 착수 세션이 그것을 물었을 때
> 사용자가 되물어 갈음했다 — ***"대결 1위 그림이 대표가 되면 이미지 다양성이 아쉽잖아.
> 확률이면 모를까."***
>
> **지적이 옳다.** ②를 세우면 대결을 한 캐릭터는 **언제나 같은 한 장**만 보이게 되어, D3가
> *"화면 진입마다 다시 뽑는다"*로 일부러 지킨 다양성이 그 캐릭터에서만 통째로 사라진다.
> 대표를 **지정하지 않은** 캐릭터인데도 그렇게 되는 것이라, 지정과 미지정의 구별도 흐려진다.
>
> **갈음: ③의 추첨을 균등에서 점수 가중으로 바꾼다.**
> - 이긴 그림이 자주, 진 그림도 가끔 나온다 — 다양성을 지키면서 대결 결과가 값을 한다.
> - **세기는 설정으로 조절한다**(꺼둠=균등 / **약하게**(기본) / 세게).
> - **한 판도 안 친 그림은 가중 없는 기본값**이라, 대결을 안 한 캐릭터는 지금과 완전히 같다.
> - 사다리 **②는 서지 않는다** — 칸이 하나 줄어 `Source`에 새 값도 붙지 않는다.
>
> **원안이 옳게 본 것 둘은 그대로다:** ①(직접 고정)이 언제나 이긴다는 것과, D2 함수 하나만
> 고치면 호출부가 따라온다는 것. **다만 가중치는 인자로 들어와야 하므로 "호출부가 안 바뀐다"는
> 더 이상 참이 아니다** — 그 배선이 ⓑ 슬라이스의 실제 비용이고, 착수 시 여기부터 볼 것.
> 어느 축의 점수를 쓰는지는 **축 편집 창의 스위치**가 정한다(세계관당 하나 — 같은 날 확정).
> 설계 원문은 `docs/duel_system_design_2026-08.md` **13-5**.
>
> ---
>
> **✅ 구현 완료 (2026.08.09) — 설계 정본은 `duel_system_design_2026-08.md` 13-8이다.**
>
> 갈음안 그대로 섰다. 이 문서에 되먹여 둘 사실 셋:
> - **사다리가 셋으로 줄었다** — `CharacterRepresentativeImage`의 머리 주석이 그 자취다.
>   `Source`에 새 값은 붙지 않았다(가중은 여전히 `RANDOM`이다 — *무엇이 골랐는가*는
>   *어떻게 골랐는가*와 다른 물음이고, 새 값을 만들면 그것을 읽는 아홉 자리가 전부 늘어난다).
> - **"호출부가 안 바뀐다"의 실제 답: 인자에 기본값 null을 두어 *안 드는 호출부는 안 바뀌었다*.**
>   무게를 실제로 넘기는 자리는 **캐릭터 목록 하나**이고, 나머지 열셋을 왜 안 걸었는지는
>   13-8-3의 표가 자리마다 근거를 든다(특히 **상세**는 뽑는 것이 그림이 아니라 **페이저의
>   시작 위치**라 늦게 도착한 무게가 *보고 있던 페이지를 움직인다*).
> - **D3의 다양성은 지켜졌다** — 무게가 전부 같으면 계산이 `null`을 내어 **종전 `randomIndex`
>   그대로**를 타므로, 대결을 안 한 캐릭터는 *한 비트도 다르지 않은* 그림을 본다.
>   벌어진 점수는 한 눈금(400점)에서 끊어 최악 비율이 `10^α`로 묶인다 — *확률*이 *고정*으로
>   변질되는 것을 막는 빗장이고, 그것이 이 갈음의 존재 이유다.

*(아래는 폐기된 원안 — 기록으로 남긴다.)*

D2 사다리의 **①과 ③ 사이**가 그 자리다. 대결이 이미지 축 승자를 내면 **기본값 후보**로 ②에 들어간다.

- **호출부는 바뀌지 않는다** — D2 함수 하나만 고치면 아홉 종류가 동시에 따라온다. 이것이 ⓐ를 먼저 하는 이유다.
- **직접 고정이 이긴다**는 것은 B-104 행이 이미 확정한 사실이다(*"단 직접 고정이 이긴다"*).
- **지금 인자를 미리 만들지 않는다** — 쓰지 않는 인자는 방편식이다. 자리와 규칙만 여기 적어 둔다.

---

## 4. 화면

| 화면 | 무엇이 달라지는가 |
|---|---|
| 캐릭터 목록 | 겉보기 무변화(진입마다 랜덤은 그대로). **대표를 지정한 캐릭터만 언제나 그 장** |
| 홈 대시보드 생일 배너 | 0번 고정 → 대표 또는 랜덤 (**사용자 요청 ①**) |
| 캐릭터 상세 | 이미지 위 ☆ 토글. 대표가 있으면 그 장에서 시작. 대표 실종 시 인라인 고지(D6ⓑ) |
| 캐릭터 편집 / 보충 인라인 | 스트립 썸네일 코너 ☆. 롱프레스 삭제 확인문에 대표 고지 한 줄(D6ⓐ) |
| 이미지 관리 | 삭제 확인문에 대표 고지 한 줄(D6ⓐ) |
| 통계(순위·드릴다운) · 어시스턴트 · 중복 확인 | 0번 → 대표/랜덤 |
| 세계관·작품 카드 | 연동 캐릭터의 **대표를 존중** |

**문구는 전부 합니다체**, 확인 질문은 `~하시겠습니까?`, 표1 금지어 없음, 대시(—) 미사용.

---

## 5. 새 기능 체크리스트 판정 (CLAUDE.md 7항목)

1. **확장성** — 대표는 사용자가 정하고 언제든 해제한다. 사다리에 칸(②)이 열려 있어 B-104가 붙는다. ✅
2. **실질성** — 등재된 문제(같은 판정이 아홉 종류로 갈림)를 없앤다. 요청 ①②가 둘 다 이뤄진다. ✅
3. **완결성** — 쓰기 열여섯 지점을 전수로 든다(D5). 고지가 사전·사후 두 갈래를 다 덮는다(D6). ✅
4. **연결성** — 세계관·작품 연동이 대표를 존중하고(원칙 05), 통계·어시스턴트·엑셀·월드패키지·휴지통이 같은 값을 본다. ✅
5. **조작성** — 지정은 ☆ 1탭. 기존 제스처를 뺏지 않는다. ✅
6. **의도 부합** — 아래 4단계 + 4질문. ✅
7. **텍스트** — 4장 문구 규정. 새 문자열은 `check_text_style.sh` 기준선 0을 유지해야 한다. ✅

**받쳐주는 확장성 — `scalability_performance_2026-07.md` 7장 4단계 판정**

| 단계 | 판정 |
|---|---|
| ① 새 상한을 도입하는가 | **아니오.** 상한 없음 — 4장 상한 표에 더할 것이 없다 |
| ② 비용이 어느 축에 붙는가 | **캐릭터 수 축.** 표시 1회당 O(1) 해시. **지금 있는 비용이 사라진다** — 목록 진입 시 prefs 전량 읽기(`loadAll`)와 bind마다의 prefs 쓰기가 없어진다. 순감이다 |
| ③ `StatsSnapshot`에 목록을 추가하는가 | **아니오.** 캐릭터당 짧은 문자열 하나이고 **그것을 순회하는 계산이 없다**(7장이 말한 진짜 질문) |
| ④ 엑셀 왕복에 열·시트를 더하는가 | **예 — 캐릭터 시트에 1열.** 값은 파일명 길이. 시트가 커지는 방향이지만 열 하나이며 스트리밍 적재 단위에 미치는 영향은 무시 가능하다 |

**개발 의도 4질문**: 받쳐주는 확장성(위) · 변수 제어(D6 두 갈래 — 무음 폴백 없음) ·
자율성 우선(지정은 선택이고 기본은 랜덤) · 엑셀 왕복 무결성(D8 사다리 5단).

---

## 6. 적대 자기 검토 — 예상 반론과 답

| 반론 | 답 |
|---|---|
| **진입마다 바뀌면 "내 캐릭터 카드"를 눈으로 못 찾는다** | 그래서 대표 고정이 있고 **그것이 이 항목의 본체다.** 랜덤은 지정하지 않은 캐릭터의 기본값일 뿐이며, 그 동작은 지금도 그렇다(1-1) |
| **경로 저장은 파일이 움직이면 깨진다** (등재가 든 위험) | D5가 열여섯 지점을 전수로 든다. 앱이 모르게 깨지는 유일한 길은 **앱 밖에서 파일을 옮기는 것**이고, 그때는 `imagePaths` 자체가 이미 깨져 있다 — **대표만의 약점이 아니다.** 그 경우는 `pinnedMissing` → D6ⓑ가 받는다 |
| **인덱스가 더 싸지 않은가** | 인덱스는 **지금 있는 방식**이고 이미 스테일 위험을 안고 있다(`idx % paths.size`로 가려 둔 상태다 — 이미지가 줄면 **다른 그림을 가리킨다**). 사용자 확정(ㄱ1)이기도 하다 |
| **시드 방식은 진짜 랜덤이 아니다** | 필요한 성질은 "진입마다 다르고, 진입 안에서는 같다"이다. 균등성은 시드·id 혼합 품질이 정하며 **순수 테스트로 분포를 고정한다**(같은 시드에서 캐릭터마다 독립적일 것) |
| **☆가 이미지 탭 제스처를 가린다** | 코너에 두고 크기·터치 영역을 규격으로 정한다. 이미 `imageCountBadge`가 같은 자리에 겹쳐 있다. **자동 검증이 못 보는 축이므로 실기기 확인 항목이다**(착수 규칙 4번) |
| **엑셀 파일명 기준은 겹치면 틀린다** | 겹치면 내보내기가 **전체 경로로 떨어지고**, 가져오기는 "그 행 안에서 유일할 때만" 파일명을 쓴다. 못 찾으면 버리지 않고 **경고 + 기존 값 유지** |
| **대표를 "이미지" 시트(라이브러리 meta)에 두면 안 되나** | 안 된다. 그 시트는 **파일 단위 전역**이고 대표는 **(캐릭터, 이미지) 관계**다. 한 파일을 두 캐릭터가 쓰면 표현할 자리가 없다 |
| **휴지통에서 되살리면 대표가 사라지지 않나** | payload가 엔티티 통째이고 복원이 `copy()`라 함께 돌아온다(`TrashRepository:1600`). 구버전 payload는 D1의 `String?` 취급이 받는다(R-2) |
| **`ImageIndexPrefs`를 캐릭터에서만 걷어내면 절반만 정리된 것 아닌가** | 그렇다. 작품·세계관 몫은 **이 슬라이스 밖**이라 백로그에 올린다(착수 규칙 2번: 밖이면 등재만 하고 진행) |

---

## 7. 착수 절차 (구현 세션용)

등재된 순서 **ⓐ → ⓑ → ⓒ**를 그대로 쓴다. 순수 계층부터 세워 **검증 가능한 것을 먼저 만든다.**

| 단계 | 무엇을 | 무엇으로 검증하는가 |
|---|---|---|
| 1 | `CharacterRepresentativeImage` + `ImagePathMatch.canonical` (D2·D3) | **순수 JVM 테스트 신설** — 사다리 4갈래 · `pinnedMissing` · 같은 시드 안정성 · 시드가 바뀌면 분포가 갈리는가 |
| 2 | 마이그레이션 46→47 (D1) | `tools/verify_room_migration_47.py` 신설(46판과 동형) + 기존 하네스 5종 회귀 |
| 3 | 쓰기 열여섯 지점 정합 (D5) | 실클래스패스 프로브(`excel`·`model`·`dao`·`util`·`data/repository`가 범위 안이다) · 차분 컴파일 |
| 4 | 표시 아홉 종류 전환 + `ImageIndexPrefs` 캐릭터 몫 제거 (D4) | 차분 컴파일 · `check_prefs_keys.sh` · **CI**(어댑터·프래그먼트는 프로브 범위 밖이다) |
| 5 | ☆ 지정 UI + 고지 2종 (D6·D7) | `check_resources.sh` · `check_text_style.sh` · `check_dialog_validation.sh` · `check_dialog_scroll.sh` |
| 6 | 엑셀 열 + 해석 사다리 + 월드패키지 (D8) | 순수 JVM(사다리 5단 테스트) · 왕복 시험 |

**착수 전에 확인할 것 둘**
- 재현 기준선: `remaining_work_2026-07.md` **5장 2번**이 단일 소스다(여기 숫자를 적지 않는다 — 적으면 낡는다).
- 1-2의 실측 셋(`ImageIndexPrefs` · 상세의 랜덤 시작 · `RankingAdapter`)이 **아직 그대로인가.**
  이 저장소의 착수 대조는 **연속으로 등재를 정정해 왔다.**

> **이 슬라이스는 세션 착수 규칙 4번이 정면으로 겨누는 자리다.** 바뀌는 것의 대부분이
> **보이는 것**(어댑터·프래그먼트·다이얼로그)이고, 그중 다수가 실클래스패스 프로브 범위 밖이다.
> 게다가 손대는 자리(캐릭터 목록 랜덤)는 **실기기 보고로만 발견된 결함이 한 번 났던 곳**이다(1-12).
> **CI 초록과 실기기 확인 전까지 완료로 보고하지 않는다.**

**실기기 확인 항목(초안 — 구현 세션이 3장에 절을 신설한다)**

> **→ 신설 완료: `remaining_work_2026-07.md` 3-91이 정본이다**(2026.08.03, 1-bs장.
> **번호는 2026.08.10에 `3-50`에서 옮겼다** — B-98과 겹쳐 회신 주소가 갈렸다. B-109 ⓐ).
> 아래 초안 7항은 그 절에 **16항으로 펼쳐졌다** — 초안이 한 줄로 뭉쳐 둔 것(왕복·고지·옛 데이터)이
> 실제로는 확인 동작이 여럿이었기 때문이다. **여기 목록을 두 벌로 두지 않는다**(적으면 낡는다) —
> 아래는 설계 시점의 기록으로 남긴다.
1. 이미지 2장 이상인 캐릭터: 목록을 나갔다 들어오면 그림이 바뀌는가(반복). 같은 화면에서 검색·정렬 중에는 안 튀는가
2. 대표를 지정한 캐릭터: 목록·배너·순위·통계 드릴다운에서 **언제나 그 장**인가
3. 대표로 지정한 이미지를 이미지 관리에서 삭제 → 경고가 뜨는가, 취소가 듣는가, 지운 뒤 랜덤으로 돌아가는가
4. 캐릭터를 저장한 뒤에도 대표가 남아 있는가 (**`CharacterSaveCoordinator` 축 — `partSlots`가 여기서 지워졌었다**)
5. 폴더 왕복으로 이미지를 내보냈다 되받은 뒤 대표가 유지되는가
6. 엑셀 내보내기 → `대표이미지` 열을 다른 이름으로 고쳐 가져오기 → 경고가 뜨고 기존 값이 남는가
7. 상세 ☆ 토글이 이미지 탭·스와이프를 방해하지 않는가

---

## 8. P-3 — **확정 (2026.08.03, 사용자 확정 "같은 주기")**

확정 문서 3장의 **P-3**: *"ㄴ3(화면 진입마다 랜덤)이 목록에서도 매번 바뀌는가."*

**답:** **같은 주기를 쓴다 — 화면 진입 1회 재추첨, 그 화면 안에서는 고정.**
목록·배너·상세가 **한 규칙**을 쓴다. **이로써 이 설계를 막는 판정은 남아 있지 않다.**

근거 셋:
1. **목록은 이미 그렇게 동작한다**(1-1·1-2). 사용자가 고른 ㄴ3과 현행 목록 동작이 **이미 일치한다.**
2. 매 바인드마다 다시 뽑으면 **스크롤할 때 그림이 깜빡인다** — 등재 자신이 ③에서 그것을 경계했다.
3. 방출마다 다시 뽑는 것은 **P2-1이 성능 때문에 없앤 바로 그 동작**이다(세션 로그 1-12).

→ **사용자가 이 답을 확정했다**(2026.08.03). 확정 문서 3장의 P-3은 해소됐고,
**D3이 그대로 선다** — 시드 하나 = 화면 진입 한 번.

> **묻는 방식이 답을 만들었다.** 선택지 셋을 나열만 하지 않고 **현행 동작을 실측해 함께 냈다** —
> *"캐릭터 목록은 이미 그렇게 돌고 있습니다"*가 근거의 첫 줄이었다. 확정 문서 3장이
> 이 방식을 파생 판정을 묻는 본보기로 등재했다.

---

## 9. 착수 대조가 정정한 것 (2026.08.03 — 이 세션)

| 등재가 말한 것 | 실제 |
|---|---|
| 판정 자리는 `firstImagePath`(4파일) · 배너 `paths[0]` · `UniverseViewModel:344` · `StatsDataProvider` 2곳 | **아홉 종류·열아홉 지점.** `ImageIndexPrefs`(목록)·상세의 랜덤 시작 위치·`RankingAdapter`가 빠져 있었다 |
| 랜덤의 주기를 정해야 한다 | 목록은 **이미 "화면 진입마다"**다. 정할 것은 새 주기가 아니라 **나머지 여덟 종류를 여기에 맞추는 것** |
| 인덱스는 순서 편집에 밀린다 | 캐릭터 이미지에는 **순서 편집 화면이 없다.** 실제로 일어나는 위험은 **경로 변경**(여섯 갈래 중 넷)과 **목록 축소로 인한 인덱스 오조준** |
| (없음) | **`CharacterSaveCoordinator`가 `Character`를 새로 조립한다** — 새 칸을 적지 않으면 저장할 때마다 조용히 지워진다(`partSlots` 전례) |

---

## 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.3 | 2026.08.03 | **구현 완료**(세션 로그 1-bs). 7장 6단계를 그대로 밟았고 **설계가 두 자리 틀렸다**: ① **D8 ①의 "`WorldPackageContents`의 `allPresent(...)` 캐릭터 행에 추가"는 따르면 안 된다.** `scrub`은 검사에 걸린 **행을 버리는** 물건이라, 새 칸을 넣으면 **v47 이전에 내보낸 패키지의 캐릭터가 전부 사라진다.** 이 칸의 null은 파손이 아니라 "그때는 없던 칸"이므로 **접는 것은 쓰는 쪽(임포터)의 몫**으로 옮겼다 ② **Kotlin `copy`는 넘기지 않아 기본값으로 채워지는 인자에도 null 검사를 건다** — 그래서 새 non-null 칸을 더하면 구버전 payload를 `copy()`하는 자리가 **전부 NPE 지뢰**가 된다. `WorldPackageParserTest`(code가 null인 행을 살려 두는 계약)가 곧바로 잡았고, 임포터·휴지통 복원 두 자리를 명시 전달로 고쳤다. 회귀 테스트를 `CharacterSnapshotPayloadTest`에 더했다. **설계에 없던 것 둘을 더 했다**: `tools/check_image_pointer.sh`(D5를 기계가 지키게 — R-32로 승격) · `CharacterSpecColumnOrderTest`(머리글은 spec 순서인데 데이터 셀은 `col++`라 열 삽입이 뒤를 통째로 민다). **1-1 표의 `UniverseViewModel:344`는 표시 자리가 아니었다** — 경로를 꺼내 버리고 "이미지가 있는가"만 보므로 바꾸지 않았다. 재현 기준선 1371 → **1425** |
| v1.2 | 2026.08.03 | **B-107 설계가 낸 변경점 3자리 반영**(세션 로그 1-bq. 사용자 지시로 B-107 설계를 먼저 썼다 — *"전체의 조화를 봐야지"*). ① **D1 — 마이그레이션 번호를 선점하지 않는다**: B-107도 칸을 더하므로 **먼저 착수하는 쪽이 47을 갖고 뒤가 48**이다. 이 문서의 `47`은 작성 시점의 다음 번호이지 예약이 아니다 ② **D5 — 쓰기 지점이 열여섯 → 열여덟**: `_삭제승인/` 삭제 실행과 편집창 명시 삭제가 더해지고, 둘 다 *"경로가 사라진다"* 갈래라 처분은 같다. **B-103이 먼저인 편이 싸다**(반대 순서면 이 설계가 B-107이 만든 자리까지 다시 훑는다) ③ **D6 — 삭제 고지 자리가 셋으로**: `_삭제승인` 받아오기가 세 번째 경로이고, **편집창 롱프레스 창은 B-107에서 2지 선택창이 되므로 둘은 같은 창이다** — 창을 둘로 띄우면 마찰이고 한쪽만 말하면 거짓이라 **한 창이 두 줄을 함께 말한다.** D2·D3·D4와 엑셀(다른 시트)은 영향 없음을 확인했다 |
| v1.1 | 2026.08.03 | **P-3 확정 반영**(사용자 확정 — "같은 주기"). 8장을 "남은 판정"에서 **확정 기록**으로 바꿨다. **이제 이 설계를 막는 판정은 없다** — 구현 세션은 7장에서 바로 시작한다. D3은 무변경(추천이 그대로 확정됐다) |
| v1.0 | 2026.08.03 | 최초 작성 — 확정 4건(ㄱ1·ㄴ3·ㄷ1·ㄹ1)의 구현 설계. 현행 실측(판정 아홉 종류·쓰기 여섯 갈래), 결정 D1~D9, 체크리스트 7항목 + 확장성 4단계 판정, 적대 검토 9건, 착수 절차 6단계. **P-3에 근거 있는 추천을 붙였다**(현행 목록이 이미 그 주기다). 착수 대조가 등재를 **4건 정정**했다 |
