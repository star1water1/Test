# 엑셀 파일 시각 디자인 리뷰 (2026.08)

> **상태: 확정·시행 완료 (2026.08.14).** 사용자 지시(*"엑셀 파일 디자인에 대해 불만이 많은데 시각적인 부분을 중점으로 봐줘. 물론 빠진 게 없는지도 봐주고"*)로 수행한 **대기열 밖** 리뷰였고, 이어진 답(*"너 권고대로 할게 중요한 건 그게 최고냐니까. 그 부분으로 생각해줘. 구현해"*)으로 **Q-1 ⓐ · Q-2 ⓐ · Q-3 ⓑ · Q-4 ⓑ가 확정돼**(11차 확정 — `judgment_confirmations_2026-08.md` 16장) P-1~P-9를 시행했다. 발견(V-1~V-10 · N-1)의 정본은 이 문서, 시행 형태(재검토 상향 둘 포함)는 **8장**이다.

## 0. 왜 이 문서가 따로 있는가

2026.08.14의 엑셀 왕복 전수 감사 2차(`excel_audit_2026-08.md`, PR #303)는 `workbook-design` 슬라이스를 두고도 **문구·용어·드롭다운 한도**까지만 봤다 — 판정 기준이 "왕복 규약 + R-36·R-47·R-49 + 개발 의도 4문항"이라 **미관·가독의 기준 자체가 없었다.** 색·폰트·열 너비·틀 고정·정렬·셀 서식은 어느 문서에도 결정이 없고(문서에서 유추 가능한 현행 규칙은 "필수 헤더=빨강" · "읽기전용=회색" 둘뿐), 코드에만 있다. 이 문서가 그 축을 처음 본다.

**왕복 무결성·문구 축은 이 문서의 몫이 아니다** — 그 정본은 감사 문서 3장(잔여 B-217~B-230)이고, 여기 반복하지 않는다.

## 1. 방법 — 코드 정독 + 실파일 렌더

코드만 읽고 "괜찮을 것"이라 적지 않았다(체크리스트 6번의 금지 형태). 검증 절차:

1. `ExcelExporter.kt`의 `ExcelStyles`(350~417행)·`writeHeaderRow`·`finishDataRow`·`applySpecFormatting`·가이드 시트(540~766행)와 `SheetSpec.kt`의 열 사양(너비·필수·읽기전용·드롭다운)을 **그대로 옮긴 재현 워크북**을 POI 5.3.0(앱과 같은 판)으로 생성 — 대표 7시트(사용 안내 · 세계관 · 캐릭터 · 필드 정의 · 사건 연표 · 전체 캐릭터 · 빈 시트), 한글 실데이터(장문 설명·개행 메모·따옴표 쉼표 값·13자리 밀리초 포함).
2. LibreOffice Calc로 렌더해 육안 확인. (한계: Calc는 명시 높이 없는 행을 로드 시 재계산하지만 **엑셀은 하지 않는다** — 5-3 구현 노트에 반영. 색·너비·잘림·정렬은 두 프로그램이 같게 그린다.)
3. 같은 데이터로 개선안(B안) 워크북을 만들어 나란히 렌더 — 처방이 실제로 낫는지를 눈으로 확인한 뒤에만 제안에 올렸다.

## 2. 시각 진단 — 10건

### V-1. 색 체계가 25년 전 인덱스 팔레트다 🟠

`ExcelStyles`가 쓰는 색은 전부 `IndexedColors`(엑셀 97 고정 팔레트)다: 헤더 `DARK_BLUE` #000080, 필수 `RED` #FF0000, 읽기전용 `GREY_50/GREY_25`. 순채도 원색이라 화면에서 낡고 공격적으로 읽히고, **앱 자신의 색(테두리 프리셋 1번 #5C6BC0 인디고 계열, `BORDER_COLOR_PRESETS`)과 아무 관계가 없다.** POI는 스트리밍 워크북에서도 커스텀 RGB(`XSSFColor`)를 그대로 지원한다(스타일은 워크북 수준 객체라 R-49와 무관).

### V-2. "필수=순빨강"의 신호 인플레이션 🟠

빨강은 스프레드시트 문화에서 오류·경고색인데, 이 파일에서는 "채워 달라"는 뜻으로 쓰인다. 그리고 그 빨강이 **몰려 나온다**: '필드 정의' 시트는 앞 4열(세계관·필드키·필드명·타입)이 연속 순빨강이고, '캐릭터 상태변화'는 빨강 3곳이 남색과 교차하며, 빈 시트(B-88)에서는 데이터 없이 헤더의 빨강·남색 교차 패턴만 남는다. 신호가 흔하면 신호가 아니다. 게다가 WD-1 수정이 인정했듯 빨강의 의미는 이미 절대적이지 않다(필드 계열 시트의 세계관 칸은 "비우면 전역"). — 렌더로 확인: 필드 정의 시트 첫인상이 "반쯤 경고"다.

### V-3. 읽기전용 회색이 안 읽힌다 — 대비 1.6:1 🟠

읽기전용 셀은 `GREY_50` 글자(#969696)를 `GREY_25` 바탕(#C0C0C0)에 얹는다. **명도 대비 1.6:1** — WCAG 최소선(본문 4.5:1)의 1/3이다. 렌더에서 코드·이미지경로·생성일이 물 빠진 자국처럼 보인다. 문제는 이 셀들이 **읽으라고 있는 값**이라는 것이다: 사용 안내 자신이 *"동명 세력이 둘 이상이면 '세력' 시트의 코드 값을 (읽고) 참조코드 열에 붙여넣으라"*고 시킨다 — 그 워크플로의 원재료가 가장 안 보이는 셀에 있다.

**최악의 자리는 '전체 캐릭터' 시트다.** 전 열이 읽기전용이라 **시트 전체가 회색-위-회색**인데, 이 시트의 존재 이유가 "전 인원 정렬·필터·피벗"(U-12a) — **가장 많이 읽을 시트가 가장 안 읽히는 시트가 됐다.**

### V-4. 여러 줄 텍스트가 접혀서 안 보인다 🟠

데이터 셀에는 어떤 스타일도 없다(읽기전용 회색 제외). 그래서 `wrapText`가 없고, 설명·메모의 **개행(\n)은 화면에서 사라진다**(한 줄로 이어 그려짐) — 렌더 실측: 세계관 설명의 둘째 문단("주요 갈등: …")과 캐릭터 메모의 2·3행("2부 예정: …", "말버릇: …")이 **완전히 비가시**. 오른쪽 셀이 차 있으면 첫 줄마저 열 폭에서 잘린다. 값은 파일에 온전한데 화면에 없다 — *"일일이 확인하지 않으면 존재를 알 수 없는 데이터가 있어서는 안 된다"*(원칙 04)의 정확한 위반 형태다.

### V-5. 13자리 밀리초가 날것으로 보인다 — B-222 ①의 시각 실증 🟡

생성일·판정일 열이 `1721834567890`을 그대로 보여 준다(`setCellValue(createdAt.toDouble())` — `ExcelExporter.kt:1446` 등). 이미 백로그에 있다(B-222 ①). 이 리뷰의 추가 증거: 그 값이 **회색-위-회색(V-3)과 겹쳐** 있어, "지우지 말라"고 안내되는 매칭 키가 가장 흐릿한 픽셀로 존재한다. 열 폭 5000이라 서식 없이도 과학표기는 면하지만, 사람에게 아무 뜻이 없다.

### V-6. 틀 고정이 모든 시트에서 헤더 한 줄뿐이다 🟡

`freezeAndFilter`가 시트 모양과 무관하게 `createFreezePane(0, 1)`이다(`ExcelExporter.kt:475`). 캐릭터 시트는 커스텀 필드가 붙으면 20~50열이 된다(실사용 표본 49·38·42열 — `allCharactersSpec` 주석). **오른쪽으로 세 화면 스크롤하면 지금 고치는 행이 누구인지 알 수 없다.** 엑셀에서 가장 값싼 처방(이름 열 고정)이 빠져 있다.

### V-7. 색을 설명하는 유일한 자리에 색이 없다 🟡

사용 안내의 색상 범례가 `■ 파란 헤더 = …` 식의 **검정 텍스트**다(`ExcelExporter.kt:548~551` — `guideBody`에 색 없음). 렌더에서 세 개의 검정 네모가 파랑·빨강·회색을 "설명"한다. 실제 헤더 스타일을 입힌 견본 셀이면 될 자리다. 부수 관찰: 본문이 전부 A열(너비 15000)에 들어가고 B열(25000)은 항상 비어 있으며(섹션 행의 `text`가 현재 전례 없이 빈 문자열이라 743~762행의 B열 분기가 죽은 경로다), 약 200행의 텍스트 벽을 파란 굵은 글씨 하나로만 끊는다. '주의사항'(시트명 변경 금지 등 가장 중요한 경고)이 맨 끝 행에 있다.

### V-8. 커스텀 필드 열 너비가 일률 5000이다 🟡

`ColumnSpec` 기본 너비 5000(≈영문 19.5자·한글 ~9자)이 모든 커스텀 필드에 적용된다(`SheetSpec.kt:21`, `characterSpec`의 동적 열). 헤더가 `성격 키워드 (쉼표 구분)`처럼 접미사(B-177)까지 달면 헤더 자체가 잘리고, MULTI_TEXT 값은 두 토큰이면 이미 넘친다. 고정 열은 손튜닝돼 있는데(이름 6000·메모 10000·설명 15000) 정작 **사용자가 만든 필드**만 기성복 치수다 — 타입별 기본값(MULTI/장문 TEXT 넓게, GRADE/SELECT 좁게) 없이는 원칙 01(사용자 확장이 일급)과 어긋난 대접이다.

### V-9. 숫자가 전부 왼쪽 정렬 텍스트다 — 표가 계산을 거부한다 🟠

커스텀 필드 값은 타입 불문 `setTextSafe`(문자열 셀)로 나간다(`ExcelExporter.kt:1407~1409`). NUMBER 필드의 24, CALCULATED의 78.50이 전부 **텍스트 셀**이다. 시각으로는 좌측 정렬이라 숫자로 안 읽히고, 기능으로는 N-1(4장)이다. 유일한 숫자 셀은 시스템 열(정렬순서·연도·밀리초)뿐이다.

### V-10. 탭 24+개가 무색·무그룹이다 🟢

기본 내보내기가 시트 24종 + 세계관별 캐릭터 시트 + 오버플로/전체 캐릭터를 만든다. 탭 색이 없어 안내/구조(세계관·작품·필드)/캐릭터/기록(연표·관계·대결)/프리셋·설정/읽기전용 파생의 구획이 탭줄에서 보이지 않는다. 탭 순서 자체는 가져오기 의존 순서와 일치하므로 문제가 아니다 — 색만 없다.

## 3. 진단의 공통 뿌리

다섯 스타일(`header`·`requiredHeader`·`readOnly`·`readOnlyHeader`·가이드 3종)이 시각 체계의 전부이고, **데이터 셀은 무스타일**이다. 즉 지금 파일은 "서식을 입힌 표"가 아니라 "헤더에만 색을 칠한 덤프"다. 왕복 무결성에 쏟은 공(감사 2회·규약 10여 개)에 비해, 그 파일을 **사람이 보는 축**은 한 번도 설계되지 않았다 — 개발 의도의 *"엑셀 파일 아웃풋·인풋의 자유로움"*에서 아웃풋의 절반(보이는 것)이 비어 있는 셈이다.

## 4. 완전성 점검 — "빠진 게 없는지"

### 4-1. 시트·엔티티 수준 신규 누락: 0건

`ExportSheetStep` 24종 + 세계관별 캐릭터/미분류/오버플로 3종/전체 캐릭터를 데이터 모델(`data/model/`) 전수와 대조했다. 실리지 않는 엔티티는 전부 근거 있는 의도된 제외다:

| 안 실리는 것 | 근거 |
|---|---|
| 휴지통(TrashSnapshot)·작업 이력(OperationLog)·최근 활동 | 로컬 안전망·이력이지 데이터가 아니다. 스냅샷 개선은 1차 감사 N1로 별도 등재됨 |
| FTS 색인 | B-10 확정 — *"싣지 않는다, 복원 뒤 재색인한다"* |
| AI API 키 | B-105 확정 — 기본 제외 + 별도 동의 |
| '전체 캐릭터'의 전역 필드 열 | B-149 확정 — *"의도한 제외다"* (공유 필드 집계 시트의 정의) |
| '전체 캐릭터'의 메모 열 | 파생 시트 성격(피벗용 발췌)이고 정본은 캐릭터 시트에 온전 — 유실 아님 |
| 스냅샷·캐시류(EntitySnapshots·ConfigParseCache 등) | 파생·재계산 가능 |

왕복·고지 축의 잔여는 `excel_audit_2026-08.md` 3장(B-217~B-230)이 정본이며 이 리뷰에서 재발견된 것은 없다(기각 23건도 다시 파지 않았다).

### 4-2. N-1. 신규 — NUMBER·CALCULATED 값이 텍스트라 '전체 캐릭터'의 목적이 반쪽이다 🟠

U-12a의 존재 이유는 *"엑셀의 최대 강점인 전체 정렬·필터·피벗"*인데, 숫자 필드가 텍스트 셀이라:

- **정렬이 사전순이다** — "10"이 "9" 앞에 온다. 나이·수치 필드 정렬이 조용히 틀린다.
- **피벗·평균·합계가 0이 된다** — 통계 화면이 못 하는 것을 엑셀에서 하라고 만든 시트에서, 엑셀의 계산이 안 된다.

원칙 02(실질적 기능성)의 문제이지 단순 미관이 아니라 4장에 둔다. 처방은 P-6, 단 **왕복 멱등성 단서**가 있다(그쪽 참조).

## 5. 개선안(B안) — 렌더로 검증한 처방

### 5-0. 손대면 안 되는 것 (교환 형식 동결)

시각 개선은 **스타일 레이어만** 만진다. 아래는 형식이므로 이 제안 어디에서도 바꾸지 않는다:

- **헤더 문자열** — 별표(`*`) 등 어떤 글자도 덧붙이지 않는다. `findColumnTolerant`의 정규화가 `[\s_\-()（）]`만 지우므로 붙인 글자는 열 인식을 깨뜨린다. 필수 표시는 색·테두리로만 한다.
- 시트명·열 구성·값 표기(코드·CSV 따옴표 규약 R-47·`▼` B-173·밀리초 **값**) — 사용자 확정 전량(B-149·B-162·B-173·B-177·B-178·7-6·B-105·B-52) 유지.
- R-49 — 행 서식은 그 행을 쓰는 자리(`finishDataRow`)에서. 시트 수준(너비·고정·필터·유효성·탭색)은 마지막에.

### 5-1. 팔레트 — 앱의 인디고에 정렬 (P-1 · P-2)

| 용도 | 현행 | 제안 | 대비(글자:바탕) |
|---|---|---|---|
| 일반 헤더 | #000080 + 흰 굵게 | **#3949AB**(인디고 600) + 흰 굵게 | 7.7:1 ✓ |
| 필수 헤더 | #FF0000 + 흰 굵게 | **#B23B36**(벽돌 빨강) + 흰 굵게 | 5.9:1 ✓ |
| 읽기전용 헤더 | GREY_50 + 흰 굵게 | **#757F8C**(슬레이트) + 흰 굵게 | 4.6:1 ✓ |
| 읽기전용 셀 | #969696 글자 / #C0C0C0 바탕 (**1.6:1**) | **#4B5563 글자 / #EFF1F4 바탕** | **6.6:1** ✓ |

필수를 빨강 계열에 남긴 것은 의도다 — 사용 안내와 사용자의 기존 학습이 "빨간 헤더=필수"이고, 그 문장을 유지하면서 채도만 정제하면 WD-1 문구 수정과도 충돌하지 않는다. (남색 통일 + 액센트 밑줄 대안은 Q-2.)

읽기전용을 "흐린 글자"에서 **"옅은 바탕 + 또렷한 글자"**로 뒤집는 것이 P-2의 요점이다: "손대지 말라"는 신호는 바탕색·헤더·안내가 이미 세 겹으로 주고 있으므로, 글자까지 흐릴 이유가 없다. '전체 캐릭터' 시트가 이 한 줄로 읽히는 시트가 된다(렌더 대조 확인).

### 5-2. 데이터 행 기본 서식 (P-3 · P-9)

- **장문 열에 `wrapText` + 세로 상단 정렬**: 설명(세계관·작품 15000) · 메모(10000) · 사건 설명(15000) · 필드설명(12000). 개행이 살아나고 둘째 문단이 보인다(렌더 확인).
- **구현 노트 — 엑셀은 로드 시 행 높이를 재계산하지 않는다.** LibreOffice·구글시트는 자동이지만 데스크톱 엑셀은 저장된 높이(기본 15pt)로 그려 첫 줄만 보인다. 따라서 wrap 열이 있는 행은 **쓰는 자리에서** 줄수를 추정(개행 수 + ⌈글자수/열폭⌉, 상한 4줄쯤)해 `setHeightInPoints`를 함께 기록한다 — R-49와 같은 자리라 스트리밍에 안전하다. 실엑셀 확인 항목(5-6).
- 헤더 행 높이 24pt — 자동 필터 화살표가 글자를 덮지 않게(P-9).
- (선택) 짝수 행 밴딩 #F6F8FB — 넓은 시트에서 행 추적을 돕는다. 비용: 지금 무스타일인 편집 셀 전부에 스타일 참조가 붙어 수만 행 시트의 XML이 커진다(셀당 ~7B, '대결 기록' 5만 행 × 10열 ≈ 3.5MB 비압축). 켤지, 큰 시트만 끌지는 Q-3. *(→ 시행 후 정정: 이 비용 산정은 무스타일 셀 전제였고, 바로 위 P-3의 세로 상단 정렬이 전 데이터 셀에 스타일을 입혀 그 참조 비용은 밴딩과 무관하게 든다 — 8-1 ③)*

### 5-3. 밀리초 서식 (P-4 — B-222 ① 흡수)

값은 그대로 두고 숫자 서식 `0`만 입힌다 — 좁은 열에서도 과학표기(1.72E+12)로 무너지지 않고, 왕복은 값 불변이라 무해하다. 사람이 읽는 날짜를 원하면 그것은 서식이 아니라 열 추가라 별도 판정 사안이다(이번 제안에 없음).

### 5-4. 틀 고정 (P-5)

시트별 고정 열을 spec에 한 칸 더한다: 캐릭터류 `(1,1)`(이름), '전체 캐릭터'·'필드 정의' `(3,1)`(세계관~이름/필드명), 나머지 `(0,1)` 유지. `freezeAndFilter(lastCol, dataRowCount)`에 인자 하나 느는 크기의 변경이고 스트리밍 실측표(고정 창 ✓)가 이미 있다.

### 5-5. 숫자 셀 전환 (P-6 — N-1의 처방, **Q-1 판정 뒤에만**)

NUMBER 필드값과 CALCULATED 표시값 중 **숫자로 파싱되고, 숫자를 도로 문자열로 만들었을 때 저장 표기와 정확히 일치하는 값만** 숫자 셀로 쓴다. 이 단서가 왕복 멱등성(무편집 왕복에서 값이 변하면 결함)의 방어선이다: `"24"`→24→`"24"` ✓ 전환, `"24.50"`→24.5→`"24.5"` ✗ 텍스트 유지. CALCULATED는 가져오기가 읽지 않는 열이라(F4) 자유도가 더 크다. 가져오기는 이미 숫자 셀을 관대하게 읽지만("숫자/문자 혼합 인식") 셀 타입은 교환 형식의 일부이므로 Q-1로 묻는다.

### 5-6. 안내 시트 개조 (P-7) · 탭 색 (P-8)

- **P-7**: 색상 범례를 실제 헤더 스타일을 입힌 견본 셀 3개로(렌더 확인 — 설명이 필요 없어진다). 섹션 행에 옅은 인디고 밴드, 본문은 B열 하나(26000)로 통일하고 A열은 좁은 여백/견본 열로. 제목 16pt. 이 시트는 가져오기가 무시하므로(GUIDE_SHEET_NAME) 왕복 무해 — 시각 개선의 가장 안전한 자리다. '주의사항' 절은 색상 안내 바로 아래로 올린다(WD-8 잔여 보강과 같은 편집 기회에).
- **P-8**: 탭 색 5그룹(안내=진남 · 구조=인디고 · 캐릭터=틸 · 기록=주황 · 파생/설정=슬레이트). 구현 노트: `SXSSFSheet`에는 `setTabColor`가 없어 `wb.getXSSFWorkbook().getSheet(name)`로 내려가 건다 — 시트 수준 속성이라 흘려보낸 행과 무관하지만, **패리티 테스트에 탭 색 비교를 한 줄 추가**해 두 구현이 같은 파일을 낸다는 전제를 잠근 뒤에 넣는다.
- 커스텀 필드 열 너비(V-8): 타입별 기본값 — MULTI_TEXT·장문 TEXT 8000, SELECT·GRADE 4500, 그 외 5000. `ColumnSpec` 기본값 자리 하나로 끝난다.

### 5-7. 구현 가드레일 요약

1. 스타일은 `ExcelStyles`에서 **재사용**한다(셀마다 `createCellStyle` 금지 — 워크북 스타일 64k 한도).
2. 행 서식·행 높이는 `finishDataRow` 자리에서(R-49 · `tools/check_export_row_finish.sh` 통과 유지).
3. 헤더 문자열·시트명·값 불변(5-0).
4. 검증: `ExportWorkbookParityTest`에 새 서식(색·wrap·높이·탭색·숫자 셀) 비교 확장 → JVM 테스트 → **실엑셀·실기기 확인**(3-121에 "ㅅ. 서식: 데스크톱 엑셀에서 wrap 행 높이·탭 색·틀 고정이 보이는가" 추가 제안). 화면 파일 0인 순수 계층 변경이라 CI는 몰아서 기준을 따른다.

## 6. 판정 대기 → **전부 확정됐다** (2026.08.14, 11차 확정)

> 아래 표는 물었던 원문 그대로다. **답은 넷 다 권고안(†)** 이되 *"중요한 건 그게 최고냐"*는
> 재검토 위임이 붙어, 시행 형태가 권고에서 상향된 자리가 있다 — 정본은
> `judgment_confirmations_2026-08.md` **16장**, 요약은 이 문서 **8장**.

| # | 질문 | 선택지 (†=권고) |
|---|---|---|
| **Q-1** | NUMBER·CALCULATED 값을 조건부 숫자 셀로 바꾸는가? (셀 타입은 교환 형식 — 멱등 단서는 5-5) | ⓐ† 멱등 보장 조건부 전환 ⓑ 전부 텍스트 유지(현행) |
| **Q-2** | 필수 헤더 색 | ⓐ† 벽돌 빨강 #B23B36 (기존 학습·안내 문구 유지) ⓑ 남색 통일 + 굵은 액센트 밑줄 (빨강=오류 의미론 완전 회피, 단 안내 문구·학습 갱신 필요) |
| **Q-3** | 짝수 행 밴딩 | ⓐ 전 시트 ⓑ† 행 1만 미만 시트만 ⓒ 안 함 |
| **Q-4** | 연표의 '세계관' 열이 코드·생성일 **뒤**에 있다(하위 호환 append 흔적 — 편집 열이 회색 지대 너머에 고립). 헤더 매칭이라 위치 이동은 안전하지만 열 순서도 형식이다. 앞(관련 작품 옆)으로 옮기는가? | ⓐ 옮긴다 ⓑ† 현행 유지(다음 형식 변경 때 같이) |

## 7. 백로그 연계 (등재는 처분 뒤)

- **B-222**(워크북 디자인 소품)의 ①밀리초 서식은 P-4가 그대로 흡수한다. 이 문서 전체를 B-222의 확장으로 묶거나, 시각 개편을 신규 항목으로 세우는 것은 사용자 처분.
- **B-221**(드롭다운 255자 한도)은 시각이 아니라 유효성 결함이므로 여기 다루지 않았다 — 다만 P-8류 작업과 같은 파일을 만지므로 같은 세션에 묶기 좋다.
- WD-8 잔여(안내 시트 보강)는 P-7과 같은 자리다.

## 8. 시행 기록 (2026.08.14 — v1.1)

P-1~P-9 전부 시행됐다(`SheetSpec.kt`·`ExcelExporter.kt` 둘 — 마이그레이션 없음 · 화면 파일 0).
**"최고냐" 재검토가 권고에서 바꾼 것 셋:**

1. **Q-1 상향 — CALCULATED는 조건 없이 숫자다.** 5-5의 멱등 단서대로면 소수 표시가 전부
   `%.2f`("78.50")라 **소수 전체가 탈락해 처방이 반쪽**이 된다. 가져오기가 이 열을 읽지 않으므로
   (F4) 멱등성이 애초에 걸리지 않는다 — 파싱되면 숫자 셀 + 소수는 서식 `0.00`으로 앱 표시와
   글자까지 맞췄다. NUMBER는 5-5 그대로(멱등 조건부)이되 **BIRTH_DATE 시맨틱 필드를 제외**했다:
   가져오기가 그 열만 `dateHint=true`로 읽어, 서식 없는 정수 숫자 셀(1~2958465)을 날짜로
   해석한다(`ExcelCellValue.isLikelyDate` — 구현 중 실측). 판정 소스는 가져오기와 같은
   `SemanticRole.fromConfig` 하나다.
2. **철회 — 텍스트 잔존 값의 우측 정렬.** 숫자/텍스트가 정렬로 갈리는 엑셀 기본 동작이
   "이 값은 전환되지 않았다"(예: "24.50")는 진단 신호라, 강제로 맞추면 신호를 지운다.
3. **Q-3 보강 — 읽기전용의 홀짝 음영.** 밴딩 행(#F6F8FB)에서 읽기전용 바탕(#EFF1F4)이 묻히므로
   밴딩 행의 읽기전용은 반 단계 진한 #E9ECF1로 갈랐다.

구현 가드레일(5-0·5-7)은 전부 지켜졌고 기계가 잠근다: R-49(`check_export_row_finish.sh` 통과 —
행 서식·행 높이 전부 `finishDataRow` 자리), 헤더 문자열·시트명·값 동결(diff에 헤더 리터럴 변경
0), 스타일 재사용(워크북당 고정 세트), 탭 색 패리티(`ExportWorkbookParityTest` 신설 시험 —
SXSSF는 XSSF 레이어로 걸므로 이 패리티가 유일한 방어), 숫자 셀 왕복 성질
(`ExportPresentationSpecTest` — 수용값을 가져오기 정규화에 실제로 돌려 원문 대조).
검증: JVM 2876→2888(신규 12) · 정적 33종 · 프로브 480→480 신규 0 · 차분 신규 전부 노이즈
부류(triage 통과). **실기기·실엑셀 확인은 `remaining_work` 3-121 ㅅ** — 특히 wrap 행 높이는
데스크톱 엑셀에서만 검증된다(LibreOffice·구글시트는 재계산해 가려진다). CI는 PR #304.

**Q-3 캐비앳(재확인):** 밴딩은 rowNum 기반이라 사용자가 엑셀에서 재정렬하면 무늬가 섞인다 —
값에는 무해하고, 동적 밴딩(엑셀 표)은 자동 필터·교환 형식과 충돌해 채택하지 않았다.

### 8-1. 후속 콜드 검토 (2026.08.14, 병합 뒤 리뷰 세션 — v1.2)

병합된 시행(PR #304)을 콜드 리뷰(사용자 지시: *"최근 업데이트가 올바르게 됐는지 검토해줘"* →
*"일괄수정해줘"*)가 전수 재검증하고 다섯 자리를 바로잡았다. 재검증 자체는 전부 초록이었다
(기준선 2888 재현 · 정적 33종 · 하네스 16종 · PR #304 CI가 병합 전 초록이었음 · 왕복 멱등의
거울 대조). **발견·수정 다섯은 전부 값·왕복 무해 — 시각·문서·성능 층이다:**

1. **'전체 캐릭터'의 CALCULATED 소수 서식** — 스타일 선택에서 읽기전용 분기가 소수 서식(`0.00`)
   분기보다 앞서, 전 열이 읽기전용인 이 시트만 "78.5"로 보였다(캐릭터 시트·앱은 "78.50").
   분기가 사적 클래스 안이라 어느 시험도 못 닿던 자리다 — 종류 판정을 순수 함수
   `cellStyleKindFor`(SheetSpec.kt)로 갈라 **읽기전용이 서식(밀리초 `0`·소수 `0.00`)을 가리지
   않게** 했고, 우선순위는 `ExportPresentationSpecTest`(+2)가 잠근다.
2. **밴딩의 시트 단위 판정 통일** — 다섯 시트(등급 체계·필드 데이터·대결 기록·대결 상성·앱 설정)가
   행 번호(`rowIndex < 1만`)로 판정해 1만 행 초과 시트에서 **첫 1만 행만 무늬**가 됐다. Q-3 ⓑ의
   문구(*"행 1만 미만 시트만"*)대로 총 행 수를 먼저 재서 시트 단위로 켠다 — 대결 둘은 축별
   목록을 미리 모았다(질의·행 순서 무변경).
3. **5-2 비용 산정의 전제 정정** — P-3(전 데이터 셀 세로 상단)로 모든 데이터 셀이 스타일 참조를
   가지므로 **밴딩 자체의 파일 크기 비용은 없다.** 상한(`BANDING_ROW_LIMIT`)이 지키는 것은
   크기가 아니라 무늬의 일관성이다(5-2 본문에 정정 표식 · KDoc 정정).
4. **죽은 참조 둘** — `SheetSpec.kt` KDoc의 `SheetTabColorsTest`는 존재하지 않는 이름이었다
   (실체는 `ExportPresentationSpecTest`의 전수 분할 시험). 아키텍처 지도 R-49 행의
   `finishDataRow` 예시 시그니처도 2-인자로 낡아 있었다.
5. **NUMBER 판정 캐시** — BIRTH_DATE 판정(`SemanticRole.fromConfig`)이 NUMBER 셀마다 config
   JSON을 파싱했다(행×필드 — 통계가 S6 5차에 걷어낸 그 모양). config 원문을 키로 캐시해 한
   번만 파싱한다(키가 원문이라 값이 낡을 수 없다).

교환 형식·사용자 확정은 건드리지 않았다 — 수정은 전부 **확정의 문구 쪽으로 코드를 붙인 것**이다
(①은 Q-1 시행 형태의 "앱 표시와 글자까지", ②는 Q-3 ⓑ의 "행 1만 미만 시트만").
검증: JVM 2888→2890 · 정적 33종 · 실기기 항목은 3-121 ㅅ에 '전체 캐릭터' 소수 표기 확인이 더해졌다.

## 9. P-10 — 글꼴 (2026.08.17, v1.3)

**사용자 지시:** *"생성하는 엑셀파일의 폰트를 고딕으로 바꿔줄 수 있나?"*

### 9-1. 이 문서가 열어 둔 자리였다

1장이 *"색·폰트·열 너비·틀 고정·정렬·셀 서식은 어느 문서에도 결정이 없다"*고 적어 놓고
**P-1~P-9는 폰트만 빼고 나머지를 정했다.** V-1~V-10에도 글꼴 항목이 없다 — 렌더 검증을
LibreOffice로 했는데 **글꼴 부재는 렌더에서 드러나지 않는다**(뷰어가 대체해 그려 주므로
"한글이 잘 보인다"로 읽힌다). 그래서 이 축만 남았다.

### 9-2. 무엇이 문제였나 — 한 셀 안에서 글꼴이 둘이었다

지금까지 워크북에는 글꼴 이름이 **한 번도 지정된 적이 없다.** POI 기본값은 `Calibri`이고,
Calibri에는 **한글 글자체가 없다.** 그래서 뷰어가 글자마다 대체를 건다:

- **한글** → 뷰어가 고른 대체 글꼴(윈도우 엑셀이면 대개 맑은 고딕)
- **숫자·영문·코드** → Calibri

즉 `코드 C-a3f9` 같은 칸과 이름 칸이 **서로 다른 글꼴로** 그려졌고, 대체 결과가 기기마다
달라 파일의 모양이 고정되지 않았다. 이름을 못박으면 그 섞임과 흔들림이 함께 없어진다.

### 9-3. 처방 — `맑은 고딕`, 두 자리

`EXPORT_FONT_NAME`(SheetSpec.kt) 하나가 이름을 들고, 워크북에 거는 자리는 **둘**이다.
**한쪽만으로는 덮이지 않는다** — 이 판의 요점이 그 사실이다:

| 자리 | 무엇을 덮는가 | 왜 필요한가 |
|---|---|---|
| `applyExportBaseFont` (폰트 0번을 제자리에서) | 스타일이 아예 없는 셀 · **폰트를 걸지 않은 스타일**의 셀 | `dataStyle`의 비-읽기전용 갈래와 `guideBody`는 `setFont`를 부르지 않는다 — **평범한 데이터 셀 대부분이 이쪽**이다 |
| `createExportFont` (새 폰트마다) | 헤더 3종 · 읽기전용 글자 · 안내 제목·섹션 | POI의 `createFont()`는 **매번 새 폰트를 Calibri로 세워 돌려준다** — 기본 폰트를 고쳐도 미치지 않는다 |

`ExcelStyles`가 앱 전체에서 폰트·스타일을 만드는 유일한 자리라는 것은 실측으로 확인했다
(`createFont`·`createCellStyle` 전수 검색 — 다른 파일 0건).

### 9-4. 실측이 근거다 (POI 5.3.0, 앱과 같은 판)

코드만 읽고 "괜찮을 것"이라 적지 않았다(1장의 방법). 프로브로 잰 것 넷, **DOM·스트리밍 양쪽에서 같은 답**:

1. `createFont()`의 기본 이름은 정말 `Calibri`다 → 두 자리가 필요한 이유.
2. 폰트 0번의 **제자리 수정이 파일 왕복에서 살아남는다**.
3. 스타일 없는 셀과 폰트 없는 스타일의 셀이 **둘 다 fontIndex=0**으로 떨어진다.
4. 두 구현의 답이 같다 — `ExportWorkbooks` 머리의 전제를 깨지 않는다.

### 9-5. 왕복에는 관여하지 않는다 — **그러나 레이아웃은 이 값에 묶여 있다**

가져오기는 **셀의 값만** 읽는다. 스타일에서 들여다보는 것은 `ExcelCellValue.Primitives`의
`dataFormatString`(표시 서식) 하나뿐이고 **글꼴 이름은 그 접근면에 아예 없다.** 그래서
5-0의 동결 목록(헤더 문자열·시트명·값 표기)을 건드리지 않는다 — 교환 형식·왕복 멱등성
무변경, 마이그레이션 없음, 화면 파일 0.

> ⚠️ **콜드 검토 정정(2026.08.17).** 이 절의 첫 원고는 *"스타일 레이어에서만 끝난다"*로
> 맺었는데 **절반만 맞다.** 값과 왕복은 그대로지만 **레이아웃의 물리적 결과는 함께 움직인다** —
> OOXML에서 **열 너비의 단위가 '기본 글꼴의 최대 숫자 폭'**이고 행 높이는 그리는 글꼴에
> 견주는 값이기 때문이다. **저장된 숫자는 그대로인 채 그 숫자의 뜻이 바뀐다.**
>
> **실측(POI 5.3.0):** `setColumnWidth(5000)`은 글꼴과 무관하게 **5000 그대로 저장되고**
> `defaultRowHeightInPoints`도 **15.0 그대로**다. POI는 환산해 주지 않는다 — 환산은 여는 쪽이
> 하고, **그 기준이 방금 바뀌었다.**
>
> 걸리는 자리 둘: **⑴ `ColumnSpec.width`의 손튜닝 값들**(이름 6000·메모 10000·설명 15000 —
> V-8이 타입별로 갈라 둔 그 값들이 전부 Calibri 기준으로 정해졌다) · **⑵ `finishDataRow`의
> `lines * defaultRowHeightInPoints`**(15pt/줄). ⑵가 모자라면 **wrap 여러 줄이 다시 잘리는데,
> 그것이 V-4가 고친 바로 그 결함이다.**
>
> **다만 한글 몫은 대체로 그대로다** — 한글 글자는 이 변경 전에도 Calibri에 없어 *뷰어가 고른
> 한글 글꼴*이 그렸다(9-2). 즉 한글의 글자 높이는 이미 한글 글꼴의 것이었고, **이 판이 새로
> 움직이는 것은 숫자·영문 쪽 기준**이다. 열 너비 단위가 하필 **최대 *숫자* 폭**이라 그 축이
> 직접 걸린다.
>
> **크기는 로컬에서 원리적으로 못 잰다** — 글꼴 파일이 있어야 metric이 나온다. 그래서
> 처방(너비 재조정·기본 행 높이 상향)을 **지금 정하지 않는다**: 재지 않고 숫자를 고르는 것이
> 이 저장소가 금지한 형태다(체크리스트 6번). **실기기 확인 3-121 ㅇ가 그 답을 가져온다** —
> 거기서 *헤더가 잘리는가 · wrap 여러 줄이 여전히 보이는가*를 보고, 어긋나면 그때 백로그를 연다.
>
> **이 정정 자체가 이 판의 교훈이다** — *"가져오기가 안 읽는다"*(참)에서 *"아무것도 안 바뀐다"*
> (거짓)로 미끄러지는 것이 쉬웠다. **읽지 않는 것과 영향이 없는 것은 다르다.**

### 9-6. 남는 것 — 없는 글꼴은 뷰어가 대체한다

맥·안드로이드·구글시트에는 맑은 고딕이 없어 뷰어가 대체한다. **그것은 이 변경이 새로
들여오는 위험이 아니다** — 9-2대로 지금도 일어나는 일이고, 오히려 대체의 출발점이
Calibri(한글 없음)에서 한글 고딕으로 바뀐다. 다른 고딕(돋움·굴림 등)으로 바꾸려면
`EXPORT_FONT_NAME` **한 줄**만 고치면 되고, `check_export_font.sh`가 그 한 줄 밖에 사본이
생기지 않게 지킨다.

### 9-7. 검증

- **JVM 3101 → 3103**(신규 2 — `ExportPresentationSpecTest`). 시험이 **재현이 아니라 호출**이다:
  `ExcelStyles`가 private이라 닿지 않는데 규칙을 베껴 적으면 그 사본이 곧 두 벌이 되므로,
  두 함수를 실제로 부르고 **파일에 써서 되읽어** 세 모양(스타일 없음·폰트 없는 스타일·명시 폰트)을 잰다.
- **되돌려 빨간불 확인**: 두 자리를 각각 무력화해 해당 칸만 `Calibri`로 뜨는 것을 실제로 봤다.
- **`tools/check_export_font.sh` 신설** — 시험은 *함수*를 잠글 뿐 **`ExcelStyles`가 그것을 쓰는지**는
  못 본다. 그 자리가 이 저장소가 반복해 겪은 부류라(R-29·B-147 — 규약으로만 적으면 반드시 빠진다)
  기계를 세웠다: ① 직접 `createFont()` 금지 ② 기본 폰트를 못박는가 ③ 이름이 생문자열로
  두 벌이 되지 않는가. **세 축 전부 깨뜨려 exit 1을 확인했다.** 주석 줄은 일부러 뺀다 —
  규칙을 설명하는 주석이 위반으로 잡히면 다음 사람이 **설명을 지워** 검사를 통과시킨다.
- 정적 검사 **43종 전종 통과** · 실클래스패스 프로브 신규 **0**(base 1966 = cur 1966) ·
  `ExcelExporter.kt`가 정말 검사되는지 **일부러 오타를 내어** 확인(1966 → **1967**, 그 오타가
  신규 줄로 떴다 — B-190이 *"목록에 있지만 실제로 검사되지 않는다"*고 적어 둔 파일이라
  근거를 따로 세웠다) · 마이그레이션 없음.
- ⚠️ **실기기·실엑셀 확인은 남아 있다**(착수 규칙 4번 — 자동 검증은 보이는 것을 못 본다).
  `remaining_work` **3-121 ㅇ**에 항목을 세웠다. LibreOffice 렌더로는 원리적으로 못 재는 축이다(9-1).

### 9-8. 콜드 검토 (병합 전) — 셋을 잡았고 **셋 다 이 판의 산출물이다**

이 저장소가 최근 판마다 겪은 그 모양이 또 나왔다(v10.58·v10.62가 같다).

1. **가장 나쁜 것 — 9-5의 결론이 과장이었다.** 첫 원고가 *"스타일 레이어에서만 끝난다"*로
   맺었는데, **값과 왕복은 그대로지만 레이아웃의 물리적 결과는 함께 움직인다.** OOXML에서
   열 너비의 단위가 *기본 글꼴의 최대 숫자 폭*이라 **저장된 숫자는 그대로인 채 뜻이 바뀐다.**
   실측으로 확인했다(POI는 환산해 주지 않는다 — 5000은 5000, 기본 행 높이는 15.0 그대로).
   **미끄러진 경로가 뚜렷하다: *"가져오기가 안 읽는다"*(참) → *"아무것도 안 바뀐다"*(거짓).
   읽지 않는 것과 영향이 없는 것은 다르다.** 처방은 정하지 않고 실기기(3-121 ㅇ-1·ㅇ-2)로
   넘겼다 — 글꼴 metric은 글꼴 파일이 있어야 나오고, **재지 않고 숫자를 고르는 것**이
   체크리스트 6번이 금지한 형태다.
2. **검사가 시험 소스를 안 봤다.** `check_export_font.sh` ③이 `app/src/main`만 훑었는데,
   **시험이 글꼴 이름을 베껴 적으면 이름을 바꾸는 날 그 시험이 *옛 글자를 지키는 시험*으로
   뒤집힌다** — 이 저장소가 헤더 접미사(B-222 ②)에서 이미 겪고 *"베껴 적지 말고 조립해서
   견주라"*고 적어 둔 그 모양이다. 시험 소스를 범위에 넣고 **심어서 확인했다.**
   함께 `grep -F`로 바꿨다 — 글꼴 이름은 사람이 고치는 값이라 언젠가 정규식 특수문자를
   품을 수 있고, 그때 검사가 **엉뚱한 줄을 잡거나 놓친다.**
3. **시험 이름이 시험보다 많이 말했다.** *"…셀이 POI 기본값을 쓴다"*인데 **셀을 만들지
   않는다**(폰트 객체만 잰다). 이름을 실제로 재는 것에 맞췄다.

재검증은 전부 초록이었다(시험 3103 재현 · 검사 43종 · 프로브 신규 0).

### 9-9. CI (PR #333, run `32016263130`, head `a47d357`) — 초록

**배지가 아니라 단계를 봤다:** `Static checks` ✓ **38초** · `Schema harnesses` ✓ ·
**`Run unit tests` ✓ 3분 23초** · **`Build Debug APK` ✓ 55초** · `Upload/Commit Room schemas` ✓ ·
`Upload Debug APK` ✓ · `Build Summary` ✓ (Release 둘의 skipped는 조건부라 정상). 전체 **5분 21초**.

**`Commit Room schemas`가 아무것도 얹지 않았다** — 원격 머리가 `a47d357` 그대로인 것을 실측했다
(*마이그레이션 없음 · 스키마 그대로*를 기계가 확인해 준 자리다).

**이 실행이 새로 준 것을 정직하게 적으면 작다.** 바꾼 코틀린 둘이 전부 실클래스패스 프로브
범위 안이라 타입은 로컬에서 이미 갈렸다. **CI가 새로 준 것은 둘이다** — Room 처리기·리소스
링크를 실제로 돌린 것, 그리고 **`Static checks`가 신규 `check_export_font.sh`를 CI 환경에서
처음 돌린 것**(그 단계는 `tools/check_*.sh`를 통째로 도는 루프라 새 검사가 자동으로 실린다.
실행 권한 `100755`이 커밋됐는지도 병합 전에 확인했다 — 안 붙었으면 그 자리에서 죽는다).

## 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.3 | 2026.08.17 | **9장 신설 — P-10 글꼴 + 9-8 콜드 검토(병합 전, 셋 다 이 판의 산출물)**(사용자 지시 *"생성하는 엑셀파일의 폰트를 고딕으로 바꿔줄 수 있나?"*). **이 문서가 스스로 열어 둔 마지막 축이다** — 1장이 *"폰트는 어느 문서에도 결정이 없다"*고 적어 두고 P-1~P-9가 나머지만 정했고, V 진단에도 없었다(**렌더 검증으로는 원리적으로 안 잡힌다** — 뷰어가 대체해 그려 주므로 "한글이 잘 보인다"로 읽힌다). 실제 상태는 **글꼴 미지정 = POI 기본값 Calibri**였고, Calibri에 한글 글자체가 없어 **한 셀 안에서 한글과 숫자·영문이 다른 글꼴로** 그려지고 있었다(대체 결과는 기기마다 다르다). `EXPORT_FONT_NAME = "맑은 고딕"` 한 자리로 못박되 **거는 자리는 둘이다** — 폰트 0번 제자리 수정(스타일 없는 셀·**폰트를 안 건 스타일**의 셀 = 데이터 셀 대부분)과 `createExportFont`(POI의 `createFont()`가 매번 Calibri로 세워 돌려주므로 기본 폰트가 미치지 않는다). **넷을 실측했다**(POI 5.3.0, DOM·스트리밍 같은 답). 가져오기는 글꼴을 읽지 않아(`Primitives`에 접근면 자체가 없다) **교환 형식·왕복 무변경**. JVM 3101→3103 · 정적 43종 · 되돌려 빨간불 확인 · `check_export_font.sh` 신설(세 축 전부 깨뜨려 확인). **9-8 콜드 검토가 셋을 잡았고 셋 다 이 판의 산출물이다** — ⓐ **9-5의 결론이 과장이었다**(*"스타일 레이어에서만 끝난다"* → 값·왕복은 그대로지만 **레이아웃의 물리적 결과는 함께 움직인다**: OOXML 열 너비의 단위가 *기본 글꼴의 최대 숫자 폭*이라 **저장된 숫자는 그대로인 채 뜻이 바뀐다**. 실측으로 POI가 환산해 주지 않음을 확인했고, 처방은 정하지 않고 실기기 **3-121 ㅇ-1·ㅇ-2**로 넘겼다 — 재지 않고 숫자를 고르는 것이 체크리스트 6번의 금지 형태다. **미끄러진 경로가 뚜렷하다: *"가져오기가 안 읽는다"*(참) → *"아무것도 안 바뀐다"*(거짓)**) ⓑ 새 검사가 **시험 소스를 안 봤다**(시험이 이름을 베껴 적으면 개명하는 날 *옛 글자를 지키는 시험*으로 뒤집힌다 — B-222 ②에서 겪은 그 모양. 범위에 넣고 심어서 확인 + `grep -F`) ⓒ 시험 이름이 시험보다 많이 말했다(셀을 안 만드는데 *"셀이 …"*). **실기기 확인 미완** |
| v1.2 | 2026.08.14 | **8-1 신설 — 병합 뒤 콜드 검토의 재검증(전부 초록)과 수정 다섯**(사용자 지시 *"검토해줘"* → *"일괄수정해줘"*). '전체 캐릭터' CALCULATED 소수 `0.00`(읽기전용이 서식을 가렸다 — `cellStyleKindFor` 순수 판정으로 갈라 시험이 잠금) · 밴딩 시트 단위 통일(다섯 시트가 행 단위였다) · 5-2 비용 전제 정정(P-3가 전 셀에 스타일을 입혀 밴딩 비용은 0) · 죽은 참조 둘 · BIRTH_DATE 판정 캐시. JVM 2888→2890 |
| v1.1 | 2026.08.14 | **Q-1~Q-4 확정(11차) 반영 + 시행 기록(8장) 신설** — 상태를 '처분 대기'에서 '확정·시행 완료'로. 재검토 상향 둘(CALCULATED 상시 숫자+`0.00` · 읽기전용 홀짝 음영)과 철회 하나(텍스트 우측 정렬)를 근거와 함께 기록. 검증 수치와 실기기 항목(3-121 ㅅ) 연결 |
| v1.0 | 2026.08.14 | 최초 작성 — 사용자 지시로 시각 축 리뷰(V-1~V-10), 완전성 점검(신규 N-1 · 의도된 제외 표), 렌더 검증 개선안(P-1~P-9)과 판정 대기 4건(Q-1~Q-4) 수록. 재현·검증 방법은 1장. 구현·등재는 하지 않았다(리뷰 지시) |
