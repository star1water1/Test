# 캐릭터 관리 앱 — 개발 지침서

## 문서 목적

이 문서는 캐릭터 관리 앱의 개발 방향과 의사결정 기준을 명문화한 지침서다. 기능 설계, 구현, 리팩토링 등 모든 개발 단계에서 일관성을 유지하기 위해 작성되었으며, 모든 기능 구현 시 이 문서의 원칙을 우선적으로 참조해야 한다.

## 어디서부터 읽는가

| 목적 | 문서 |
|------|------|
| **판단 기준** — 모든 설계·구현·리뷰의 최상위 | **이 문서** |
| **지금 할 일 · 착수 절차 · 재현 기준선 · 살아 있는 백로그** | **`docs/status.md`** — 500줄을 넘기지 않는 단일 진입점 |
| **실기기 확인** — 8회차로 접은 워크스루 | **`docs/device_walkthrough.md`** |
| 규약 원문 — 착수 전 **손댈 영역만** 읽는다 | `docs/conventions.md` |
| 지형 파악 — 계층·데이터 모델·단일 소스·확장점 | `docs/architecture_2026-07.md` |
| 확장 한계·성능 — 목표 규모, 무엇이 먼저 무너지는가 | `docs/scalability_performance_2026-07.md` |
| 사용자가 확정한 것 — **다시 묻지 않는다** | `docs/judgment_confirmations_2026-08.md` |
| 이미지 방향(EXIF)·인앱 회전 | `docs/image_orientation_2026-08.md` |
| 백로그 본문 · 실기기 절차 원문 (닫힌 문서) | `docs/remaining_work_2026-07.md` |
| 2026.08.19 이전의 경위 (닫힌 문서들) | `docs/remaining_work_2026-07.md` 「문서 이력」 · `docs/session_log_2026-07.md` · `docs/session_roadmap_2026-08.md` |

> **2026.08.19에 지도를 줄였다.** 종전 지도는 아홉 행이었고 그중 셋이 *"지금 할 일"*을
> 서로 다르게 가리켰다(5장의 🧭 · 로드맵 · 판). 그 셋이 가리키던 문서가 **21,691줄**로 자라자
> **자기가 든 부채를 스스로 은폐했다** — 인수인계 판이 실기기 대기를 *"열다섯"*이라 적는 동안
> 실측은 **124건**이었고 마지막 통과는 **2026.08.01**, 그 사이 **52판**이 지나 있었다.
> **진입점은 하나이고 500줄을 넘지 않는다.**

## 핵심 철학

이 앱은 **"도구를 제공하는 앱"이 아니라 "도구를 만들 수 있는 도구를 제공하는 앱"**이다. 사용자가 앱의 틀 안에 맞추는 것이 아니라, 앱이 사용자의 필요에 맞춘다. 엑셀에서 계산식을 자유롭게 지정하듯, 각 필드의 속성과 동작을 사용자가 정의할 수 있어야 한다. 하지만 단지 사용자가 설정한 것만을 하는 것이 아니라 거기에 더한 편의성과 기능성은 최대한으로 제공한다.

> "개발자가 예상한 범위 안에서만 쓰는 앱이 아니라, 내가 필요한 대로 구조 자체를 정의하고 그 위에서 실질적인 인사이트를 뽑아낼 수 있는 앱"

---

## 개발 의도 — 최상위 판단 기준

모든 기능의 설계·구현·리뷰·우선순위 판단 시 아래 개발 의도를 판단 기준으로 삼는다. 5대 개발 원칙은 이 의도를 실현하기 위한 수단이다.

> "창작자를 위한 무한한 확장성(단, 이 무한을 받쳐줄 수 있어야 함)과 변수 제어(잘못된 입력을 바로잡는) 능력을 지닌 앱. 자율성을 우선시하므로 기능의 쓸모는 사용자가 가리도록 한다. 즉 개발 단계에서는 일단 이것도 저것도 가능하게끔, 다만 실사용 시 원하는 대로 편하게 설정 가능하게끔. 복잡하고 귀찮은 작업들을 인앱 기능으로 도울 수 있고, 핵심 기능 중 하나가 바로 엑셀 파일 아웃풋·인풋의 자유로움, 그리고 엑셀 파일 내보내기·들이기 간의 오류 없고 유연한 처리. 한마디로 **창작자를 위한 맥가이버 칼 같은 프로그램**."

이 의도를 실무 판단에 쓸 때는 다음 4가지 질문으로 검증한다:

1. **받쳐주는 확장성** — 확장은 무한히 열되, 그 무한이 실제로 동작해야 한다. 커스텀 필드 수십 개, 캐릭터 수백 명, 대형 엑셀 파일에서도 깨지지 않는가? (받쳐주지 못하는 확장은 확장이 아니라 결함이다)
2. **변수 제어** — 잘못된 입력을 조용히 버리거나 무시하지 않는다. 검증 → 사용자에게 알림 → 바로잡을 경로 제공의 순서를 지키는가? 어떤 경우에도 사용자의 데이터가 말없이 유실되지 않는가?
3. **자율성 우선** — 기능의 쓸모는 개발자가 아니라 사용자가 가린다. 가능성은 넓게 제공하되(개발 단계: 이것도 저것도 가능하게), 합리적 기본값과 설정으로 실사용은 간편하게 한다. 자율성이 조작 마찰(원칙 04)로 변질되어서는 안 된다.
4. **엑셀 왕복 무결성** — 내보내기 → 외부 편집 → 들이기 왕복에서 데이터가 유실·왜곡되지 않아야 하며, 외부에서 편집된 파일의 오류(형식 이탈, 누락, 중복)를 거부가 아니라 유연한 수용·교정으로 처리한다.

---

## 5대 개발 원칙

### 원칙 01. 열린 구조 (Open Architecture)

모든 기능은 확장 가능해야 한다. 고정된 틀, 닫힌 프리셋, 확장 불가능한 시스템은 허용되지 않는다.

- 필드는 사용자가 자유롭게 추가/삭제/편집할 수 있어야 한다.
- 프리셋과 템플릿은 불러오기뿐 아니라 추가/편집/삭제가 모두 가능해야 한다.
- 신규 필드가 자동으로 통계 시스템에 편입되고, 분석 방법도 사용자가 설정할 수 있어야 한다.
- 다른 작품에서 사용한 필드를 불러와 재사용할 수 있는 구조를 갖춰야 한다.

### 원칙 02. 실질적 기능성 (Substantive Functionality)

기능의 '존재'가 아니라 '실질적 쓰임'이 기준이다. 겉핥기식 기능은 없는 것보다 못하다.

- 통계는 '데이터 입력량'이 아니라 '분석적 인사이트'를 제공해야 한다. 성별 비율, 특성 분포, 거주지 분포, 사건 연계율 등 실제로 작가가 캐릭터 군상을 조감하고 편향이나 패턴을 발견할 수 있는 정보를 줘야 한다.
- 사건 기능은 연표에서 사건 존재를 한눈에 파악할 수 있어야 한다.
- 모든 필드(머리카락 색, 거주지 등 세부 데이터까지)가 통계에서 분석 가능해야 한다.
- 방편식 패치(임시 땜빵)는 금지한다. 모든 기능은 완결된 형태로 구현한다.

### 원칙 03. 기능 우선, 구조 후순 (Function Over Simplicity)

구조의 간결함과 기능의 풍부함이 충돌할 때, 항상 기능을 선택한다.

- 구조가 복잡해지더라도 기능이 조금이라도 좋으면 그쪽을 채택한다.
- 기능 간소화는 절대 허용되지 않는다.
- '할 수 있는 건 다 한' 수준이 기준선이다.
- 단, UX 복잡도는 이중 경로(러프 입력 / 정밀 조정)로 해결한다.

### 원칙 04. 조작 마찰 최소화 (Minimal Friction)

기능의 깊이는 최대한 깊되, 접근과 조작은 최대한 짧아야 한다.

- 저장을 위해 버튼 여러 단계를 거치는 플로우는 금지한다.
- 러프하게 입력하고 나중에 정밀 조정하는 이중 경로를 제공한다. 예를 들어 사건 배치 시 대략적 위치만 먼저 잡고 세부 조정은 나중에 할 수 있어야 한다.
- 일일이 확인하지 않으면 존재를 알 수 없는 데이터가 있어서는 안 된다.
- 순서 편집 등 반복 작업은 드래그앤드롭 등 직관적 조작을 우선 제공한다.

### 원칙 05. 데이터 유기적 연결 (Organic Data Linkage)

작품–세계관–캐릭터–사건은 고립된 저장소가 아니라 하나의 유기적 데이터베이스처럼 작동해야 한다.

- 필드를 다른 작품에서 불러와 재사용할 수 있어야 한다.
- 세계관/작품 이미지를 캐릭터 이미지에서 자동 연동(랜덤/선택)할 수 있어야 한다.
- 작품 선택 시 관련 캐릭터가 자동 필터링되어야 한다.
- 통계가 작품별로 다른 필드 구조를 인지하고 적응적으로 분석해야 한다.

---

## 판단 기준: Do / Don't

### 필드 시스템

**Do:**
- 필드의 타입, 파싱 방식, 통계 포함 여부를 각각 설정 가능하게 구현
- 이명/별칭은 콤마로 구분하여 복수 입력 지원
- 다른 작품의 필드를 불러와 재사용할 수 있는 기능 제공
- 이름 필드를 First Name / Last Name으로 분리 가능하게 설계

**Don't:**
- 필드 기능(콤마 파싱 등)을 특정 필드에만 고정으로 부여하는 것
- 사용자가 추가한 필드가 통계에서 소외되는 구조
- 프리셋이 읽기 전용으로만 존재하는 것 (추가/편집/삭제 불가)

### 통계 시스템

**Do:**
- 모든 필드(시스템 + 사용자 추가)를 분석 대상으로 포함
- 성별 비율, 특성 분포, 거주지 분포 등 실질적 인사이트 제공
- 캐릭터별 사건 연계 비율, 관계도 비율, 데이터 양 등 개별 통계 제공
- 작품별로 다른 필드 구성을 인지하고 적응적으로 분석
- 시각화(차트, 그래프)로 직관적인 정보 전달

**Don't:**
- '데이터 입력량'만 보여주는 통계 (입력 완료율, 필드 채움 비율 등)
- 시스템 기본 필드만 분석하고 사용자 커스텀 필드는 무시하는 구조
- 시각화 없이 숫자만 나열하는 통계

### UX / 조작

**Do:**
- 순서 변경은 드래그앤드롭으로 즉시 반영
- 사건은 연표 그래프에 시각적 표시가 있어 존재 파악 가능
- 대략적 배치 → 세부 조정의 이중 경로 제공
- 작품 선택 시 관련 캐릭터 자동 필터링

**Don't:**
- 저장을 위해 버튼 여러 단계를 거쳐야 하는 플로우
- 일일이 연도를 열어봐야 사건이 있는지 알 수 있는 구조
- 숫자 직접 입력만으로 순서를 조정해야 하는 것

### 이미지 / 카드

**Do:**
- 세계관/작품 카드에 이미지 등록 가능 (직접 등록 / 캐릭터 이미지 연동 선택)
- 카드 이미지 크기를 기본 높이(설명 2줄 기준)에 맞춰 적절히 표시

**Don't:**
- 카드 이미지가 지나치게 작게 표시되는 것
- 설명 유무/길이에 따라 카드 높이가 들쭉날쭉하여 레이아웃이 불안정한 것

---

## 품질 기준

### 방편식 패치 금지

모든 기능은 완결된 형태로 구현되어야 한다. 임시 땜빵, 하드코딩된 예외 처리, 특정 케이스에만 동작하는 코드는 허용되지 않는다. 기능을 구현할 때는 항상 "이 기능이 어떤 경우에도 제대로 동작하는가?"를 기준으로 점검한다.

### 새 기능 체크리스트

새로운 기능을 추가하거나 기존 기능을 수정할 때 아래 항목을 순서대로 검증한다:

1. **확장성** — 사용자가 이 기능을 커스텀/확장할 수 있는가?
2. **실질성** — 이 기능이 실제로 쓸모 있는 정보/기능을 제공하는가?
3. **완결성** — 방편식 패치나 임시 처리가 없는가?
4. **연결성** — 기존 데이터/기능과 유기적으로 연결되는가?
5. **조작성** — 사용자가 최소 단계로 목적을 달성할 수 있는가?
6. **의도 부합** — 개발 의도(최상위 판단 기준)의 4가지 질문(받쳐주는 확장성 / 변수 제어 / 자율성 우선 / 엑셀 왕복 무결성)을 통과하는가? 이 중 **받쳐주는 확장성은 `docs/scalability_performance_2026-07.md` 7장의 4단계로 판정한다**(새 상한을 도입하는가 / 비용이 어느 축에 붙는가 / 스냅샷을 키우는가 / 엑셀 왕복을 키우는가). 판정 근거 없이 "괜찮을 것"이라고 적지 않는다.
7. **텍스트** — 화면 문구가 `docs/text_style_guide_2026-07.md`(톤·용어·목적문)를 따르는가? 새 설정 스위치·섹션에 목적문 한 줄("무엇이 어디서 어떻게")이 붙어 있는가? (`tools/check_text_style.sh` 통과 포함)

---

## 통계 시스템 설계 기준

### 통계의 목적

통계 시스템의 목적은 창작 의사결정을 보조하는 분석 도구이다. 단순히 '얼마나 입력했는가'를 보여주는 것이 아니라, '내 캐릭터 군상에 어떤 편향과 패턴이 있는가'를 발견할 수 있어야 한다.

### 필수 분석 범위

- **전체 캐릭터 비율 분석:** 성별, 나이대, 태그 분포 등
- **필드별 분포 분석:** 모든 필드(시스템 + 커스텀)의 값 분포
- **캐릭터별 개별 통계:** 사건 연계 비율, 관계도 비율, 데이터 완성도
- **작품별 비교 분석:** 작품 간 필드 구성 차이, 캐릭터 특성 비교
- **시각화:** 차트, 그래프 등으로 직관적인 정보 전달

### 적응적 통계 원칙

작품별로 필드 구성이 다를 수 있으므로, 통계 시스템은 정적으로 고정된 분석 항목이 아니라 필드 구성에 따라 동적으로 생성되는 분석을 제공해야 한다. 사용자가 새 필드를 추가하면 해당 필드가 통계에 자동으로 편입되고, 분석 방법(비율/분포/추세 등)도 사용자가 선택할 수 있어야 한다.

---

## 세션 착수 규칙

### 1. **한 판은 사용자가 앱에서 볼 수 있는 변화를 낸다** (2026.08.19 사용자 판정)

**이것이 판의 정의다.** 도구·검사·문서만 바꾸는 세션은 판이 아니라 **기능 판에 딸린
부수 작업**이다.

**왜 규칙으로 박았나 — 실측이다.** 최근 20판에서 앱 코드 4,220줄이 바뀌는 동안
도구·문서가 **6,683줄** 바뀌었고, **20판 중 6판은 앱 코드를 한 줄도 안 바꿨다.**
백로그 250건 중 **사용자가 연 것은 1건**이고 나머지 249건은 콜드 검토·점검·감사가
스스로 열었다. 그리고 그 모든 것의 뿌리에 **실기기 확인이 2026.08.01에 멈춘 것**이 있었다
(미검증 124건 · 그 사이 52판). 세션은 그 부채를 갚을 수 없으니 — 갚는 것은 사용자만
할 수 있다 — **부채를 안 만드는 일로 흘렀다.**

- **도구·검사·문서 작업은 독립 판으로 열지 않는다.** 예외는 **사용자가 지시했을 때**뿐이다.
- **검사·도구는 동결에 가깝게 다룬다.** 현재 51종·14,937줄이고, 그것들이 잡은 것의
  대부분은 *다른 검사와 도구의 결함*이었다. **새 검사는 같은 부류의 앱 결함이 실제로
  두 번 난 자리에만** 세운다.
- **발견을 버리라는 뜻은 아니다**(개발 의도 2번이 걸린다). 범위 밖의 발견은
  **커밋 메시지에 한 줄** 남기고 넘어간다 — 백로그 행으로 만드는 것은 그것이
  *다음 판의 후보*일 때뿐이다.

### 2. 완료의 정의 — **병합은 CI가, 화면은 배치 확인이**

**2026.08.19에 갈랐다.** 종전 규칙은 *"화면을 바꾸는 변경은 실기기 확인 전까지 완료로
보고하지 않는다"*였는데, 확인은 사용자만 할 수 있으므로 **그 규칙이 갚을 수 없는 부채를
만들었고 세션이 화면을 피하게 했다.** 실측: 앱 코드의 48%가 `ui/`인데 시험의 8%만 그것을 본다.

- **병합은 CI 초록으로 완료된다.** 실기기 확인은 병합을 막지 않는다.
- **실기기 확인은 `docs/device_walkthrough.md`의 회차 단위로 소화한다** — 항목 하나씩이
  아니라 앱을 한 바퀴 돌며 영역마다 *도는가*를 본다.
- **자동 검증은 "보이는 것"을 못 본다**는 사실 자체는 그대로다. 순수 JVM 시험·정적 검사·
  차분 컴파일·프로브·CI는 전부 *"컴파일되는가 / 규칙을 어겼는가"*만 본다.
  **그래서 워크스루에서 나온 결함이 어떤 백로그 항목보다 우선한다.**

### 3. 착수 — **손댈 슬라이스만 본다**

착수할 작업이 정해지면(`docs/status.md`가 정한다) **그 작업이 건드릴 코드와 그 작업을
규정한 설계 문서만** 본다. 전수 감사는 사용자가 지시할 때 하는 별도 작업이다.

| 어디에 있는가 | 무엇을 하는가 |
|---|---|
| **손댈 범위 안** | **고친 뒤 착수한다.** 어긋난 설계 위에 쌓으면 그 위의 구현도 함께 틀린다 |
| **범위 밖 — 한 줄로 끝난다** | 그 자리에서 고치고 커밋 메시지에 남긴다 |
| **범위 밖 — 그 밖의 전부** | 커밋 메시지에 한 줄. **다음 판의 후보일 때만** 백로그에 올린다 |

### 4. 묻는가, 스스로 정하는가

**기본은 스스로 진행이다.** 아래 셋에 해당할 때만 묻는다.

- **어느 쪽을 골라도 말이 되는데 결과물이 갈리는 것.**
- **사용자의 앞선 확정을 뒤집게 되는 것** — 뒤집지 말고 **근거를 들어 묻는다.**
- **되돌리기 어려운 것** — 데이터 유실 가능성, DB 마이그레이션, 저장·교환 형식 변경.

그 밖은 묻지 않는다. **문서를 읽으면 답이 나오는 것을 묻는 것 자체가 마찰이다.**

## 브랜치·병합 규칙

**모든 최신 업데이트는 항상 메인 브랜치(`master`)에 병합한다.** master가 언제나 최신 코드의 유일한 기준이며, master에 병합되지 않은 작업은 완료된 것으로 간주하지 않는다. 상세 규칙은 `docs/branch_merge_rules.md`를 따른다.

- **작업 시작:** 반드시 최신 `origin/master`에서 분기한다. 지정받은 작업 브랜치가 master보다 뒤처져 있으면 — 고유 커밋이 없을 때는 `git fetch origin master && git checkout -B <작업브랜치> origin/master`로 재분기하고, 고유 커밋이 있을 때는 `git merge origin/master`(또는 rebase)로 먼저 최신화한 뒤 작업한다.
- **작업 완료:** base 브랜치를 `master`로 지정한 PR을 만들어 병합한다. 저장소 기본 브랜치 설정과 무관하게 PR base는 항상 `master`여야 한다.
- **병합 후:** 병합이 끝난 작업 브랜치는 삭제한다.
- **금지:** master에 대한 직접 push 및 force-push. (작업 브랜치의 force-push는 금지 대상이 아니다.)
- **충돌:** 작업 브랜치 쪽에서 `origin/master`를 merge/rebase하여 해결한 뒤 병합한다.

---

## 문서 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v3.0 | 2026.08.19 | **판의 정의를 바꾸고 진입점을 잘라냈다 — 사용자 판정**(*"개발이 끝이 안 나는데 원인이 뭐고 어떻게 해결할 수 있을까?"* → 답: 판의 정의를 바꾼다 + 문서를 잘라낸다). **원인은 실측으로 찾았고, 뿌리는 하나였다: 실기기 확인이 2026.08.01에 멈췄다**(통과 7 · **미검증 124** · 그 사이 **52판**). 확인은 사용자만 할 수 있으므로 세션은 그 부채를 갚을 수 없었고, **그래서 부채를 안 만드는 일로 흘렀다** — 최근 20판에서 앱 코드 4,220줄 대 도구·문서 **6,683줄**, **20판 중 6판은 앱 코드 0줄**, 백로그 250건 중 사용자가 연 것 **1건**. 화면을 자동으로 못 보니 검사를 만들었고(51종·14,937줄), 검사는 자기가 볼 수 있는 것 — **자신과 다른 도구** — 을 검사하기 시작했다. 그 활동이 문서를 키웠고(`remaining_work` 5일 만에 16,342 → **21,691줄**), **커진 문서가 부채를 은폐했다**(인수인계 판은 줄곧 *"열다섯"*이라 적었다 — 109건이 묻혀 있었다). **고친 것 셋:** ⓐ **판의 정의** — *사용자가 앱에서 볼 수 있는 변화가 없으면 판이 아니다*. 도구·검사·문서는 독립 판으로 열지 않고, 검사는 동결에 가깝게 다룬다(새 검사는 같은 부류의 **앱** 결함이 두 번 난 자리만). ⓑ **완료의 정의** — v2.0이 *"화면 변경은 실기기 확인 전까지 완료가 아니다"*로 둔 것을 **갚을 수 없는 부채를 만든다**는 이유로 뒤집었다. 병합은 CI가 완료시키고 실기기는 **배치 큐**(`docs/device_walkthrough.md` — 124건을 8회차로 접었다)로 옮겼다. ⓒ **진입점** — `docs/status.md` 신설(**500줄 상한**)하고 `remaining_work`·`session_roadmap`을 닫았다(파일은 남긴다 — 백로그 본문과 실기기 절차 원문이 아직 유효하고 참조가 34곳이다). **자르지 않기로 한 것도 적는다:** 콜드 검토·자기 등재·검사 신설은 사용자가 이번에 자르지 않았다 — 다시 자가발전이 보이면 그때 다시 잰다 |
| v2.3 | 2026.08.18 | **「세션 착수 규칙」 2번 묶기의 화면 불변식을 완화 — 화면도 독립 2~3건까지 묶는다**(사용자 판정 — *"큰 문제 없지 않나 실질적으로?"* → 확정 19장 7번). **불변식이 사려던 것은 *어느 변경이 깨뜨렸는지 가리는 것*인데, 그것을 지키는 수단이 화면 수일 필요가 없다** — 로드맵 자신이 B-191에서 *"되돌아올 확률은 화면 수가 아니라 따로 짠 구현의 수에 붙는다"*로 정정했고, 근거 사건(2026.08.01 세 번 연속 되돌아옴)은 **화면 하나에서** 일어나 화면 제한이 막지 못한 사고였다. 실기기 확인도 이미 몰아서 하고 있어(당시 일곱 절 대기) 판을 쪼개 사려던 *하나씩 확인*이 실제로 일어나지 않았다. **완화가 안전한 것은 보호가 구조적이기 때문이다**: diff가 안 겹치면 실기기에서 깨진 화면의 범인은 그 항목의 diff뿐이고(진단), 항목별 커밋이 부분 되돌리기를(수리), 항목별 콜드 검토가 검출률을(품질) 각각 그대로 산다. **유일한 실위험이 diff 겹침이라 그것만 금지로 남겼다** — 겹치면 화면 A의 초록이 B의 무사를 말해주지 않는다. 화면 밖 묶음 판(5건, 2026.08.17)이 이미 같은 성질을 실증했다. **마이그레이션·교환 형식 판당 하나는 그대로다** — 그쪽은 되돌리기가 걸린 축이라 성질이 다르다 |
| v2.2 | 2026.08.16 | **「세션 착수 규칙」 2번에 「등재 문턱」과 「묶기」를 더했다 — 백로그가 수렴하지 않는 것이 결함이라는 사용자 판정**(*"무한세션은 안되지"*). **실측이 근거다**(2026.08.13~16): 닫는 속도 **1.2건/판** · 여는 속도 **1.3건/판** · 08.10~16 순감 **69 → 52**. **세션마다 하나를 닫고 하나를 여니 "몇 판 남았는가"가 정의되지 않았다.** 원인은 둘이고 규칙도 둘이다. ⓐ **등재에 문턱이 없었다** — 종전 2번은 범위 밖이면 크기를 묻지 않고 전부 등재했고, 그래서 *한 줄 수리*도 행이 됐다(전례 `B-246`: 행이 스스로 *"처방은 한 줄이다"*라 적어 놓고 그 한 줄을 설명하는 행이 500자다). **행이 수리보다 비싸면 등재가 아니라 처리다** — 다만 **애매하면 등재한다**를 함께 박았다. 이 문턱은 대기열을 줄이려는 것이지 **발견을 줄이려는 것이 아니다**(개발 의도 2번이 걸린다). ⓑ **판당 1건은 품질이 요구한 수가 아니라 묶지 않아서 나온 수였다** — 판의 고정비는 1건을 닫든 8건을 닫든 거의 같은데 오늘까지 그 비용이 늘 1건에 붙었다. **화면을 안 건드리는 항목은 같은 영역끼리 6~10건씩 묶는다**(로드맵 불변식 ⓒ가 이미 허용하던 것을 기본값으로 올렸다. 전례 PR #256). **실기기 화면 하나 · 마이그레이션 하나** 불변식은 그대로다 — 묶기는 화면 밖에만 적용된다. 확정 원문은 `docs/judgment_confirmations_2026-08.md` |
| v2.1 | 2026.08.10 | **문서 지도에 `docs/session_roadmap_2026-08.md` 등재** — 사용자 지시로 살아 있는 백로그 68건의 세션 단위 순서·묶음을 짰고(22판), 그 정본을 지도에 올렸다. **행을 「지금 할 일」 바로 위에 두고 *"순서 문서이지 지금 할 일 문서가 아니다"*를 그 자리에 적은 것이 이 등재의 요점이다** — 이 저장소는 *"지금 무엇을 하는가"*의 단일 소스를 `remaining_work` 5장의 **📌 세션 인수인계 중 맨 위 판**으로 못박아 두었고(v1.9의 🧭), **순서를 말하는 문서가 새로 생기면 콜드 세션이 그 둘을 같은 것으로 읽는다.** 실제로 그 위험이 이 저장소가 반복해 겪은 모양이다 — 같은 사실이 두 자리에 살면 한쪽이 반드시 낡는다(세션 로그 ↔ 문서 이력이 그래서 갈렸고, R 범위 표기가 그래서 없어졌다). 그래서 **자리를 갈랐다:** 로드맵은 *"다음 차례는 정해져 있지 않다"*고 판이 말할 때 **고를 목록과 그 근거**를 주고, *지금* 무엇을 하는가는 여전히 인수인계 판 하나가 정한다. 로드맵 문서 자신도 머리에 같은 말을 적고 있어 **어느 쪽으로 들어와도 같은 답에 닿는다** |
| v2.0 | 2026.08.07 | **「세션 착수 규칙」 4번의 완료 조건을 갈랐다 — CI와 실기기는 증명하는 것이 다르다**(사용자 판정 — *"CI 꼭 해야해? 안해도 나중에 몰아하면 되지 않을까?"*). 종전 문구는 *"CI 초록**과** 실기기 확인 전까지 완료로 보고하지 않는다"*로 **둘을 묶었는데**, CI는 *컴파일되는가*를, 실기기는 *제대로 보이는가*를 본다. **실기기는 대체 수단이 없어 그대로 두고 CI만 주기를 늦춘다** — 슬라이스마다가 아니라 **화면 파일을 건드린 변경이 2~3개 쌓이면 한 번**(순수 계층·문서만 바뀐 슬라이스는 로컬이 전부 덮으므로 대상이 아니다). **완화의 근거는 도구다:** 로컬 프로브가 `ui/**`를 통째로 빼 Fragment의 import 누락을 원리적으로 못 보는데, 그 부류를 `tools/triage_unresolved.sh`가 기계적으로 잡는다(2026.08.07에 실제로 그 오류가 master를 깨뜨린 뒤 만들어졌다). **이 도구가 없으면 완화도 성립하지 않는다.** **대신 미검증분을 반드시 적는다** — `remaining_work` 2장 **⏳ CI 미검증 목록**. 적지 않으면 나중에 빨간불이 떴을 때 **어디부터가 미검증인지 알 수 없고, 그것이 몰아서 하는 방식의 유일한 실패 지점**이다. 초록 실행 하나가 목록을 통째로 덮는다 |
| v1.0 | 2026.03.20 | 초기 작성 — 5대 원칙, 판단 기준, 품질 기준, 통계 시스템 설계 기준 수록 |
| v1.1 | 2026.07.03 | 브랜치·병합 규칙 추가 — 모든 업데이트는 항상 master에 병합 (상세: docs/branch_merge_rules.md) |
| v1.2 | 2026.07.03 | 개발 의도(맥가이버 칼) 최상위 판단 기준으로 명문화 — 4가지 검증 질문, 새 기능 체크리스트 6번 항목 추가 |
| v1.3 | 2026.07.28 | 새 기능 체크리스트 7번 **텍스트** 추가 — 화면 문구는 스타일 가이드(`docs/text_style_guide_2026-07.md`)를 따르고 설정에는 목적문이 붙는다(재발 방지: 규약 R-25 + `tools/check_text_style.sh` 기준선 검사) |
| v1.4 | 2026.07.28 | **문서 지도 신설**(어디서부터 읽는가) — 진입점 `docs/architecture_2026-07.md`(계층·단일 소스·확장점·규약 R-1~R-26 색인)와 `docs/scalability_performance_2026-07.md`(목표 규모 ×30·상한 전수·붕괴 순위·로드맵 S1~S5) 등재. 체크리스트 6번의 '받쳐주는 확장성'을 **판정 절차가 있는 항목으로** 승격 |
| v1.5 | 2026.07.29 | **문서 지도에 `docs/conventions.md` 등재** — 규약 R-1~R-27의 정의 원문이 `remaining_work` 1장 세션 로그 안에 묻혀 있던 것을 별도 문서로 분리(근거: `remaining_work` 5-a). 착수 전 읽는 현행 규칙이므로 진입점 표에 올린다. 지형 파악 행의 색인 범위도 R-1~R-26 → **R-1~R-27**로 정정 |
| v1.9 | 2026.08.04 | **지도의 "지금 할 일" 행에 *어디를 읽는가*를 명시** — 종전에는 `remaining_work_2026-07.md` **(5장이 시작점)**이라고만 적었는데, **그 장은 세션마다 인수인계 블록이 쌓이고 옛 블록도 "사용자 확정"·"이것이 먼저다" 같은 최우선 표식을 달고 있다.** 위에서부터 읽으면 **현행 인수인계에 닿기 전에 그 표식을 만나 이미 끝난 작업을 시작한다** — 2026.08.04에 콜드 세션 시뮬레이션으로 실증됐다(2026.07.31 블록이 *"아래 선택지보다 이것이 먼저다"*라 적은 채 서 있었고, 그 작업은 이틀 전에 닫혀 있었다). 그 장 머리에 **🧭 안내**를 신설하고 이 행이 그것을 가리키게 했다 — **이 문서가 자동으로 읽히는 유일한 진입점이라 여기에 적지 않으면 안내에 닿지 못하는 경로가 남는다.** 함께 등재하는 관행 하나: **완료 표시는 네 자리다**(설계 정본 · 그 작업의 인수인계 문서 · 아키텍처 지도 · **5장에서 그것을 최우선으로 지정한 블록**) |
| v1.8 | 2026.08.03 | **문서 지도에 `docs/judgment_confirmations_2026-08.md` 등재** — 판정 대기 24건을 일괄로 물어 전부 확정받았고(세션 로그 1-bm), 그 답이 **어느 한 문서에도 자연스러운 자리가 없었다.** 관행대로 `remaining_work` 4장 행에 인라인으로 적으면 **24건이 표를 읽을 수 없게 만들고**(B-81 한 행이 이미 그 크기다), 세션 로그에 적으면 *"지난 세션이 무엇을 했는가"*이지 *"지금 무엇이 정해져 있는가"*가 아니라 착수 세션이 찾지 못한다. 그래서 별도 문서로 두고 4장 행에는 답과 포인터만 남겼다. **지도에 올리는 이유는 이것이 착수 전에 읽는 부류이기 때문**이다 — 「세션 착수 규칙」 3번이 *"사용자의 앞선 확정을 뒤집지 말 것"*을 질문 기준으로 삼는데, **확정이 어디 적혀 있는지를 지도가 말하지 않으면 그 규칙이 지켜질 수 없다.** 종전에는 확정이 4장 행·세션 로그·설계 문서 9장에 흩어져 있어 전수로 찾을 길이 없었다 |
| v1.7 | 2026.08.01 | **세션 착수 규칙 신설**(사용자 요청 — "새 세션에서 작업할 땐 코드랑 설계 문서에 문제 없는지 검토하고, 문제 있으면 설계 의도와 방향성에 맞게 고친 뒤 착수. 애매한 게 없으면 스스로 착수하고 있으면 물어보게"). **다만 '전수 검토'로 적지 않았다** — 대기열 ①②가 큰 작업인데 매 세션 앞머리에 전수를 붙이면 실제 작업이 밀리고(원칙 04 위반), "문제 있으면 고치고 착수"를 전수에 걸면 **다른 슬라이스를 고치러 가 사용자가 정한 순서를 세션이 뒤집는다.** 그래서 **범위는 이번에 손댈 슬라이스로 한정**하고, 대신 처분 규칙(범위 안이면 고치고 **밖이면 백로그 등재만 하고 진행** — 조용히 넘어가지도, 고치러 가지도 않는다)과 **질문 기준 3종**(판정 대기 부류 · 사용자 확정을 뒤집는 것 · 되돌리기 어려운 것)을 구체화했다. 4번 특칙으로 **"자동 검증은 보이는 것을 못 본다"**를 등재 — 2026.08.01에 한 화면에서 세 번 연속 되돌아온 것이 근거이며, 검토를 두껍게 해도 잡히지 않는 축이라 별도 항으로 뒀다 |
| v1.6 | 2026.07.30 | **문서 지도에 `docs/session_log_2026-07.md` 등재** — `remaining_work` 1장(세션 로그 1,239줄)이 분할 방아쇠에 걸려 별도 문서가 됐다(근거: `remaining_work` 5-a 3번). 절 번호를 보존한 분리라 **`1-x장`이라는 참조만으로는 어느 문서인지 알 수 없으므로** 지도 행에 그 사실을 함께 적었다. 아울러 **지형 파악·규약 원문 두 행의 `R-1~R-27` 범위 표기를 없앴다** — 바로 위 v1.5가 `R-1~R-26 → R-1~R-27`로 정정한 그 자리가 **R-28 등재로 또 낡았다.** 두 판 연속 같은 자리가 낡았으면 고칠 것은 값이 아니라 **범위를 적는 관행**이다(같은 문서의 새 기능 체크리스트가 요구하는 '완결성' — 방편식 갱신 금지). 현행 범위는 `conventions.md`의 절 목록과 아키텍처 6장 색인표가 그대로 보여 준다 |
