# AGENTS.md — Codex 작업 지도

이 파일은 Codex가 저장소에서 길을 잃지 않기 위한 **짧은 진입 지도**다.
상세 규약·설계·이력은 여기로 복제하지 않는다. 필요한 문서만 그때 읽는다.

## 1. 프로젝트 목적

이 앱은 창작자를 위한 확장형 캐릭터·세계관 관리 도구다.
핵심 목표는 다음 네 가지다.

- 사용자가 구조 자체를 정의할 수 있는 확장성
- 잘못된 입력을 조용히 버리지 않는 변수 제어와 데이터 보존
- 깊은 기능을 유지하면서 조작 마찰을 줄이는 UX
- Excel 내보내기 → 외부 편집 → 가져오기 왕복의 무결성

최상위 제품 철학과 판단 기준은 `CLAUDE.md`에 있다. Claude 전용 문서라는 이름 때문에 무시하지 말고,
**제품 원칙의 기존 source of truth**로 취급한다. 다만 매 작업마다 전문을 다시 읽을 필요는 없다.

## 2. 세션 시작 순서

1. `docs/status.md`를 먼저 읽는다. 이 문서가 **현재 상태·현재 작업·살아 있는 백로그의 단일 진입점**이다.
2. 이번 작업이 건드리는 영역에 해당하는 `docs/conventions.md` 부분만 읽는다.
3. 필요한 경우에만 관련 설계 문서를 추가로 읽는다.
4. 문서의 줄 번호·옛 설계보다 **현재 코드와 실행 결과**를 우선한다.
5. 사용자가 이미 확정한 선택은 `docs/judgment_confirmations_2026-08.md`에서 확인하고 다시 묻지 않는다.

처음부터 `docs/*.md`를 전부 읽거나 요약하지 않는다. 대형 문서는 reference이며 기본 컨텍스트가 아니다.

## 3. 작업을 발명하지 않는다

`docs/status.md`의 **“고를 수 있는 것”이 0건이면 자율적으로 새 기능·감사·리팩터링·검사·백로그를 만들지 않는다.**
그 상태에서는 사용자가 지정한 작업, 기기 회신, 사용자 결정, 새 자료처럼 실제 방아쇠가 생긴 것만 처리한다.

특히 다음을 독립 작업으로 만들지 않는다.

- 이유 없는 전수 코드 리뷰 / 콜드 감사
- 새 검사·프로브·도구 제작 자체
- 문서 재정리 자체
- “혹시 나중에 필요할 것 같은” 리팩터링
- 기존 백로그를 다시 세거나 다시 분류하는 작업

예외는 사용자가 명시적으로 그런 메타 작업을 요청했을 때뿐이다.

## 4. 한 판의 정의

일반 기능 작업에서는 **사용자가 앱에서 확인할 수 있는 변화가 하나 이상 있어야 한 판**이다.
문서·도구·검사는 기능 작업을 뒷받침하는 범위에서만 함께 바꾼다.

- 기능을 간소화해서 구조를 예쁘게 만들지 않는다.
- 방편식 패치보다 원인을 고친다.
- 사용자가 요청한 활성 작업 중 범위 밖 문제를 발견했을 때, 한 줄로 안전하게 고칠 수 있으면 같이 고칠 수 있다.
- 그보다 큰 문제는 현재 작업을 확장하거나 새 백로그를 자동 생성하지 않는다. 필요하면 완료 보고에 **발견 사실만** 남기고 사용자가 다음 행동을 정하게 한다.

## 5. 데이터 안전 불변조건

다음 영역은 편의성보다 데이터 무결성이 우선한다.

- Room schema / migration
- Excel import / export / preview
- backup / restore
- 이미지 경로와 자산 이동
- 캐릭터·세계관·세력·관계의 연결 데이터
- 사용자 정의 필드와 값 라이브러리

규칙:

- 사용자의 값을 조용히 삭제·무시·정규화하지 않는다.
- 교정이 필요하면 검증 → 고지 → 바로잡을 경로 순서를 유지한다.
- 저장 형식·스키마·마이그레이션·되돌리기 어려운 데이터 변환은 추측으로 결정하지 않는다.
- Excel 왕복에서 코드/식별자/사용자 정의 필드/이미지 참조가 유실되지 않게 한다.
- Room 변경 시 기존 schema JSON, migration, migration test의 역할을 확인한다.

## 6. 질문해야 하는 경우

사용자에게 묻는 것은 원칙적으로 다음 세 경우다.

1. 어느 쪽을 골라도 타당하지만 결과물이 실제로 달라질 때
2. 사용자의 기존 확정을 뒤집게 될 때
3. 되돌리기 어렵거나 데이터 유실 가능성이 있는 결정일 때

그 밖의 구현 세부는 기존 원칙과 코드에서 판단하고 진행한다.

## 7. 검증 — 수정 전 기준선, 수정 후 비교

작업 전에 `docs/status.md`의 **재현 기준선** 절차를 그대로 따른다.
먼저 `git fetch origin master`로 기준 브랜치를 최신화한 뒤, 변경 전 기준선을 실제 실행 결과로 확보한다.
문서에 적힌 오래된 숫자를 정답으로 간주하지 않는다.

대표 절차:

```bash
JARS_DIR=<scratch> tools/setup_jvm_env.sh
JARS_DIR=<scratch> tools/run_jvm_tests.sh
JARS_DIR=<scratch> tools/probe_compile.sh /tmp/probe.txt
JARS_DIR=<scratch> tools/probe_view_compile.sh /tmp/view.txt
for f in tools/check_*.sh; do "$f" || echo "FAIL $f"; done
for p in tools/verify_room_migration*.py tools/verify_reset_coverage.py; do python3 "$p"; done
```

주의:

- `run_jvm_tests.sh`는 종료 코드만 보지 말고 출력의 `OK (` / `FAILURES!!!`를 확인한다.
- 알려진 baseline 경고·프로브 그림자를 새 회귀로 오인하지 않는다.
- 변경 뒤에는 수정한 코드의 호출처와 데이터 흐름을 확인하고, 관련 검증을 다시 실행한다.
- UI/실기기 전용 확인은 `docs/device_walkthrough.md`의 별도 큐에 남길 수 있으며 CI 성공을 대신하지 않는다.
- 앱 코드 변경 PR은 관련 CI가 초록인지 확인하기 전 완료로 간주하지 않는다.

## 8. 문서 사용 규칙

문서는 **진실원천을 하나씩만** 둔다.

- 현재 상태: `docs/status.md`
- 제품 원칙: `CLAUDE.md`
- 작업별 규약: `docs/conventions.md`
- 구조/단일 소스/확장점: `docs/architecture_2026-07.md`
- 성능·규모 한계: `docs/scalability_performance_2026-07.md`
- 사용자 확정: `docs/judgment_confirmations_2026-08.md`
- 실기기 확인: `docs/device_walkthrough.md`
- 과거 경위: 닫힌 문서와 `git log`

완료 이력을 `status.md`에 계속 누적하지 않는다. 경위는 `git log`가 가진다.
새 문서를 만들기 전에 기존 진실원천에 들어갈 내용인지 먼저 확인한다.

## 9. Git / PR 규칙

`master`가 유일한 최신 기준이다. `docs/branch_merge_rules.md`를 따른다.

- 항상 최신 `origin/master`에서 작업 브랜치를 시작한다.
- `master`에 직접 push하거나 force-push하지 않는다.
- 완료한 작업은 base가 `master`인 PR로 병합한다.
- 충돌은 작업 브랜치에서 해결한다.
- 이미 병합된 낡은 `claude/*` 브랜치를 작업 기준으로 사용하지 않는다.
- 작업 브랜치는 목적을 드러내는 이름을 사용한다 (`codex/...` 권장).

## 10. 완료 보고

완료 시 간결하게 다음을 남긴다.

- 무엇을 바꿨는지
- 왜 그 방식으로 바꿨는지
- 실행한 검증과 결과
- 남은 실기기 확인 / 사용자 결정 / 자료 대기
- 작업 중 발견했지만 범위를 넓히지 않은 문제
- PR 또는 작업 브랜치

검증하지 않은 것을 검증했다고 쓰지 않는다. 앱에서 확인할 수 없는 메타 변경을 기능 성과처럼 부풀리지 않는다.
