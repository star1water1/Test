# AGENTS.md — Codex 작업 지도

이 파일은 Codex가 저장소에서 길을 잃지 않기 위한 **짧은 진입 지도**다.
상세 규약·설계·이력은 복제하지 않는다. 필요한 문서만 작업에 맞춰 읽는다.

## 프로젝트와 진실원천

이 앱은 창작자를 위한 확장형 캐릭터·세계관 관리 도구다. 핵심은 **사용자 정의 확장성, 데이터 보존, 깊은 기능과 낮은 조작 마찰, Excel 왕복 무결성**이다.

- 현재 상태·현재 작업·살아 있는 백로그: `docs/status.md`
- 제품 철학·판단 기준: `CLAUDE.md`의 관련 절 (이름과 무관하게 현재의 제품 원칙 정본)
- 작업별 규약: `docs/conventions.md`의 관련 절만
- 구조·단일 소스·확장점: `docs/architecture_2026-07.md`
- 성능·규모 한계: `docs/scalability_performance_2026-07.md`
- 사용자 확정: `docs/judgment_confirmations_2026-08.md`
- 실기기 확인: `docs/device_walkthrough.md`
- 과거 경위: 닫힌 문서와 `git log`

처음부터 `docs/*.md`를 전부 읽거나 요약하지 않는다. 문서의 옛 줄 번호·설계보다 **현재 코드와 실행 결과**를 우선한다.

## 세션 시작

1. `docs/status.md`를 먼저 읽어 현재 상태와 작업 가능 여부를 확인한다.
2. 이번 작업이 건드리는 영역의 `docs/conventions.md` 부분만 읽는다.
3. 필요한 설계 문서와 `CLAUDE.md`의 관련 원칙만 추가로 읽는다.
4. 이미 확정된 선택은 `docs/judgment_confirmations_2026-08.md`에서 확인하고 다시 묻지 않는다.
5. 코드 작업이면 최신 `origin/master`를 기준으로 삼고 변경 전 기준선을 확인한다.

## 작업을 발명하지 않는다

`docs/status.md`의 **“고를 수 있는 것”이 0건이면 새 기능·감사·리팩터링·검사·백로그를 자율적으로 만들지 않는다.** 사용자가 지정한 작업, 기기 회신, 사용자 결정, 새 자료처럼 실제 방아쇠가 생긴 것만 처리한다.

독립 작업으로 만들지 말 것:

- 이유 없는 전수 코드 리뷰 / 콜드 감사
- 검사·프로브·도구 제작 자체
- 문서 재정리 자체
- “나중에 필요할 것 같은” 리팩터링
- 기존 백로그의 재분류·재계수

예외는 사용자가 명시적으로 그런 메타 작업을 요청한 경우다.

## 범위와 판단

일반 기능 작업은 사용자가 앱에서 확인할 수 있는 변화를 하나 이상 내는 것이 기본이다. 다만 사용자가 리팩터링·테스트·문서·검토 자체를 요청했다면 그 요청이 우선한다.

- 기능을 간소화해서 구조만 예쁘게 만들지 않는다.
- 방편식 패치보다 원인을 고친다.
- 활성 작업 중 범위 밖 문제를 발견했을 때 한 줄로 안전하게 고칠 수 있으면 함께 고칠 수 있다.
- 그보다 크면 작업을 확장하거나 새 백로그를 자동 생성하지 말고 완료 보고에 발견 사실만 남긴다.

사용자에게 추가 결정을 요청하는 것은 주로 다음 경우다: 결과가 갈리는 선택, 기존 사용자 확정을 뒤집는 선택, 되돌리기 어렵거나 데이터 유실 가능성이 있는 선택. 그 밖의 구현 세부는 기존 원칙과 코드에서 판단한다.

## 데이터 안전

다음 영역은 편의성보다 데이터 무결성이 우선한다: Room schema/migration, Excel import/export/preview, backup/restore, 이미지 자산과 경로, 캐릭터·세계관·세력·관계 연결, 사용자 정의 필드와 값 라이브러리.

- 사용자의 값을 조용히 삭제·무시·정규화하지 않는다.
- 교정은 검증 → 고지 → 바로잡을 경로를 유지한다.
- 저장 형식·스키마·마이그레이션·되돌리기 어려운 데이터 변환은 추측으로 정하지 않는다.
- Excel 왕복에서 코드/식별자/사용자 정의 필드/이미지 참조를 보존한다.
- Room 변경 시 schema JSON, migration, migration test를 함께 확인한다.

## 환경의 특이점

코드만 보고 추론하기 어려운 프로젝트 특성:

- Android/Kotlin 프로젝트이며 Java 17을 사용한다.
- Room schema JSON은 `app/schemas/`에 있고 마이그레이션 검증의 일부다.
- 순수 JVM 하네스는 `JARS_DIR` 스크래치 디렉터리를 사용한다.
- `tools/run_jvm_tests.sh`는 시험 실패에도 종료 코드가 0일 수 있으므로 출력의 `OK (` / `FAILURES!!!`를 확인한다.
- 컴파일 프로브에는 알려진 stub shadow가 있다. 변경 전 기준선과 비교해서 새 회귀만 판단한다.
- Robolectric은 현재 프로젝트 방침상 도입하지 않는다. 필요한 판정은 가능한 경우 순수 로직으로 추출하고 기존 하네스를 사용한다.
- UI/실기기 확인은 `docs/device_walkthrough.md`의 별도 큐이며 CI를 대신하지 않는다.

## 검증

**검증의 정본은 `docs/status.md`의 「재현 기준선」과 해당 작업 규약이다.** 이 파일에 전체 명령 목록을 중복하지 않는다.

- 코드·데이터 동작을 바꾸는 작업: `git fetch origin master` 후 status의 변경 전 기준선을 재현하고, 변경 뒤 관련 검증을 다시 실행한다.
- 문서만 바꾸는 작업: 앱 전체 기준선을 기계적으로 돌리지 않는다. 문서 링크·규칙 충돌 등 변경에 맞는 검증만 한다.
- 작업별로 더 구체적인 검사 명령이 있으면 그것을 우선한다.
- 앱 코드 변경 PR은 관련 CI가 초록인지 확인하기 전 완료로 간주하지 않는다.
- 검증하지 않은 것을 검증했다고 보고하지 않는다.

## 문서 규칙

문서는 진실원천을 하나씩만 둔다. 완료 이력을 `status.md`에 계속 쌓지 말고 경위는 `git log`에 남긴다. 새 문서를 만들기 전에 기존 진실원천에 들어갈 내용인지 확인한다.

## Git / PR

`master`가 유일한 최신 기준이다. 상세 규칙은 `docs/branch_merge_rules.md`를 따른다.

- 최신 `origin/master`에서 작업 브랜치를 시작한다.
- `master`에 직접 push하거나 force-push하지 않는다.
- 완료 작업은 base가 `master`인 PR로 병합한다.
- 충돌은 작업 브랜치에서 해결한다.
- 낡은 `claude/*` 브랜치를 작업 기준으로 사용하지 않는다.
- 작업 브랜치는 목적이 드러나는 이름을 사용한다 (`codex/...` 권장).

## 완료 보고

간결하게 남긴다: 무엇을/왜 바꿨는지, 실행한 검증과 결과, 남은 실기기 확인·사용자 결정·자료 대기, 범위를 넓히지 않은 발견 사항, PR 또는 작업 브랜치.
